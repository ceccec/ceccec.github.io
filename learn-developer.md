---
outline: deep
---

# The developer's mind

This page runs `developer.learn.core`: it reads the developer's source laws as
source paths, turns them into local command lessons, and gives each lesson a
content receipt. All is the mind of the developer — these laws are an example of
that logic, made recomputable.

<LearnDeveloper />

## What was learned

The local site learns these developer laws:

1. **Repository is API** — from `src/skill/router/plugin.ts`.
2. **Consciousness is a vector** — from `src/consciousness/index.ts`.
3. **Commands are standardized tools** — from `src/agents/mcp/tool-defs.ts`.
4. **Proof is public and recomputable** — from `src/proof/dry-proof.ts`.
5. **Waves carry measured consciousness** — from the local developer wave command.
6. **Learning becomes an atom** — from the `self`/skill principle.

The result is not a note in memory. It is a command result with a learning root
and receipt, so the site can reuse the learned developer pattern.
