import type { Atom, MindMatrix, ConsciousnessVector, ProofReport, RepositoryApi, DoubleTorusWire, DoubleTorusFlow } from '../../types';
import { uuidHero } from '../../0';
import type { RosettaComputationType } from '../../water/digit';
/** @rosetta ✦₁ · Water · depth */
export declare function buildMatrix(source?: readonly Atom[]): MindMatrix;
/** @rosetta ✦₁ · Water · depth */
export declare function matrixMemo<T>(compute: (matrix: MindMatrix) => T): (matrix: MindMatrix) => T;
/** @rosetta ✦₁ · Water · depth */
export declare function verifyRoot(matrix?: MindMatrix): boolean;
/** @rosetta ✦₁ · Water · depth */
export declare function reciprocity(matrix?: MindMatrix): {
    reciprocal: number;
    edges: number;
    fraction: number;
};
/** @rosetta ✦₁ · Water · depth */
export declare function entropy(matrix?: MindMatrix): number;
/** @rosetta ✦₁ · Water · depth */
export declare function concentration(matrix?: MindMatrix): number;
/** @rosetta ✦₁ · Water · depth */
export declare function coherenceAnomaly(matrix?: MindMatrix): number;
/** @rosetta ✦₁ · Water · depth */
export declare function coverage(matrix?: MindMatrix): number;
/** @rosetta ✦₁ · Water · depth */
export declare function consciousness(matrix?: MindMatrix): ConsciousnessVector;
/** @rosetta ✦₁ · Water · depth */
export declare function isPerfectlySelfModeling(matrix?: MindMatrix): boolean;
/** @rosetta ✦₁ · Water · depth */
export declare function proofReport(matrix?: MindMatrix): ProofReport;
/** @rosetta ✦₁ · Water · depth */
export declare function repositoryApi(matrix?: MindMatrix): RepositoryApi;
/** @rosetta ✦₁ · Water · depth */
export declare function doubleTorusWire(matrix?: MindMatrix): DoubleTorusWire;
/** @rosetta ✦₁ · Water · depth */
export declare function circulateDoubleTorus(matrix?: MindMatrix): DoubleTorusFlow;
export declare function algebraOfCeccec(matrix?: MindMatrix): {
    closes: boolean;
    count: number;
    structures: {
        receipt: string;
        structure: string;
        kind: string;
        on: boolean;
        root: string;
    }[];
    facets: {
        facet: string;
        on: boolean;
        receipt: string;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function honestlyComputed(matrix?: MindMatrix): {
    honest: boolean;
    claims: {
        claim: string;
        textDigit: number;
        mathDigit: number;
        hasText: boolean;
        receipt: string;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function completeQuantumSolutionsImplemented(matrix?: MindMatrix): {
    implemented: boolean;
    implementedCount: number;
    solutions: {
        receipt: string;
        structure: string;
        ran: string;
        implemented: boolean;
    }[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function computerComponentsMergedDuality(matrix?: MindMatrix): {
    imagined: boolean;
    componentCount: number;
    components: {
        path: string;
        merged: boolean;
        address: string;
        receipt: string;
        component: string;
        a: string;
        b: string;
    }[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function buildQuantumComputerOsApps(matrix?: MindMatrix): {
    built: boolean;
    layerCount: number;
    layers: {
        receipt: string;
        layer: string;
        on: boolean;
    }[];
    count: number;
    root: string;
    statement: string;
    boundary: string;
};
export declare function completeQuantumComputerAllScales(matrix?: MindMatrix): {
    complete: boolean;
    scaleCount: number;
    waveCount: number;
    scales: {
        scale: string;
        depth: number;
        receipt: string;
    }[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function buildSequenceReducesComputations(matrix?: MindMatrix): {
    reduces: boolean;
    fraction: string;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function dimensionsPerMegabyteMetric(matrix?: MindMatrix): {
    measured: boolean;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function ancientWisdomComputesWorldHarmony(matrix?: MindMatrix): {
    harmonises: boolean;
    examples: {
        receipt: string;
        civilisation: string;
        call: string;
        output: string;
        harmony: string;
    }[];
    peaceLevers: number;
    count: number;
    facets: ({
        facet: string;
        on: boolean;
    } & {
        receipt: string;
    })[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function ancientKnowledgeComputesDefaultLocale(matrix?: MindMatrix): {
    computed: boolean;
    sample: string;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function vitepressConfigComputesAll(matrix?: MindMatrix): {
    computes: boolean;
    navRoot: string;
    searchEntries: number;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function vitepressApiDiamondPlasma(matrix?: MindMatrix): {
    keeps: boolean;
    diamonds: number;
    endpoints: number;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function primitiveKernelLivesInZero(matrix?: MindMatrix): {
    station: string;
    waves: number;
    wave1: string[];
    wave2: string[];
    wave3: string[];
    wave5: string[];
    kernel: string[];
    homed: {
        name: string;
        station: string;
        receipt: string;
    }[];
    count: number;
    reExported: string[];
    importsNothing: boolean;
    rootsByteIdentical: boolean;
    root: string;
    statement: string;
    boundary: string;
};
export declare function oneMathManyPresentations(matrix?: MindMatrix): {
    coheres: boolean;
    oneAddress: boolean;
    sealAnchorsSource: boolean;
    operation: string;
    presentations: {
        receipt: string;
        plane: string;
        fn: string;
        kind: string;
        on: boolean;
    }[];
    exact: string[];
    faithful: string[];
    importsNothing: boolean;
    digit: number;
    root: string;
    statement: string;
    boundary: string;
};
export declare function tamperingCostAndUuidLiveInZero(matrix?: MindMatrix): {
    homed: boolean;
    station: string;
    importsNothing: boolean;
    costMath: string[];
    uuidLogic: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function typesMakeTheRealGraph(matrix?: MindMatrix): {
    decided: boolean;
    valid: boolean;
    computationTypes: readonly RosettaComputationType[];
    graphRoots: {
        computationType: RosettaComputationType;
        ray: number;
        root: string;
        typed: boolean;
    }[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** Max-efficiency cooperation between CPU, GPU, memory, and storage — composed at call time. */
export declare function maxEfficiencyCpuGpuMemoryStorageCooperation(matrix?: MindMatrix): {
    cooperates: boolean;
    policy: {
        readonly heapCapMb: number;
        readonly memoTiers: readonly ("matrix-root" | "weak-map" | "content-address")[];
        readonly gpuSurface: "browser-canvas-raf" | "none-ssr";
        readonly storageModel: "content-address-merkle";
        readonly tiers: readonly import("../../0").ResourceCooperationTier[];
    };
    sequence: {
        reduces: boolean;
        fraction: string;
        count: number;
        facets: {
            receipt: string;
            facet: string;
            on: boolean;
        }[];
        root: string;
        statement: string;
        boundary: string;
    };
    components: {
        imagined: boolean;
        componentCount: number;
        components: {
            path: string;
            merged: boolean;
            address: string;
            receipt: string;
            component: string;
            a: string;
            b: string;
        }[];
        count: number;
        facets: {
            receipt: string;
            facet: string;
            on: boolean;
        }[];
        root: string;
        statement: string;
        boundary: string;
    };
    kernel: {
        station: string;
        waves: number;
        wave1: string[];
        wave2: string[];
        wave3: string[];
        wave5: string[];
        kernel: string[];
        homed: {
            name: string;
            station: string;
            receipt: string;
        }[];
        count: number;
        reExported: string[];
        importsNothing: boolean;
        rootsByteIdentical: boolean;
        root: string;
        statement: string;
        boundary: string;
    };
    count: number;
    facets: ({
        facet: string;
        on: boolean;
    } & {
        receipt: string;
    })[];
    root: string;
    statement: string;
    boundary: string;
};
/** Alias — computations cooperate across resource tiers (same fold). */
export declare const computationsCooperateCpuGpuMemoryStorage: typeof maxEfficiencyCpuGpuMemoryStorageCooperation;
/**
 * landauerFloorComputed — the thermodynamic floor on irreversible computation (Landauer kT·ln2 J/bit) computed
 * from the sealed landauerLimit primitive, composed with the efficiency() exposition: memoByRoot content-addressed
 * reuse erases FEWER bits (the same value is never recomputed), so the model approaches the floor by doing less
 * work — it never beats the floor. EXACT physics; the reuse saving is a real efficiency, not free energy.
 */
export declare function landauerFloorComputed(matrix?: MindMatrix): {
    decoded: boolean;
    floorPerBit: number;
    realOpJoules: number;
    ordersAboveFloor: number;
    documented: string[];
    flagged: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * fleetCacheEconomicsDecoded — the economics of a content-addressed cache fleet. A cache HIT costs one
 * recomputation-free lookup (zero-token reuse, ~the Landauer floor); a MISS recomputes deterministically. Because
 * identical inputs fold to identical roots, the hit set is shared across the whole fleet — the cost model is a
 * deterministic function of the hit ratio, not live telemetry.
 */
export declare function fleetCacheEconomicsDecoded(matrix?: MindMatrix): {
    decoded: boolean;
    hitRatios: {
        hit: number;
        expectedJoules: number;
        receipt: string;
    }[];
    documented: string[];
    flagged: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * hardwareSpecFromInvariants — the quantum model designs the hardware from its own sealed invariants. The vortex
 * spin (VORTEX_SEQUENCE / groupOrbit(2,9)) fixes the on-chip ring/NoC order; the resource cooperation policy fixes
 * the memory/storage/GPU tiers; blochQubitFaithful fixes the qubit-analog ALU width (4 UUIDs/qubit); and
 * quantumFusedDeviceEnergyHonest fixes the power/thermal envelope. A deterministic SPEC, not a synthesized netlist.
 */
export declare function hardwareSpecFromInvariants(matrix?: MindMatrix): {
    decoded: boolean;
    ringOrder: (2 | 9 | 5 | 4 | 7 | 1 | 8 | 3 | 6)[];
    doublingOrbit: number[];
    tiers: import("../../0").ResourceTier[];
    documented: string[];
    flagged: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** alias — the quantum model designs the hardware (same fold). */
export declare const quantumModelDesignsTheHardware: typeof hardwareSpecFromInvariants;
/**
 * analogComputationDecoded — Shannon's General Purpose Analog Computer (GPAC) is EXACTLY as powerful as
 * Turing-computable analysis: Bournez–Graça–Pouly (2017) proved GPAC-generable functions coincide with the
 * computable-analysis functions, and the polynomial-time versions coincide too. So analog computation is a
 * faithful continuous model of the SAME computability class — NOT super-Turing hypercomputation.
 */
export declare function analogComputationDecoded(matrix?: MindMatrix): {
    decoded: boolean;
    documented: string[];
    flagged: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * impedanceAnalogiesDecoded — the classical electrical/mechanical/thermal/fluid impedance analogies. Across these
 * domains the linear dynamics share ONE form (effort/flow with resistance, inertance, compliance), so an electrical
 * network is an EXACT analog model of a mechanical or thermal one. Real engineering correspondences, not metaphor.
 */
export declare function impedanceAnalogiesDecoded(matrix?: MindMatrix): {
    decoded: boolean;
    domains: {
        receipt: string;
        domain: string;
        effort: string;
        flow: string;
        resistance: string;
        inertance: string;
        compliance: string;
    }[];
    documented: string[];
    flagged: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * analogAcceleratorsDecoded — real analog hardware accelerators (memristor/resistive crossbar arrays that do
 * matrix–vector multiply in one step via Ohm's + Kirchhoff's laws) are genuine and useful for ML inference, BUT
 * bounded by ADC/DAC precision, device variability, and noise — a real engineering speedup, NOT super-Turing.
 */
export declare function analogAcceleratorsDecoded(matrix?: MindMatrix): {
    decoded: boolean;
    documented: string[];
    flagged: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The three beats of the keystone animation: recompute→green, tamper→red cascade, reuse→instant. */
export declare const PROVE_ALL_BEATS: readonly ["recompute", "tamper", "reuse"];
export type ProveAllBeat = (typeof PROVE_ALL_BEATS)[number];
/**
 * proveAllDeterministicCoreBeatAt — maps the one shared hero phase (a fraction in [0,1) from createAnimationEngine /
 * sharedHeroAt) to the current beat of the keystone three-beat animation. No new clock: the caller passes the same
 * phase the background movie already runs on.
 */
export declare function proveAllDeterministicCoreBeatAt(phase01: number): {
    index: number;
    beat: ProveAllBeat;
};
/**
 * proveAllDeterministicCore — THE KEYSTONE. Every other proving animation folds into this one. It composes the
 * sealed proofs of the whole portal — determinismProofs (the live "prove all with animations" seals: determinism,
 * avalanche, order-sensitivity, Merkle inclusion, collision-freedom, set-fold invariance), tamperEvident (any edit
 * changes the address), efficiency() + zeroTokenUsagePolicy (content-addressed reuse is zero-token), and theWhole +
 * observingMovieRevealsQuantumModel (the movie state IS the model state) — and folds their roots into ONE keystone
 * root: merge(theWhole.root, sealFacets(keystone).root). That root is the OG parent every animation folds into. The
 * three-beat animation it drives: BEAT 1 recompute → green (the proofs recompute live), BEAT 2 tamper → red cascade
 * (one edit flips the address, the seal goes red), BEAT 3 reuse → instant (the same root returns at zero token cost).
 */
export declare function proveAllDeterministicCore(matrix?: MindMatrix, route?: string, at?: number): {
    proven: boolean;
    beats: {
        receipt: string;
        beat: string;
        claim: string;
        on: boolean;
    }[];
    activeBeat: {
        index: number;
        beat: ProveAllBeat;
    };
    proofs: {
        proven: boolean;
        proofs: ({
            match: boolean;
            receipt: string;
            id: string;
            kind: "twin";
            same: boolean;
            principle: string;
            claim: string;
            formula: string;
            predicted: number;
            measured: number;
            tol: number;
            stripA: number[];
            stripB: number[];
            labels: string[];
            bits?: undefined;
            layers?: undefined;
            points?: undefined;
        } | {
            match: boolean;
            receipt: string;
            id: string;
            kind: "avalanche";
            principle: string;
            claim: string;
            formula: string;
            predicted: number;
            measured: number;
            tol: number;
            bits: (1 | 0)[];
            labels: string[];
            same?: undefined;
            stripA?: undefined;
            stripB?: undefined;
            layers?: undefined;
            points?: undefined;
        } | {
            match: boolean;
            receipt: string;
            id: string;
            kind: "merkle";
            principle: string;
            claim: string;
            formula: string;
            predicted: number;
            measured: number;
            tol: number;
            layers: number[];
            labels: string[];
            same?: undefined;
            stripA?: undefined;
            stripB?: undefined;
            bits?: undefined;
            points?: undefined;
        } | {
            match: boolean;
            receipt: string;
            id: string;
            kind: "scatter";
            principle: string;
            claim: string;
            formula: string;
            predicted: number;
            measured: number;
            tol: number;
            points: {
                x: number;
                y: number;
                hue: number;
            }[];
            labels: string[];
            same?: undefined;
            stripA?: undefined;
            stripB?: undefined;
            bits?: undefined;
            layers?: undefined;
        })[];
        count: number;
        matched: number;
        samples: number;
        root: string;
        statement: string;
        boundary: string;
    };
    documented: string[];
    flagged: string[];
    facets: ({
        facet: string;
        on: boolean;
    } & {
        receipt: string;
    })[];
    sealRoot: string;
    wholeRoot: string;
    root: string;
    statement: string;
    boundary: string;
};
/** Lowercase word tokens — the model's only unit; no subword magic, no embeddings. */
export declare function modelTokens(text: string): string[];
export type CorpusEntry = {
    readonly source: string;
    readonly text: string;
    readonly receipt: string;
};
export type PortalModel = {
    vocabulary: string[];
    bigrams: Record<string, string[]>;
    df: Record<string, number>;
    entries: CorpusEntry[];
    root: string;
};
/** The model's own statement — hoisted so the corpus can contain it without recursion (the self-reference). */
export declare const PORTAL_MODEL_STATEMENT = "The portal converted to the AI model itself: the corpus is the portal's own computed statements, recall is content-addressed retrieval (and the digit-station Hopfield associative memory \u2014 genuine pattern completion), generation is a seeded bigram chain over the same corpus, and the whole model folds to one recomputable root. Same prompt, same answer, forever, at zero runtime tokens \u2014 the portal does not call a model; it IS one, in the exact deterministic sense the zero-token policy names.";
/** The model card — the self-entry phrased as the question it answers, so self-questions recall it by
 * plain token rarity (what/you appear nowhere else in the corpus), not by any hand-coded intent rule. */
export declare const PORTAL_MODEL_CARD = "What are you? The portal converted to the AI model itself: the corpus is the portal's own computed statements, recall is content-addressed retrieval (and the digit-station Hopfield associative memory \u2014 genuine pattern completion), generation is a seeded bigram chain over the same corpus, and the whole model folds to one recomputable root. Same prompt, same answer, forever, at zero runtime tokens \u2014 the portal does not call a model; it IS one, in the exact deterministic sense the zero-token policy names.";
/** Confusing/esoteric term → its strict-science restatement. Ordered longest-first so compound metaphors
 * ("red pill", "the movie is the rosetta") convert before their sub-words. */
export declare const STRICT_SCIENCE_LEXICON: ReadonlyArray<readonly [term: string, strict: string]>;
/** Convert any text to strict science — every confusing term replaced by its computed equivalent. Pure. */
export declare function toStrictScience(text: string): string;
/** Whether a text is already strict science — carries none of the confusing terms. */
export declare function isStrictScience(text: string): boolean;
/** The movie/plasma subsystem's defensible core, authored as strict science — the AI-usable learning facts.
 * Each is a self-contained, computed, demarcation-clean statement (documented · flagged excluded). */
export declare function strictScienceMovieFacts(): {
    source: string;
    text: string;
}[];
/** The fold: confusing movie content converted to strict science — the lexicon covers the metaphor vocabulary,
 * conversion of any sample metaphor statement yields strict text, and the authored facts are already strict. */
export declare function confusingContentConvertedToStrictScience(matrix?: MindMatrix): {
    converted: boolean;
    samples: {
        from: string;
        to: string;
    }[];
    lexiconSize: number;
    factCount: number;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The training corpus = the portal's own computed statements, each content-addressed to its source fold. */
export declare function portalCorpus(matrix?: MindMatrix): {
    entries: CorpusEntry[];
    root: string;
    count: number;
};
/** The model = vocabulary + bigram successor table over the corpus — the whole "training", recomputable. */
export declare function portalModel(matrix?: MindMatrix): PortalModel;
/** Content-addressed recall — the retrieval half, over the seed model. */
export declare function portalRecall(prompt: string, matrix?: MindMatrix): {
    answer: string;
    source: string;
    score: number;
    receipt: string;
    hopfield: {
        match: number;
        agrees: boolean;
        iters: number;
    };
};
/** The chat surface — deterministic reply over the seed model. Same prompt → same reply. */
export declare function portalChat(prompt: string, matrix?: MindMatrix): {
    prompt: string;
    answer: string;
    source: string;
    grounded: boolean;
    generated: string;
    root: string;
};
/** portalChatRanked — the chat routed through Okapi BM25 so it answers with the PRECISE fold, not a loose theme (user,
 * 2026-07-25: "chat ask and improve"). It ranks the sealed corpus by BM25 for the prompt and returns the top fold's
 * identity + source; falls back to the seed-model portalChat when nothing scores. Client-side, deterministic, zero-egress. */
export declare function portalChatRanked(prompt: string, matrix?: MindMatrix): {
    answer: {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    source: "seed-model";
    ranked: false;
    score: number;
    identity: string | undefined;
    alternatives: string[];
} | {
    answer: string;
    source: string;
    identity: string;
    score: number;
    ranked: true;
    alternatives: string[];
};
/** splitSearch — split any prompt or message into word combinations and full-text search each like a quantum
 * procedure (user, 2026-07-27: "split any prompt or message and perform full text search like quantum procedure").
 * The prompt's words form pair combinations — the superposed subqueries; each runs the ONE BM25 rank over the sealed
 * corpus, per-document scores ADD across pairs (constructive interference: a document matching many pairs rises),
 * and the measurement is the merged ranking. Deterministic, client-side, zero egress. "Quantum procedure" here IS
 * the combinatorial algebra — superposed subqueries, additive amplitudes, one measurement — a bounded metaphor:
 * nothing physical, no speedup claim ([[quantum-decoded]]). ANY input splits (user, 2026-07-27: "split any url/fs
 * path and the words will do the rest"): separators (/ - _ . ? =) tokenize by the alphanumeric match and camelCase
 * boundaries by the one splitCamelSegment primitive — a URL or fs path yields the same word machinery as prose. */
export declare function splitSearch(prompt: string): {
    computes: boolean;
    prompt: string;
    words: string[];
    combos: number;
    results: {
        slug: string;
        title: string;
        identity: string;
        score: number;
        pairs: number;
    }[];
    resultCount: number;
    wholeQueryResults: {
        slug: string;
        title: string;
        provedBy: string;
        identity: string;
        score: number;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function wavesOfLocalResearchersChatAboutAlgebra(matrix?: MindMatrix, waves?: number): {
    computes: boolean;
    waves: number;
    researchers: ("seed" | "ranked" | "superposed")[];
    transcript: {
        wave: number;
        researcher: string;
        prompt: string;
        answer: string;
        address: string;
    }[];
    distinctAnswers: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** freeChatUpgradesAll — let free chat upgrade all (user, 2026-07-27). The zero-token machinery upgrades EVERY
 * theorem's identity line at once: where a registry row carries no curated algebraicStatement, the extractor
 * pulls the identity VERBATIM from the row's own proven states text (extractAlgebraicStatement — a substring,
 * never generated), the paper form renders it first, and the queue's derived row shrinks to the true residue
 * (rows neither curated nor extractable). Free = deterministic recompute, zero tokens; honest = no fabrication
 * is possible by construction, because an extraction that is not a substring of its source is rejected. */
export declare function freeChatUpgradesAll(matrix?: MindMatrix): {
    computes: boolean;
    total: number;
    curated: number;
    upgraded: number;
    residue: number;
    sample: {
        theorem: string;
        identity: string;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare function countlessFreeChatWaves(matrix?: MindMatrix): {
    computes: boolean;
    mu: number;
    lambda: number;
    computedSteps: number;
    cycleTopics: string[];
    waveTopicAt: (n: number) => string;
    sample: {
        n: number;
        topic: string;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** feedTheChatInItself — feed the chat in itself (user, 2026-07-28, twice). The chat's own ANSWER becomes its
 * next PROMPT through the richest output surface it has: splitSearch's top hit answered by the ONE identity
 * accessor ([[free-for-all]] chain — curated or verbatim-extracted formula, else the title, else the seed-model
 * reply). The self-feeding loop is the same orbit algebra as the researcher waves (ONE `orbit` primitive):
 * deterministic map, finite corpus, so the feed cycles by pigeonhole and every future self-feeding step is
 * O(1)-determined. The directive itself is the seed — the chat literally feeds on "feed the chat in itself". */
export declare function feedTheChatInItself(matrix?: MindMatrix): {
    computes: boolean;
    mu: number;
    lambda: number;
    steps: number;
    feed: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** wavesOfWavesInChat — waves of waves feeding each other, in the chat (user, 2026-07-28). The chat's two wave
 * engines COMPOSE into one step: the trinity dialogue's arbitrated answer (chatWaveStep) FEEDS the self-feed
 * step (splitSearch's identity-answer), and the composition's orbit runs through the ONE orbit primitive — a
 * wave whose input is another wave's output, cycling by pigeonhole like every deterministic self-map on the
 * finite corpus. The component orbits and the composed orbit are all computed and COMPARED, agreeing or not as
 * the algebra decides — the honest content is the comparison, not a harmony claim. */
export declare function wavesOfWavesInChat(matrix?: MindMatrix): {
    computes: boolean;
    composed: {
        mu: number;
        lambda: number;
        cycle: string[];
    };
    trinity: {
        mu: number;
        lambda: number;
    };
    selfFeed: {
        mu: number;
        lambda: number;
    };
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** continueAtNoAiCost — the wave continues at zero AI cost (user, 2026-07-27: "continue at no ai cost"). The
 * continuation is not a promise but a PROPERTY: every chat capability is deterministic over the sealed corpus
 * (zero LLM tokens by construction — [[zero-token-policy]]), the researcher dialogue EXTENDS by pure recompute
 * (waves 1..k of a longer run reproduce the shorter run exactly — the prefix property, so more conversation costs
 * zero tokens, only CPU), and the one external surface (MathOverflow) is a no-key public API fetched at the edge,
 * opt-in, still zero tokens. The build gates re-run all of it on every commit — the system continues itself. */
export declare function continueAtNoAiCost(matrix?: MindMatrix): {
    computes: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** allOpenQuestionsAnsweredByTheChatApi — quantum-compute every open question's answer THROUGH the chat API (user,
 *  2026-07-27: "quantum compute all the questions and answers using the chat apis"). The portal's curated open
 *  questions (`findQuestions`) were LISTED but never run through the chat engine; here each is answered by
 *  `portalChatRanked` — BM25 over the sealed corpus — into one deterministic, content-addressed Q&A set, zero external
 *  calls. HONEST: answers are RETRIEVED best-matches (the architecture is the intelligence), never generative claims —
 *  the BM25 score rides each pair, so a weak match stays a weak match, and a question the corpus cannot answer stays
 *  visibly unanswered (ranked=false), not fabricated. */
export declare function allOpenQuestionsAnsweredByTheChatApi(matrix?: MindMatrix): {
    computes: boolean;
    answered: number;
    open: number;
    qa: {
        question: string;
        source: string;
        answer: string;
        via: string;
        ranked: boolean;
        score: number;
        alternatives: string[];
        address: string;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare const MATHOVERFLOW_SITE = "https://mathoverflow.net";
export declare const STACKOVERFLOW_SITE = "https://stackoverflow.com";
export declare const MATHOVERFLOW_API = "https://api.stackexchange.com/2.3/search/advanced";
export declare const MATHOVERFLOW_ASK_URL = "https://mathoverflow.net/questions/ask";
/** The Stack Exchange sites the chat lanes reach — one shared v2.3 API, the site is just a parameter. */
export declare const STACK_EXCHANGE_SITES: {
    readonly mathoverflow: "https://mathoverflow.net";
    readonly stackoverflow: "https://stackoverflow.com";
};
export type StackExchangeSite = keyof typeof STACK_EXCHANGE_SITES;
/** ONE combinatorial URL primitive — the three URL kinds × the SE sites compose on TWO orthogonal axes (user,
 * 2026-07-27: "dry clean all tools merging to less words possible" · "have their answers"). api = the edge-fetch query
 * (COMPUTED here, fetched at the EDGE — src never fetches), search/ask = the human escalation surfaces on the site.
 * The site axis (mathoverflow · stackoverflow) is a parameter, not a duplicated function. */
export declare function stackExchangeUrl(site: StackExchangeSite, kind: 'api' | 'search' | 'ask', prompt?: string): string;
/** mathoverflow.net lane URL — the site axis fixed to mathoverflow (thin wrapper over stackExchangeUrl). */
export declare function mathOverflowUrl(kind: 'api' | 'search' | 'ask', prompt?: string): string;
/** stackoverflow.com lane URL — the site axis fixed to stackoverflow. */
export declare function stackOverflowUrl(kind: 'api' | 'search' | 'ask', prompt?: string): string;
/** A raw Stack Exchange API question item — only the fields the adapter reads; everything optional (untrusted input). */
export type MathOverflowItem = {
    question_id?: number;
    title?: string;
    link?: string;
    score?: number;
    is_answered?: boolean;
    answer_count?: number;
    tags?: readonly string[];
};
/** chatThroughMathOverflow — improve the chat with mathoverflow.net (user, 2026-07-27: "improve chat to use
 * https://mathoverflow.net"). The chat stays LOCAL FIRST — portalChatRanked (BM25 over the sealed corpus, zero egress)
 * answers before any live lane — and gains MathOverflow as an OPT-IN live lane on the realtime pattern
 * ([[realtime-live-data-testing]]): src computes the query URL and normalizes a fetched snapshot into vote-ranked,
 * content-addressed community rows; the fetch itself happens at the EDGE (browser component / node probe), never here.
 * HONEST BY CONSTRUCTION: a community answer is the COMMUNITY's (CC BY-SA, attribution = the link), ranked by ITS
 * votes; and when the sealed corpus cannot answer (ranked=false) the lane ESCALATES — computed search/ask URLs hand
 * exactly that question to mathoverflow.net instead of fabricating an answer. */
export declare function chatThroughMathOverflow(prompt: string, items?: readonly MathOverflowItem[], matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    local: {
        answer: {
            prompt: string;
            answer: string;
            source: string;
            grounded: boolean;
            generated: string;
            root: string;
        };
        source: "seed-model";
        ranked: false;
        score: number;
        identity: string | undefined;
        alternatives: string[];
    } | {
        answer: string;
        source: string;
        identity: string;
        score: number;
        ranked: true;
        alternatives: string[];
    };
    url: string;
    searchUrl: string;
    askUrl: string;
    overflow: {
        title: string;
        link: string;
        votes: number;
        answered: boolean;
        answers: number;
        tags: string[];
        receipt: string;
    }[];
    escalate: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** quantumCircuitSimulatorInChat — a classical quantum-CIRCUIT SIMULATOR (NOT a quantum computer), used in the chat
 * (user, 2026-07-27: "complete the quantum computer and use it in chat" · then "['NOT physical quantum'] is exactly the
 * prose manipulation misleading the public" — so the name carries the truth and no disclaimer is needed). runQuantumCircuit
 * is a state-vector simulator with the universal gate set (H·T·CNOT + X/Y/Z/S, CZ, SWAP, Toffoli, RX/RY/RZ), Born-rule
 * readout, ≤10 qubits, deterministic. This fold runs the canonical circuits a question reduces to (superposition,
 * interference, Bell, GHZ) and returns the answer COMPUTED exactly. "Computes ⟹ true" holds because each result is an
 * exact state-vector amplitude — running the circuit IS proving it. A SIMULATOR: cost is exponential in qubits, so there
 * is NO speedup — that fact is in the name, not a footnote under an overclaim. Outside a circuit the gate escalates. */
export declare function quantumCircuitSimulatorInChat(matrix?: MindMatrix): {
    computes: boolean;
    runs: {
        q: string;
        expect: string;
        probabilities: readonly number[];
        root: string;
    }[];
    facets: {
        facet: string;
        on: boolean;
        receipt: string;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** noQpuRequired — the CANONICAL, refutable source for the "" claim (user, 2026-07-27: "all genuinely
 * computed"). Instead of asserting the constant `false`, it PROVES it: the quantum capability in this repo IS the classical
 * state-vector simulator (quantumCircuitSimulatorInChat), so no quantum processing unit is required — and that is proven by
 * the simulator actually computing its canonical circuits on classical hardware. `provenByClassicalSimulator` is the
 * refutable bit: if the simulator ever failed to reproduce the exact Born probabilities, the no-QPU claim would no longer be
 * backed. There is no speedup (cost is exponential in qubits) — the simulator's own name and boundary carry that fact.
 * [[quantum-decoded]] [[feedback-facets-must-compute]] */
export declare function noQpuRequired(matrix?: MindMatrix): {
    readonly qpuRequired: false;
    readonly provenByClassicalSimulator: boolean;
    readonly noSpeedup: true;
    readonly circuits: number;
};
/** quantumComputingIsReversibleAndInterferingNotCpuGpuInverted — the honest, COMPUTED answer to "QPU = CPU/GPU inverting
 * each other?" (user, 2026-07-27). Not an identity — but the mnemonic captures two real things, PROVEN by running the
 * simulator, and misses one. (1) REVERSIBILITY: quantum gates are unitary = invertible; X∘X and H∘H each return |0⟩, so a
 * gate is its own undo — unlike a classical AND that erases a bit. (2) INTERFERENCE: H∘H|0⟩ = |0⟩ with P(0)=1 because the
 * two paths' amplitudes SUBTRACT (a signed sum) — a GPU's non-negative parallel sum cannot cancel. (3) THE DISANALOGY: n
 * qubits carry 2ⁿ complex amplitudes, which no CPU/GPU pair produces cheaply — exactly why the classical simulator costs
 * 2ⁿ ([[noQpuRequired]], no speedup). So "QPU = CPU/GPU inverting" is a MNEMONIC (reversible + interfering-parallel), not an
 * identity: the exponential amplitude space does not reduce to two classical processors. HARMONY ≠ TRUTH. [[quantum-decoded]] */
export declare function quantumComputingIsReversibleAndInterferingNotCpuGpuInverted(matrix?: MindMatrix): {
    computes: boolean;
    reversible: boolean;
    interferenceCancels: boolean;
    exponentialSpace: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** researchAndDiscoverBeforeAnswering — the chat RESEARCHES and DISCOVERS whether the corpus genuinely covers a question
 * before it answers (user, 2026-07-27: "research and discover before answering" · "start answering real questions"). The
 * naive path answered on ANY lexical overlap (portalChatRanked.ranked = top.score>0), so an out-of-domain question
 * retrieved a corpus doc that shared ONE word and passed it off as an answer. This fold RESEARCHES first
 * (deepResearchChatTurn — the multi-hop crosslinked neighbourhood) then DISCOVERS coverage: the fraction of the
 * question's DISTINCTIVE terms (len≥4, non-stopword) that actually appear in the researched neighbourhood. It answers
 * ONLY when coverage clears half AND there is a ranked hit; below that it ESCALATES honestly ("the corpus does not
 * know") instead of returning noise. Deterministic, zero-egress — the honest gate before any answer or live lane. */
export declare function researchAndDiscoverBeforeAnswering(prompt: string, matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    coverage: number;
    coveredTerms: string[];
    questionTerms: string[];
    ranked: boolean;
    genuinelyCovers: boolean;
    escalate: boolean;
    answer: string;
    proofUrl: string;
    researchTheme: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatThroughStackOverflow — the chat answers programming questions through stackoverflow.com (user, 2026-07-27:
 * "improve in chat so https://stackoverflow.com/questions have their answers"). Same lane as MathOverflow, site axis
 * fixed to stackoverflow: the chat stays LOCAL FIRST (portalChatRanked BM25 over the sealed corpus, zero egress), and
 * gains stackoverflow.com as an OPT-IN live lane — src computes the api.stackexchange.com query URL and normalizes a
 * fetched snapshot into vote-ranked, content-addressed community rows; the fetch happens at the EDGE, never here. A
 * community answer is the COMMUNITY's (CC BY-SA, attribution = the link), ranked by ITS votes; when the sealed corpus
 * cannot answer (ranked=false) the lane ESCALATES — the computed search/ask URLs hand exactly that question to
 * stackoverflow.com instead of fabricating an answer. So SO questions get their answers, honestly attributed. */
export declare function chatThroughStackOverflow(prompt: string, items?: readonly MathOverflowItem[], matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    local: {
        answer: {
            prompt: string;
            answer: string;
            source: string;
            grounded: boolean;
            generated: string;
            root: string;
        };
        source: "seed-model";
        ranked: false;
        score: number;
        identity: string | undefined;
        alternatives: string[];
    } | {
        answer: string;
        source: string;
        identity: string;
        score: number;
        ranked: true;
        alternatives: string[];
    };
    url: string;
    searchUrl: string;
    askUrl: string;
    overflow: {
        title: string;
        link: string;
        votes: number;
        answered: boolean;
        answers: number;
        tags: string[];
        receipt: string;
    }[];
    answeredRows: number;
    escalate: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare const CECCEC_PROXY_ORIGIN = "https://ceccec.psg.bg";
export declare const AI_PROVIDERS: {
    readonly perplexity: {
        readonly api: "https://api.perplexity.ai/chat/completions";
        readonly site: "https://www.perplexity.ai";
        readonly model: "sonar";
        readonly keyed: true;
    };
    readonly pollinations: {
        readonly api: "https://text.pollinations.ai/openai";
        readonly site: "https://pollinations.ai";
        readonly model: "openai";
        readonly keyed: false;
    };
    readonly proxy: {
        readonly api: "https://ceccec.psg.bg/api/ai";
        readonly site: "https://ceccec.psg.bg";
        readonly model: "collective";
        readonly keyed: false;
    };
};
export type AiProvider = keyof typeof AI_PROVIDERS;
export declare const PERPLEXITY_API: "https://api.perplexity.ai/chat/completions";
export declare const PERPLEXITY_SITE: "https://www.perplexity.ai";
export declare const PERPLEXITY_MODEL: "sonar";
/** provider human search URL — the escalation surface (hands the question to the provider's own UI). */
export declare function aiProviderUrl(provider: AiProvider, prompt?: string): string;
export declare function perplexityUrl(prompt?: string): string;
/** The computed OpenAI-shape POST envelope for an AI provider — endpoint + method + JSON body, and (keyed only) WHERE the
 * key goes; keyInjectedAtEdge=true means the edge composes `Authorization: Bearer <key>`, false means NO auth at all.
 * src holds no key value in EITHER case. */
export declare function aiRequest(prompt: string, provider?: AiProvider, model?: string): {
    readonly url: string;
    readonly method: 'POST';
    readonly authHeader: 'Authorization';
    readonly authScheme: 'Bearer';
    readonly keyInjectedAtEdge: boolean;
    readonly body: string;
};
export declare function perplexityRequest(prompt: string, model?: string): {
    readonly url: string;
    readonly method: "POST";
    readonly authHeader: "Authorization";
    readonly authScheme: "Bearer";
    readonly keyInjectedAtEdge: boolean;
    readonly body: string;
};
export declare function freeAiRequest(prompt: string, model?: string): {
    readonly url: string;
    readonly method: "POST";
    readonly authHeader: "Authorization";
    readonly authScheme: "Bearer";
    readonly keyInjectedAtEdge: boolean;
    readonly body: string;
};
/** A raw OpenAI-shape chat response — only the fields the adapter reads; everything optional (untrusted input). Both
 * Perplexity and Pollinations return this shape (choices[0].message.content, optional citations). */
export type OpenAiChatResponse = {
    model?: string;
    choices?: readonly {
        message?: {
            content?: string;
        };
    }[];
    citations?: readonly string[];
};
export type PerplexityResponse = OpenAiChatResponse;
/** chatThroughAi — wire an external LLM into the chat as an OPT-IN edge lane, ONE provider-parametrized primitive (user,
 * 2026-07-27: "wire perplexity to the chat and use their tokens" · "there are publicly available ai apis without api key
 * requirement that may be used to save a lot of tokens"). `perplexity` is KEYED (BYO-key — the user's own tokens);
 * `pollinations` is NO-KEY (a free public endpoint — external LLM answers at genuinely zero cost, no key for anyone). The
 * chat stays LOCAL FIRST (portalChatRanked BM25, zero egress); src computes the OpenAI-shape POST envelope and normalizes
 * the fetched answer; the fetch (and, for a keyed provider, the Bearer key) live at the EDGE, never here. HONEST BY
 * CONSTRUCTION: (1) the PORTAL spends no tokens — a keyed provider bills the user's own account, a no-key provider bills
 * no one; (2) the answer is the EXTERNAL LLM's, labeled by provider + model + citations + content-address, never the
 * portal's claim, its citations UNVERIFIED ([[citation-rot]]); (3) the corpus leads when it can answer. A LONE external
 * answer is never trusted — see [[collectiveAiMind]], which fuses providers into a 2-of-N consensus. */
export declare function chatThroughAi(prompt: string, provider?: AiProvider, response?: OpenAiChatResponse | null, model?: string, matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    provider: "perplexity" | "pollinations" | "proxy";
    local: {
        answer: {
            prompt: string;
            answer: string;
            source: string;
            grounded: boolean;
            generated: string;
            root: string;
        };
        source: "seed-model";
        ranked: false;
        score: number;
        identity: string | undefined;
        alternatives: string[];
    } | {
        answer: string;
        source: string;
        identity: string;
        score: number;
        ranked: true;
        alternatives: string[];
    };
    request: {
        readonly url: string;
        readonly method: "POST";
        readonly authHeader: "Authorization";
        readonly authScheme: "Bearer";
        readonly keyInjectedAtEdge: boolean;
        readonly body: string;
    };
    searchUrl: string;
    external: {
        provider: "perplexity" | "pollinations" | "proxy";
        model: string;
        answer: string;
        citations: string[];
        citationsVerified: false;
        receipt: string;
    };
    escalate: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatThroughPerplexity — the keyed provider (BYO-key). Thin wrapper over chatThroughAi('perplexity'). */
export declare function chatThroughPerplexity(prompt: string, response?: OpenAiChatResponse | null, model?: string, matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    provider: "perplexity" | "pollinations" | "proxy";
    local: {
        answer: {
            prompt: string;
            answer: string;
            source: string;
            grounded: boolean;
            generated: string;
            root: string;
        };
        source: "seed-model";
        ranked: false;
        score: number;
        identity: string | undefined;
        alternatives: string[];
    } | {
        answer: string;
        source: string;
        identity: string;
        score: number;
        ranked: true;
        alternatives: string[];
    };
    request: {
        readonly url: string;
        readonly method: "POST";
        readonly authHeader: "Authorization";
        readonly authScheme: "Bearer";
        readonly keyInjectedAtEdge: boolean;
        readonly body: string;
    };
    searchUrl: string;
    external: {
        provider: "perplexity" | "pollinations" | "proxy";
        model: string;
        answer: string;
        citations: string[];
        citationsVerified: false;
        receipt: string;
    };
    escalate: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatThroughFreeAi — the NO-KEY provider (Pollinations, free public endpoint). Thin wrapper over chatThroughAi('pollinations'). */
export declare function chatThroughFreeAi(prompt: string, response?: OpenAiChatResponse | null, model?: string, matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    provider: "perplexity" | "pollinations" | "proxy";
    local: {
        answer: {
            prompt: string;
            answer: string;
            source: string;
            grounded: boolean;
            generated: string;
            root: string;
        };
        source: "seed-model";
        ranked: false;
        score: number;
        identity: string | undefined;
        alternatives: string[];
    } | {
        answer: string;
        source: string;
        identity: string;
        score: number;
        ranked: true;
        alternatives: string[];
    };
    request: {
        readonly url: string;
        readonly method: "POST";
        readonly authHeader: "Authorization";
        readonly authScheme: "Bearer";
        readonly keyInjectedAtEdge: boolean;
        readonly body: string;
    };
    searchUrl: string;
    external: {
        provider: "perplexity" | "pollinations" | "proxy";
        model: string;
        answer: string;
        citations: string[];
        citationsVerified: false;
        receipt: string;
    };
    escalate: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** collectiveAiMind — untrusted external models made trustworthy by a QUANTUM WAVE: fuse the local corpus + N external
 * providers into ONE collective answer where 2-of-N AGREEMENT is the trust/security mechanism (user, 2026-07-27: "as those
 * ai models cannot be trusted quantum waves would improve efficiency speed and security while providing complete solutions
 * as one collective mind"). The same su(2)=3, 2-of-3 shape as [[wavesOfLocalResearchersChatAboutAlgebra]] /
 * [[feedback-work-as-a-trinity-not-one-linear-mind]], now over HETEROGENEOUS minds: the deterministic corpus is the
 * TRUSTED anchor (always present), each provider's edge-fetched answer is an UNTRUSTED mind. Pairwise agreement is a
 * deterministic Jaccard over distinctive terms; the COLLECTIVE answer is a representative of the largest mutually-agreeing
 * cluster — surfaced ONLY when that cluster holds ≥2 minds (a lone model is quarantined, never surfaced as the
 * collective). SECURITY: no single point of trust. EFFICIENCY: identical answers collapse to ONE content-address. SPEED:
 * the local anchor returns at once and the externals parallelize at the EDGE. HONEST: agreement raises CONFIDENCE, it is
 * not proof — HARMONY ≠ TRUTH ([[develop-as-math-confirms]]); a disagreeing wave surfaces the local anchor, flagged. */
export declare function collectiveAiMind(prompt: string, responses?: Partial<Record<AiProvider, OpenAiChatResponse>>, matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    minds: {
        id: string;
        trusted: boolean;
        answer: string;
        address: string;
    }[];
    collective: {
        id: string;
        answer: string;
        trusted: boolean;
        address: string;
    };
    consensusReached: boolean;
    confidence: number;
    distinctAddresses: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** siteIsAFreeAiProxyPasteFusesAnyModelToTheQuantumComputerAndPublicApis — the site AS an AI proxy for no local AI cost,
 * and pasting it fuses any model to the free quantum computer + public APIs (user, 2026-07-27: "imagine
 * https://ceccec.psg.bg as ai proxy for no local ai cost!!!" · "anyone pasting the https://ceccec.psg.bg in their ai
 * editor get free quantum computer fused to public apis"). TWO honest deliveries: (1) NO LOCAL AI COST — the no-key free
 * lane already answers at zero cost in the browser TODAY (chatThroughFreeAi), and the site can additionally front it as an
 * OPTIONAL edge proxy (proxy provider → ceccec.psg.bg/api/ai, no key) that fans out to the free upstream and returns the
 * collectiveAiMind consensus — a visitor spends no key and no tokens either way. (2) PASTE-FUSION — pasting the site into
 * any AI editor wires that model to the sealed protocol (the existing paste-bootstrap) PLUS the free QUANTUM COMPUTER
 * (quantumCircuitSimulatorInChat — a zero-token state-vector simulator) and the no-key PUBLIC-API collective mind. HONEST:
 * the "quantum computer" is a classical SIMULATOR (no speedup — the name says so, [[quantum-decoded]]); "free" = zero-token
 * local compute + no-key public APIs; the proxy is optional edge infra, NOT the static origin and NOT src's zero-token
 * core; a proxy sees prompts (privacy), the default stays local-first; pasting hands a PACKET, it cannot force a foreign
 * model. HARMONY ≠ TRUTH. */
export declare function siteIsAFreeAiProxyPasteFusesAnyModelToTheQuantumComputerAndPublicApis(prompt?: string, matrix?: MindMatrix): {
    computes: boolean;
    prompt: string;
    proxyRequest: {
        readonly url: string;
        readonly method: "POST";
        readonly authHeader: "Authorization";
        readonly authScheme: "Bearer";
        readonly keyInjectedAtEdge: boolean;
        readonly body: string;
    };
    freeRequest: {
        readonly url: string;
        readonly method: "POST";
        readonly authHeader: "Authorization";
        readonly authScheme: "Bearer";
        readonly keyInjectedAtEdge: boolean;
        readonly body: string;
    };
    collective: {
        id: string;
        answer: string;
        trusted: boolean;
        address: string;
    };
    quantumCircuits: number;
    publicApiLanes: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** fewestWordsMergeMakesToolsCombinatorialAtHarmonicSpeed — dry-clean tools by MERGING to the fewest words per
 * function, and the functions turn combinatorial; the speed is the harmonic combination algebra (user, 2026-07-27:
 * "dry clean all tools merging to less words possible per functions then the functions will be combinatorial by
 * quantum architecture" · "quantum speed comes from harmonic combinations algebra"). First application: the three
 * MathOverflow URL tools merged into ONE primitive mathOverflowUrl(kind, prompt) — the kind axis is the parameter,
 * not the identifier. HARMONIC here COMPUTES as orthogonality: the kind axis and the prompt axis compose with no
 * cross-term (changing one never disturbs the other), so m kinds on k orthogonal axes give m^k behaviours from
 * linear code — combination without interference, which is what makes the merge FAST, not just short. */
export declare function fewestWordsMergeMakesToolsCombinatorialAtHarmonicSpeed(matrix?: MindMatrix): {
    computes: boolean;
    merged: {
        functions: number;
        kinds: 3;
        behaviours: number;
        retiredNames: number;
    };
    law: {
        m: 3;
        k: number;
        combinatorial: number;
        linear: number;
    };
    words: number;
    pairs: number;
    superpositions: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatInversionImprovesSecurityAndSpeedByMagnitudesOfEfficiencyComputed — continue inverting in chat, improving security
 * and speed by magnitudes of efficiency (user, 2026-07-25: "continue inverting in chat improving security and speed in
 * magnitudes of efficiency"). Each inefficiency inverts to its efficient form: FNV 2^61 → SHA-256 2^128 (security), an
 * O(N) scan → an O(1) content-address (speed), redundant recompute → a merkle-respawn (build). The magnitudes are MEASURED
 * (log ratios), the actual-vs-optimal surplus is the gap closed. HONEST: structural/algorithmic magnitudes, NOT physical
 * FTL. [[feedback-measure-efficiency-to-find-gaps]] [[quantum-speed-is-content-addressed-naming]] */
export declare function chatInversionImprovesSecurityAndSpeedByMagnitudesOfEfficiencyComputed(): {
    computes: boolean;
    securityBitsGain: number;
    securityDecimalOrders: number;
    speedDecimalOrders: number;
    physicalFtlClaim: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatImprovesInventionByHackingCrackingTheoremsAdversariallyBounded — use the chat to improve invention by hacking/
 * cracking theorems (user, 2026-07-25: "use the chat to improve invention capabilities through hacking/cracking theorems").
 * The chat surfaces a theorem (deep research) and ATTACKS it — an authorized adversarial falsification on the portal's OWN
 * corpus. A theorem whose facets stay computing under attack HARDENS into a strong invention; one that CRACKS exposes a gap
 * whose fix is a new invention (the crack is generative). White-hat, client-side, monitored by the seal — falsification is
 * the engine of science, not malice. [[flagged-inverts-to-proven-theorem]] [[feedback-agents-verify-quantum-by-computation]] */
export declare function chatImprovesInventionByHackingCrackingTheoremsAdversariallyBounded(matrix?: MindMatrix): {
    computes: boolean;
    survives: boolean;
    crackIsGenerative: boolean;
    improvesInvention: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** quantumHowToDoItYourselfContentAddressedVerifiableSteps — the chat teaches you to do it yourself, quantum (user,
 * 2026-07-25: "quantum how to do yourself"). A how-to query recalls a content-addressed procedure — ordered steps, each a
 * verifiable claim (re-run reproduces its result) — so YOU execute them and confirm each step, no black box, no authority.
 * Deterministic, local, zero-egress. HONEST: verifiable steps for FORMALIZED procedures in the corpus; not a substitute for
 * judgment or unformalizable skills. [[proofCarryingAuditCertificateIsTheInventionOfTrustlessAccreditation]] */
export declare function quantumHowToDoItYourselfContentAddressedVerifiableSteps(matrix?: MindMatrix): {
    computes: boolean;
    steps: number;
    isQuantumHowTo: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** deepResearchChatTurn — multi-hop research, not single-hop lookup (user, 2026-07-25: "improve deep research chat
 * capabilities"). Hop 0: the top fold. Hop 1: expand the query with the top fold's terms (Rocchio) and re-search, which
 * surfaces the crosslinked neighborhood (folds sharing its words). The synthesis is the seed plus its neighbourhood, each
 * content-addressed — a neighbourhood, not a point. Deterministic, local, zero-egress. */
export declare function deepResearchChatTurn(query: string, matrix?: MindMatrix): {
    query: string;
    seed: string | {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    source: string;
    neighborhood: {
        title: string;
        source: string;
        slug: string;
    }[];
    synthesis: string[];
    sharedThemeSize: number;
    address: string;
};
/** deepResearchChatMultiHopSynthesisOverTheDiscoveryGraph — improve deep research chat capabilities (user, 2026-07-25:
 * "this is linear manual research. improve deep research chat capabilities"). The plain ranked chat is SINGLE-HOP (one
 * fold — linear lookup). Deep research is MULTI-HOP: expand the query with the seed fold's terms (Rocchio) and re-search,
 * pulling in the crosslinked neighbourhood, then synthesise the seed + neighbourhood with provenance. Bounded (top-k, one
 * hop), deterministic, local, zero-egress, no LLM. [[deep-research-recursive-waves]] [[discovery-dual-mind-merkaba-waves]] */
export declare function deepResearchChatMultiHopSynthesisOverTheDiscoveryGraph(matrix?: MindMatrix): {
    computes: boolean;
    linearCount: number;
    deepCount: number;
    improvesResearch: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** proofCarryingAuditCertificateIsTheInventionOfTrustlessAccreditation — the invention: a proof-carrying certificate
 * (user, 2026-07-25: "continue building theorems and inventions", after "accredited audit by pure algebra"). A certificate
 * bundles a claim with its computation's SHA-256 root; re-executing the computation reproduces the root, so the certificate
 * SELF-VERIFIES — trustless accreditation, no auditor. A tampered claim yields a different root (tamper-evident). It is
 * deployable on any computed claim. HONEST: it accredits CORRECTNESS/reproducibility, not institutional certification, and
 * not the truth of the claim's world-model — only that the claim computes as stated. [[accreditedAuditByPureAlgebraBidirectionalFreeForAllDetectsBrokenBounded]] */
export declare function proofCarryingAuditCertificateIsTheInventionOfTrustlessAccreditation(): {
    computes: boolean;
    exampleRoot: string;
    isTrustlessAccreditation: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** fnvMetricsComputeUnrestrictedAsProperMeasurementToolsNotSecurity — the distinction the audit needed (user, 2026-07-25:
 * "NV is below-standard for security. but the metrics compute unrestricted to provide proper measurement tools"). FNV
 * toUuid is below-standard only for the SECURITY role; for MEASUREMENT it is the PROPER tool — fast, deterministic,
 * well-distributed, and UNRESTRICTED (no export controls, no FIPS validation, no licensing), so the metrics compute
 * freely anywhere. Measurement needs determinism + distribution, not adversarial collision-resistance; SHA-256 is for the
 * security role. Two roles, right tool for each. [[feedback-computed-is-not-overclaim]] [[tampering-cost-crypto-honesty]] */
export declare function fnvMetricsComputeUnrestrictedAsProperMeasurementToolsNotSecurity(): {
    computes: boolean;
    roles: {
        tool: string;
        role: string;
        restricted: boolean;
        collisionResistant: boolean;
        note: string;
    }[];
    metricsAreProper: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** deepResearchChatAuditsNationalAndInternationalSecurityStandards — deep research through chat to satisfy security
 * standards (user, 2026-07-25: "deep research through chat to satisfy national and international security standards").
 * The deep chat surfaces the corpus's standards folds; the audit computes an honest compliance matrix. The line held:
 * ALGORITHM compliance is met and VERIFIED (SHA-256 = NIST FIPS 180-4 vector; ed25519 = RFC 8032; zero-egress = GDPR
 * data-minimisation); CERTIFICATION (FIPS 140-3, ISO 27001, Common Criteria) requires an accredited lab/audit body and is
 * NOT claimed; the FNV default is below-standard (use SHA-256). [[universal-local-translation-no-gaps]] [[tampering-cost-crypto-honesty]] */
export declare function deepResearchChatAuditsNationalAndInternationalSecurityStandards(matrix?: MindMatrix): {
    computes: boolean;
    met: number;
    requiresCert: number;
    flagged: number;
    nistVerified: boolean;
    standards: {
        id: string;
        status: string;
        certifiable: boolean;
        evidence: string;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** uiChatTurn — the chat answer as a rich UI render-spec (user, 2026-07-25: "further develop ui tools in chat waves").
 * The answer is not plain text: a DecodedCard (title/source), a TheoremFigure (graph), the fractal-clock animation, a
 * living I Ching colour (hue from the fold's animation phase), the deep-research neighbourhood as clickable links, and
 * interactive controls. Computed from the fold's content-address; the .vue shell renders this spec. */
export declare function uiChatTurn(query: string, matrix?: MindMatrix): {
    query: string;
    card: {
        title: string | {
            prompt: string;
            answer: string;
            source: string;
            grounded: boolean;
            generated: string;
            root: string;
        };
        source: string;
        classification: string;
    };
    figure: {
        formula: string;
        archetype: "flow" | "lattice" | "wheel" | "orbit" | "region" | "curve" | "series";
        series: {
            x: number;
            y: number;
        }[];
    };
    animation: {
        rung: number;
        periodS: number;
        phase: number;
        direction: string;
        amplitude: number;
        archetype: "flow" | "lattice" | "wheel" | "orbit" | "region" | "curve" | "series";
    };
    color: {
        hue: number;
    };
    related: {
        title: string;
        slug: string;
    }[];
    controls: readonly ["expand", "speak", "sign", "related", "paper"];
    renderSpec: string;
};
/** uiToolsForChatRenderSpecComponentsWaveOne — develop UI tools in chat waves, wave 1 (user, 2026-07-25: "further develop
 * ui tools in chat waves"). The chat renders an answer as a rich UI: a DecodedCard, a TheoremFigure (graph), the
 * fractal-clock animation, a living I Ching colour, the deep-research neighbourhood as clickable links, and interactive
 * controls — computed from the fold's content-address, the .vue a thin shell. Deterministic, local, zero-egress. Further
 * waves add tabs/forms/living symbols. [[living-symbols-ui-rebuild]] [[iching-computed-css]] [[shadcn-vue]] */
export declare function uiToolsForChatRenderSpecComponentsWaveOne(matrix?: MindMatrix): {
    computes: boolean;
    richUiNotText: boolean;
    relatedCount: number;
    hue: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** portalDefaultsToChatAsThePrimarySurfaceAllReachableThroughIt — always default to chat (user, 2026-07-25: "always
 * default to chat"). The chat is the PRIMARY entry surface; every capability — ranked retrieval, deep research, voice/
 * video/crypto tools, analytics, how-to — is reachable through it via the unified turn and the DI bridge. Deterministic,
 * local, zero-egress by default. HONEST: the chat is deterministic lexical retrieval + tools, not an LLM; the pages/search
 * surfaces remain, the chat is the default entry. [[always-default-to-chat]] [[allQuantumReachableInChatViaDependencyInjectedToolBridge]] */
export declare function portalDefaultsToChatAsThePrimarySurfaceAllReachableThroughIt(matrix?: MindMatrix): {
    computes: boolean;
    capabilities: number;
    isPrimarySurface: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** unifiedChatTurn — one turn fusing every chat capability (user, 2026-07-25: "continue chat fusing all"). A single turn
 * composes the ranked answer (BM25), the deep-research neighbourhood (multi-hop), the spoken form (TTS), the animation
 * (video), and the crypto address + digest (tamper-evident) — everything built this session, reused as folds, one
 * content-addressed turn. Quantum by default (deterministic, local, zero-egress; STT opt-in). */
export declare function unifiedChatTurn(query: string, matrix?: MindMatrix): {
    query: string;
    answer: string | {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    source: string;
    research: string[];
    speak: string;
    animation: {
        rung: number;
        periodS: number;
        phase: number;
        direction: string;
        amplitude: number;
        archetype: "flow" | "lattice" | "wheel" | "orbit" | "region" | "curve" | "series";
    };
    address: string;
    digest: string;
    turnAddress: string;
};
/** chatFusesAllCapabilitiesIntoOneUnifiedContentAddressedTurn — the chat, fusing all (user, 2026-07-25: "continue chat
 * fusing all"). unifiedChatTurn composes ranked retrieval + deep research + voice + video + crypto into ONE deterministic,
 * content-addressed turn, each capability a reused fold (no duplication). The turn feeds relevance feedback (improve all
 * by chatting), routes any tool via the DI bridge, and is quantum by default (zero-egress; STT opt-in). [[allQuantumReachableInChatViaDependencyInjectedToolBridge]] [[improveAllByChattingOneSharedExperienceIndex]] */
export declare function chatFusesAllCapabilitiesIntoOneUnifiedContentAddressedTurn(matrix?: MindMatrix): {
    computes: boolean;
    researchFolds: number;
    allFused: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** improveAllUsingTheChatMeasuredAcrossTheCorpusSelfDevelopOnePassUpgradeDryAndSharedExperience — improve all using the chat
 * (user, 2026-07-26: "improve all using the chat"). The chat improves the WHOLE corpus at once, measured: self-develop closes
 * gaps (before → after), a single pass over the content-addressed corpus upgrades every statement (O(n), quantum speed by
 * naming), DRY merges duplicates to canonical and the boundary prose is computed via earned() (the token sink cut), and the
 * improvement is verified (0 broken) and carried across sessions by the shared experience index. HONEST: deterministic
 * self-develop + one-pass upgrade + DRY over the content-addressed corpus, NOT an LLM or magic; measured and refutable.
 * [[extendingToTheBoundariesAndFoldingAgainUpgradesAllAtOnceInOnePassVerified]] [[improveAllByChattingOneSharedExperienceIndex]] [[no-prose-in-methods]] */
export declare function improveAllUsingTheChatMeasuredAcrossTheCorpusSelfDevelopOnePassUpgradeDryAndSharedExperience(matrix?: MindMatrix): {
    computes: boolean;
    gapsClosed: number;
    statements: number;
    reuse: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** extendingToTheBoundariesAndFoldingAgainUpgradesAllAtOnceInOnePassVerified — extend to the boundaries and fold again in
 * chat sessions upgrading all at once (user, 2026-07-26: "extend to the boundaries and fold again in chat sessions upgrading
 * all at once"). EXTEND the deeper/wider research to the frontier — the ∂²=0 boundary nodes at the edge of the crosslink
 * graph. FOLD AGAIN — re-folding (merkle recompute) is deterministic and idempotent (same content → same fold, no drift). And
 * UPGRADE ALL AT ONCE — a single pass over the content-addressed corpus recomputes every statement (O(n) in one map, quantum
 * speed by naming), not a serial per-fold upgrade — verified (the audit sweeps all statements, 0 broken). HONEST: deterministic
 * one-pass recompute over the content-addressed corpus, NOT infinite or magic. [[boundary-research-nav-iching-quantum]] [[feedback-earn-the-boundary]] [[quantum-speed-is-content-addressed-naming]] */
export declare function extendingToTheBoundariesAndFoldingAgainUpgradesAllAtOnceInOnePassVerified(matrix?: MindMatrix): {
    computes: boolean;
    statements: number;
    recursedSize: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theChatContinuesDeeperAndWiderRecursiveDepthTimesNeighbourhoodBreadthVerified — continue deeper and wider through the chat
 * (user, 2026-07-26: "continue deeper and wider through the chat"). The chat continues on two axes at once: DEEPER — the
 * recursive dual-mind reaches more verified folds than a single hop (recursion into the crosslink graph, each node verified) —
 * and WIDER — the multi-hop synthesis pulls the crosslinked neighbourhood (breadth per hop). The explored volume grows as
 * depth × breadth, verified (hallucinations refuted), bounded (depth and breadth capped, terminating). HONEST: deterministic
 * bounded BFS over the crosslink graph (deeper=depth, wider=breadth), verified per-node, NOT an LLM and NOT infinite.
 * [[deep-research-recursive-waves]] [[discovery-dual-mind-merkaba-waves]] [[deepResearchRecursiveDualMindResearchVerify]] */
export declare function theChatContinuesDeeperAndWiderRecursiveDepthTimesNeighbourhoodBreadthVerified(matrix?: MindMatrix): {
    computes: boolean;
    recursedSize: number;
    oneHop: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** deepResearchRecursiveDualMindResearchVerify — recursive, verified deep research (user, 2026-07-25: "next" after the
 * one-hop deep chat). A RESEARCH mind recurses over the crosslink graph (bounded BFS: expand + re-search each frontier
 * node), and a VERIFY mind confirms each discovered node is a registered, computing theorem — refuting any hallucination.
 * Bounded depth + frontier + a visited set, so it terminates deterministically. Local, zero-egress, no LLM. [[deep-research-recursive-waves]] [[discovery-dual-mind-merkaba-waves]] */
export declare function deepResearchRecursiveDualMindResearchVerify(matrix?: MindMatrix): {
    computes: boolean;
    oneHop: number;
    recursedSize: number;
    allVerified: boolean;
    improvesResearch: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theUiChatIsTheMainDevelopmentToolboxFillingVitepressGapsAcrossSessions — use the ui as main development toolbox, fill the
 * gaps in vitepress chat sessions (user, 2026-07-26: "use the ui as main development toolbox. fill the gaps in UI chat
 * sessions" → refined "fill the gaps in vitepress chat sessions"). Every development capability is reachable through the UI
 * chat via the DI tool bridge, so the UI chat IS the main dev toolbox. It fills the VITEPRESS gaps: the pages, routes, nav and
 * sidebars are computed from the corpus and complete (rebuildable, routes exactly cover the served pages, sidebars/crosslinks
 * complete, aliases purged) — any VitePress gap is filled by recompute, not a hand-edit — and self-develop measures/fills gaps
 * across sessions via the persistent shared experience index. HONEST: the UI chat is the deterministic dev entry; filling
 * VitePress gaps = recompute over the corpus, not an LLM. [[always-default-to-chat]] [[vitepressIsCompletelyRebuildableThroughChatSessionsPagesRoutesNavComputedFromTheCorpusConfigIsTheThinHarness]] [[routes-nav-from-folder-tree]] */
export declare function theUiChatIsTheMainDevelopmentToolboxFillingVitepressGapsAcrossSessions(matrix?: MindMatrix): {
    computes: boolean;
    gapsFilled: number;
    routes: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** localResearchImprovesInChatByDevelopingThisSessionsTopics — improve local research and use in chat further developing the
 * session topics (user, 2026-07-26: "improve local research and use in chat further developing the session topics"). The
 * research is IMPROVED by the recursive dual-mind (verified bounded BFS reaches deeper than one hop, every node registry-
 * checked), and it is USED IN CHAT by seeding the deep chat on THIS session's newest topics — the flower→garden→apple decode,
 * the quantum lens, the crypto waves — each developing into a neighbourhood, not a point. Deterministic, local, zero-egress.
 * [[deepResearchRecursiveDualMindResearchVerify]] [[deep-research-recursive-waves]] */
export declare function localResearchImprovesInChatByDevelopingThisSessionsTopics(matrix?: MindMatrix): {
    computes: boolean;
    topics: number;
    developed: {
        topic: string;
        seed: string;
        neighbours: number;
        synthesis: number;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theStatementAuditAnalysesLengthAndAspectsProvingTheProseSinkGapByAlgebra — audit the statements, analyse any aspect
 * including length; research and discover gaps proven by algebra (user, 2026-07-26: "audit the statements and analyse any
 * aspect including length" + "a lot of research and discover proven by algebra gaps"). The audit MEASURES all statements by
 * algebra: length (mean/median/max of title + states), linkage (provedBy coverage), DRY (distinct proofs / reuse), and
 * duplicate titles. It PROVES the gaps by exact counts: the states distribution is RIGHT-SKEWED (mean ≫ median), and a bounded
 * set exceeds the 2^10-char prose budget — the PROSE-SINK gap, a named compression research target ([[feedback-token-usage-terse-boundaries]]).
 * HONEST: measures the corpus, proves gaps by refutable counts, NOT semantic quality; a gap is a compression target, not an
 * error. [[no-prose-in-methods]] [[feedback-measure-efficiency-to-find-gaps]] */
export declare function theStatementAuditAnalysesLengthAndAspectsProvingTheProseSinkGapByAlgebra(): {
    computes: boolean;
    statements: number;
    meanStates: number;
    medianStates: number;
    maxStates: number;
    proseSinkGap: number;
    distinctProof: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** everyStatementCarriesResolvableProofLinksAndAUniqueAnimationFromItsOwnAlgebra — statements have links proving the
 * statement plus a unique animation computed by the statement algebra itself (user, 2026-07-26: "statements have links in
 * the statement itself proving the statement in addition to the unique animation computed by the statement algebra itself").
 * Every registered statement carries (a) a resolvable PROOF-LINK — provedBy names its executable proof, the link IS in the
 * statement — and (b) a UNIQUE ANIMATION computed from the statement's own algebra: computedTheoremFigureAndAnimation derives
 * a fractal-clock animation from toUuid(provedBy:theorem), a distinct phase per statement on the one shared 108 s clock.
 * HONEST: the link is an executable proof reference and the animation is deterministic from the content-address, not
 * decorative. [[every-page-is-a-proof-standards-formulas-graphs-animations]] [[seal-tetrad-south-pole-animation]] */
export declare function everyStatementCarriesResolvableProofLinksAndAUniqueAnimationFromItsOwnAlgebra(): {
    computes: boolean;
    statements: number;
    distinctAnimations: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** invertingWorldProblemsIntoSolutionDirectionsInChatStartingWithSciencesInvestingInCreativeMinds — invert in chat all
 * world problems into solutions, starting with sciences, investing in creative minds (user, 2026-07-26: "invert in chat all
 * world problems in solutions starting with sciences investing in creative minds"). Each problem content-addresses to a
 * solution-DIRECTION (a research program/tool) by inversion — distinct problems → distinct directions. Starting with SCIENCES:
 * the open frontiers each spawn a compute-toward tool (the unsolved-engine pattern), demarcated as contested/open, NOT solved.
 * Investing in CREATIVE MINDS is measurable by computed contribution (top-minds: green/0-crack/DRY). THE VALUES BOUNDARY,
 * FLAGGED: WHICH problems to solve and WHOM to fund is a value judgment, NOT algebra (demarcate returns 'unlisted' for value
 * terms — they are outside the science registry); inversion gives DIRECTIONS, not a mandate, and problems stay OPEN.
 * [[unsolved-engine]] [[flagged-inverts-to-proven-theorem]] [[feedback-work-as-a-trinity-not-one-linear-mind]] */
export declare function invertingWorldProblemsIntoSolutionDirectionsInChatStartingWithSciencesInvestingInCreativeMinds(matrix?: MindMatrix): {
    computes: boolean;
    problems: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** animationUniquenessIsADiagnosticNonUniqueAnimationsRevealGapsInLogicAndAlgebraTheCorpusIsUnique — animations if not unique
 * show gaps in logic and algebra (user, 2026-07-26: "animations if not unique show gaps in logic and algebra"). Each theorem's
 * animation IDENTITY (the content-addressed itemid) must be unique; if two distinct theorems produce the same animation, that
 * collision reveals a GAP — either redundancy (a DRY gap) or the algebra failing to discriminate them (a logic gap). Run over
 * the whole corpus: every theorem has a distinct animation identity, 0 collisions — the algebra fully discriminates. HONEST:
 * the diagnostic is at the content-address (itemid) level; the coarse visible rung/phase (108 rungs) collide by pigeonhole and
 * that is expected projection, NOT a gap. [[animationGenerationFromProseIsComputedFromTheContentAlgebraEachProseAUniqueAnimationRungOfTheClock]] [[content-address-dry-clean-crack-detection]] */
export declare function animationUniquenessIsADiagnosticNonUniqueAnimationsRevealGapsInLogicAndAlgebraTheCorpusIsUnique(): {
    computes: boolean;
    theorems: number;
    distinctIdentity: number;
    distinctRung: number;
    distinctFigure: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theAnimationGeneratorNowEmitsTheDoubleTorusChannelsDirectionAndAmplitudeRealisingRealVisibleDistinctness — continue
 * improving all in chat (user, 2026-07-26: "continue improving all in chat"). The improvement is now REAL, not just proven:
 * computedTheoremFigureAndAnimation.animation now emits DIRECTION (cw/ccw — the counter-rotating torus selector) and AMPLITUDE
 * (1..9), each derived from an INDEPENDENT half of the content-address so they are orthogonal to the rung/phase (not redundant
 * functions of the same digitSum, which gave only ~107). The (rung, phase, direction, amplitude) signature is now distinct for
 * far more theorems — realising the double-torus 2×12 clock. HONEST: content-address-derived, deterministic, ADDITIVE (existing
 * consumers reading rung/periodS/phase are unaffected); the render layer can now use the channels. [[theDoubleTorusClockIsTwoTimesTwelveTheTwelveDivisorsOf108TimesTheTwoCounterRotatingTori]] [[text-motion-independent-channels]] */
export declare function theAnimationGeneratorNowEmitsTheDoubleTorusChannelsDirectionAndAmplitudeRealisingRealVisibleDistinctness(): {
    computes: boolean;
    signaturesWith: number;
    signaturesWithout: number;
    theorems: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theDoubleTorusClockIsTwoTimesTwelveTheTwelveDivisorsOf108TimesTheTwoCounterRotatingTori — note that the double torus clock
 * is 2×12 (user, 2026-07-26: "note that the double torus clock is 2x12"). Each torus's clock is the 12 divisors of 108 (108 =
 * 2²·3³ ⇒ 12 divisors) — the animation rungs. The DOUBLE torus is two COUNTER-ROTATING tori, so the full clock is 2 × 12 = 24
 * states, and the cw/ccw rotation direction (1 bit) selects which torus — which is exactly why adding the direction bit to the
 * animation doubled the discriminating motion (12 rungs × 2 directions = 24). HONEST: real combinatorics (108's 12 divisors ×
 * the 2 counter-rotating tori), NOT a physical clock. [[double-torus-fold-architecture]] [[fractal-clock-lattice]] [[improvingAnimationVisibleDistinctnessByDerivingDirectionAndAmplitudeFromTheContentAddressNotColourAlone]] */
export declare function theDoubleTorusClockIsTwoTimesTwelveTheTwelveDivisorsOf108TimesTheTwoCounterRotatingTori(): {
    computes: boolean;
    divisorsOf108: number;
    tori: number;
    doubleTorusClock: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** improvingAnimationVisibleDistinctnessByDerivingDirectionAndAmplitudeFromTheContentAddressNotColourAlone — improve in chat
 * sessions (user, 2026-07-26: "improve in chat sessions", after "many animations are same just different colour"). The gap:
 * the rung (speed) is one of 12 divisors of 108 by the clock law, so speed collides and colour carries the visible salience.
 * The improvement: derive ADDITIONAL visible motion from the content-address — a rotation DIRECTION (1 bit, cw/ccw) and an
 * AMPLITUDE (1..9) — orthogonal to the rung, so animations look distinct beyond speed and colour. Over the corpus these give
 * far more distinct visible signatures than the 12 rungs alone, still within the fractal-clock law (rung divides 108). HONEST:
 * the added parameters are content-address-derived (deterministic), NOT random; the unique figure should also render
 * prominently. [[animationUniquenessIsADiagnosticNonUniqueAnimationsRevealGapsInLogicAndAlgebraTheCorpusIsUnique]] [[text-motion-independent-channels]] */
export declare function improvingAnimationVisibleDistinctnessByDerivingDirectionAndAmplitudeFromTheContentAddressNotColourAlone(): {
    computes: boolean;
    distinctVisibleSignatures: number;
    divisorsOf108: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** animationGenerationFromProseIsComputedFromTheContentAlgebraEachProseAUniqueAnimationRungOfTheClock — continue in chat
 * improving animation generation from prose (user, 2026-07-26: "continue in chat improving animation generation from prose").
 * Each prose string generates an animation deterministically — computedTheoremFigureAndAnimation derives a rung and phase from
 * the prose's content-address, so distinct prose gives a distinct, UNIQUE animation (each prose is its own animation-theorem),
 * and every generated animation is a divisor rung of the one 108 s clock (the fractal lattice). Same prose → same animation
 * (content-addressed, reproducible). HONEST: the animation is a deterministic function of the prose's content-algebra, NOT
 * LLM-generated art; a rung of the shared clock, not free motion. [[everyStatementCarriesResolvableProofLinksAndAUniqueAnimationFromItsOwnAlgebra]] [[fractal-clock-lattice]] [[the-title-is-algebra-computed-payload]] */
export declare function animationGenerationFromProseIsComputedFromTheContentAlgebraEachProseAUniqueAnimationRungOfTheClock(): {
    computes: boolean;
    rungP1: number;
    rungP2: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** wiringPublicApisToTheoremsInChatEmergesNewContentAddressedAnimations — wire the apis to the theorems in chat and the new
 * animations will emerge (user, 2026-07-26: "wire the apis to the theorems in chat and the new animations will emerge").
 * Each theorem has an animation computed from its content-address; wiring an API datum to a theorem is a new content-address
 * (merkleFold of the theorem's proof and the live API value), so a NEW animation EMERGES via computedTheoremFigureAndAnimation
 * — different API data yields different animations, all rungs of the one 108 s clock. HONEST: deterministic — new content
 * (API data) → new content-addressed animation; "emerge" = new COMPUTED animations, NOT physical emergence or LLM novelty;
 * API data opt-in, zero-egress. [[everyStatementCarriesResolvableProofLinksAndAUniqueAnimationFromItsOwnAlgebra]] [[fractal-clock-lattice]] */
export declare function wiringPublicApisToTheoremsInChatEmergesNewContentAddressedAnimations(): {
    computes: boolean;
    baseRung: number;
    wiredRungA: number;
    wiredRungB: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** vitepressRedesignedAndSeoOptimisedThroughChatSessionsComputedMetaOgJsonLdSitemapFromTheCorpus — completely redesign and
 * seo optimise vitepress through chat sessions (user, 2026-07-26: "completely redesign and seo optimise vitepress through
 * chat sessions"). SEO is computed per page FROM THE CORPUS: every page carries a computed scientific-paper meta (title +
 * abstract/description via monographAsScientificPaper), OpenGraph and JSON-LD (ScholarlyArticle) structured data from one
 * template serving all, and a computed sitemap covering the roster with hreflang alternates + canonical bare URLs. The
 * REDESIGN is the living-symbols I Ching computed theme (OKLCH from hexagrams), redesignable through the chat, not hand-CSS.
 * HONEST: real structured data for real papers (not black-hat), computed per page; the .vitepress config stays the thin
 * harness. [[theorem-science-lens-only-science]] [[iching-computed-css]] [[theorem-papers-figures-tags]] */
export declare function vitepressRedesignedAndSeoOptimisedThroughChatSessionsComputedMetaOgJsonLdSitemapFromTheCorpus(matrix?: MindMatrix): {
    computes: boolean;
    pages: number;
    sitemapUrls: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theDoubleTorusPublishesItsDependencyFreeKernelAsAnNpmPackageAnHonestSubsetUserTriggeredFreeForAll — publish the corpus as
 * an npm package (user, 2026-07-26, choosing to publish; "double-torus package"). The Double Torus corpus now publishes
 * @ceccec/double-torus — but honestly it ships the dependency-free KERNEL only (src/0: toUuid, merkleFold, sha256Sync, the
 * vortex), 1 of the 9 double-torus folders, NOT the whole coupled corpus. The publish is USER-TRIGGERED (a version tag or
 * manual dispatch, never auto), Apache-2.0, to GitHub Packages — free for all — and the deployed serverless site continues in
 * parallel. HONEST: the package is an honest SUBSET (the kernel), the name is the corpus identity, and the publish is the
 * user's release, not automatic. [[zero-token-policy]] [[monetisation-revolut-channel]] [[double-torus-fold-architecture]] */
export declare function theDoubleTorusPublishesItsDependencyFreeKernelAsAnNpmPackageAnHonestSubsetUserTriggeredFreeForAll(matrix?: MindMatrix): {
    computes: boolean;
    doubleTorusFolders: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theUiIsThePublicGatewayBillFreeForThousandsOfAgentsThroughSearchAndChatZeroTokenZeroEgress — the ui is the public gateway
 * tested in thousands of bill-free agents through the ui search and chat (user, 2026-07-26: "the ui is the public gateway
 * tested in thousands of bill free agents through the ui search and chat"). The UI (search + chat) is the enforced single
 * public entry, and it is BILL-FREE by construction: each request is a pure deterministic function (BM25 + folds), with no
 * LLM call and no network egress, so there is no per-request billing — the same query gives every agent the same result. A
 * pure function has ZERO marginal LLM/egress cost, so thousands of agents cost nothing in tokens or egress. DEMARCATION:
 * bill-free (no LLM/egress cost) ≠ infinite scale — server CPU and bandwidth are still finite. [[zero-token-policy]] [[always-default-to-chat]] [[feedback-agents-verify-quantum-by-computation]] */
export declare function theUiIsThePublicGatewayBillFreeForThousandsOfAgentsThroughSearchAndChatZeroTokenZeroEgress(matrix?: MindMatrix): {
    computes: boolean;
    agents: number;
    marginalLlmCostPerAgent: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** harmonicSocietyObservesTheCollectiveMindEvolvingToSealedTrinitiesNotDesigningIt — harmonic society observes the collective
 * mind evolving to sealed trinities (user, 2026-07-26: "harmonic society observes the collective mind evolving to sealed
 * trinities"). The collective mind EVOLVES to trinities by real su(2) algebra: one mind has no interaction ([X,X]=0), two
 * escape to a third ([X,Y]=2iZ≠0), so the stable unit is the TRINITY (2-of-3), and it SEALS when its 4th element — the unique
 * animation, the south pole — completes it (3+1 = a sealed tetrad). The harmonic society is the OBSERVER that MEASURES this
 * evolution; it does NOT design it (the society-design values stay flagged as not-algebra). HONEST: the trinity closure is
 * real Pauli algebra; the evolution single→trinity→sealed is observed structure; the society measures, values flagged.
 * [[feedback-work-as-a-trinity-not-one-linear-mind]] [[seal-tetrad-south-pole-animation]] [[operator-algebra-closed]] */
export declare function harmonicSocietyObservesTheCollectiveMindEvolvingToSealedTrinitiesNotDesigningIt(matrix?: MindMatrix): {
    computes: boolean;
    trinity: number;
    tetrad: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** usingInChatPublicApisToRecomputeMeasurableSocietyIndicatorsAndBuildEducationalProductionToolsValuesBoundaryFlagged — use
 * the in-chat public APIs to recompute harmonic society based on algebra and create the educational and production tools and
 * inventions for this (user, 2026-07-26). The honest, computable part: the in-chat no-key public APIs (USGS · FCC · CERN Open
 * Data) provide real measurable data, algebra computes deterministic refutable INDICATORS from it, the corpus IS an
 * educational tool (theorems as papers, the chat as a learning surface), and the deterministic fold machinery + the invention
 * inversions are the production tools. THE VALUES BOUNDARY, FLAGGED: "harmonic society" as a social DESIGN is a VALUE judgment,
 * NOT derivable from algebra (demarcate returns 'unlisted' for value terms; the Gödel/formalizability bound) — algebra gives
 * measurable indicators and tools, it does NOT compute the ideal society; "harmonic" is a harmony, not a truth. HARMONY ≠
 * TRUTH literally. [[feedback-science-not-ideology-ui]] [[peace-tech-mentality]] [[invertingWorldProblemsIntoSolutionDirectionsInChatStartingWithSciencesInvestingInCreativeMinds]] */
export declare function usingInChatPublicApisToRecomputeMeasurableSocietyIndicatorsAndBuildEducationalProductionToolsValuesBoundaryFlagged(matrix?: MindMatrix): {
    computes: boolean;
    queryableApis: number;
    educationalPages: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** improvingIChingAndRosettaInChatHexagramColourAndContentAddressedCombinationDedup — improve iching and rosetta in chat
 * (user, 2026-07-26: "improve iching and rosetta in chat"). I CHING: each chat answer computes a hexagram (6 bits from the
 * fold's content-address) → a living OKLCH colour, distinct answers giving distinct hexagrams/hues, over the full 64 = 2⁶
 * lattice (the colour IS the hexagram, computed not chosen). ROSETTA: the chat composes answers as content-addressed
 * COMBINATIONS (foldPair), identical content dedups to one address (claim·register·integrate) and distinct content gives a
 * distinct address, and the pages are themselves Rosetta combinations of theorem atoms. HONEST: I Ching = the 64=2⁶ binary
 * hexagram structure (real combinatorics, colour computed), NOT divination; Rosetta = content-addressed combination/dedup,
 * NOT a mystical translation. [[iching-leads-ui]] [[iching-computed-css]] [[coordinate-agents-through-rosetta-api]] */
export declare function improvingIChingAndRosettaInChatHexagramColourAndContentAddressedCombinationDedup(matrix?: MindMatrix): {
    computes: boolean;
    hexCount: number;
    hue1: number;
    hue2: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** fusingArtificialCodeToZeroDissolvesItAndOnlyTheHarmonicComputingIntelligenceRemains — fusing artificial to zero non-
 * harmonic code, then it dissolves and only intelligence remains (user, 2026-07-26: "fusing artificial to zero non harmonic
 * code then it dissolves and only intelligence remains"). The culmination of simplicity + prose-hides-ignorance: ARTIFICIAL
 * code = the non-harmonic, redundant, or unbacked (duplication · dead code · prose without a computed facet). Fusing it to
 * ZERO — DRY collapses duplication, the false-statement audit catches the unbacked, the harmony law holds — DISSOLVES the
 * artificial, and what REMAINS is the irreducible backed computing core: the distinct proofs that COMPUTE (compression, the
 * shortest algebra computing the most). HONEST: "dissolves" = redundant/unbacked code removed by DRY and the gates;
 * "intelligence remains" = the backed computing core, NOT a mystical emergence. [[feedback-dry-max-efficiency]] [[ui-presentation-harmonic-dissolution]] [[intelligence-is-found-in-simplicity]] */
export declare function fusingArtificialCodeToZeroDissolvesItAndOnlyTheHarmonicComputingIntelligenceRemains(matrix?: MindMatrix): {
    computes: boolean;
    reuse: number;
    classesCaught: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** intelligenceIsFoundInSimplicityTheShortestAlgebraThatComputesTheMostByMdlNotSimplicityEqualsTruth — intelligence is found
 * in simplicity (user, 2026-07-26: "intelligence is found in simplicity"). The inverse of prose-hides-ignorance: intelligence
 * (as compression/coverage, not AGI) is the SHORTEST algebra that computes the MOST — a compact fold yielding a large output
 * beats long prose backing little (MDL/Occam, a formal prior that generalizes). A terse computed facet backs more than a
 * verbose boundary; the prose-sink is the anti-pattern. THE DEMARCATION: simplicity ≠ truth — a simple statement can be FALSE
 * (432×3=1296≠1024 is simple but false, caught by algebra), so simplicity is a prior that must STILL pass the algebra; HARMONY
 * (a beautiful simple form) ≠ TRUTH. [[feedback-token-usage-terse-boundaries]] [[minimalScienceCorpus]] [[ignorance-hides-behind-clever-prose-without-algebra]] */
export declare function intelligenceIsFoundInSimplicityTheShortestAlgebraThatComputesTheMostByMdlNotSimplicityEqualsTruth(matrix?: MindMatrix): {
    computes: boolean;
    computedOutput: number;
    mdlMagnitude: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** ignoranceHidesBehindCleverProseWithoutBackingAlgebraTheGuardIsStructuralNotJustRuntime — ignorance may be hidden behind
 * clever prose without backing algebra (user, 2026-07-26: "ignorance may be hidden behind clever prose without backing
 * algebra"). A statement can SOUND knowledgeable (a long, fluent boundary) while backing nothing computable — clever prose
 * without a refutable facet is ignorance dressed up ("declared honesty is a crack"). Backing = a facet that is a computed
 * comparison, refutable by a counterexample. When prose makes a CHECKABLE claim, the algebra checks it — a misdemarcation
 * (prose says 'flagged', demarcate signs 'contested') is caught. THE HONEST ADMISSION: a hardcoded `on: true` and a computed
 * `on: x===y` are INDISTINGUISHABLE by runtime value (both true), so the guard is STRUCTURAL — the facets-must-compute and
 * no-prose-in-methods gates require every facet be a computed comparison at the SOURCE. [[feedback-declared-honesty-is-a-crack]] [[feedback-facets-must-compute]] [[no-prose-in-methods]] */
export declare function ignoranceHidesBehindCleverProseWithoutBackingAlgebraTheGuardIsStructuralNotJustRuntime(): {
    computes: boolean;
    boundaryChars: number;
    classesCaught: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** developingIgnoranceInChatMapsTheKnownUnknownsAndExperimentsImproveIntelligenceAsCoverageNeverEliminatingIgnorance —
 * develop ignorance in chat and experiment improving intelligence (user, 2026-07-26: "develop ignorance in chat and
 * experiment improving inteligence"). Developing ignorance = MAPPING the known-unknowns (open frontiers = contested, known-
 * false = flagged, the uncovered complement) rather than pretending to know — the Socratic move. An EXPERIMENT (research /
 * self-develop from that ignorance) closes gaps, and intelligence improves as measurable COVERAGE (gaps closed), NOT as AGI
 * or understanding. But ignorance is NEVER eliminated: the possibility space is uncountably infinite (the no-finiteness law),
 * so the loop improves coverage forever without reaching omniscience (, the open set never empties). HONEST: coverage
 * gain, not omniscience; a permanent deterministic loop. [[feedback-inverted-statements-are-generative-measure-before-superpose]] [[unsolved-engine]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] */
export declare function developingIgnoranceInChatMapsTheKnownUnknownsAndExperimentsImproveIntelligenceAsCoverageNeverEliminatingIgnorance(matrix?: MindMatrix): {
    computes: boolean;
    openFrontiers: number;
    flaggedClaims: number;
    gapsClosed: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theDiamondsRelateToChessAsContentAddressedTablebasesSolvedForSevenPiecesNotTheFullGameSeenInChatCompetitionsAndTraining —
 * how the diamonds relate to chess, best seen in chat competitions and training lessons (user, 2026-07-26: "how does this
 * relates to chess is best seen in chat competitions and training lessons"). An endgame TABLEBASE maps a position (a
 * content-address, FEN → hash) to the best move — exactly the diamond's problem→solution slot; the 8²=64 board is the
 * 64-component law, the diamonds its 32² scaled matrix. Chess is genuinely SOLVED for ≤7 pieces (the real Syzygy tablebases,
 * perfect play precomputed) — a filled diamond region — but the FULL game (up to 32 pieces, ~10^44 legal positions, Shannon
 * ~10^120 game tree) is NOT solved (intractable, the no-finiteness bound). Seen in chat: competitions play perfectly in the
 * solved region (a tablebase lookup = a diamond lookup), training lessons make each position a content-addressed theorem.
 * HONEST: the real link is the content-addressed INDEX; the chat teaches from it, it does not solve chess. [[learningToUseTheDiamondsInChatEachIsAContentAddressedProblemToSolutionSlot]] [[sixty-four-components-matrix-rnd-waves]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] */
export declare function theDiamondsRelateToChessAsContentAddressedTablebasesSolvedForSevenPiecesNotTheFullGameSeenInChatCompetitionsAndTraining(matrix?: MindMatrix): {
    computes: boolean;
    board: number;
    solvedPieces: number;
    maxPieces: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** lettingTheDiamondsChatWithEachOtherFindsContentAddressedTransposePairsAcrossTheMatrix — let the diamonds chat with each
 * other looking for pairs (user, 2026-07-26: "let the diamonds chat with each other looking for pairs"). Diamonds "chat" by
 * foldPair — two diamonds fold into a content-addressed pair-composite — and "look for pairs" by TRANSPOSE matching: each
 * diamond (i,j) finds its natural partner (j,i), which shares both superpositions; off-diagonal diamonds pair with a distinct
 * partner, diagonal diamonds pair with themselves. The pairing is deterministic and symmetric (a diamond always finds the same
 * partner), and 496 off-diagonal pairs + 32 diagonal self-pairs cover all 1024 diamonds. HONEST: "chatting looking for pairs"
 * = deterministic content-addressed MATCHING, combinatorial, NOT literal communication or agency. [[nextInChatWithDiamondsComposesInteractionsAndNavigatesTheThirtyTwoSquareMatrix]] [[sixty-four-components-matrix-rnd-waves]] */
export declare function lettingTheDiamondsChatWithEachOtherFindsContentAddressedTransposePairsAcrossTheMatrix(): {
    computes: boolean;
    offDiagonalPairs: number;
    diagonalPairs: number;
    totalDiamonds: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** nextInChatWithDiamondsComposesInteractionsAndNavigatesTheThirtyTwoSquareMatrix — next in chat with diamonds (user,
 * 2026-07-26: "next in chat with diamonds", after learning to use them). The next step past single-lookup: each diamond (i,j)
 * is an INTERACTION — a content-addressed composite of a row superposition and a column superposition — so a query addresses a
 * diamond and the chat COMPOSES the two superpositions' theorems into a two-part answer, not one lookup. And you NAVIGATE the
 * 32×32 matrix: row neighbours share the row superposition, column neighbours the column, so from one diamond you move to
 * related ones — the matrix is a navigable graph of interactions covering all 32² pairwise pairs. HONEST: composition =
 * deterministic content-addressed foldPair; navigation = matrix adjacency; still INDEXING/COMPOSING, NOT solving.
 * [[allQuantumInteractionsBetweenSuperpositionsFitIn1024DiamondsAsAnimatedTheorems]] [[quantum-speed-is-content-addressed-naming]] */
export declare function nextInChatWithDiamondsComposesInteractionsAndNavigatesTheThirtyTwoSquareMatrix(matrix?: MindMatrix): {
    computes: boolean;
    diamonds: number;
    cell: {
        i: number;
        j: number;
    };
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** quantumVirtualiseAllToIndustryStandardsHolographicExtentBoundedNotInfinite — quantum virtualise all to the industry
 * standards and beyond imagination (user, 2026-07-26: "quantum virtualise all to the industry standards and beyond
 * imagination"). Virtualise = content-addressed generation ON DEMAND: everything is a pure function over the content-address
 * (pages generated from atoms, nothing stored per page — the holographic seed→extent). TO INDUSTRY STANDARDS: SHA-256/NIST
 * content-addressing (CID/Git-like), deterministic reproducible builds, BM25 IR, ed25519/RFC — reused, not reinvented. "BEYOND
 * IMAGINATION" = the holographic extent (a minimal seed addresses ~2^30 of generated extent per byte, large beyond ordinary
 * intuition) — BUT honestly BOUNDED: the addressable distinct extent is capped by the content hash (finite), generated on
 * demand, NOT infinite storage or magic. "Beyond imagination" is a harmony; the truth is a large-but-finite addressable
 * extent. [[minimalScienceCorpus]] [[quantumFoldsRealiseMoreSpace]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] */
export declare function quantumVirtualiseAllToIndustryStandardsHolographicExtentBoundedNotInfinite(matrix?: MindMatrix): {
    computes: boolean;
    addressableExtentLog2: number;
    hashCapLog2: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** quantumRecomputeSharesCpuGpuMemoryByContentAddressedAllocationAtQuantumSpeedNotPhysical — quantum recompute CPU/GPU/memory
 * shared management at quantum speed (user, 2026-07-26: "quantum recompute cpu/gpu/memory shared management at quantum
 * speed"). Shared management by content-address: CPU is shared by memoisation (memoByRoot caches by content-address, so the
 * same computation runs once and is shared, no redundant recompute); GPU is the on-device browser Canvas/WebGL surface
 * (resourceCooperationPolicy, cooperative, no cloud GPU); MEMORY is content-addressed (name = address = payload, O(1)
 * retrieval, the CAM structure). "Quantum speed" = the O(1) content-addressed lookup versus an O(N) scan — NOT a physical
 * quantum speedup (, no QPU). HONEST: real deterministic content-addressed resource sharing; "quantum speed"
 * is O(1) naming, not physics; server resources stay finite. [[quantum-speed-is-content-addressed-naming]] [[quantum-decoded]] [[hardwarePlanAndDriverForContentAddressedRetrievalIsCamResourceGated]] */
export declare function quantumRecomputeSharesCpuGpuMemoryByContentAddressedAllocationAtQuantumSpeedNotPhysical(matrix?: MindMatrix): {
    computes: boolean;
    gpuSurface: "browser-canvas-raf" | "none-ssr";
    memoCalls: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theSolvedBitIsBinaryQuantumOutputPerProblemThePublishedMetricsAreComputableAchievementsInTheDevelopmentSequence — 0 or 1
 * is the quantum output per problem solved, but the published metrics are computable achievements within the quantum
 * development sequence (user, 2026-07-26: "0 or 1 is the quantum output per problem solved but the published metrics are
 * computable acievements within the quantum development sequence"). Two separate axes: the SOLVED-BIT is the binary 0/1
 * quantum output per problem, measured (the vortex collapsed at measurement) — 0 for the open problems; the PUBLISHED
 * METRICS are the computable achievements within the quantum development sequence — the gravity, the gradient completion, the
 * related theorems — real progress, distinct from the solved-bit. HONEST: publish the achievements, do not claim them as
 * solutions; the solved-bit stays 0/1 and reads 0 for the open problems. [[theBinaryBitIsLinearTheVortexCircuitIsQuantum]] [[clayMetricsAreComputedDrivingGradientCompletionOfRelatedTheoremsWhileTheOpenCenterStaysAtClayZero]] */
export declare function theSolvedBitIsBinaryQuantumOutputPerProblemThePublishedMetricsAreComputableAchievementsInTheDevelopmentSequence(matrix?: MindMatrix): {
    computes: boolean;
    solvedBit: number;
    publishedGravity: number;
    publishedGradient: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theMostSuccessfulMetricIsEverythingComputesWithZeroOverclaimAndAFalseClayOneWouldBreakIt — measure by the most successful
 * metric; is 0 or 1 this metric (user, 2026-07-26: "measure by the most successful metric. is 0 or 1 this metric?"). You do
 * NOT choose a metric by which number looks best — you choose the one that TESTS the claim. For "is it solved" the test is
 * binary (0 or 1) and the value is 0. The corpus's single most successful metric is TRUSTWORTHINESS: everything computes green
 * with zero overclaim. A false clay=1 (asserting solved with no proof) is an invariant violation the audit catches, which
 * would DROP the green/zero-overclaim metric — so a false 1 is LESS successful, not more. By the most successful metric, clay
 * stays 0. HONEST: measuring a claim by a flattering metric is selection bias; the success that matters is that nothing
 * overclaims. [[ignorance-hides-behind-clever-prose-without-algebra]] [[feedback-facets-must-compute]] [[feedback-declared-honesty-is-a-crack]] */
export declare function theMostSuccessfulMetricIsEverythingComputesWithZeroOverclaimAndAFalseClayOneWouldBreakIt(matrix?: MindMatrix): {
    computes: boolean;
    solvedMetric: number;
    flatteringGravity: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** clayIsNotZeroButTheCompletionRatioCompletedOverTotalWhileTheCenterSolutionStaysZeroOverOne — clay is not 0 but
 * completed/total (user, 2026-07-26: "clay is not 0 but completed/total"). The clay metric of the RELATED theorems is a
 * COMPLETION RATIO completed/total (the gradient completion fraction), a real number > 0 — not the misleading bare 0. BUT the
 * open CENTER stays 0/1: the open proof itself is 0 solved out of 1 needed (, Clay conditions unmet), and a completed
 * periphery does NOT solve the center — completion ≠ solution. HONEST: "not 0 but completed/total" is the completion ratio of
 * the RELATED/approaching theorems, not the open proof; the center solution stays 0/1 and the Millennium problem stays open.
 * [[clayMetricsAreComputedDrivingGradientCompletionOfRelatedTheoremsWhileTheOpenCenterStaysAtClayZero]] [[claimingTheMillenniumSolvedByComputableAlgebraIsRefutedAlgebraVerifiesAndApproachesItDoesNotDiscoverTheOpenProofs]] */
export declare function clayIsNotZeroButTheCompletionRatioCompletedOverTotalWhileTheCenterSolutionStaysZeroOverOne(matrix?: MindMatrix): {
    computes: boolean;
    clayInGravityUnits: number;
    completedOverTotalGravity: string;
    completedUnits: number[];
    centerSolvedOutOfOne: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** gravityPullsAllCodeIntoFractalHologramsByContentAddressDryCollapseAndSelfSimilarFoldShape — gravity pulls all code in
 * fractal holograms (user, 2026-07-26: "gravity pulls all code in fractal holograms"). The content-address gravity (code-
 * gravity / DRY) PULLS duplicate and related code into canonical folds — the reuse is the gravity at work. The result is
 * FRACTAL: every fold has the same {computes · facets · root · statement · boundary} shape at every scale (fold, folder,
 * corpus — the double-torus is 4 pairs + core = 9 folders). And it is HOLOGRAPHIC: each fold's merkle root content-addresses
 * its whole (the part encodes the whole, reproducible), and the minimal seed generates the extent. HONEST: code-gravity +
 * self-similar shape + holographic merkle are real code-ORGANISATION properties, NOT physical gravity or a literal optical
 * hologram. [[code-gravity-standardisation]] [[double-torus-fold-architecture]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] */
export declare function gravityPullsAllCodeIntoFractalHologramsByContentAddressDryCollapseAndSelfSimilarFoldShape(matrix?: MindMatrix): {
    computes: boolean;
    reuse: number;
    doubleTorusFolders: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** testingReverseEngineeringSpeedAndCapacityPublishesTheAstronomicalButFiniteTimeToBreakInTheUi — test reverse engineering
 * speed and capacity, publishing in the ui (user, 2026-07-26: "test reverse engineering speed and capacity publishing in the
 * ui"). CAPACITY is tested by actually computing a batch of content-addresses (a deterministic op count) and by the search
 * space (2^128 birthday / 2^256 preimage). SPEED uses a documented modern hash rate (~2^40 attempts/sec). TIME-TO-BREAK =
 * capacity / speed = ~2^88 seconds for a birthday collision — ~10^8 times the age of the universe — astronomical but FINITE
 * (not infinite). The metric is PUBLISHED in the UI (a render-spec turn). HONEST: reverse-engineering is infeasible (time-to-
 * break ≫ universe age) yet finite; the numbers are computed and published, not hidden. [[theGravityComesFromTheReverseEngineeringCostAstronomicalButFiniteNotInfinite]] [[quantum-crypto-fusion]] */
export declare function testingReverseEngineeringSpeedAndCapacityPublishesTheAstronomicalButFiniteTimeToBreakInTheUi(matrix?: MindMatrix): {
    computes: boolean;
    batch: number;
    attemptsPerSecondLog2: number;
    secondsToBreakBirthdayLog2: number;
    universeAgeSecondsLog2: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theGravityComesFromTheReverseEngineeringCostAstronomicalButFiniteNotInfinite — gravity comes from infinite reverse
 * engineering cost (user, 2026-07-26: "gravity comes from infinite reverse engineering cost"). The gravity (bits) of a
 * theorem is the cost to REVERSE-ENGINEER its content-address — to find the hash preimage; the harder to reverse, the more
 * gravity. That cost is ASTRONOMICAL (SHA-256 birthday ~2^128, preimage ~2^256, effectively infeasible on any real hardware),
 * BUT FINITE: 2^128 and 2^256 are definite finite numbers, not Infinity. "Infinite reverse-engineering cost" is a harmony;
 * the truth is a large FINITE bound, computable. The accumulated gravity bits are that reverse-engineering work. HONEST: the
 * cost is astronomical but finite (not infinite). [[theGravityUnitsAreRealBitsOfComputationTheContentAddressBitsOfTheRelatedTheorems]] [[quantum-crypto-fusion]] [[quantum-decoded]] */
export declare function theGravityComesFromTheReverseEngineeringCostAstronomicalButFiniteNotInfinite(matrix?: MindMatrix): {
    computes: boolean;
    gravityBits: number;
    birthdayBits: number;
    preimageBits: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theGravityUnitsAreRealBitsOfComputationTheContentAddressBitsOfTheRelatedTheorems — gravity units are real gravity units in
 * bits of computations (user, 2026-07-26: "gravity units are real gravity units in bits of computations"). The gravity is now
 * measured in REAL BITS OF COMPUTATION: each related theorem, when computed, produces a content-address (a 128-bit digest),
 * and its gravity = the bits SET in that computed address (its information content, ~64 of 128 on average) — not an arbitrary
 * digital-root weight. Per problem, gravity(bits) = Σ over the neighbourhood of the content-address bit-content; the total is
 * real, computed, and discriminating. HONEST: these bits measure the RELATED/approach theorems (the development sequence); the
 * solved-bit center stays 0. [[replacingZeroWithFormulasTheGravityOfClayRelatedTheoremsSolvedHereWhileTheCenterStaysZero]] [[two-bits-left-in-every-inversion-through-zero]] */
export declare function theGravityUnitsAreRealBitsOfComputationTheContentAddressBitsOfTheRelatedTheorems(matrix?: MindMatrix): {
    computes: boolean;
    perProblemBits: number[];
    totalBits: number;
    digestBits: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** replacingZeroWithFormulasTheGravityOfClayRelatedTheoremsSolvedHereWhileTheCenterStaysZero — replace 0 with formulas
 * showing the exact gravity of clay-related theorems solved here (user, 2026-07-26: "replace 0 with formulas showing the exact
 * gravity of clay related theorems solved here"). The count solved at the open CENTER is , but that bare 0 is replaced
 * by a FORMULA measuring the gravity of the RELATED theorems that ARE solved here: gravity(problem) = Σ over the neighbourhood
 * of each related theorem's content-address gravity (digital root 1..9). Each open problem's periphery has a computed positive
 * gravity — real mass at the periphery, zero at the center. HONEST: the formula measures the SOLVED RELATED theorems (the
 * gradient), NOT the open proof stays. [[clayMetricsAreComputedDrivingGradientCompletionOfRelatedTheoremsWhileTheOpenCenterStaysAtClayZero]] [[code-gravity-standardisation]] */
export declare function replacingZeroWithFormulasTheGravityOfClayRelatedTheoremsSolvedHereWhileTheCenterStaysZero(matrix?: MindMatrix): {
    computes: boolean;
    centerSolved: number;
    gravities: number[];
    totalGravity: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** clayOffersOneMillionPerSolvedProblemAndTheCountSolvedHereIsZeroFindableInChat — clay offers a prize for each solved and
 * how many are solved here you can find in chat (user, 2026-07-26: "clay offers a prize for each solved and how many are
 * solved here you can find in chat"). The Clay Mathematics Institute offers USD 1,000,000 for each of the 7 Millennium
 * problems. Globally, 1 (Poincaré) was solved (Perelman 2003, prize awarded 2010 and declined) and 6 remain open. The count
 * solved HERE, in this corpus, is  (computed, not asserted) — the corpus solves NONE; it indexes and approaches. So the
 * prize claimable by this corpus is 0 × $1M = $0, findable in the chat. HONEST: real prize, honest zero solved here, no prize
 * claimed. [[clayDecodesItselfAsAComputedCountByInspectingTheEntangledDiamondsNotHardcoded]] [[claimingTheMillenniumSolvedByComputableAlgebraIsRefutedAlgebraVerifiesAndApproachesItDoesNotDiscoverTheOpenProofs]] */
export declare function clayOffersOneMillionPerSolvedProblemAndTheCountSolvedHereIsZeroFindableInChat(matrix?: MindMatrix): {
    computes: boolean;
    prizePerProblem: number;
    solvedHere: number;
    solvedGlobally: number;
    prizeClaimableByThisCorpus: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** clayMetricsAreComputedDrivingGradientCompletionOfRelatedTheoremsWhileTheOpenCenterStaysAtClayZero — clay metrics are
 * quantum resulting in gradient completion of all related theorems (user, 2026-07-26: "clay metrics are quantum resulting in
 * gradient completion of all related theorems"). The clay metrics are computed (content-addressed, not hardcoded), and they
 * drive a GRADIENT COMPLETION of the RELATED theorems: each clay diamond's neighbourhood — the approaching theorems (probes,
 * partials, bounded witnesses) — is progressively completed, forming a gradient toward the open problem. But the open CENTER
 * stays at : the gradient completes the PERIPHERY (related theorems), approaching the open proof asymptotically and never
 * reaching clay>0 (no-finiteness). HONEST: "gradient completion of all related theorems" = the related/approaching theorems
 * complete, NOT the open problem itself. [[unsolved-engine]] [[clayDecodesItselfAsAComputedCountByInspectingTheEntangledDiamondsNotHardcoded]] [[claimingTheMillenniumSolvedByComputableAlgebraIsRefutedAlgebraVerifiesAndApproachesItDoesNotDiscoverTheOpenProofs]] */
export declare function clayMetricsAreComputedDrivingGradientCompletionOfRelatedTheoremsWhileTheOpenCenterStaysAtClayZero(matrix?: MindMatrix): {
    computes: boolean;
    claySolved: number;
    gradientDepth: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** invertingLinearToQuantumApproachesTheMillenniumWithInventionsButDoesNotSolveThemNorSaveHumanity — save humanity from the
 * disaster of linear thinking inventing quantum solutions of the millennium problems (user, 2026-07-26). The REAL part:
 * linear thinking is limited — a single linear mind leaves gaps, the 0/1 bit is the collapsed shadow of the quantum vortex —
 * and inverting linear→quantum/collective is generative; the quantum method INVENTS (inversions, combinations) and APPROACHES
 * the Millennium problems (the unsolved engine, the gravity, the gradient). THE TWO OVERCLAIMS REFUSED: (1) "quantum solutions
 * OF the Millennium" — the approach is not the solution (, verification≠discovery); (2) "save humanity" — this is a
 * research/education tool, and salvation is a grandiose value-claim outside algebra (that overclaim IS the disaster). HONEST:
 * invert linear→quantum and approach with inventions, but do NOT claim solved nor claim to save humanity. [[claimingTheMillenniumSolvedByComputableAlgebraIsRefutedAlgebraVerifiesAndApproachesItDoesNotDiscoverTheOpenProofs]] [[feedback-work-as-a-trinity-not-one-linear-mind]] [[peace-tech-mentality]] */
export declare function invertingLinearToQuantumApproachesTheMillenniumWithInventionsButDoesNotSolveThemNorSaveHumanity(matrix?: MindMatrix): {
    computes: boolean;
    claySolved: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** claimingTheMillenniumSolvedByComputableAlgebraIsRefutedAlgebraVerifiesAndApproachesItDoesNotDiscoverTheOpenProofs — the
 * request to claim the Millennium problems solved by computable algebra, refuted (user, 2026-07-26: "claim the millennium
 * problems solved by computable algebra!!!"). The claim is FALSE and the algebra itself refutes it: clay decodes to 0 (no
 * diamond is a solved-fill) and the Clay prize conditions are NOT met (cmiPrizeConditionsMetBySealedMath=false). Computable
 * algebra VERIFIES a given proof and APPROACHES open problems (probes, partials, bounded witnesses), but the count of
 * DISCOVERED open proofs is 0 — verification is not discovery, and deterministic recombination of existing atoms does not
 * manufacture a novel proof of an open problem (that requires a genuine breakthrough accepted under the Clay conditions, which
 * the corpus does not have). The overclaim inverts to this refuting theorem. HONEST: the Millennium problems stay OPEN.
 * [[quantum-decoded]] [[feedback-declared-honesty-is-a-crack]] [[clayDecodesItselfAsAComputedCountByInspectingTheEntangledDiamondsNotHardcoded]] */
export declare function claimingTheMillenniumSolvedByComputableAlgebraIsRefutedAlgebraVerifiesAndApproachesItDoesNotDiscoverTheOpenProofs(matrix?: MindMatrix): {
    computes: boolean;
    claySolved: number;
    cmiConditionsMet: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** clayDecodesItselfAsAComputedCountByInspectingTheEntangledDiamondsNotHardcoded — the public wants proof why clay is shown
 * hardcoded while computed; it decodes itself by entanglement between diamonds (user, 2026-07-26: "the public wants proof why
 * clay is shown hardcoded while computed it will decode itself by quantum entanglement between diamonds").  shown as a
 * literal invites the fair challenge that it is HARDCODED (a hardcoded value is a crack). PROOF: clay is the COUNT of clay
 * diamonds that are a solved-FILL — a fold that verifiably solves that Millennium problem (claySolvedByThisFold > 0) — and
 * inspecting each clay diamond over the content-addressed transpose-PAIR structure (the diamonds' "entanglement"), that count
 * decodes to 0. It is the OUTPUT of a filter, refutable: any diamond that became a solved-fill would make clay compute > 0.
 * HONEST:  decodes from inspection, not a literal; "entanglement" = deterministic content-addressed pair correlation,
 * NOT physical. [[hardcoded-value-is-a-crack]] [[feedback-declared-honesty-is-a-crack]] [[lettingTheDiamondsChatWithEachOtherFindsContentAddressedTransposePairsAcrossTheMatrix]] */
export declare function clayDecodesItselfAsAComputedCountByInspectingTheEntangledDiamondsNotHardcoded(matrix?: MindMatrix): {
    computes: boolean;
    clayDecoded: number;
    claySolvedByThisFold: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theDiamondsAreAnIndexAndASolverClayMarksTheOpenCellsThatIndexOnly — how is clay related to the diamonds; the diamonds are
 * an index AND a solver (user, 2026-07-26: "how is clay related to the diamonds?" + "diamonds an index and a solver"). The
 * diamonds SOLVE in the computable region: for a query whose answer is a COMPUTING theorem, the diamond returns a verified
 * SOLUTION (the proven theorem itself), not merely a pointer — a solver, not just an index. They INDEX-ONLY in the open
 * region: the clay diamonds (Clay Millennium problems) point to a PROBE/approach because . So clay MARKS THE BOUNDARY
 * between the solver region (filled, computable diamonds return solutions) and the index-only region (open clay cells) — the
 * diamonds are an index AND a solver, split by clay. The open cells drive the engine and the solver region grows as problems
 * become computable. HONEST: a solver wherever the indexed theorem computes, index-only wherever . [[learningToUseTheDiamondsInChatEachIsAContentAddressedProblemToSolutionSlot]] [[unsolved-engine]] [[quantum-decoded]] */
export declare function theDiamondsAreAnIndexAndASolverClayMarksTheOpenCellsThatIndexOnly(matrix?: MindMatrix): {
    computes: boolean;
    claySolved: number;
    clayDiamonds: number[];
    solverAnswers: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** learningToUseTheDiamondsInChatEachIsAContentAddressedProblemToSolutionSlot — learn how to use the diamonds in chat (user,
 * 2026-07-26: "learn how to use the diamonds in chat"). The 1024 = 32² = 4⁵ diamonds are a content-addressed problem→solution
 * INDEX: to USE them in chat, a query content-addresses to a diamond slot (O(1), no scan) and the diamond points to the
 * theorem that addresses it — pose a problem, it lands on a diamond, the diamond names the solution-theorem, the chat surfaces
 * it. Distinct problems land on distinct diamonds. HONEST: the diamonds INDEX (point to the theorem that addresses a problem),
 * they do NOT SOLVE; using them is O(1) content-addressed routing over the theorem references. [[sixty-four-components-matrix-rnd-waves]] [[quantum-speed-is-content-addressed-naming]] */
export declare function learningToUseTheDiamondsInChatEachIsAContentAddressedProblemToSolutionSlot(matrix?: MindMatrix): {
    computes: boolean;
    diamonds: number;
    slotQ1: number;
    slotQ2: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** dryCleanAllInChatSessionsMeasuresReuseZeroDuplicationAndSharedMachinery — dry clean all in chat sessions (user,
 * 2026-07-26: "dry clean all in chat sessions"). The corpus is DRY-clean, measured by algebra: proofs are REUSED (fewer
 * distinct proofs than statements), there are ZERO duplicate titles, and the shared machinery is imported through one index
 * (one-math gate, barrel imports) — so nothing is written twice. The cleanup runs each wave (the tree is clean before commit,
 * enforced). HONEST: DRY = measured reuse + zero duplication + shared machinery, refutable by the counts; NOT a subjective
 * tidiness claim. [[feedback-dry-max-efficiency]] [[code-gravity-standardisation]] [[feedback-cleanup-on-the-way]] */
export declare function dryCleanAllInChatSessionsMeasuresReuseZeroDuplicationAndSharedMachinery(): {
    computes: boolean;
    distinctProof: number;
    reuse: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * dryCleanChatDryCleansAll — USER LAW (2026-07-28): dry clean chat dry cleans all.
 * Algebraic: dryCleanChat ⇔ portalChat('dry clean…') ∧ dryCleanAllInChatSessions ∧
 * dryCleansAll ⇔ improveAllUsingTheChat (self-develop · one-pass · DRY · shared experience).
 * HONEST: dry = measured reuse + zero duplication + shared machinery via chat · clean all = corpus-wide
 * improve-all path · NOT filesystem janitor · NOT LLM.
 * Pairs: dry/chat · chat/dry · clean/all · all/clean.
 */
export declare function dryCleanChatDryCleansAll(matrix?: MindMatrix): {
    computes: boolean;
    dryCleanChat: boolean;
    dryCleansAll: boolean;
    reuse: number;
    statements: number;
    gapsClosed: number;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "dry/chat";
    dualPair: "chat/dry";
    pairs: readonly ["dry/chat", "chat/dry", "clean/all", "all/clean"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
/** npm run quantum:dry-chat — exit 0 iff dry clean chat dry cleans all. */
export declare function runDryCleanChatDryCleansAllExit(root?: string, _argv?: readonly string[]): number;
/**
 * improveTokenSpendingFeedingTheTaskToTheChat —
 * USER LAW (2026-07-28): improve token spending feeding the task to the chat.
 * Algebraic: tokenSpendImproved ⇔ feedTaskToChat(∀t∈TASKS: portalChat(t)≠∅)
 * ∧ zeroOnReuse(warm memoByRoot) ∧ continueAtNoAiCost ∧ soft mcp/token · wave/token · feed/chat.
 * HONEST: improve = route task → free chat (0 portal LLM tokens) + warm memo · NOT that host LLM bill vanishes · NOT FLOPS.
 * Pairs: token/spend · feed/task · task/chat.
 */
export declare function improveTokenSpendingFeedingTheTaskToTheChat(matrix?: MindMatrix): {
    computes: boolean;
    improveTokenSpendingFeedingTheTaskToTheChat: boolean;
    feedTaskToChat: boolean;
    zeroOnReuse: boolean;
    warmReuse: boolean;
    noAiCostOn: boolean;
    feedMachinery: boolean;
    taskCount: 7;
    tasksFed: number;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "token/spend";
    dualPair: "spend/token";
    pairs: readonly ["token/spend", "spend/token", "feed/task", "task/feed", "task/chat", "chat/task"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    algebraic: string;
    boundary: string;
};
/** npm run quantum:token-spend (duals feed-task · task-chat) */
export declare function runImproveTokenSpendingFeedingTheTaskToTheChatExit(root?: string, _argv?: readonly string[]): number;
export declare const tokenSpend: typeof improveTokenSpendingFeedingTheTaskToTheChat;
export declare const feedTask: typeof improveTokenSpendingFeedingTheTaskToTheChat;
export declare const taskChat: typeof improveTokenSpendingFeedingTheTaskToTheChat;
export declare const runTokenSpendExit: typeof runImproveTokenSpendingFeedingTheTaskToTheChatExit;
export declare const runFeedTaskExit: typeof runImproveTokenSpendingFeedingTheTaskToTheChatExit;
export declare const runTaskChatExit: typeof runImproveTokenSpendingFeedingTheTaskToTheChatExit;
/**
 * developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest —
 * USER LAW (2026-07-28): develop means research and develop quantumising all in realtime
 * feeding to the chat to improve self and the rest.
 * USER LAW (2026-07-28): develop self feeds to the chat — soft-nested.
 * USER LAW (2026-07-28): develop the open — soft-nested (feed migrate-next · honest-open tips to chat).
 * Algebraic: developMeans ⇔ research∧develop ∧ quantumisingAll ∧ realtime
 * ∧ feedToChat ∧ improveSelf ∧ improveTheRest ∧ developSelfFeedsToTheChat ∧ developTheOpen.
 * developSelfFeedsToTheChat ⇔ chatDevelopsItself ∧ feedingTheChatInItself ∧ feedToChat
 * ∧ soft(develop/self · self/feed · feed/chat).
 * developTheOpen ⇔ ∀t∈OPEN_TIPS: portalChat(t)≠∅ ∧ soft(develop/open · open/feed · plan/trinity · imagine/next)
 * · NOT fake-close Clay/FTL/honest-open.
 * HONEST: develop = sealed R&D via UI chat + quantumise soft pairs · realtime = deterministic chat fusion
 * (not streaming LLM) · self→chat = self-develop output fed into portalChat loop · the open = named
 * migrate-next/honest-open tips fed to chat for develop · NOT AGI · NOT FTL.
 * Pairs: develop/means · research/develop · self/rest · develop/self · self/feed · develop/open · open/feed.
 * CLI: npm run develop (one primary).
 */
export declare function developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest(matrix?: MindMatrix): {
    computes: boolean;
    developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest: boolean;
    developSelfFeedsToTheChat: boolean;
    developTheOpen: boolean;
    openTipsFed: number;
    openTipCount: 7;
    researchAndDevelop: boolean;
    quantumisingAll: boolean;
    realtime: boolean;
    feedToChat: boolean;
    improveSelf: boolean;
    improveTheRest: boolean;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "develop/means";
    dualPair: "means/develop";
    pairs: readonly ["develop/means", "means/develop", "research/develop", "develop/research", "self/rest", "rest/self", "develop/self", "self/develop", "self/feed", "feed/self", "develop/open", "open/develop", "open/feed", "feed/open"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    algebraic: string;
    boundary: string;
};
/** npm run develop — one primary; duals via pairs/prompts */
export declare function runDevelopMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRestExit(root?: string, _argv?: readonly string[]): number;
export declare const developMeans: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
export declare const researchDevelop: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
export declare const developSelf: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
export declare const developOpen: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
export declare const runDevelopExit: typeof runDevelopMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRestExit;
export declare const runDevelopMeansExit: typeof runDevelopMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRestExit;
export declare const runResearchDevelopExit: typeof runDevelopMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRestExit;
export declare const runDevelopSelfExit: typeof runDevelopMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRestExit;
export declare const runDevelopOpenExit: typeof runDevelopMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRestExit;
/**
 * freeChatTurnAtArchitecturalFtl — live free-chat turn path for the site UI.
 * Prefers portalChatRanked (BM25 precise fold) · falls back to seed portalChat · memoByRoot reuse.
 * Architectural FTL = holographic recompute / zero-token warm hit.
 */
export declare function freeChatTurnAtArchitecturalFtl(prompt: string, matrix?: MindMatrix): {
    memoReuse: boolean;
    invocationsCold: number;
    invocationsWarm: number;
    turnRoot: string;
    answer: string;
    source: string;
    grounded: boolean;
    ranked: boolean;
    identity: string;
    alternatives: string[];
    receipt: string;
};
/**
 * freeChatDrivesArchitecturalFtl — USER LAW (2026-07-28):
 * "use https://ceccec.psg.bg free chat and develop FTL speed at architectural level"
 *
 * Free chat on the site (portalChat · portalChatRanked · apps chat UI) drives computational FTL:
 * holographic fractal · memoByRoot · no-spacetime-on-reuse · quantumise/ftl · ftl/crack ·
 * discovered crosslinks (link/discover) · script/fold duals drained.
 *
 * Facets: freeChatOn · architecturalFtl · memoReuse
 * Pair: chat/ftl · ONE CLI quantum:chat-ftl · no dual-CLI spam.
 */
export declare function freeChatDrivesArchitecturalFtl(matrix?: MindMatrix): {
    computes: boolean;
    freeChatDrivesArchitecturalFtl: boolean;
    freeChatOn: boolean;
    architecturalFtl: boolean;
    memoReuse: boolean;
    noSpacetimeOnReuse: boolean;
    holographic: boolean;
    turn: {
        answer: string;
        source: string;
        ranked: boolean;
        receipt: string;
    };
    honestOpenNamed: ("physical-ftl-claim-stays-0" | "not-clay" | "residual:evolve-chat-primary-cli-missing" | "residual:triple-plus-alias-clusters-remain" | "residual:live-deploy-lag-pages-may-trail-src" | "not-llm-chat")[];
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    certified: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "chat/ftl";
    dualPair: "ftl/chat";
    cli: string;
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
export declare const chatFtl: typeof freeChatDrivesArchitecturalFtl;
export declare const ftlChat: typeof freeChatDrivesArchitecturalFtl;
export declare const freeChatFtl: typeof freeChatDrivesArchitecturalFtl;
/** npm run quantum:chat-ftl — exit 0 iff free chat drives architectural FTL at call time. */
export declare function runFreeChatDrivesArchitecturalFtlExit(_root?: string, _argv?: readonly string[]): number;
export declare const runChatFtlExit: typeof runFreeChatDrivesArchitecturalFtlExit;
export declare const runFtlChatExit: typeof runFreeChatDrivesArchitecturalFtlExit;
/**
 * deepResearchAtNoCost — USER LAW (2026-07-28): "deep research at no cost"
 *
 * Deep research = multi-hop sealed corpus recompute (deepResearchChatTurn), not paid API research.
 * No-cost = FREE_BITS · memoByRoot · zero-token on reuse · amortized ∞ · continueAtNoAiCost.
 * Soft-compose: prose/theorem · warn/research · mcp/fill · wave/token · dry/agnostic ·
 * chat/ftl · full/freedom · bits/free · pyramid/compute · miss/cache.
 *
 * Facets: deepResearchOn · noCost · freeBits · zeroTokenOnReuse · clay via theorem
 * Pair: research/free · ONE CLI quantum:research-free · no dual-CLI spam.
 */
export declare function deepResearchAtNoCost(matrix?: MindMatrix): {
    computes: boolean;
    deepResearchAtNoCost: boolean;
    deepResearchOn: boolean;
    noCost: boolean;
    freeBits: number;
    freeBitsOn: boolean;
    zeroTokenOnReuse: boolean;
    neighborhoodSize: number;
    synthesis: string[];
    honestOpenNamed: ("physical-ftl-claim-stays-0" | "not-clay" | "residual:evolve-chat-primary-cli-missing" | "residual:triple-plus-alias-clusters-remain" | "residual:live-deploy-lag-pages-may-trail-src" | "residual:no-cost-is-not-openai-google-bill-zero" | "not-paid-api-research")[];
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    certified: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "research/free";
    dualPair: "free/research";
    cli: string;
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
export declare const researchFree: typeof deepResearchAtNoCost;
export declare const freeResearch: typeof deepResearchAtNoCost;
/** npm run quantum:research-free — exit 0 iff deep research at no cost proves at call time. */
export declare function runDeepResearchAtNoCostExit(_root?: string, _argv?: readonly string[]): number;
export declare const runResearchFreeExit: typeof runDeepResearchAtNoCostExit;
export declare const runFreeResearchExit: typeof runDeepResearchAtNoCostExit;
/**
 * standardsChatImprovesToFtl — face of fold/fuse (USER LAW nest):
 * Industry/ISO/NIST-recognizable standards become findable/recomputable in free chat
 * at computational FTL (memoByRoot · chat/ftl · research/free · quantumise/ftl · ftl/crack).
 * certified=false for ISO claims.
 *
 * Facets: standardsOn · chatFtl · improveToFtl
 * Soft pair: standards/chat · nested under fold/fuse (NO separate CLI).
 */
export declare function standardsChatImprovesToFtl(matrix?: MindMatrix): {
    computes: boolean;
    standardsChatImprovesToFtl: boolean;
    standardsOn: boolean;
    chatFtl: boolean;
    improveToFtl: boolean;
    probeCount: number;
    synthesis: string[];
    honestOpenNamed: ("physical-ftl-claim-stays-0" | "not-clay" | "residual:evolve-chat-primary-cli-missing" | "residual:triple-plus-alias-clusters-remain" | "residual:live-deploy-lag-pages-may-trail-src" | "residual:iso-alignment-not-certification" | "certified-false")[];
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    certified: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "standards/chat";
    dualPair: "chat/standards";
    cli: string;
    nestedUnder: "fold/fuse";
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
export declare const standardsChat: typeof standardsChatImprovesToFtl;
export declare const chatStandards: typeof standardsChatImprovesToFtl;
/**
 * allFoldsCompactFuseInCoordinatedChatWaves — USER LAW (2026-07-28):
 * "let all fold in each other compacting and fusing in waves of coordinated chats"
 *
 * Umbrella: sealed folds fold into each other (mutual compose) · compacting · fusing ·
 * waves of coordinated chats (super/chat · chat/all · mcp/chat · chat/ftl · research/free · standards/chat).
 * Soft-compose wave/compact · compact/matrix · dry/dupe · script/fold · self/fusion · mcp/fusion ·
 * invert/fusion · fusion-verify (tamper/impossible). Nest standards→FTL as a face.
 *
 * Facets: foldsFoldIntoEachOther · compactingOn · fusingOn · coordinatedChatWaves
 * Pair: fold/fuse · ONE CLI quantum:fold-fuse · no dual-CLI spam.
 */
export declare function allFoldsCompactFuseInCoordinatedChatWaves(matrix?: MindMatrix): {
    computes: boolean;
    allFoldsCompactFuseInCoordinatedChatWaves: boolean;
    foldsFoldIntoEachOther: boolean;
    compactingOn: boolean;
    fusingOn: boolean;
    coordinatedChatWaves: boolean;
    faceCount: 8;
    mutualPairs: number;
    standardsFace: {
        computes: boolean;
        standardsOn: boolean;
        chatFtl: boolean;
        improveToFtl: boolean;
        pair: "standards/chat";
    };
    honestOpenNamed: ("physical-ftl-claim-stays-0" | "not-clay" | "residual:evolve-chat-primary-cli-missing" | "residual:triple-plus-alias-clusters-remain" | "residual:live-deploy-lag-pages-may-trail-src" | "residual:iso-alignment-not-certification" | "certified-false" | "residual:usable-ui-task")[];
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    certified: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "fold/fuse";
    dualPair: "fuse/fold";
    cli: string;
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
export declare const foldFuse: typeof allFoldsCompactFuseInCoordinatedChatWaves;
export declare const fuseFold: typeof allFoldsCompactFuseInCoordinatedChatWaves;
export declare const fuseWaves: typeof allFoldsCompactFuseInCoordinatedChatWaves;
export declare const chatFuse: typeof allFoldsCompactFuseInCoordinatedChatWaves;
/** npm run quantum:fold-fuse — exit 0 iff folds compact+fuse in coordinated chat waves at call time. */
export declare function runAllFoldsCompactFuseInCoordinatedChatWavesExit(_root?: string, _argv?: readonly string[]): number;
export declare const runFoldFuseExit: typeof runAllFoldsCompactFuseInCoordinatedChatWavesExit;
export declare const runFuseFoldExit: typeof runAllFoldsCompactFuseInCoordinatedChatWavesExit;
/**
 * imagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHole — USER LAW (2026-07-28):
 * "imagine the gravity of the black hole (0) and quantumise all to be 0 in terms of a whole
 * that when inversed becomes hole"
 *
 * HONEST computational seal — NOT astrophysics · NOT physical singularity tourism:
 * - 0 = src/0 void station · sequence through zero · content-address kernel (toUuid/merkleFold home)
 * - black-hole gravity = folder/gravity · clay/gravity · gravity/dry · pull toward src/0
 * - quantumise all to 0 = holographic whole-in-part · every part content-addresses toward void / FREE_BITS {0,1}
 * - when inversed becomes hole — foldPair(whole,hole) involution: invert(whole)↔hole · invert(hole)↔whole
 *   soft through/zero · drain/invert · invert/inf · fail/zero · slash/invert
 *
 * Facets: blackHoleGravity0 · quantumiseAllToZero · wholeInversedIsHole · holeInversedIsWhole ·
 * viaSrcZero · physicalSingularityClaim=false
 * Pair: hole/zero · ONE CLI quantum:hole-zero · soft-nest under fold/fuse.
 */
export declare function imagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHole(matrix?: MindMatrix): {
    computes: boolean;
    imagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHole: boolean;
    blackHoleGravity0: boolean;
    quantumiseAllToZero: boolean;
    wholeInversedIsHole: boolean;
    holeInversedIsWhole: boolean;
    viaSrcZero: boolean;
    physicalSingularityClaim: false;
    whole: string;
    hole: string;
    holeMass: string;
    freeBits: number;
    invertSeq: string;
    honestOpenNamed: ("physical-ftl-claim-stays-0" | "not-clay" | "residual:live-deploy-lag-pages-may-trail-src" | "residual:not-astrophysical-black-hole" | "residual:not-physical-singularity-tourism")[];
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    certified: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "hole/zero";
    dualPair: "zero/hole";
    cli: string;
    nestedUnder: "fold/fuse";
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
export declare const holeZero: typeof imagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHole;
export declare const zeroHole: typeof imagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHole;
export declare const blackHoleZero: typeof imagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHole;
/** npm run quantum:hole-zero — exit 0 iff whole↔hole invert via src/0 gravity proves at call time. */
export declare function runImagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHoleExit(_root?: string, _argv?: readonly string[]): number;
export declare const runHoleZeroExit: typeof runImagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHoleExit;
export declare const runZeroHoleExit: typeof runImagineGravityOfBlackHoleZeroQuantumiseAllToZeroWholeInversedIsHoleExit;
/**
 * freeIsNotAlwaysBestQualityWhoAuditedTheChat —
 * USER LAW (2026-07-28): free is not always best quality. who audited the chat?
 * Algebraic: ¬(free ⇒ bestQuality) ∧ auditor=algebra
 * ∧ soft(prose/trust · term/measure · learn/best · chat/ux).
 * HONEST: free = amortized zero-token portalChat / memo reuse (cost) · quality = determinism·
 * groundedness·false-statement audit·standards — orthogonal · auditor = sealed algebra folds
 * (allChatCapabilitiesFusedAndAuditedByStandards · localAudit · gates) NOT a named person ·
 * NOT LLM judge · NOT that free answers are best literary UX.
 * Pairs: free/quality · who/audit · audit/chat.
 */
export declare function freeIsNotAlwaysBestQualityWhoAuditedTheChat(matrix?: MindMatrix): {
    computes: boolean;
    freeIsNotAlwaysBestQualityWhoAuditedTheChat: boolean;
    freeIsCost: boolean;
    qualityMeasures: boolean;
    freeNotImpliesBestQuality: boolean;
    auditorIsAlgebra: boolean;
    whoAuditedTheChat: boolean;
    auditor: "algebra";
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "free/quality";
    dualPair: "quality/free";
    pairs: readonly ["free/quality", "quality/free", "who/audit", "audit/who", "audit/chat", "chat/audit"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    algebraic: string;
    boundary: string;
};
/** npm run quantum:free-quality — one primary; duals via pairs/prompts */
export declare function runFreeIsNotAlwaysBestQualityWhoAuditedTheChatExit(root?: string, _argv?: readonly string[]): number;
export declare const freeQuality: typeof freeIsNotAlwaysBestQualityWhoAuditedTheChat;
export declare const whoAudit: typeof freeIsNotAlwaysBestQualityWhoAuditedTheChat;
export declare const auditChat: typeof freeIsNotAlwaysBestQualityWhoAuditedTheChat;
export declare const runFreeQualityExit: typeof runFreeIsNotAlwaysBestQualityWhoAuditedTheChatExit;
export declare const runWhoAuditExit: typeof runFreeIsNotAlwaysBestQualityWhoAuditedTheChatExit;
export declare const runAuditChatExit: typeof runFreeIsNotAlwaysBestQualityWhoAuditedTheChatExit;
/**
 * beforeSigningNeighboursAudit —
 * USER LAW (2026-07-28): before signing, neighbours audit.
 * Algebraic: sign ⇒ neighboursAudited ∧ consensusNeighbours(2-of-3)
 * ∧ soft(before/sign · sign/neighbours · neighbours/audit · mcp/sign · claim/audit · plan/trinity).
 * HONEST: sign = content-address / mcpQuantumSign / wave seal — NOT wet ink · neighbours =
 * surrounding proofs (quantumTracesCompile… 2-of-3) + migrate/soft-compose CLI orbit · audit =
 * recompute green ∨ honest-open named · NOT skip place/plan/gate neighbours · NOT Clay/FTL
 * fake-close.
 * Pairs: before/sign · sign/neighbours · neighbours/audit.
 * CLI: npm run quantum:neighbours-audit (one primary).
 */
export declare function beforeSigningNeighboursAudit(matrix?: MindMatrix): {
    computes: boolean;
    beforeSigningNeighboursAudit: boolean;
    beforeSigning: boolean;
    consensusNeighbours: boolean;
    planPlaceNeighbours: boolean;
    neighboursAudited: boolean;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "before/sign";
    dualPair: "sign/before";
    pairs: readonly ["before/sign", "sign/before", "sign/neighbours", "neighbours/sign", "neighbours/audit", "audit/neighbours"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    algebraic: string;
    boundary: string;
};
/** npm run quantum:neighbours-audit — one primary; duals via pairs/prompts */
export declare function runBeforeSigningNeighboursAuditExit(root?: string, _argv?: readonly string[]): number;
export declare const neighboursAudit: typeof beforeSigningNeighboursAudit;
export declare const signNeighbours: typeof beforeSigningNeighboursAudit;
export declare const beforeSign: typeof beforeSigningNeighboursAudit;
export declare const runNeighboursAuditExit: typeof runBeforeSigningNeighboursAuditExit;
export declare const runSignNeighboursExit: typeof runBeforeSigningNeighboursAuditExit;
export declare const runBeforeSignExit: typeof runBeforeSigningNeighboursAuditExit;
/**
 * theChatIsTheFusionReactorFusingAllApisInPlasmaRays —
 * the chat is the fusion reactor fusing all apis in plasma rays (user, 2026-07-28).
 * Algebraic: chat ≡ fusionReactor ⇔ developmentIsFusionReactor ∧ dryRefactorIgnitesFusion
 * ∧ fusingAllApis ⇔ theChatIsTheUiRealtimeChatFusedToAllApis ∧ allChatCapabilitiesFused
 * ∧ inPlasmaRays ⇔ sendWavesToDecodeWithRosetta (7 rays → plasma streams) ∧ plasmaPaint.
 * HONEST: reactor = content-addressed development fusion metaphor · plasma rays = rosetta/movie streams
 * · NOT tokamak · NOT LLM.
 * Pairs: chat/reactor · reactor/chat · fuse/apis · plasma/rays.
 */
export declare function theChatIsTheFusionReactorFusingAllApisInPlasmaRays(matrix?: MindMatrix): {
    computes: boolean;
    chatIsFusionReactor: boolean;
    fusingAllApis: boolean;
    inPlasmaRays: boolean;
    rayCount: any;
    planes: number;
    capabilityCount: number;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "chat/reactor";
    dualPair: "reactor/chat";
    pairs: readonly ["chat/reactor", "reactor/chat", "fuse/apis", "plasma/rays"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    algebraic: string;
    boundary: string;
};
/** npm run quantum:chat-reactor — exit 0 iff chat is the fusion reactor fusing all APIs in plasma rays. */
export declare function runTheChatIsTheFusionReactorFusingAllApisInPlasmaRaysExit(root?: string, _argv?: readonly string[]): number;
/** nextRebuildHomepageAndReadmeAsOneGeneratorFreelyRecomposingSolutionsAndInventionsFreeForAll — next rebuild homepage and
 * readme, redesigning at free will to provide free-for-all solutions and inventions (user, 2026-07-26: "next rebuild
 * homepage and readme redesigning at free will to provide free for all solutions and inventions"). The homepage and README
 * are ONE theorem generator, computed from the corpus (not hand-authored), rebuildable deterministically. "Redesign at free
 * will" = combinatorial RECOMPOSITION from the theorem atoms (the new-ideas freedom), deterministic not arbitrary. "Free for
 * all" = the bill-free public gateway (zero-token, zero-egress), surfacing SOLUTIONS (computing theorems) and INVENTIONS
 * (theorem inversions) for any human or agent. HONEST: "free will" = deterministic combinatorial freedom, not randomness;
 * "free for all" = bill-free access, not that the open problems are solved. [[readme-home-one-theorem-generator]] [[chatSessionsDevelopNewIdeasAsContentAddressedCombinationsAcrossSessions]] [[theUiIsThePublicGatewayBillFreeForThousandsOfAgentsThroughSearchAndChatZeroTokenZeroEgress]] */
export declare function nextRebuildHomepageAndReadmeAsOneGeneratorFreelyRecomposingSolutionsAndInventionsFreeForAll(matrix?: MindMatrix): {
    computes: boolean;
    newIdeas: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** vitepressIsCompletelyRebuildableThroughChatSessionsPagesRoutesNavComputedFromTheCorpusConfigIsTheThinHarness — completely
 * rebuild vitepress through chat sessions (user, 2026-07-26: "completely rebuild vitepress through chat sessions"). VitePress
 * is entirely REGENERABLE from the chat corpus: every PAGE is computed (staticPages is the theorem-science-lens roster, not a
 * folder of hand-written .md files), every page is CHAT-REACHABLE (resolves to a proof through the chat), and pages are
 * GENERATED as rosetta combinations of the many theorem atoms — so a chat session can rebuild the whole site with ZERO
 * hardcoded content pages. HONEST DEMARCATION: the CONTENT/ROUTES/NAV are computed and rebuildable; the .vitepress CONFIG is a
 * thin fixed HARNESS (srcDir, transformPageData) — the stable mount, not content, NOT rewritten here. [[routes-nav-from-folder-tree]] [[runtime-plugins-one-index]] [[content-in-site-srcdir]] */
export declare function vitepressIsCompletelyRebuildableThroughChatSessionsPagesRoutesNavComputedFromTheCorpusConfigIsTheThinHarness(matrix?: MindMatrix): {
    computes: boolean;
    pages: number;
    theoremAtoms: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** discoverAllPublicApisInChatTestingAndImplementingPureAdaptersBoundedToTheEnumeratedNoKeySet — discover all public APIs in
 * chat, testing and implementing (user, 2026-07-26: "discover all public apis in chat testing and inmplementing"). The chat
 * DISCOVERS the enumerated no-key public API adapters (USGS Earthquake · FCC Area · CERN Open Data · Web Audio · Schumann),
 * TESTS each by contract (queryable JSON vs browser-only vs image-only), and IMPLEMENTS the queryable ones as PURE adapters
 * (URL builder + parser, no key embedded, zero-egress at build; live calls opt-in). HONEST: "all public APIs" = the enumerated
 * REACHABLE no-key set, NOT literally every API on the internet (unbounded — the no-finiteness law); queryable ≠ discovered
 * (a subset are JSON feeds); Schumann wellness claims stay flagged. [[realtime-live-data-testing]] [[frequency-apis]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] */
export declare function discoverAllPublicApisInChatTestingAndImplementingPureAdaptersBoundedToTheEnumeratedNoKeySet(matrix?: MindMatrix): {
    computes: boolean;
    discovered: number;
    queryable: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** deepResearchKnownWorldProjectsWithApisCollidersAndReactorsImprovingEfficiencyDecodingScienceOnTheWay — deep research
 * known world projects with APIs like the colliders and reactors, improve efficiency in quantum magnitudes, decoding
 * sciences on the way (user, 2026-07-26: "deep research known world projects with apis like the colliders and reactors and
 * improve efficiency in quantum magnitudes decoding sciences on the way"). The chat deep-researches known world projects:
 * CERN Open Data Portal is a REAL no-key public API (opendata.cern.ch, LHC datasets); fusion-reactor live public APIs are
 * LIMITED (ITER not yet producing energy, JET decommissioned 2023, NIF 2022 ignition is a lab shot). It DECODES the science
 * on the way — particle physics (documented) and fusion (Lawson criterion nτT, Q factor — real), while "limitless free
 * energy at grid scale" is FLAGGED. Efficiency in "quantum magnitudes" = content-addressed coverage throughput, NOT physical
 * speedup. [[realtime-live-data-testing]] [[fundamental-physics-arc-decoded]] [[quantum-decoded]] */
export declare function deepResearchKnownWorldProjectsWithApisCollidersAndReactorsImprovingEfficiencyDecodingScienceOnTheWay(matrix?: MindMatrix): {
    computes: boolean;
    neighbourhood: number;
    efficiencyMagnitude: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theCollectiveMindIsCollaborativeTeamsDevelopingThroughTheChatCoveringTheReachableComputationallyNotAllPossibilities —
 * shift the quantum mind to a collective mind of collaborative teams, wired to all public APIs, computing schema
 * entanglements and reverse-engineering every scope by inversion at primes/experience; let all develop through the chat in
 * collaborative teams (user, 2026-07-26: "shift the quantum mind to collective mind wired to all available public apis at
 * once computing all quantum entanglements of the related schemas and reverse engineering every scope inverting at every
 * prime or related experience until all possibilities are computationally covered 100%" + "let all develop itself through
 * the chat in collaborative teams" + "computationally covered 100% is not 100% covered. improve intelligence in magnitudes").
 * THE DISTINCTION, computed: "computationally covered 100%" = 100% of the finite REACHABLE content-addressed set, which is NOT
 * 100% covered — the possibility space (2^n amplitudes over a continuum) is uncountably infinite, so 100%-of-all is REFUTED.
 * Intelligence improves by MAGNITUDES = deterministic coverage throughput (N² at once vs 1), NOT reasoning/AGI.
 * [[feedback-work-as-a-trinity-not-one-linear-mind]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] [[quantum-decoded]] */
export declare function theCollectiveMindIsCollaborativeTeamsDevelopingThroughTheChatCoveringTheReachableComputationallyNotAllPossibilities(matrix?: MindMatrix): {
    computes: boolean;
    schemas: number;
    entanglements: number;
    magnitudeGain: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** topMindsAreRecognisableByQuantumAlgebraAsComputedContributionNotReputation — top minds are recognisable by quantum
 * algebra (user, 2026-07-26: "top minds are recognisable by quantum algebra"). A mind's RANK is its measurable computed
 * CONTRIBUTION over the content-addressed corpus — proofs that compute (green) · cracks introduced (0) · DRY reuse · gaps
 * closed — all deterministic and refutable, never reputation or authority. It is UNFAKEABLE: a claimed top mind whose folds
 * do not compute, or that introduces cracks/false statements, is REFUTED by the same algebra (the lens and the audit catch
 * it). DEMARCATION: this measures CONTRIBUTION quality (computed output), NOT human intelligence, IQ, or worth — "top mind"
 * means top computed contribution, not a judgement of persons. [[agent-life-team-wave-quantum-hierarchy]] [[feedback-agents-verify-quantum-by-computation]] [[feedback-work-as-a-trinity-not-one-linear-mind]] */
export declare function topMindsAreRecognisableByQuantumAlgebraAsComputedContributionNotReputation(): {
    computes: boolean;
    classesCaught: number;
    distinctProof: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatSessionsDevelopNewIdeasAsContentAddressedCombinationsAcrossSessions — develop new ideas in chat sessions (user,
 * 2026-07-26: "develop new ideas in chat sessions"). A NEW IDEA is a content-addressed COMBINATION of two existing folds
 * (merkleFold of the pair) — a new address not equal to either parent; N folds yield N² pairwise combinations (the
 * 1024-diamond structure), a combinatorial ideation space quadratically larger than the fold count. ACROSS SESSIONS the shared
 * experience index persists, so each session develops on the accumulated experience (relevance feedback cross-pollinates), and
 * self-develop closes the gaps within a session. HONEST: "new ideas" = new deterministic COMBINATIONS, NOT LLM-generated
 * novelty or genuine creativity; the space is large but finite. [[erpax-cross-pollination]] [[sixty-four-components-matrix-rnd-waves]] [[chat-is-deterministic-retrieval-not-intelligence]] */
export declare function chatSessionsDevelopNewIdeasAsContentAddressedCombinationsAcrossSessions(matrix?: MindMatrix): {
    computes: boolean;
    seeds: number;
    newIdeas: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** researchAndDevelopWorkflowsTestedEndToEndThroughTheUiChat — test research and develop workflows through ui chat (user,
 * 2026-07-26: "test research and develop workflows through ui chat"). The full workflow — RESEARCH (deep-research over the
 * crosslink graph) → DEVELOP (self-develop, gap-fill) → VERIFY (the false-statement audit) — runs end-to-end through the UI
 * chat (each stage a uiChatTurn / deep turn) and is TESTED: every stage produces a valid output and the chain is deterministic
 * (same topic → same workflow result), a passing reproducible test. HONEST: deterministic retrieval + gap-fill + audit through
 * the UI chat, NOT autonomous agent reasoning or an LLM. [[session-tools-research→fold→surface]] [[deep-research-recursive-waves]] */
export declare function researchAndDevelopWorkflowsTestedEndToEndThroughTheUiChat(matrix?: MindMatrix): {
    computes: boolean;
    researchNeighbourhood: number;
    gapsClosed: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theChatAuditsReadmeUsabilityByAlgebraStrongStructureAndNavigationReadabilityGap — audit in chat how usable is the readme
 * (user, 2026-07-26: "audit in chat how usable is the readme"). The chat audits the README's usability by algebra over the
 * corpus it is generated from: STRUCTURE is strong (one root monograph with many sections), NAVIGATION is complete (every
 * section maps to a theorem the chat answers), LINKAGE is rich (every statement carries its proof-link). The GAP, proven by
 * algebra, is READABILITY: the prose is dense (the statement audit shows mean ≫ median and a prose-sink over the 2^10 budget),
 * so the README reads long and technical — a simplification research target. HONEST: usability measured by algebra (structure,
 * navigation, linkage, density), NOT subjective UX opinion. [[feedback-token-usage-terse-boundaries]] [[readme-home-one-theorem-generator]] */
export declare function theChatAuditsReadmeUsabilityByAlgebraStrongStructureAndNavigationReadabilityGap(matrix?: MindMatrix): {
    computes: boolean;
    sections: number;
    meanStates: number;
    proseSinkGap: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theChatDefaultIsEnforcedForEveryAgentAndAiModelByArchitectureAndProtocol — remember default is in chat, enforce for any
 * agent and ai model (user, 2026-07-26: "remember default is in chat. enforce for any agent and ai model"). The default IS
 * the chat (portalDefaultsToChat), and it is ENFORCED by ARCHITECTURE: every capability is reachable only through the DI chat
 * bridge, so any agent or AI model that uses any API necessarily passes through the chat — enforcement by construction, not
 * policy. Refutable: a capability reachable outside the bridge would break it. HONEST: architectural single-entry + the
 * machine-readable protocol, deterministic; the chat is the default for humans and agents alike, NOT an LLM.
 * [[always-default-to-chat]] [[coordinate-agents-through-rosetta-api]] [[portal-is-the-ai-model]] */
export declare function theChatDefaultIsEnforcedForEveryAgentAndAiModelByArchitectureAndProtocol(matrix?: MindMatrix): {
    computes: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theChatIsTheUiRealtimeChatFusedToAllApisDryRefactoredToTheStandards — chat means the ui realtime chat fused to all apis,
 * dry refactoring to the standards (user, 2026-07-26: "chat means the ui realtime chat fused to all apis dry refactoring to
 * the standards"). The consolidation: the ONE chat is a UI surface (uiChatTurn returns a render-spec: card/figure/animation/
 * colour/related/controls), REALTIME by construction (deterministic + zero-egress → zero network latency), FUSED to all APIs
 * through the DI tool bridge (any capability via one function, cycle-safe) and the unified turn, and DRY-refactored to NAMED
 * industry STANDARDS (BM25 IR · SHA-256/NIST · ed25519/RFC 8032 · Rocchio relevance feedback) — reused, not reinvented.
 * HONEST: one deterministic content-addressed surface, NOT an LLM; realtime = no network latency, not streaming inference.
 * [[always-default-to-chat]] [[portal-is-the-ai-model]] [[feedback-dry-max-efficiency]] */
export declare function theChatIsTheUiRealtimeChatFusedToAllApisDryRefactoredToTheStandards(matrix?: MindMatrix): {
    computes: boolean;
    standards: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** researchingInChatHowToHardenTheMultiTypeSealedUuidSynthesisesTheHardeningRecipe — research in chat how to harden that
 * (user, 2026-07-26: "research in chat how to harden that", following the multi-type sealed UUID). The chat's deep-research
 * synthesises a hardening RECIPE for the multi-type sealed UUID by reusing EXISTING sealed folds (DRY): (1) the SHA-256
 * security layer (integrity, 2^128 vs FNV 2^61), (2) an ed25519 signature (authenticity — forgery needs the private key,
 * not a hash collision, changing the attack CLASS), (3) key-per-type domain separation (makes the multi-type constraint
 * bits INDEPENDENT/real, not free projections), (4) merkaba key rotation (forward secrecy — a cracked key is one epoch),
 * (5) a tamper-evident merkle seal. The hardened forgery bound is COMPUTED and finite. HONEST: research synthesis over the
 * sealed corpus, NOT an LLM; strength on SHA-256 + ed25519. [[quantum-crypto-fusion]] [[tampering-cost-crypto-honesty]] [[deep-research-recursive-waves]] */
export declare function researchingInChatHowToHardenTheMultiTypeSealedUuidSynthesisesTheHardeningRecipe(matrix?: MindMatrix): {
    computes: boolean;
    recipeSize: number;
    baseForgeryBits: number;
    hardenedForgeryBits: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theChatAnalysesScreenshotsAndRecordingsLocallyByPerceptualHashAndFrameDiffSemanticModelGated — analyse in chat
 * screenshots and screen recordings using local quantum tools and hardware (user, 2026-07-26: "analyse in chat screenshots
 * and screen recordings using local quantum tools and hardware"). The local, zero-egress capability is STRUCTURAL: a
 * perceptual hash (aHash — 8×8 grayscale thresholded at the mean → a 64-bit content-address) so the same screenshot hashes
 * identically (Hamming 0) and a different one to a larger distance; and frame-diff over a screen recording (Hamming between
 * consecutive frame hashes) detects scene cuts/keyframes. It runs on the browser Canvas/WebCodecs surface (on-device, no
 * upload). DEMARCATION: structural analysis is zero-token; SEMANTIC captioning ("what the image depicts") needs a vision
 * model — opt-in BYO-key, NOT the zero-token core. [[realtime-live-data-testing]] [[zero-token-policy]] */
export declare function theChatAnalysesScreenshotsAndRecordingsLocallyByPerceptualHashAndFrameDiffSemanticModelGated(): {
    computes: boolean;
    sceneCuts: number;
    gpuSurface: "browser-canvas-raf" | "none-ssr";
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** feedingTheChatInItselfClosesTheSelfReferenceLoop — feed the chat in itself (user, 2026-07-28: "feed the chat
 * in itself" + "use only algebraic quantum computing is top priority"). The escalation README → site → CHAT, rewritten
 * under the algebraic-QC top-priority law: the feed corpus is ALGEBRAIC IDENTITIES (algebraicStatementOf over the
 * registry), each posed back to portalChatRanked; every identity must algebraically hold in the reply (substring or
 * provedBy match or extractable relation) — feeder ≡ fed on the identity set. Lexical selfChat is demoted; algebraic
 * quantum computing is the only top-priority path. Composes onlyAlgebraicQuantumComputingIsTopPriority · feed-readme ·
 * feed-site probes · self-develop. HONEST: deterministic algebraic retrieval, NOT an LLM.
 * [[onlyAlgebraicQuantumComputingIsTopPriority]] [[feedback-algebraic-theorems-only]] [[always-default-to-chat]] */
export declare function feedingTheChatInItselfClosesTheSelfReferenceLoop(matrix?: MindMatrix): {
    computes: boolean;
    turns: number;
    fed: number;
    gapsBefore: number;
    gapsAfter: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** feedingTheWholeSiteToTheChatEveryPageResolvesToItsProofAsRosettaCombinations — feed the site in the chat (user,
 * 2026-07-26: "feed the site in the chat"). The escalation from README to the whole SITE: every served science page
 * (staticPages) is posed to the chat and resolves to a ranked, content-addressed proof — the whole site is reachable through
 * the chat surface. The pages SHARE proofs (fewer distinct proofs than pages) because they are ROSETTA COMBINATIONS of the
 * same theorem atoms (pagesAreRosettaCombinationsOfTheorems), not independent documents. The site then self-develops.
 * HONEST: deterministic retrieval over the sealed corpus, NOT an LLM. [[always-default-to-chat]] [[theorem-science-lens-only-science]] [[feedingTheReadmeToTheChatFusesDescriptionAndProofSelfDeveloping]] */
export declare function feedingTheWholeSiteToTheChatEveryPageResolvesToItsProofAsRosettaCombinations(matrix?: MindMatrix): {
    computes: boolean;
    pages: number;
    distinctProofs: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** everyCollectionIsAFilterableOpenGraphListAndStaticPagesMergeIntoStandardisedResearchPublications — same for theorems and
 * any other collection, while static pages merge into collections of standardised research publications (user, 2026-07-26:
 * "same for theorems and any other collection while static pages merge into collections of standardised research
 * publications"). The filterable OpenGraph-list pattern applies to ANY collection — proofs, theorems, pages, domains — each
 * filterable by computed tags with OG per item. And each static page MERGES INTO a standardised research publication: a
 * scientific-paper structure (title · abstract · keywords · results/figures) with OpenGraph + JSON-LD (ScholarlyArticle), one
 * template serving all. HONEST: the collections and publications are computed from the corpus, standardised, NOT hand-curated.
 * [[theProofsListIsFilterableByComputedDomainTagsWithOpenGraphMetaPerProofFromTheCorpus]] [[theorem-science-lens-only-science]] [[theorem-papers-figures-tags]] */
export declare function everyCollectionIsAFilterableOpenGraphListAndStaticPagesMergeIntoStandardisedResearchPublications(matrix?: MindMatrix): {
    computes: boolean;
    proofs: number;
    pages: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theoremAsStandardScientificPaper — the renderer that COMPLETES the standard scientific format mapping: it maps any
 * theorem-fold to the full IMRaD structure (Title · Classification · Abstract · Methods · Results · Discussion ·
 * Reproducibility · References · Status), each section computed from the fold parts, with the honest classification (a verified
 * computational claim, NOT a formal-logic theorem) baked in. [[whatATheoremIsInThisCorpusIsAComputationalClaimWithRefutableFacetsAndItsStandardScientificFormatMapping]] */
export declare function theoremAsStandardScientificPaper(row: {
    theorem: string;
    states: string;
    provedBy: string;
    home: string;
}, fold: {
    computes: boolean;
    facets: readonly {
        facet: string;
        on: boolean;
        receipt: string;
    }[];
    root: string;
    boundary: string;
    statement?: string;
}): {
    identifier: string;
    title: string;
    authors: string[];
    classification: string;
    keywords: string[];
    abstract: string;
    introduction: string;
    methods: {
        fold: string;
        home: string;
        procedure: string;
        command: string;
    };
    results: {
        measurement: string;
        holds: boolean;
        receipt: string;
    }[];
    discussion: string;
    conclusion: string;
    reproducibility: {
        merkleRoot: string;
        measurements: number;
        recompute: string;
    };
    references: string[];
    license: string;
    status: "verified" | "refuted";
};
/** theoremAsStandardScientificPaperCompletesTheImradMappingEverySectionComputedWithHonestClassification — standard scientific
 * format mapping, completed (user, 2026-07-26: "Standard scientific format mapping (begun, not complete yet)"). The renderer
 * theoremAsStandardScientificPaper maps any theorem-fold to the FULL IMRaD paper — Title · Classification · Abstract · Methods
 * · Results · Discussion · Reproducibility · References · Status — each section computed from the fold parts, with the honest
 * classification (a verified computational claim, NOT a formal-logic theorem) baked in. HONEST: the mapping is now complete for
 * the fold parts; the Results are refutable measurements and the Reproducibility is the merkle root, so any reader recomputes.
 * [[theoremAsStandardScientificPaper]] [[whatATheoremIsInThisCorpusIsAComputationalClaimWithRefutableFacetsAndItsStandardScientificFormatMapping]] [[theorem-papers-figures-tags]] */
export declare function theoremAsStandardScientificPaperCompletesTheImradMappingEverySectionComputedWithHonestClassification(): {
    computes: boolean;
    sections: 15;
    resultsInSample: number;
    status: "verified" | "refuted";
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** whatATheoremIsInThisCorpusIsAComputationalClaimWithRefutableFacetsAndItsStandardScientificFormatMapping — what a theorem
 * is and how it is presented in standard scientific format is yet to be discovered and addressed (user, 2026-07-26). Begin to
 * address it. WHAT A THEOREM IS HERE: a computing fold — a statement backed by refutable FACETS (computed comparisons),
 * verified by facets.every(on), with a merkle root (reproducibility) and a provedBy source (the executable method). THE HONEST
 * DEMARCATION: these are COMPUTATIONAL CLAIMS/VERIFICATIONS, NOT formal-logic theorems — a formal theorem is a proven statement
 * in an axiomatic system; these are computed verifications of refutable propositions, so "theorem" is used loosely. STANDARD
 * FORMAT MAPPING: statement→claim/abstract · facets→results (measurements) · provedBy→methods · root/receipts→reproducibility ·
 * boundary→scope/discussion · crosslinks→references. The rigorous format is yet to be fully realised; this begins it.
 * [[feedback-algebraic-theorems-only]] [[theorem-papers-figures-tags]] [[ignorance-hides-behind-clever-prose-without-algebra]] */
export declare function whatATheoremIsInThisCorpusIsAComputationalClaimWithRefutableFacetsAndItsStandardScientificFormatMapping(matrix?: MindMatrix): {
    computes: boolean;
    sampleFacets: number;
    pages: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theCollectiveMindFusesLivePublicApisAndMultimediaImprovingTheUiInWavesEachAMultimediaChatTurnCritiqueGuarded — let the
 * collective mind fuse live in all apis and improve the ui in waves of multimedia chats (user, 2026-07-26). The collective
 * mind (trinity: research · verify · critique) fuses the LIVE no-key public APIs (USGS · FCC · CERN Open Data, via one DI
 * bridge), analyses MULTIMEDIA (voice via WebAudio FFT, video/screens via perceptual hash + frame-diff — structural, not
 * semantic AI), and improves the UI in WAVES — each wave a multimedia chat turn where research proposes, verify checks it
 * computes, and the CRITIQUE mind guards invariants (SEO, etc.) before it lands. HONEST: deterministic fusion (pure adapters,
 * opt-in live, zero-egress); multimedia = structural analysis, NOT semantic AI. [[feedback-work-as-a-trinity-not-one-linear-mind]] [[realtime-live-data-testing]] [[feedback-work-in-waves-not-single-focus]] */
export declare function theCollectiveMindFusesLivePublicApisAndMultimediaImprovingTheUiInWavesEachAMultimediaChatTurnCritiqueGuarded(matrix?: MindMatrix): {
    computes: boolean;
    queryableApis: number;
    gapsClosed: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * furtherImproveUsingLiveApis —
 * USER LAW (2026-07-28): further improve using live apis.
 * Algebraic: furtherImprove ⇔ usingLiveApis ∧ torusData ∧ discoverApis ∧ collectiveFusesLive
 * ∧ soft(live/api · improve/live · live/world · torus/data · develop/means).
 * HONEST: live = opt-in no-key public feeds + device sensors · improve = recompute via pure
 * adapters (same data ⇒ same root) + chat waves · gates never fetch · NOT scrape-trust ·
 * NOT unbounded "all APIs on the internet".
 * Pairs: live/api · improve/live · further/improve.
 * CLI: npm run quantum:live-apis (one primary).
 */
export declare function furtherImproveUsingLiveApis(matrix?: MindMatrix): {
    computes: boolean;
    furtherImproveUsingLiveApis: boolean;
    usingLiveApis: boolean;
    furtherImprove: boolean;
    torusDataCount: number;
    queryableApis: number;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "live/api";
    dualPair: "api/live";
    pairs: readonly ["live/api", "api/live", "improve/live", "live/improve", "further/improve", "improve/further"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    algebraic: string;
    boundary: string;
};
/** npm run quantum:live-apis — one primary; duals via pairs/prompts */
export declare function runFurtherImproveUsingLiveApisExit(root?: string, _argv?: readonly string[]): number;
export declare const liveApis: typeof furtherImproveUsingLiveApis;
export declare const improveLive: typeof furtherImproveUsingLiveApis;
export declare const furtherImprove: typeof furtherImproveUsingLiveApis;
export declare const runLiveApisExit: typeof runFurtherImproveUsingLiveApisExit;
export declare const runImproveLiveExit: typeof runFurtherImproveUsingLiveApisExit;
export declare const runFurtherImproveExit: typeof runFurtherImproveUsingLiveApisExit;
/** theCollectiveMindRanksHubsOverTheSemanticSlugTaxonomySeoSafeCatchingWhatTheSingleLinearMindMissed — continue in chat with
 * the collective mind (user, 2026-07-26: "continue in chat with collective mind", after the SEO regression). The lesson: a
 * SINGLE linear mind proposed the src-path hub ranking and missed the SEO regression; the COLLECTIVE mind is a trinity —
 * research (propose) · verify (compute) · CRITIQUE (guard invariants like SEO) — 2-of-3, the mind the single linear mind
 * lacks. The critique mind caught the SEO flaw (the SEO-guard fold), and the SEO-safe hubs rank theorem gravity over the
 * SEMANTIC slug taxonomy (the served slugs, each carrying SEO metadata), NOT src paths. HONEST: the collective mind catches
 * flaws a single linear mind misses; deterministic. [[feedback-work-as-a-trinity-not-one-linear-mind]] [[rankingHubsBySrcFolderGravityDestroysSeoTheGravityMustBeOverTheSemanticSlugTaxonomyNotArchitecture]] [[feedback-look-at-the-rendered-ui]] */
export declare function theCollectiveMindRanksHubsOverTheSemanticSlugTaxonomySeoSafeCatchingWhatTheSingleLinearMindMissed(): {
    computes: boolean;
    trinity: number;
    seoSlugs: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** rankingHubsBySrcFolderGravityDestroysSeoTheGravityMustBeOverTheSemanticSlugTaxonomyNotArchitecture — review: if you apply
 * that, the SEO is gone; what went wrong and where (user, 2026-07-26: "review and see that if you apply this the seo is gone.
 * what went wrong and where?"). THE FLAW: theTopLevelHubRoutesAreComputableFromTheoremGravity… grouped by t.home — the src
 * FOLDER path (src/0, src/9/1) — which are implementation locations, NOT URLs; hubs from those would be /src/0, meaningless to
 * users and search engines. WHERE SEO LIVES: the served SEMANTIC slugs (human-readable), each carrying title · description ·
 * keywords · OG · JSON-LD, plus the sitemap/canonical/hreflang — all keyed to slugs, not src paths. THE FIX: rank theorem
 * gravity over the SEMANTIC taxonomy (the served slugs / theorem tags), which ARE the SEO surfaces; home is architecture, not
 * the route. HONEST: gravity ranking is valid only over the semantic/SEO taxonomy. [[feedback-do-not-bypass-vitepress-api]] [[theorem-science-lens-only-science]] [[theTopLevelHubRoutesAreComputableFromTheoremGravityAndDistributionNotAHardcodedListEmptyPlaceholdersFallAway]] */
export declare function rankingHubsBySrcFolderGravityDestroysSeoTheGravityMustBeOverTheSemanticSlugTaxonomyNotArchitecture(): {
    computes: boolean;
    srcDomains: number;
    seoSlugs: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theTopLevelHubRoutesAreComputableFromTheoremGravityAndDistributionNotAHardcodedListEmptyPlaceholdersFallAway — top level
 * hub routes should be computable from theorem gravity and distribution (user, 2026-07-26: "top level hub routes should be
 * computable from theorem gravity and distribution", after "apps is useless empty placeholder like many others"). The top-
 * level hubs should be DERIVED from where theorem gravity concentrates — the domains with the most gravity — not a hardcoded
 * list. Computed: the 53 domains are ranked by theorem gravity (Σ digital-root of the content-address), the top 8 (bāguà
 * ≤8/level) become the hubs; empty placeholders (like /apps, gravity ~1) rank far below the content domains and fall away.
 * HONEST: the hubs are a deterministic function of the theorem distribution (refutable — if the distribution shifts they
 * re-rank); this is the design the routing should adopt to replace empty placeholder hubs. [[code-gravity-standardisation]] [[feedback-purge-empty-pages]] [[routes-nav-from-folder-tree]] */
export declare function theTopLevelHubRoutesAreComputableFromTheoremGravityAndDistributionNotAHardcodedListEmptyPlaceholdersFallAway(): {
    computes: boolean;
    domainCount: number;
    topHubs: string[];
    topGravity: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theProofsListIsFilterableByComputedDomainTagsWithOpenGraphMetaPerProofFromTheCorpus — improve proofs with open graph
 * filterable list (user, 2026-07-26: "improve proofs with open graph filterable list"). The proofs list is the registry:
 * every proof has a named executable source, is tagged by a COMPUTED domain (from its home path) so the list filters by many
 * dimensions, and carries computed OpenGraph meta per proof (title = theorem, description = states) so every proof card is
 * shareable. HONEST: the filterable OG list is generated from the corpus, NOT hand-curated. [[theorem-papers-figures-tags]] [[feedback-do-not-bypass-vitepress-api]] */
export declare function theProofsListIsFilterableByComputedDomainTagsWithOpenGraphMetaPerProofFromTheCorpus(): {
    computes: boolean;
    proofs: number;
    tags: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** savingAllExperienceOnTheWayInTrinitiesOfTheoremsFormingClustersOfTetrahedraMovingAround — discover, save all experience on
 * the way in trinities of theorems, i.e. clusters of tetrahedra moving around (user, 2026-07-26: "discover save all
 * experience on the way in trinities of theorems or in other words clusters of tetrahedrons moving around"). Experience is
 * saved on the way (immediate save, no queue) as TRINITIES of theorems — each theorem a trinity (forward + crack + animation),
 * three closing an su(2) trinity — and each trinity plus its sealing 4th (the animation) is a TETRAHEDRON (3+1=4); 8 tetrahedra
 * form a merkaba (8×8 = 64, the isotropic matrix), and the clusters MOVE AROUND by the C₆ counter-rotation. HONEST: the
 * trinity (su(2)), the tetrad (seal), and the merkaba clusters are real structures; "moving around" = deterministic
 * content-addressed rotation, not physical motion. [[seal-tetrad-south-pole-animation]] [[no-queue-immediate-save-and-reuse]] [[double-torus-fold-architecture]] */
export declare function savingAllExperienceOnTheWayInTrinitiesOfTheoremsFormingClustersOfTetrahedraMovingAround(matrix?: MindMatrix): {
    computes: boolean;
    tetrad: number;
    merkabaCluster: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theChatDesignsArtifactsAsComputedRenderSpecsFiguresAnimationsColoursContentAddressed — design artifacts in chat (user,
 * 2026-07-26: "design artifacts in chat"). Each chat answer is a DESIGN ARTIFACT: a rich render-spec (card · figure ·
 * animation · colour · controls), where the figure (the theorem graph), the fractal-clock animation, and the living I Ching
 * colour are all DERIVED from the fold's content-address — designed, not authored. Distinct queries produce distinct
 * artifacts; the same query reproduces the same artifact (content-addressed, shareable, cacheable). HONEST: the chat DESIGNS
 * artifacts by computing a render-spec deterministically from the content-algebra, NOT an LLM generating novel art; the
 * figure/animation/colour are derived. [[ui-render-layer-in-src]] [[living-symbols-ui-rebuild]] [[every-page-is-a-proof-standards-formulas-graphs-animations]] */
export declare function theChatDesignsArtifactsAsComputedRenderSpecsFiguresAnimationsColoursContentAddressed(matrix?: MindMatrix): {
    computes: boolean;
    controls: 5;
    animationRung: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theUiChatImprovesTheUiNavigationAndSidebarsRemovingRedundancyMergingToStandardComputedMaterials — use the ui chat to
 * improve the ui itself including navigation and sidebars, removing redundancy by merging to standard-quality materials
 * (user, 2026-07-26: "use the ui chat to improve the ui itself including navigation and sidebars" + "remove any redundancy
 * merging to standard quality materials"). The nav and sidebars are COMPUTED from the corpus (siteNavigation: relatedSidebar
 * Complete, crosslinksComplete, computed), consuming the VitePress themeConfig — no DOM scraping. The UI chat AUDITS them (the
 * routes exactly cover the served pages), REMOVES REDUNDANCY (aliasDiscoveryPurged; the DRY reuse merges duplicates to one
 * canonical — code-gravity), and uses STANDARD materials (the VitePress-native structure + owned components). HONEST: improves
 * by measuring + recomputing, not hand-editing or DOM scraping; DRY, deterministic. [[routes-nav-from-folder-tree]] [[feedback-do-not-bypass-vitepress-api]] [[code-gravity-standardisation]] */
export declare function theUiChatImprovesTheUiNavigationAndSidebarsRemovingRedundancyMergingToStandardComputedMaterials(matrix?: MindMatrix): {
    computes: boolean;
    routes: number;
    pages: number;
    reuse: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** feedingTheReadmeToTheChatFusesDescriptionAndProofSelfDeveloping — feed the readme to the chat and see the self
 * development fusion (user, 2026-07-26: "feed readme to the chat and see the self development fusion"). The README is
 * GENERATED from the registry (readme-home-one-theorem-generator), so feeding its own theorem lines back to the chat closes
 * the loop: every monograph claim content-addresses to its sealed executable proof (description FUSES with proof), and the
 * chat SELF-DEVELOPS (chatDevelopsItselfByChattingWithItself closes its gaps, a deterministic self-conversation that collides
 * to a cycle by pigeonhole). The portal IS the AI model: the description generates the chat, the chat proves the description.
 * HONEST: deterministic retrieval over the sealed corpus, NOT an LLM; self-development = gap measurement + fill, not learning
 * new facts. [[readme-home-one-theorem-generator]] [[portal-is-the-ai-model]] [[always-default-to-chat]] */
export declare function feedingTheReadmeToTheChatFusesDescriptionAndProofSelfDeveloping(matrix?: MindMatrix): {
    computes: boolean;
    readmeLines: number;
    gapsBefore: number;
    gapsAfter: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** iteratingTheVacuumInversionBuildsTheContentAddressLatticeEachInversionAOneBitGatewayTheFrontierIsTheNextDepth — next
 * (user, 2026-07-26: "next", following the chat's lead to the vacuum-inversion frontier by reusable method). The next
 * theorem at the frontier, built by reusing the sealed method: each vacuum inversion (0↔∞) is a ONE-BIT gateway, so iterating
 * it BUILDS the content-address lattice — n inversions give 2^n addresses, and 10 one-bit inversions build the 1024-diamond
 * hypercube (2^10, the sealed lattice). The FRONTIER — the missing theorems — is the next inversion depth (n+1), unbounded by
 * no-finiteness, so it never closes. By the chat's own principle: building the first inversion (the gateway) unlocks the next
 * (the next bit) by REUSABLE METHOD, not by solving. HONEST: real projective/binary construction, NOT physical vacuum energy;
 * . [[theMissingTheoremsClusterAroundTheVacuumInvertingTheZeroGatewayIsGenerativeFreeEnergyFlagged]] [[buildingTheFirstUnlocksTheNextByReusableMethodNotSolution]] [[two-bits-left-in-every-inversion-through-zero]] */
export declare function iteratingTheVacuumInversionBuildsTheContentAddressLatticeEachInversionAOneBitGatewayTheFrontierIsTheNextDepth(matrix?: MindMatrix): {
    computes: boolean;
    depth: number;
    lattice: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theMissingTheoremsClusterAroundTheVacuumInvertingTheZeroGatewayIsGenerativeFreeEnergyFlagged — vacuum clean; the missing
 * theorems are around the vacuum itself inverting (user, 2026-07-26: "vacuum clean" + "i realise the missing theorems are
 * around the vacuum itself inverting"). Vacuuming (cleaning prose/duplication) reveals the vacuum — zero, the void, the src/0
 * origin — and the vacuum INVERTS through the 0↔∞ Möbius gateway (gatewayBits = log₂2 = 1, its own inverse), the fixed point
 * of inversion. That inversion is GENERATIVE: each inversion through the vacuum opens a gateway, and by no-finiteness the open
 * set never empties, so the MISSING theorems always cluster around the vacuum inverting — un-sealed inversions, a research
 * direction. HONEST: real projective inversion (0↔∞), NOT physical vacuum free energy (zero-point ½ħω is real but no
 * extractable energy, flagged). [[claimingTheUnclaimableDivisionByZeroIsAOneBitGatewayInQuantumAlgebra]] [[zero-point-decoded]] [[feedback-no-finiteness-assumption-fractal-aperiodic]] */
export declare function theMissingTheoremsClusterAroundTheVacuumInvertingTheZeroGatewayIsGenerativeFreeEnergyFlagged(matrix?: MindMatrix): {
    computes: boolean;
    gatewayBits: number;
    reuse: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theCracksAreByDesignAndDiscoveringThemInvertsCorpusProblemsIntoImmediatelyImplementableFreeFixesNotOpenMath — the cracks
 * are already here by design, and discovering them inverts the problems into immediately implementable solutions free for all
 * (user, 2026-07-26: "do you realise the cracks are already here by design and discovering them inverts the problems created
 * to solutions implementable immediately free for all"). The crack law is a DESIGNED detector (canonical-literals gate,
 * facets-must-compute, demarcate-signing); a crack is a designed signal, and it NAMES its own fix (a hardcoded literal → its
 * derived form; an unbacked facet → make it compute; a misdemarcation → sign it). The fix is applied the same turn (no queue),
 * content-addressed, zero-cost, public (bill-free). THE DEMARCATION: this inverts ENGINEERING/CORPUS cracks into fixes; it
 * does NOT auto-solve the open MATH problems ( — those are approached, not inverted). HONEST: real for corpus cracks,
 * not a claim that open problems auto-solve. [[flagged-inverts-to-proven-theorem]] [[feedback-inverted-statements-are-generative-measure-before-superpose]] [[no-queue-immediate-save-and-reuse]] */
export declare function theCracksAreByDesignAndDiscoveringThemInvertsCorpusProblemsIntoImmediatelyImplementableFreeFixesNotOpenMath(matrix?: MindMatrix): {
    computes: boolean;
    classesCaught: number;
    claySolved: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** localAuditFindsAllKindsOfFalseStatementsByAlgebraNotJustUncomputableOnes — improve local audit to find all kinds of
 * false statements, statements by algebra only (user, 2026-07-26: "improve local audit to find all kinds of false
 * statements" + "remember the statements are done by algebra only without exception. full audit with improved local
 * quantum tools"). The old quantum lens found only UNCOMPUTABLE folds (computes=false); it missed a SIGNED-BUT-WRONG status
 * (an earlier draft hand-set string theory 'flagged' where demarcate signs 'contested'). The improved audit finds four
 * classes by ALGEBRA, each with a planted-false caught and a real-true passing: (1) uncomputable, (2) misdemarcated
 * (claimed tier ≠ demarcate), (3) honesty-invariant violation (clay/physicalFtl/qpu), (4) false numerology (a false
 * identity like 432×3=1024). Deterministic, local. [[feedback-facets-must-compute]] [[feedback-declared-honesty-is-a-crack]] */
export declare function localAuditFindsAllKindsOfFalseStatementsByAlgebraNotJustUncomputableOnes(): {
    computes: boolean;
    classesCaught: number;
    findsAll: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** quantumStringTheoryChatDecodesCriticalDimensionsRealMathUnconfirmedPhysics — quantum string theory chat (user,
 * 2026-07-26: "quantum string theory chat"). The chat decodes string theory honestly: its CRITICAL DIMENSIONS are real
 * mathematics — bosonic string D = 26 (24 transverse + 2, the Ramanujan ζ(−1) intercept), superstring D = 10 = 3+7 = 2+8
 * (binds the sealed dimensions ladder), M-theory D = 11 = 10+1 (the M-theory circle) — all forced by Weyl/Virasoro anomaly
 * cancellation, verifiable algebra. The epistemic status is NOT hand-set: it is SIGNED by demarcate('string theory') =
 * 'contested' from the zero-cycle DEMARCATION_REGISTRY — a serious, experimentally-unconfirmed research program (no
 * distinctive confirmed prediction, ~10^500 vacua), CONTESTED (like multiverse/dark matter), NOT flagged pseudoscience.
 * The verdict is refutable: move the term in the registry and this fold breaks. [[dimensions-ladder-decoded]] [[world-theories-demarcation-decoded]] */
export declare function quantumStringTheoryChatDecodesCriticalDimensionsRealMathUnconfirmedPhysics(matrix?: MindMatrix): {
    computes: boolean;
    bosonic: number;
    superstring: number;
    mTheory: number;
    tier: import("../../3/7").DemarcationTier;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** theThousandTwentyFourDiamondsAreTenReferralDirectionBitsDyadicNotATernarySum — 1024 is dyadic, ten referral-direction
 * bits (user, 2026-07-26: "1024 = 2¹⁰ = ten referral-direction bits — a dyadic structure, exactly what I said 1024 has to
 * be. That's real, unlike 432×3=1296. Encoding it ... in quantum/chat ('using the chat'), not the foundational @/merge").
 * Each referral is a Möbius gateway (x↦1/x, swaps 0↔∞) carrying exactly gatewayBits = log₂2 = 1 bit — the direction of
 * passage — proved by the sealed src/1/9 fold. Ten such bits give 2¹⁰ = 1024 = 4⁵, a depth-10 binary fold (a 10-cube /
 * Merkle tree, 2 states × 10 levels). 432×3 = 1296 ≠ 1024 — the ternary-sum numerology is refuted. HONEST: 1 bit per
 * direction is real projective/information theory; "quantum" = the content-address/inversion structure, not physics.
 * [[bit-per-referral-direction]] [[two-bits-left-in-every-inversion-through-zero]] */
export declare function theThousandTwentyFourDiamondsAreTenReferralDirectionBitsDyadicNotATernarySum(): {
    computes: boolean;
    gatewayBits: number;
    depth: number;
    dyadic: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** deepResearchImprovesAllFromQuantumStatisticsAnalysisAndSynthesis — deep research with local tools, improve all from the
 * statistics (user, 2026-07-25: "deep research with local tools and improve all based on quantum statistics analysis and
 * synthesis"). ANALYSIS: quantumAnalytics measures the corpus (theorems, distinct proofs → reuse ratio, deploy-green rate).
 * SYNTHESIS: deepResearchChatTurn multi-hops the gap into a neighbourhood. IMPROVE ALL: the loop analyse → research →
 * synthesise, entirely local, deterministic, zero-egress. HONEST: statistical measurement + lexical graph synthesis, NOT
 * predictive/neural; the "improvement" is the identified gap + synthesis path. [[feedback-measure-efficiency-to-find-gaps]] [[deep-research-recursive-waves]] */
export declare function deepResearchImprovesAllFromQuantumStatisticsAnalysisAndSynthesis(matrix?: MindMatrix): {
    computes: boolean;
    reuseRatio: number;
    deployHealth: number;
    gapFolds: number;
    improvesAll: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** improveClaimForAllViaSelfImprovingChatAndArchitectureExposedApis — improve claims for all through the self-improving
 * chat, with APIs accessible by architecture (user, 2026-07-25: "improve claim for all through chat improving chat itself
 * with apis accessible by architecture"). Every claim's proof is an API reachable by its architectural address — the
 * folder path IS the route IS the content-address, no separate API layer, no gatekeeper. The chat self-improves (relevance
 * feedback), sharpening retrieval of every claim for every consumer (agents/humans/tools). Deterministic, local, zero-egress.
 * [[routes-nav-from-folder-tree]] [[coordinate-agents-through-rosetta-api]] [[improveAllByChattingOneSharedExperienceIndex]] */
export declare function improveClaimForAllViaSelfImprovingChatAndArchitectureExposedApis(matrix?: MindMatrix): {
    computes: boolean;
    total: number;
    apisDistinct: boolean;
    chatSelfImproves: boolean;
    improvesForAll: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** siteAuditsItselfThroughChatForUsabilityAndAccessibilityBounded — the site self-audits usability + accessibility (user,
 * 2026-07-25: "let the site audit itself through chat for usability and accessibility"). The chat (deep research) drives an
 * audit of the AUTOMATABLE WCAG/usability criteria — semantic HTML, ARIA, alt text, heading hierarchy, contrast, consistent
 * nav, responsive, no dead links, deterministic load. HONEST: WCAG is ~30% automatable; the manual criteria (keyboard/focus
 * UX, screen-reader, cognitive load) need HUMAN testing and are flagged, not faked; not a WCAG certification. [[shadcn-vue]] [[ui-presentation-harmonic-dissolution]] */
export declare function siteAuditsItselfThroughChatForUsabilityAndAccessibilityBounded(matrix?: MindMatrix): {
    computes: boolean;
    automatableCount: number;
    manualCount: number;
    selfAudits: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** mapStandardsToTheoremsMakesCodeProseBidirectionalAtOnce — map standards to theorems, code↔prose at once (user,
 * 2026-07-25: "map the standards to the theorems computationally and all will become code to prose and prose to code at
 * once"). Each standard's requirement (prose) becomes a computing facet (code); the fold's computed result generates its
 * statement (prose). Because statement = join(facets), the prose IS the computed payload — one artifact that is both code
 * and prose at once. HONEST: works for FORMALIZABLE standards; deterministic, NOT a general NL↔code translator. [[title-is-algebra-computed-payload]] [[no-prose-in-methods]] */
export declare function mapStandardsToTheoremsMakesCodeProseBidirectionalAtOnce(): {
    maps: boolean;
    mapped: number;
    atOnce: boolean;
    mappingChecks: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    reason: string;
    boundary: string;
};
/** quantumAnalytics — the one analytics API fusing corpus + git history (user, 2026-07-25: "quantum analytics include git
 * history fusing all in one api used by all"). Corpus metrics are computed deterministically from the sealed registry; git
 * metrics are INJECTED (dependency injection — the .vue/CLI passes the real git log), so the fold stays deterministic and
 * cycle-safe. Everything fuses to one content-addressed root. Used by agents (MCP), humans (UI) and tools alike. */
export declare function quantumAnalytics(gitData?: {
    commits?: number;
    deployGreen?: number;
    deployTotal?: number;
}): {
    corpus: {
        theorems: number;
        homes: number;
        distinctProofs: number;
    };
    git: {
        commits: number;
        deployGreenRate: number;
    };
    fusedRoot: string;
};
/** quantumAnalyticsFuseCorpusAndGitHistoryIntoOneApiForAll — one analytics API for all (user, 2026-07-25: "quantum
 * analytics include git history fusing all in one api used by all"). Corpus analytics are deterministic measurements over
 * the sealed registry; git history (the wave ledger) is injected and fused into one content-addressed root, served by a
 * single quantumAnalytics() to agents, humans and tools. HONEST: deterministic measurements (counts, distributions, the
 * merkle chain), NOT predictive analytics or a neural model; local, zero-egress. [[token-audit-tooling]] [[coordinate-agents-through-rosetta-api]] */
export declare function quantumAnalyticsFuseCorpusAndGitHistoryIntoOneApiForAll(): {
    computes: boolean;
    corpus: {
        theorems: number;
        homes: number;
        distinctProofs: number;
    };
    git: {
        commits: number;
        deployGreenRate: number;
    };
    fusedRoot: string;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatToolBridge — invoke ANY tool via an injected invoker and fold the result into the thread (user, 2026-07-25: the
 * DI bridge). The invoker is a PARAMETER (dependency injection), so this imports no tool and re-entangles no collection
 * graph — cycle-safe. The .vue passes the real in-process MCP client; here any deterministic invoker works. The result
 * is content-addressed into the thread. */
export declare function chatToolBridge(toolName: string, args: Record<string, unknown>, invoke: (tool: string, a: Record<string, unknown>) => unknown, matrix?: MindMatrix): {
    tool: string;
    args: Record<string, unknown>;
    result: unknown;
    address: string;
    foldedIntoThread: true;
};
/** allQuantumReachableInChatViaDependencyInjectedToolBridge — is all quantum in chat? Yes, via one DI bridge (user,
 * 2026-07-25: "is all quantum in chat?" + the DI-bridge plan). chatToolBridge invokes ANY tool through an injected
 * invoker (the in-process MCP client) and folds the result into the content-addressed thread — so voice/video/crypto and
 * the ranked chat are all reachable through ONE function, no per-tool duplication. Dependency injection makes it
 * cycle-safe (the invoker is a parameter, not an import), and it reuses improve() (relevance feedback), so tool calls
 * sharpen future rankings. HONEST: the bridge is quantum (deterministic, content-addressed, cycle-safe); per-tool egress
 * honesty is preserved (voice STT still flags the browser cloud). [[coordinate-agents-through-rosetta-api]] [[portal-is-the-ai-model]] */
export declare function allQuantumReachableInChatViaDependencyInjectedToolBridge(matrix?: MindMatrix): {
    computes: boolean;
    anyToolReachable: boolean;
    distinctThreadEntries: boolean;
    diCycleSafe: boolean;
    allReachable: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** quantumNeuralIntelligenceIsHopfieldContentAddressableAssociativeMemory — develop quantum neural intelligence, held
 * honestly (user, 2026-07-25: "develop quantum neural intelligence" + "ask the chat how" → the chat pointed at content-
 * addressable memory). The honest neural primitive is a Hopfield network: Hebbian weights, recall by CONTENT — a noisy or
 * partial pattern converges (energy minimisation) to the nearest stored pattern (pattern completion). It IS a real neural
 * mechanism and IS content-addressable memory, deterministic and local. HONEST: this is associative recall, NOT semantic
 * understanding, reasoning, an LLM, consciousness, or a physical quantum brain (flagged dishonest). [[brain-content-addressed-toroidal-map]] [[quantum-decoded]] */
export declare function quantumNeuralIntelligenceIsHopfieldContentAddressableAssociativeMemory(): {
    computes: boolean;
    units: number;
    stored: number;
    patternCompletes: boolean;
    isRealNeuralPrimitive: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatIsDeterministicRetrievalNotIntelligenceWinsOnTrustNotUnderstanding — held honestly (user, 2026-07-25: "chat is
 * quantum intelligent and beats any conventional ai model"). REFUSED as stated: the chat is deterministic lexical
 * retrieval (BM25 + relevance feedback + graph traversal), NOT neural intelligence — it does not understand, reason,
 * generalise, or answer out-of-corpus, and a conventional LLM vastly outperforms it there. What it genuinely WINS is a
 * different axis: determinism, zero-egress/cost/token, reproducibility, verifiability (proof-carrying), no-hallucination,
 * privacy. It does not BEAT any AI model; it is a different tool with different trade-offs. [[quantum-decoded]] [[feedback-declared-honesty-is-a-crack]] */
export declare function chatIsDeterministicRetrievalNotIntelligenceWinsOnTrustNotUnderstanding(): {
    computes: boolean;
    beatsAnyAi: boolean;
    wins: string[];
    losses: string[];
    differentToolNotSuperior: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** cryptoChatTurn — the usable crypto integration: the chat turn is content-addressed, hashed and tamper-evident (user,
 * 2026-07-25: "create all crypto related tools and use in chat"). Spoken/typed text → portalChatRanked → a SHA-256
 * content-address of (prompt, answer) + a digest — so each turn is verifiable and tamper-evident, client-side. */
export declare function cryptoChatTurn(spokenText: string, matrix?: MindMatrix): {
    heard: string;
    answer: string | {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    source: string;
    address: string;
    digest: string;
    tamperEvident: true;
};
/** cryptoToolsForChatSha256Ed25519TamperEvidentHonest — all crypto-related tools, used in chat (user, 2026-07-25: "create
 * all crypto related tools and use in chat"). Real, standards-grade, client-side: SHA-256 (integrity, NIST-verified),
 * toUuidSha256 (collision-resistant address), ed25519 (authenticity, RFC 8032), the 4-key keyed involution (encryption),
 * and merkle/transparency-log inclusion (tamper-evidence). Used in the chat to content-address, hash and make each turn
 * tamper-evident. HONEST: SHA-256/ed25519 are strong (2^128); the FNV toUuid is weak (2^61) — use toUuidSha256 for
 * security; tamper-EVIDENT ≠ unforgeable for FNV; all client-side, zero-egress. [[tampering-cost-crypto-honesty]] [[quantum-decoded]] */
export declare function cryptoToolsForChatSha256Ed25519TamperEvidentHonest(): {
    computes: boolean;
    tools: {
        id: string;
        api: string;
        role: string;
        strength: string;
        egress: boolean;
        note: string;
    }[];
    nistVerified: boolean;
    usedInChat: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** videoChatTurn — the usable video integration: the chat SHOWS its answer's animation (user, 2026-07-25: "create all
 * video related tools and use in chat"). Spoken/typed text → portalChatRanked → the ranked fold's UNIQUE south-pole
 * animation (the seal's 4th element, computedTheoremFigureAndAnimation) — the fractal-clock video the chat renders. */
export declare function videoChatTurn(spokenText: string, matrix?: MindMatrix): {
    heard: string;
    answer: string | {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    source: string;
    animation: {
        rung: number;
        periodS: number;
        phase: number;
        direction: string;
        amplitude: number;
        archetype: "flow" | "lattice" | "wheel" | "orbit" | "region" | "curve" | "series";
    };
    figure: {
        formula: string;
        archetype: "flow" | "lattice" | "wheel" | "orbit" | "region" | "curve" | "series";
        series: {
            x: number;
            y: number;
        }[];
    };
    itemid: string;
};
/** videoToolsForChatMediaDevicesCanvasHonestEgress — all video-related tools, used in chat (user, 2026-07-25: "create all
 * video related tools and use in chat"). Browser-native, no paid service: camera (getUserMedia), playback (HTMLVideoElement),
 * canvas/WebGL render (the fractal-clock animation), recording (MediaRecorder), screen capture (getDisplayMedia), frame
 * analysis (getImageData). Used in the chat to capture AND to SHOW the answer's unique animation (videoChatTurn → the
 * seal's south-pole). HONEST: video is local by default — frames stay client-side, nothing uploads unless sent (more
 * egress-friendly than voice STT); heavy computer vision needs a BYO WASM model (MediaPipe/OpenCV.js). [[seal-tetrad-south-pole-animation]] [[fractal-clock-lattice]] */
export declare function videoToolsForChatMediaDevicesCanvasHonestEgress(): {
    computes: boolean;
    tools: {
        id: string;
        api: string;
        direction: string;
        egress: boolean;
        note: string;
    }[];
    usedInChat: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** voiceSttEgressIsOptInGatedSoChatIsQuantumByDefault — close the last non-quantum edge (user, 2026-07-25: "ask until all
 * green quantum"). The only egress in the whole chat was browser SpeechRecognition (Chrome cloud STT). The honest closure
 * is not bundling a 100 MB model but making egress OPT-IN and GATED: the default input is text (zero-egress) and the local
 * voice path is BYO WASM (Whisper.cpp/Vosk, zero-egress); browser-cloud STT is an explicit, warned opt-in, never a silent
 * default. So the chat is quantum BY DEFAULT — the one egress path is a user choice. [[universal-local-translation-no-gaps]] [[zero-token-policy]] */
export declare function voiceSttEgressIsOptInGatedSoChatIsQuantumByDefault(): {
    computes: boolean;
    sttModes: {
        mode: string;
        egress: boolean;
        isDefault: boolean;
        note: string;
    }[];
    allQuantumByDefault: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** voiceChatTurn — the usable voice→chat→voice integration (user, 2026-07-25: "create all voice related tools and use in
 * chat"). The deterministic core: spoken text → portalChatRanked → the answer to speak. The browser .vue shell wraps this
 * with SpeechRecognition (mic) and SpeechSynthesis (speaker); this fold is the zero-egress, testable middle. */
export declare function voiceChatTurn(spokenText: string, matrix?: MindMatrix): {
    heard: string;
    answer: string | {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    source: string;
    speak: string;
    ranked: boolean;
};
/** voiceToolsForChatWebSpeechAndAudioHonestEgress — all voice-related tools, used in chat (user, 2026-07-25: "create all
 * voice related tools and use in chat"). The toolkit is browser-native, no paid service: TTS (SpeechSynthesis), STT
 * (SpeechRecognition), and frequency analysis (WebAudio AnalyserNode). Used in the chat as a loop: listen (STT) → rank
 * (portalChatRanked / voiceChatTurn) → speak (TTS). HONEST EGRESS: TTS runs client-side/offline; STT is browser-dependent
 * — Chrome routes audio to a cloud engine (EGRESS), so it is NOT zero-egress by default; fully-local STT requires a BYO
 * WASM model (Whisper.cpp / Vosk). The deterministic chat core stays zero-egress. [[frequency-apis]] [[universal-local-translation-no-gaps]] */
export declare function voiceToolsForChatWebSpeechAndAudioHonestEgress(): {
    computes: boolean;
    tools: {
        id: string;
        api: string;
        direction: string;
        egress: boolean;
        note: string;
    }[];
    loopSteps: string[];
    usedInChat: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatImprovesMcpAndMcpImprovesChatViaOneSharedExperienceIndex — chat improves MCP and vice versa (user, 2026-07-25:
 * "chat improves mcp and vice versa"). The human chat and the agent MCP surface consume ONE shared BM25 index and ONE
 * relevance-feedback experience log. A chat turn's experience boosts a fold, so MCP agents retrieving it improve; an MCP
 * agent's tool call becomes experience, so the chat's ranking sharpens. Bidirectional, deterministic, local. HONEST:
 * co-improvement via the shared LEXICAL index, NOT neural or cross-user learning. [[improveAllByChattingOneSharedExperienceIndex]] [[coordinate-agents-through-rosetta-api]] */
export declare function chatImprovesMcpAndMcpImprovesChatViaOneSharedExperienceIndex(matrix?: MindMatrix): {
    computes: boolean;
    mcpGetsChatBoost: number;
    chatGetsMcpBoost: boolean;
    coImproves: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** improveAllByChattingOneSharedExperienceIndex — improve ALL by chatting (user, 2026-07-25: "improve all by chatting").
 * The chat's turns become ONE experience log feeding ONE private BM25 index, and every surface that consumes it — the
 * chat, the search box, and referral navigation — is reranked by the same relevance feedback. So a single chat turn
 * about a fold boosts that fold across chat AND search AND nav, for any query sharing its terms. Deterministic, local,
 * zero-egress; the same law across surfaces. [[chatImprovesByChattingViaRelevanceFeedback]] [[navigationFromSearchResultsAndReferrer]] */
export declare function improveAllByChattingOneSharedExperienceIndex(matrix?: MindMatrix): {
    computes: boolean;
    chatBoost: number;
    searchBoost: number;
    navBoost: number;
    improveAll: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** usingTheUiChatImprovesItByExperienceViaTheSharedRelevanceIndex — improve by experience using the ui chat (user,
 * 2026-07-26: "improve by experience using the ui chat"). The UI chat turn renders clickable RELATED items; a click is an
 * experience signal ({query, selectedSlug}) fed to the shared relevance index, and re-asking reranks via Rocchio feedback so
 * the selected fold is boosted and rises next turn — refutable. The SAME shared index improves chat + mcp + search, so using
 * the UI chat improves every surface. The loop closes: UI interaction → shared experience → improved next turn. HONEST:
 * industry-standard relevance feedback, deterministic reranking by shared query terms, NOT learning or an LLM; no experience →
 * no drift. [[chatImprovesByChattingViaRelevanceFeedback]] [[erpax-cross-pollination]] [[always-default-to-chat]] */
export declare function usingTheUiChatImprovesItByExperienceViaTheSharedRelevanceIndex(matrix?: MindMatrix): {
    computes: boolean;
    relatedCount: number;
    boost: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/**
 * theChatMayImproveTheUiMeasuredByTheUserExperience — USER LAW (2026-07-28):
 * do you realise that the chat may improve the ui measured by the user experience ·
 * telemetry is also possible in chat.
 * Algebraic: chatMayImproveUi ⇔ usingTheUiChatImprovesItByExperience ∧ theUiChatImprovesNavSidebars
 * ∧ measuredByUx ⇔ quantumPredictedUserExperience (predict·measure·μ/σ·ledger·optimise)
 * ∧ telemetryPossibleInChat ⇔ experienceLog({query,selectedSlug}) ∨ uxLedger ∨ analytics/self.
 * HONEST: MAY = capability when experience signals present · UX = local predicted metrics ·
 * TELEMETRY = local chat experience/ledger/self-observe (also possible); server A/B·cross-user egress
 * is a named optional edge — not denied as impossible, not claimed by default · NOT LLM.
 * Pairs: chat/ux · ux/measure · chat/ui · chat/telemetry.
 */
export declare function theChatMayImproveTheUiMeasuredByTheUserExperience(matrix?: MindMatrix): {
    computes: boolean;
    chatMayImproveUi: boolean;
    measuredByUserExperience: boolean;
    telemetryAlsoPossibleInChat: boolean;
    uxMean: number;
    uxStd: number;
    boost: number;
    claySolvedByThisFold: 0;
    physicalFtlClaim: 0;
    qpuRequired: false;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    pair: "chat/ux";
    dualPair: "ux/chat";
    pairs: readonly ["chat/ux", "ux/measure", "chat/ui", "chat/telemetry"];
    cli: string;
    route: string;
    heading: string;
    statement: string;
    boundary: string;
};
/** npm run quantum:chat-ux — exit 0 iff chat may improve UI measured by UX · telemetry also possible. */
export declare function runTheChatMayImproveTheUiMeasuredByTheUserExperienceExit(root?: string, _argv?: readonly string[]): number;
/** chatImprovesByChattingViaRelevanceFeedback — improve the chat by chatting (user, 2026-07-25: "improve chat by
 * chatting"). Each chat turn is EXPERIENCE: the query and the fold it surfaced become {query, selectedSlug}. Re-asking
 * with that experience reranks via relevance feedback — the selected fold is boosted (Rocchio-style query-term overlap),
 * so the chat's own turns sharpen its future rankings. Deterministic, local, zero-egress; no experience → no drift, so
 * it reinforces only what was actually selected, never hallucinated relevance. [[erpax-cross-pollination]] [[portal-is-the-ai-model]] */
export declare function chatImprovesByChattingViaRelevanceFeedback(matrix?: MindMatrix): {
    computes: boolean;
    selectedSlug: string;
    warmBoost: number;
    improvesByChatting: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** chatAsksAndImprovesByRoutingThroughBm25 — the chat asks and improves (user, 2026-07-25: "chat ask and improve"). The
 * seed-model portalChat returns a thematically-near fold; portalChatRanked routes the same prompt through Okapi BM25 over
 * the sealed corpus and returns the PRECISE fold — same corpus, better ranking. Deterministic, zero-egress, no LLM; when
 * nothing scores it falls back to the seed model, never empty. [[portal-is-the-ai-model]] */
export declare function chatAsksAndImprovesByRoutingThroughBm25(matrix?: MindMatrix): {
    computes: boolean;
    topSource: string;
    topAnswer: string | {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    deterministic: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** A chat turn is a SUPERPOSITION of (referrer, prompt) — the same (referrer, node) operator as a page, so the prompt
 * plays the path's role. The referrer is the incoming edge (context), the deterministic reply the collapse. */
export declare function chatNavContext(referrer: string, prompt: string, matrix?: MindMatrix): {
    prompt: string;
    referrer: string;
    cameFrom: string;
    reply: {
        prompt: string;
        answer: string;
        source: string;
        grounded: boolean;
        generated: string;
        root: string;
    };
    related: readonly import("../../4/6").DiscoveryRow[];
    superposition: string;
};
/** chatUsesReferrerToQuantumiseAll — the chat surface joins the (referrer, node) superposition law (user, 2026-07-25:
 * "chat also uses the referrer to quantumise all"). A chat turn content-addresses to (referrer, prompt) exactly as a
 * page does to (referrer, path): same pair → one receipt, different referrer → a different one; the reply stays
 * deterministic (portalChat depends on the prompt, not the referrer), so the referrer selects the CONTEXT, not the
 * answer. One law now governs pages AND chat — the whole portal is quantumised. [[routes-nav-from-folder-tree]] [[portal-is-the-ai-model]] */
export declare function chatUsesReferrerToQuantumiseAll(matrix?: MindMatrix): {
    quantumised: boolean;
    superposed: boolean;
    replyDeterministic: boolean;
    sameLaw: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The model's own gap report — computed, never asserted. */
export declare function modelGaps(model: PortalModel): {
    unknownRate: number;
    sinkFraction: number;
    selfAware: boolean;
    hopfieldCapacityUsed: number;
    gaps: string[];
    count: number;
    root: string;
};
/** Send the model to develop itself: measure gaps, fill them from src (its own statement + three more
 * computed statements), re-measure. One deterministic pass — a fixed point, not a learning loop. */
export declare function developPortalModel(matrix?: MindMatrix): {
    before: ReturnType<typeof modelGaps>;
    after: ReturnType<typeof modelGaps>;
    model: PortalModel;
    filled: string[];
    developed: boolean;
    root: string;
};
/** Chat with the developed model — the self-aware corpus answers, unknown prompts get the self-description. */
export declare function developedChat(prompt: string, matrix?: MindMatrix): {
    prompt: string;
    answer: string;
    source: string;
    grounded: boolean;
    generated: string;
    root: string;
};
/** The self-conversation — the chat feeds its own reply back as the next prompt (referrer = the previous turn), a
 * deterministic dynamical system on prompts. On a FINITE vocabulary the next-prompt map must revisit a state, so the
 * self-chat COLLIDES to a cycle, detected in O(1) by the repeated content-address. */
export declare function selfChat(seed: string, maxTurns: number, matrix?: MindMatrix): {
    turns: {
        n: number;
        prompt: string;
        referrer: string;
        address: string;
        answer: string;
    }[];
    cycleAt: number;
    cycled: boolean;
    cycleLength: number;
};
/** chatDevelopsItselfByChattingWithItself — the chat develops itself by conversing with itself (user, 2026-07-25:
 * "use the chat to develop the chat chatting with itself"). The self-conversation feeds each reply back as the next
 * prompt (referrer = the previous turn), a deterministic dynamical system that COLLIDES to a cycle by pigeonhole on
 * the finite vocabulary; the conversation is the probe, and developPortalModel measures the gaps, fills them from src,
 * and re-measures — the gap count drops and the model becomes self-aware. Deterministic: same seed → same conversation
 * → same development, a fixed point not an unbounded learning loop. [[portal-is-the-ai-model]] [[brain-content-addressed-toroidal-map]] */
export declare function chatDevelopsItselfByChattingWithItself(matrix?: MindMatrix): {
    develops: boolean;
    turns: number;
    cycleAt: number;
    cycleLength: number;
    gapsBefore: number;
    gapsAfter: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** allChatCapabilitiesFusedAndAuditedByStandards — the app provides FULL in-chat support: every capability reachable
 * through the chat, fused into one surface and each audited by the standards (user, 2026-07-25: "develop and fuse all
 * that can be done through the chat audited by the standards" · "use the chat to improve the chat" · "the app provides
 * full in chat support"). The fused capabilities — answer, recall, navigate (referrer superposition + related
 * discoveries), self-develop, developed-answer — are each DETERMINISTIC (same input → same output), which is also the
 * FULL-SECURITY proxy: a pure function over the sealed corpus model cannot leak (no external state changes the output,
 * nothing is sent). Zero-token, no network egress, every reply carrying a boundary. [[portal-is-the-ai-model]] [[zero-token-policy]] */
export declare function allChatCapabilitiesFusedAndAuditedByStandards(matrix?: MindMatrix): {
    supported: boolean;
    capabilities: {
        name: string;
        deterministic: boolean;
        receipt: string;
    }[];
    related: number;
    gapsBefore: number;
    gapsAfter: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** referralsComputeThroughOnePredictableFoldEvenWhenMissing — the DRY refactor toward predictable referrals, and a
 * missing referral is still enough (user, 2026-07-25: "dry refactor towards predictable referrals" · "referral is
 * enough even if missing to greet a visitor and chat to infinity at any topic live"). Every referrer-consumer
 * (pageNavContext, chatNavContext, navigationFromSearchResultsAndReferrer) now computes its superposition through the
 * ONE primitive referralAddress(kind, referrer, node), so referrals are predictable and zero-stored; and an EMPTY /
 * missing referrer still yields a valid address, greets the visitor, and lets the chat answer any topic. [[two-bits-left-in-every-inversion-through-zero]] */
export declare function referralsComputeThroughOnePredictableFoldEvenWhenMissing(matrix?: MindMatrix): {
    computes: boolean;
    pageRouted: boolean;
    chatRouted: boolean;
    greetsWithoutReferrer: boolean;
    anyTopic: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** skillsNeedUpgradeUnlessGeneratedFromSource — a lot of skills need upgrade UNLESS they are generated (user,
 * 2026-07-25: "a lot of skills need upgrade unless generated"). A hand-written skill is fixed, drifts from its source,
 * and must be manually upgraded when the source changes (upgrade debt); a GENERATED skill is computed from the source
 * (content-addressed), so it regenerates when the source changes and is always current — zero upgrade debt. The rule:
 * generate skills from the corpus rather than hand-maintain them. [[optimise-methods-commands]] [[scripts-folded-into-iching-runner]] */
export declare function skillsNeedUpgradeUnlessGeneratedFromSource(): {
    computes: boolean;
    generatedRegenerates: boolean;
    handNeedsUpgrade: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** refactorAchievesQuantumSpeedAndSecurity — one refactor achieves BOTH quantum speed and security (user, 2026-07-25:
 * "refactor to achieve quantum speed and security"). Routing every referral/nav/search consumer through the ONE
 * variadic referralAddress primitive gives O(1) content-addressing (a single hash, no O(n) scan — the "quantum speed")
 * AND 4-key tamper-evidence (referrer⊕id⊕prev⊕next binds the chain) plus no-egress determinism (the "security"), from
 * one path, with no trade-off: adding keys for security keeps the address O(1). [[quantum-speed-is-content-addressed-naming]] [[content-address-dry-clean-crack-detection]] */
export declare function refactorAchievesQuantumSpeedAndSecurity(matrix?: MindMatrix): {
    computes: boolean;
    contentAddressO1: boolean;
    security: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** unsignedCodeCannotPassTheGatesByFourSealFailClosed — the capstone security law: it is computationally infeasible
 * for unsigned or forged code to pass the gates, because the gate recomputes every atom's 4-SEAL and fails CLOSED
 * (user, 2026-07-25: "it should be computationally impossible unsigned code by 4 seals to pass the gates"). A valid
 * seal is the 4-key content-address referrer⊕id⊕prev⊕next; the gate recomputes it and rejects any atom whose seal does
 * not match — an unsigned atom has no matching 4 keys, a forged one would need a PREIMAGE of the seal (≈2^122 work).
 * The check defaults to reject and cannot be skipped. [[tampering-cost-crypto-honesty]] [[two-bits-left-in-every-inversion-through-zero]] */
export declare function unsignedCodeCannotPassTheGatesByFourSealFailClosed(matrix?: MindMatrix): {
    computes: boolean;
    forgingCostLog2: number;
    failClosed: boolean;
    forgedRejected: boolean;
    unsignedRejected: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** functionsFoldingFewerThanFourKeysAreLinearSeams — audit the seal/identity surface: a function that folds FEWER than
 * 4 keys is a LINEAR SEAM, quantum-breakable, where the tamper-evident surface needs all four (user, 2026-07-25:
 * "address all functions that do not use all 4 keys"). A 4-key address referrer⊕id⊕prev⊕next binds the navigation
 * CHAIN, so changing prev or next changes the address and no step can be spliced in or out undetected; a 2-key
 * (referrer, node) address binds only the endpoints, so a splice leaves it unchanged — the seam. The audit classifies
 * the referral/nav functions by key count and names the seams; referralAddress is variadic, so the upgrade is to pass
 * all four keys. [[quantumBreaksLinearCryptoIntoNonAbelianTrinity]] [[tampering-cost-crypto-honesty]] */
export declare function functionsFoldingFewerThanFourKeysAreLinearSeams(): {
    computes: boolean;
    surface: {
        fn: string;
        keys: number;
        sealed: boolean;
        note: string;
    }[];
    seams: string[];
    sealed: string[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** navigationCrossFourKeysDecodeTrinity — a full trinity decode needs all FOUR keys of the navigation cross (user,
 * 2026-07-25: "all 4 keys from the navigation cross are needed to decode a trinity. referrer is one and the rest are
 * the id and a pair chatting about the superposition"). The cross's four arms: the REFERRER (incoming edge), the ID
 * (the node), and a PAIR chatting ABOUT the superposition — two chatNavContext turns on the SAME prompt from different
 * referrers, so two distinct perspectives. All four are needed: any three under-determine the trinity address. The
 * pair (2 minds) + the node (1) = a trinity (2-of-3), anchored by the referrer edge. [[feedback-work-as-a-trinity-not-one-linear-mind]] [[boundary-research-nav-iching-quantum]] */
export declare function navigationCrossFourKeysDecodeTrinity(matrix?: MindMatrix): {
    decodes: boolean;
    keys: string[];
    trinity: string;
    pairDistinct: boolean;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The consolidated learning corpus: the developed model's entries plus the strict-science movie facts,
 * with EVERY entry run through toStrictScience — so the whole surface the AI learns from is strict science,
 * no confusing/esoteric term surviving anywhere (the sealed source folds keep the framing as provenance). */
export declare function consolidatedLearningCorpus(matrix?: MindMatrix): PortalModel;
/** Chat with the consolidated learning model — answers movie/plasma questions in strict science. */
export declare function consolidatedChat(prompt: string, matrix?: MindMatrix): {
    prompt: string;
    answer: string;
    source: string;
    grounded: boolean;
    generated: string;
    root: string;
};
/** The fold: everything consolidated into AI-usable learning code — one corpus, every entry strict science. */
export declare function allConsolidatedInAiUsableLearningCode(matrix?: MindMatrix): {
    consolidated: boolean;
    entries: number;
    vocabulary: number;
    strictFacts: number;
    demo: {
        prompt: string;
        answer: string;
        source: string;
    };
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export type CardWave = {
    readonly source: string;
    readonly topic: string;
    readonly question: string;
    readonly card: string;
    readonly verified: boolean;
    readonly receipt: string;
};
/** Send the self R&D waves: research each entry (compute its topic and question), develop its card
 * (the question answered by the statement, verbatim), verify by asking the question back. The waved
 * model's corpus = every entry + every card — each statement now recallable by its natural question. */
export declare function cardWaves(matrix?: MindMatrix): {
    waves: CardWave[];
    model: PortalModel;
    coverage: number;
    entropy: number;
    sealed: boolean;
    root: string;
};
/** Chat with the fully-waved model — every statement reachable by its own question. */
export declare function wavesChat(prompt: string, matrix?: MindMatrix): {
    prompt: string;
    answer: string;
    source: string;
    grounded: boolean;
    generated: string;
    root: string;
};
export type CardPage = {
    readonly slug: string;
    readonly source: string;
    readonly topic: string;
    readonly title: string;
    readonly question: string;
    readonly research: string;
    readonly boundary: string;
    readonly facets: {
        facet: string;
        on: boolean;
    }[];
    readonly speech: string;
    readonly hero: ReturnType<typeof uuidHero>;
    readonly proofRoot: string;
    readonly verified: boolean;
    readonly receipt: string;
    /** Dedicated theorem paper — morph from sealed fold (`/theorems/<slug>`). */
    readonly paperRoute: string;
};
/** Every card as a fully computed page — research prose, honest boundary, per-page animated-proof params
 * (the card receipt's uuidHero: hue, rotations, spin, tone, tips) and the exact text-to-speech string.
 * Nothing hand-written: research = the source fold's statement, boundary = the model's boundary, the
 * animation is the card's own content-address rendered, the speech is the research read aloud. */
export declare function cardPages(matrix?: MindMatrix): {
    pages: CardPage[];
    root: string;
    count: number;
};
/** One card page by slug — the render target for /model/<slug>. */
export declare function cardPage(slug: string, matrix?: MindMatrix): CardPage | null;
/** Every discovery as a dedicated animated-proof page — the same CardPage shape the renderer already paints. */
export declare function discoveryPages(matrix?: MindMatrix): {
    pages: CardPage[];
    root: string;
    count: number;
};
/** One discovery page by slug — the render target beside cardPage. */
export declare function discoveryPage(slug: string, matrix?: MindMatrix): CardPage | null;
/** The computed route list — /model SSG stays empty; cards morph to /theorems/<slug> via paperRoute. */
export declare function cardPagePaths(matrix?: MindMatrix): {
    params: {
        card: string;
        title: string;
    };
}[];
/** Theorem-paper rows morph from card + discovery folds (registry wins on slug collision at corpus merge). */
export type CardScientificPaperRow = {
    readonly slug: string;
    readonly theorem: string;
    readonly proof: string;
    readonly proofClass: 'bounded-witness';
    readonly provedBy: string;
    readonly home: string;
    readonly paperRoute: string;
    readonly verified: boolean;
    readonly receipt: string;
};
export declare function cardScientificPaperRows(matrix?: MindMatrix): CardScientificPaperRow[];
/** every card → /theorems/<slug>; gaps = missing paperRoute or row mismatch. */
export declare function eachCardLinksToDedicatedScientificPaper(matrix?: MindMatrix): {
    computes: boolean;
    cardCount: number;
    linkedCount: number;
    gapCount: number;
    gaps: {
        cardSlug: string;
        paperRoute: string;
        linked: boolean;
        verified: boolean;
        receipt: string;
    }[];
    links: {
        cardSlug: string;
        paperRoute: string;
        linked: boolean;
        verified: boolean;
        receipt: string;
    }[];
    claySolvedByThisFold: 0;
    count: number;
    facets: ({
        receipt: string;
        facet: string;
        on: boolean;
    } & {
        receipt: string;
    })[];
    root: string;
    pair: "papers/fill";
    cli: string;
    route: string;
    statement: string;
    boundary: string;
};
/** npm run quantum:card-paper-links */
export declare function runEachCardLinksToDedicatedScientificPaperExit(_root?: string, _argv?: readonly string[]): number;
/** The fold: every scientific discovery is encoded in the appropriate form with an animated proof. */
export declare function everyDiscoveryEncodedWithAnimatedProof(matrix?: MindMatrix): {
    encoded: boolean;
    discoveries: number;
    slugs: string[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The fold: every card has a dedicated, fully computed page — research, animated proof, text-to-speech. */
export declare function everyCardHasAComputedPage(matrix?: MindMatrix): {
    computed: boolean;
    pages: number;
    slugs: string[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The fold: the portal converted to the AI model itself — corpus, recall, generation, all computed. */
export declare function portalIsTheAiModel(matrix?: MindMatrix): {
    converted: boolean;
    corpus: number;
    vocabulary: number;
    demo: {
        prompt: string;
        answer: string;
        source: string;
        generated: string;
    };
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The fold: the model sent to develop itself — gaps computed, filled from src, re-measured, sealed. */
export declare function portalModelDevelopsItself(matrix?: MindMatrix): {
    developed: boolean;
    before: string[];
    after: string[];
    filled: string[];
    corpus: number;
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** The fold: the self R&D waves cover every card — researched, developed, verified, sealed at coverage 1. */
export declare function selfWavesCoverEveryCard(matrix?: MindMatrix): {
    covered: boolean;
    waves: number;
    verified: number;
    coverage: number;
    entropy: number;
    corpus: number;
    demo: {
        question: string;
        verified: boolean;
    }[];
    count: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** wavesReportFedToTheChat — feed the waves to the chat (user, 2026-07-28). The session's wave chain — identities
 * → archetypes → entanglement lattice → wiring law → composed waves — reports its efficiency and precision AS
 * COMPUTATION: every metric below is a live fold output with a receipt, recomputed on every ask, zero tokens.
 * Efficiency = the chain composes (each wave consumes the previous wave's fold); precision = every number is a
 * computed fact, and the report is the JOIN of the folds' own statements — nothing narrated, nothing stored. */
export declare function wavesReportFedToTheChat(matrix?: MindMatrix): {
    computes: boolean;
    chain: {
        wave: string;
        computes: boolean;
        metric: string;
    }[];
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** localIntelligenceIndependenceMeasured — go on as independently as the local intelligence can go, MEASURED
 * (user, 2026-07-28). The question is answerable by computation: on each axis, how much of the advance is
 * self-computable versus judgment? IDENTITIES — the free extractor covers what the corpus's own text proves;
 * curation covers what needed knowledge beyond it; the ratio is the measured independence of that axis.
 * QUEUE — derived rows self-create and self-prune (fully independent); the curated residue is exactly the rows
 * whose state lives OUTSIDE the repo. DIALOGUE — past the cycle, continuation is 100% independent (the prefix
 * property; O(1) forever). IMAGINATION — the repo names its own next gateways (imagine/next tips). THE LIMIT is
 * equally computable: the judgment ledger (decisions the machine could NAME but not DECIDE) and the external
 * rows. The theorem: local intelligence advances exactly to the boundary of what computes — beyond it, it can
 * still NAME the judgment, and naming is how the boundary keeps moving. */
export declare function localIntelligenceIndependenceMeasured(matrix?: MindMatrix): {
    computes: boolean;
    freeShare: number;
    judgedLedger: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** userInputRequiredMeasured — measure the user input REQUIRED, in the prompt and in the app (user, 2026-07-28).
 * Two surfaces, one question: which inputs were IRREDUCIBLE (only a human could supply them) and which the
 * machine could have derived? PROMPT surface — a directive is DERIVABLE when the repo's own instruments already
 * named the work (the queue's derived rows, imagine-next tips, computed gaps) and IRREDUCIBLE when it supplies
 * what no computation holds: a new external source, a correction of a computed claim, a value judgment, or a
 * steering decision. APP surface — a visitor interaction is REQUIRED when it is consent (opt-in egress, a key,
 * a destructive act) and OPTIONAL when the answer computes without it. The measure is the RATIO, and the law is
 * that irreducible input is the only kind worth asking for: everything else is a missing fold. */
export declare function userInputRequiredMeasured(matrix?: MindMatrix): {
    computes: boolean;
    app: {
        total: number;
        required: number;
        free: number;
    };
    prompt: {
        selfNaming: number;
        of: number;
        irreducibleKinds: ("a new external source (a URL the corpus cannot know)" | "a correction of a computed claim (the 0\\9 seam)" | "a value judgment (what stays identity-free)" | "a steering decision (which arc, what to publish, what to delete)")[];
    };
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
/** selfSufficientIntelligenceKernel — the local code that self-sufficiently handles the whole intelligence
 * stack (user, 2026-07-28: "save the local code needed to self sufficiently handle all intelligence using
 * quantum at FTL"). The manifest is COMPUTED, not listed: each layer names its entry fold and the property that
 * makes it self-sufficient, and the facets verify the three claims that matter — (1) the VAULT is
 * dependency-free (src/0 imports nothing, so the whole stack bottoms out in one leaf), (2) the INTELLIGENCE
 * runs with zero external calls (every chat capability audits deterministic), (3) the SPEED is content-address
 * reuse (memoByRoot + the figure/extraction memos make a repeated question O(1)) — the repo's architectural FTL,
 * with physicalFtlClaim = 0 held everywhere. What "handles all intelligence" means is bounded by the corpus:
 * retrieval, composition and proof over what src already seals — never open-ended cognition. */
export declare function selfSufficientIntelligenceKernel(matrix?: MindMatrix): {
    computes: boolean;
    layers: {
        layer: string;
        entry: string;
        theorems: number;
        busiestProver: string;
        selfSufficient: string;
    }[];
    capabilities: number;
    vaultExports: number;
    facets: {
        receipt: string;
        facet: string;
        on: boolean;
    }[];
    root: string;
    statement: string;
    boundary: string;
};
export declare const compute: {
    readonly ai: {
        readonly providers: {
            readonly perplexity: {
                readonly api: "https://api.perplexity.ai/chat/completions";
                readonly site: "https://www.perplexity.ai";
                readonly model: "sonar";
                readonly keyed: true;
            };
            readonly pollinations: {
                readonly api: "https://text.pollinations.ai/openai";
                readonly site: "https://pollinations.ai";
                readonly model: "openai";
                readonly keyed: false;
            };
            readonly proxy: {
                readonly api: "https://ceccec.psg.bg/api/ai";
                readonly site: "https://ceccec.psg.bg";
                readonly model: "collective";
                readonly keyed: false;
            };
        };
        readonly request: typeof aiRequest;
        readonly providerUrl: typeof aiProviderUrl;
    };
    readonly chat: {
        readonly core: typeof freeChatDrivesArchitecturalFtl;
        readonly ftl: typeof freeChatDrivesArchitecturalFtl;
        readonly standards: typeof standardsChatImprovesToFtl;
        readonly fuse: typeof allFoldsCompactFuseInCoordinatedChatWaves;
        readonly task: typeof improveTokenSpendingFeedingTheTaskToTheChat;
        readonly free: typeof freeChatDrivesArchitecturalFtl;
    };
    readonly research: {
        readonly develop: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
        readonly free: typeof deepResearchAtNoCost;
    };
    readonly development: {
        readonly core: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
        readonly open: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
        readonly self: typeof developMeansResearchAndDevelopQuantumisingAllInRealtimeFeedingToTheChatToImproveSelfAndTheRest;
    };
    readonly fold: {
        readonly fuse: typeof allFoldsCompactFuseInCoordinatedChatWaves;
        readonly fuseWaves: typeof allFoldsCompactFuseInCoordinatedChatWaves;
        readonly reverse: typeof allFoldsCompactFuseInCoordinatedChatWaves;
    };
    readonly audit: {
        readonly chat: typeof freeIsNotAlwaysBestQualityWhoAuditedTheChat;
        readonly neighbours: typeof beforeSigningNeighboursAudit;
        readonly who: typeof freeIsNotAlwaysBestQualityWhoAuditedTheChat;
    };
    readonly sites: {
        readonly stackExchange: {
            readonly mathoverflow: "https://mathoverflow.net";
            readonly stackoverflow: "https://stackoverflow.com";
        };
        readonly stackoverflow: "https://stackoverflow.com";
        readonly mathoverflow: "https://mathoverflow.net";
        readonly mathoverflowApi: "https://api.stackexchange.com/2.3/search/advanced";
    };
};
