// English mirror of the 432-paper dynamic route. Same descriptors — paperRoutes()
// is the single source. Nothing bypasses VitePress.
import { paperRoutes } from '../../../src/ui/lib/quantumMind'

export default {
  paths: () => paperRoutes(),
}
