// English mirror of the 432-reference dynamic route. Same descriptors —
// paperReferenceRoutes() is the single source. Nothing bypasses VitePress.
import { paperReferenceRoutes } from '../../../src/ui/lib/quantumMind'

export default {
  paths: () => paperReferenceRoutes(),
}
