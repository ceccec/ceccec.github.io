// English mirror of the 1024 diamond folders. Same descriptors — diamondRoutes()
// is the single source. Nothing bypasses VitePress.
import { diamondRoutes } from '../../../src/ui/lib/quantumMind'

export default {
  paths: () => diamondRoutes(),
}
