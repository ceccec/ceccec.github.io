// ☵ Kǎn · Water — double / torus (src/torus): genus-2 machine — corpus, earth, math, vortex, merkaba.
// Logic lives here; routes/corpus re-exports for REST/universal-page mounts. Pure folds only.
// Canonical imports: src/double/torus/* · src/vortex/math · src/resonance · src/astronomy · src/quantum/dynamics · src/iching · src/rosetta · src/sound · src/plasma/ball · src/birth · src/life · src/death.

import type { MindMatrix } from '../../types'
import { buildMatrix, circulateDoubleTorus, doubleTorusWire, deepResearchChatTurn, wavesOfLocalResearchersChatAboutAlgebra, portalChat, portalChatRanked, freeChatUpgradesAll, countlessFreeChatWaves, continueAtNoAiCost, theUiIsThePublicGatewayBillFreeForThousandsOfAgentsThroughSearchAndChatZeroTokenZeroEgress, dryCleanChatDryCleansAll, improveTokenSpendingFeedingTheTaskToTheChat } from '../../heaven/compute'
import { onlyAlgebraicQuantumComputingIsTopPriority, nothingImpossibleHonestlyBounded } from '../../heaven/laws'
import { GATES, abs, applyGate, asTorus, computesGate, cos, digitalRoot, doubleTorusSurface, floor, fold, foldPair, hypot, isUuid, max, measure, memoByRoot, merge, merkleFold, probabilities, qubits, roundTo, sealFacets, sin, toUuid } from '../../0'
import {
  completeDoubleTorus,
  doubleTorusFold,
  quantumDoubleTorus,
  startIChingDoubleTorus,
  torusBreathe,
  merkabaComputes,
  knowledgeRevealedByMerkabaFold,
  merkabaTrace,
  merkabasInDoubleTorus } from '../../mountain/topology'
import {
  merkaba,
  bothEarthsRotateWithinEachOther,
  doubleTorusGeometryAlignsWithUniverseConstants,
  doubleTorusMathAtAllScalesFlowsInMovie } from '../../mountain/geometry'
import { livingTorus } from '../../fire/diamonds'
import { HERO_CYCLE_MS, heroPhaseAt } from '../../fire/plasma/ball'
import { A432_HUE, A432_FOLDED, FOLDED_CENSUS, HOMOLOGY_LOOPS, ICHING_TRIGRAMS, SCIENCE_DOMAINS, SPEED_OF_LIGHT, TAU, claySolvedTheorem, earned } from '../../3/7'
import { MEEUS_J2000_JD, meeusT, sunEclipticLongitudeDeg } from '../../heaven/sky/astronomy'
import { animationsAreGenuinely10DNotFaked, tenDimensionalAnimation } from '../../quantum/mountain/dimensions'
import { quantumDynamicsComputes, quantumChemistryToyComputes } from '../../quantum/dynamics'
import { movieCanvasHex, movieCanvasPolarity, movieCanvasRgba } from '../../quantum/science'
import { allVortexMathSaved, vortexComputes, vortexMath, vortexPaintTiers } from '../../mountain/vortex'
import {
  developmentIsFusionReactor,
  endlessFusion,
  fuseDedupeSplitReconstruction,
  fuseScreenToMovieOfMovies,
  fusionMovieComputes,
  fusionReactorPartsDeepResearched,
  fusionResearchedInWaves,
  publicApiFusion,
  socialFusion,
  travelFusion,
  vitepressFusion } from '../../wind/fusion'
import { quantumFusionIgnitesFromDashSequence } from '../../fire/li'
import {
  autoMovies8k,
  backgroundMovie,
  endlessBackgroundMovie,
  learnFromTheMovieAllEventuallyFused } from '../../thunder/movie/canvas'
import { hologram, movieAllDimensionsAtOnce, threeProjectorHologram } from '../../thunder/movie/glass'
import { movieFoldsLinearities, sevenSeedMovieIsRosettaDecodingSunMoonSymbols } from '../../thunder/movie/narrative'
import {
  formsEmergeInMovieOfLife,
  fullscreenSidebarsInMovie,
  movieReflectsSelf,
  siteIsMovieAndLibrary,
  tenDimensionalMovie,
  video64kFree,
  videoKeepsNativeQuality } from '../../thunder/movie/movielib'
import { diamondLattice, pureDiamonds } from '../../fire/diamonds'
import {
  completeCorpus,
  corpusCatchAllPaths,
  corpusRestPathRouting,
  diamondsStaticPagesPurged,
  doubleTorusCorpusRouting,
  computeUniversalPage,
  paperRoutes,
  paperReferenceRoutes } from '../../wind/routes/corpus'
import {
  analogSpeech,
  charUuids,
  humanize,
  multidimensional,
  openGraph,
  plainLanguage,
  playLearn,
  typographySeo,
  wordUuids } from '../../quantum/voice'

export { vortexComputes, allVortexMathSaved, vortexMath, vortexPaintTiers } from '../../mountain/vortex'
export {
  merkaba,
  bothEarthsRotateWithinEachOther,
  doubleTorusGeometryAlignsWithUniverseConstants } from '../../mountain/geometry'
export {
  knowledgeRevealedByMerkabaFold,
  merkabaTrace,
  merkabasInDoubleTorus } from '../../mountain/topology'

export {
  completeDoubleTorus,
  doubleTorusFold,
  quantumDoubleTorus,
  torusBreathe,
  startIChingDoubleTorus }
export { merkabaComputes } from '../../mountain/topology'

/** One gate — double torus topology + all vortex math saved at call time (dissolved from src/double/torus/topology). */
export function doubleTorusVortexComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusVortexComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const vortex = vortexComputes(matrix)
    const { computes, facets } = computesGate('double-torus-vortex-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'all vortex math saved — lattice + paint tiers + presentations', on: vortex.vortex.saved },
    ])
    return {
      computes,
      torus,
      vortex,
      facets,
      root: merge(torus.root, vortex.vortex.root),
      statement:
        'Double torus vortex computes: the genus-2 machine hosts the full vortex lattice — doubling circuit, digit folders, plasma paint tiers, harmonic ratios, and one-fold presentations — sealed at call time.',
      boundary:
        'Composition of quantumDoubleTorus and vortexComputes (src/vortex/math). src/double/torus is the torus-side import for agents.' }
  })
}

/** One gate — double torus topology + I Ching start + merkaba count at call time (dissolved from src/double/torus/topology). */
export function doubleTorusTopologyComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusTopologyComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const complete = completeDoubleTorus(matrix)
    const iching = startIChingDoubleTorus(matrix)
    const merkabaGate = merkabaComputes(matrix)
    // INVERT ∘ REFLECT IS AN ISOMETRY (user, 2026-07-26: "invert double torus and its reflection is isometry"):
    // reflection x↦−x on the genus-2 surface is (θ,lobe)↦(π−θ,−lobe); inversion y↦−y is θ↦−θ. Each preserves the ℝ³
    // distance, so their composition (θ↦θ−π with a lobe flip — a π-rotation about z) preserves it too — verified over
    // sampled surface points, distances agreeing to 6 decimals. Refutable: perturb the map and the error jumps.
    const dist = (a: { x: number; y: number; z: number }, b: { x: number; y: number; z: number }) => hypot(a.x - b.x, a.y - b.y, a.z - b.z)
    const invertReflect = (th: number, ph: number, d: number, L: number) => doubleTorusSurface(th - TAU / 2, ph, d, -L)
    const samples = Array.from({ length: 2 ** 3 }, (_, i) => ({ th: (i * TAU) / 9, ph: (i * TAU) / (2 * 8), d: i % 3, L: i % 2 ? 1 : -1 }))
    let maxIsometryError = 0
    for (const a of samples) for (const b of samples) {
      maxIsometryError = max(maxIsometryError, abs(
        dist(doubleTorusSurface(a.th, a.ph, a.d, a.L), doubleTorusSurface(b.th, b.ph, b.d, b.L)) -
        dist(invertReflect(a.th, a.ph, a.d, a.L), invertReflect(b.th, b.ph, b.d, b.L))))
    }
    const invertReflectIsIsometry = roundTo(maxIsometryError, 2 * 3) === 0 // ℝ³ distances preserved to 6 decimals
    const { computes, facets } = computesGate('double-torus-topology-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'complete double torus fold', on: complete.complete },
      { facet: 'I Ching double torus started', on: iching.started },
      { facet: 'merkabas counted in the double torus', on: merkabaGate.counted.counted },
      { facet: `invert∘reflect is an isometry — reflection x↦−x (θ↦π−θ, lobe flip) and inversion y↦−y (θ↦−θ) each preserve ℝ³ distance, so their composition (a π-rotation about z) does too (${invertReflectIsIsometry})`, on: invertReflectIsIsometry },
    ])
    return {
      computes,
      torus,
      complete,
      iching,
      merkabas: merkabaGate.counted,
      facets,
      root: merge(merge(torus.root, complete.root), merge(iching.root, merkabaGate.counted.root)),
      statement:
        'Double torus topology computes: the genus-2 surface (χ=−2), I Ching ignition, and merkaba enumeration recomputed from the matrix at call time.',
      boundary:
        'Composition of quantumDoubleTorus, completeDoubleTorus, startIChingDoubleTorus, and merkabaComputes. src/double/torus is the canonical import.' }
  })
}

export {
  completeCorpus,
  corpusCatchAllPaths,
  corpusRestPathRouting,
  diamondsStaticPagesPurged,
  doubleTorusCorpusRouting,
  computeUniversalPage,
  paperRoutes,
  paperReferenceRoutes }

export {
  doubleTorusEarthComputes,
  doubleTorusEarthLikeTheApple,
  doubleTorusEarthWeatherFlowsInMovie,
  weatherForecastFromDoubleTorusEarthPerspective,
  invertedEarthSameTimespaceProvenByMath,
  formingDoubleTorusEarthsProvenByMath,
  compassAroundEarthGatewaysImpossibleProvenByMath,
  sixtyDegreeAngleReachesCardinalForFreeProvenByMath,
  northSouthPoleNavigationProvenByMath,
  trinityGatewaysNeverMissProvenByMath,
  earthGatewayNavigationSolutionsResearched,
  earthGatewayNavigationResearchWaves,
  earthGatewayNavigationResearchSentInWaves,
  doubleTorusEarthHingeUi,
  doubleTorusEarthHingeComputesAll,
  doubleTorusEarthHingeMovieFlowsInMovie,
  doubleTorusEarthHingeMovieSeeds,
  dualEarthMerkabaCounterRotation,
  navigationGpsCelestialFromDualEarthPerspective,
  navigationGpsCelestialReexplainedFromDualEarthPerspective,
  navigationGpsCelestialComputes,
  doubleTorusEarthProvesItselfGatewaysDocumentedInHistoryMeasurableToday,
  fourTippedPyramidsFiveTipsCombinedMakeMovingMerkabas,
  earthPyramidLocationsAndGeometryComputes,
  doubleTorusEarthPyramidGatewayResearch,
  doubleTorusEarthExchangeComputes,
  fiatAndGoldFlowExplainedByDoubleEarthExchange,
  doubleTorusEarthExchangeResearch,
  universalNavigationalCrossInAllDimensions,
  universalNavigationalCrossResearch,
  universalNavigationalCrossComputes,
  type UniversalNavCrossDimensionRow,
  type UniversalNavCrossResearchSection,
  type GpsSatellitePhaseReceipt,
  type CelestialDisplayPhase,
  type NavGpsCelestialExplanationSection,
  trinityGatewayDefs,
  hingeCityLabel,
  hingeVortexMovieTiming,
  hingeMoviePaintLayers,
  DEFAULT_EARTH_HINGE_AT,
  type EarthHingePaintLayer,
  type EarthHingePaintBlend,
  type EarthHingeGatewayView,
  type EarthHingeVortexStepView,
  type TrinityGatewayDef,
  type EarthExchangeReceipt,
  type EarthExchangeReceiptKind,
  type FiatGoldFlowRow,
  type EarthExchangeResearchSection } from './earth'
import { doubleTorusEarthHingeMovieFlowsInMovie } from './earth'
import {
  allMovieSeedBundles,
  allMovieSeedBundlesFlowInPlasma,
  allMovieSeedCopyText,
  allPagesForPlasmaWiring,
  clientMovieSeedCopyText,
  clientMoviePaintPathSealed,
  plasmaClientWorkBoundedByPureMath,
  plasmaMovieStreams,
  plasmaWorkBudget,
  plasmaWiredUuidStreams,
  realtimeWiring,
  sixSeedBundlesFormSevenAsWhole,
  twoTrinitiesFuseAsDoubleTorusEarth,
  type MovieSeedBundle,
  type MovieSeedStream,
  type PlasmaWiredStream,
  type PlasmaWiredStreamKind } from '../../fire/plasma/ball'

export { diamondLattice, pureDiamonds }

export {
  allMovieSeedBundles,
  allMovieSeedBundlesFlowInPlasma,
  allMovieSeedCopyText,
  allPagesForPlasmaWiring,
  clientMovieSeedCopyText,
  clientMoviePaintPathSealed,
  plasmaClientWorkBoundedByPureMath,
  plasmaMovieStreams,
  plasmaWorkBudget,
  plasmaWiredUuidStreams,
  realtimeWiring,
  sixSeedBundlesFormSevenAsWhole,
  twoTrinitiesFuseAsDoubleTorusEarth,
  type MovieSeedBundle,
  type MovieSeedStream,
  type PlasmaWiredStream,
  type PlasmaWiredStreamKind }

export {
  allFusionResearchRoots,
  fusionReactorPartsDeepResearched,
  fusionReactorPartsDryPlasmaStreams,
  fusionReactorPartMovieSeeds,
  fusionReactorParts,
  type FusionReactorPart,
  type FusionReactorPartBand } from '../../wind/fusion'

export {
  developmentIsFusionReactor,
  endlessFusion,
  fuseDedupeSplitReconstruction,
  fuseScreenToMovieOfMovies,
  fusionMovieComputes,
  fusionResearchedInWaves,
  publicApiFusion,
  socialFusion,
  travelFusion,
  vitepressFusion }

export {
  autoMovies8k,
  backgroundMovie,
  endlessBackgroundMovie,
  learnFromTheMovieAllEventuallyFused,
  hologram,
  movieAllDimensionsAtOnce,
  threeProjectorHologram,
  movieFoldsLinearities,
  sevenSeedMovieIsRosettaDecodingSunMoonSymbols,
  formsEmergeInMovieOfLife,
  fullscreenSidebarsInMovie,
  movieReflectsSelf,
  siteIsMovieAndLibrary,
  video64kFree,
  videoKeepsNativeQuality,
  tenDimensionalMovie }

export {
  allMathSaved,
  A432_OCTAVES,
  priceFromA432,
  simpleReturns,
  backtest,
  buyAndHold,
  tradingReceipt,
  liveCapture,
  realtimeSources,
  backtestRealPrices } from '../../mountain/vortex'

/** One gate — double torus topology + vortex + merkaba math at call time (dissolved from src/double/torus/math). */
export function doubleTorusMathComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusMathComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const vortexGate = vortexComputes(matrix)
    const merkabaGate = merkabaComputes(matrix)
    const earths = bothEarthsRotateWithinEachOther(0, matrix)
    const { computes, facets } = computesGate('double-torus-math-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'vortex math saved on the torus', on: vortexGate.vortex.saved },
      { facet: 'merkaba math computes on the torus', on: merkabaGate.computes },
      { facet: 'both Earths counter-rotate within each other', on: earths.rotates },
    ])
    return {
      computes,
      torus,
      vortex: vortexGate.vortex,
      vortexGate,
      merkabaGate,
      facets,
      root: merge(merge(torus.root, vortexGate.vortex.root), merkabaGate.root),
      statement:
        'Double torus math computes: the genus-2 machine hosts trading math, vortex lattice, and merkaba geometry — all sealed at call time.',
      boundary:
        'Composition of quantumDoubleTorus, vortexComputes, and merkabaComputes. Canonical import: src/double/torus (math child dissolved into parent).' }
  })
}

/** One gate — double torus topology + site-is-movie-and-library at call time (dissolved from src/double/torus/movie). */
export function doubleTorusMovieComputes(matrix: MindMatrix = buildMatrix(), path = '/') {
  return memoByRoot('doubleTorusMovieComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const library = siteIsMovieAndLibrary(matrix)
    const reflects = movieReflectsSelf(matrix)
    const rosetta = sevenSeedMovieIsRosettaDecodingSunMoonSymbols(matrix, path)
    const hinge = doubleTorusEarthHingeMovieFlowsInMovie(path, matrix)
    const { computes, facets } = computesGate('double-torus-movie-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'the whole site is a movie and a movie library', on: library.isMovieAndLibrary },
      { facet: 'the movie reflects the self', on: reflects.reflects },
      { facet: 'seven seeds decode sun/moon symbols in the movie', on: rosetta.decoding },
      { facet: 'Sofia hinge movie flows in site movie stack', on: hinge.flows },
    ])
    return {
      computes,
      torus,
      library,
      reflects,
      rosetta,
      hinge,
      facets,
      root: merge(merge(torus.root, library.root), merge(reflects.root, merge(rosetta.root, hinge.root))),
      statement:
        'Double torus movie computes: the genus-2 machine screens the portal as one movie and a per-route library — self-reflecting, Rosetta-decoding sun/moon symbols through seven seed bundles at call time.',
      boundary:
        'Composition of quantumDoubleTorus, siteIsMovieAndLibrary, movieReflectsSelf, and sevenSeedMovieIsRosettaDecodingSunMoonSymbols. Canonical import: src/double/torus (movie child dissolved into parent).' }
  })
}

/** One gate — double torus + 1024 diamond lattice compute-only at call time. */
export function doubleTorusDiamondsComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusDiamondsComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const leaves = pureDiamonds(matrix)
    const lattice = diamondLattice(matrix)
    const routing = doubleTorusCorpusRouting(matrix)
    const purged = diamondsStaticPagesPurged(matrix)
    const { computes, facets } = computesGate('double-torus-diamonds-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: '1024 pure Merkle leaves', on: leaves.count === (64 * 16) && leaves.pure },
      { facet: 'diamond lattice kinds on index', on: lattice.length > 0 },
      { facet: 'zero diamond [id] SSG — compute-only', on: routing.ssgPathCount('diamonds') === 0 && purged.purged },
    ])
    return {
      computes,
      torus,
      leaves,
      lattice,
      purged,
      facets,
      root: merge(merge(torus.root, leaves.root), purged.root),
      statement:
        'Double torus diamonds computes: 1024 Merkle leaves and lattice kinds resolve via corpusParams — static [id] enumeration purged, math retained.',
      boundary:
        'Composition of quantumDoubleTorus, pureDiamonds, diamondLattice, and diamondsStaticPagesPurged. src/double/torus is the canonical import.' }
  })
}

/** One gate — double torus topology + corpus routing at call time. */
export function doubleTorusCorpusComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusCorpusComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const routing = doubleTorusCorpusRouting(matrix)
    const purged = diamondsStaticPagesPurged(matrix)
    const { computes, facets } = computesGate('double-torus-corpus-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'corpus routing from the torus', on: routing.routed },
      { facet: 'diamond static [id] pages purged — lattice compute-only', on: purged.purged },
    ])
    return {
      computes,
      torus,
      routing,
      purged,
      facets,
      root: merge(merge(torus.root, routing.root), purged.root),
      statement:
        'Double torus corpus computes: the genus-2 machine routes papers (432 SSG), references and diamonds (compute-only) through corpusParams — 1024 Merkle leaves without thousands of static detail pages.',
      boundary:
        'Composition of quantumDoubleTorus, doubleTorusCorpusRouting, and diamondsStaticPagesPurged. src/double/torus is the canonical import.' }
  })
}

/** One gate — double torus + plain-language voice layer at call time. */
export function doubleTorusVoiceComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusVoiceComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const plain = plainLanguage()
    const play = playLearn('play')
    const speech = analogSpeech()
    const { computes, facets } = computesGate('double-torus-voice-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'plain language lines for first-time readers', on: plain.clear },
      { facet: 'play-and-learn encodes word as hue + note', on: play.playable },
      { facet: 'analog speech folds text to sound', on: speech.analog },
    ])
    return {
      computes,
      torus,
      plain,
      play,
      speech,
      facets,
      root: merge(merge(torus.root, plain.root), merge(play.root, speech.root)),
      statement:
        'Double torus voice computes: the genus-2 machine hosts the communicative layer — plain language, play-learn sonification, typography and Open Graph recomputed at call time.',
      boundary:
        'Composition of quantumDoubleTorus and lake/voice folds. src/double/torus is the canonical import — not a second source of truth.' }
  })
}

/** One gate — topology + portal fusion + reactor research in waves. */
export function doubleTorusFusionComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusFusionComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const vitepress = vitepressFusion(matrix)
    const apis = publicApiFusion(matrix)
    const researched = fusionResearchedInWaves(matrix)
    const reactor = developmentIsFusionReactor(matrix)
    const dashIgnites = quantumFusionIgnitesFromDashSequence(matrix)
    const { computes, facets } = computesGate('double-torus-fusion-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'VitePress fused through the architecture', on: vitepress.fused },
      { facet: 'public APIs fused with the corpus root', on: apis.fused },
      { facet: 'fusion researched in waves — parts · concepts · materials · milestones', on: researched.complete },
      { facet: 'development is the fusion reactor — dry plasma bounded', on: reactor.reacts },
      { facet: '1\\2\\4\\8/7/5/3\\6\\9/0\\1 decodes ±60° — hex closes, 0/ ignites fusion', on: dashIgnites.ignites },
    ])
    return {
      computes,
      torus,
      vitepress,
      apis,
      researched,
      reactor,
      facets,
      root: merge(merge(merge(merge(torus.root, vitepress.root), apis.root), researched.root), dashIgnites.root),
      statement:
        'Double torus fusion computes: the genus-2 machine fuses live data, VitePress extension points, and peer-reviewed fusion-reactor research (eight parts in waves, MCF/ICF/MIF concepts, breeding materials, documented milestones) through the content-addressed architecture — all folds merge at call time.',
      boundary:
        'Composition of quantumDoubleTorus, vitepressFusion, publicApiFusion, fusionResearchedInWaves, and developmentIsFusionReactor. External APIs opt-in; physical fusion literature is DOCUMENTED where cited; dry plasma is sealed src/ recomputation.' }
  })
}

/** One gate — double torus topology + plasma client work bounded by pure math at call time (dissolved src/double/torus/plasma). */
export function doubleTorusPlasmaComputes(matrix: MindMatrix = buildMatrix(), path = '/') {
  return memoByRoot('doubleTorusPlasmaComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const plasma = plasmaClientWorkBoundedByPureMath(path, matrix)
    const paintPath = clientMoviePaintPathSealed(path, matrix)
    const reactorParts = fusionReactorPartsDeepResearched(matrix)
    const sixSeven = sixSeedBundlesFormSevenAsWhole(path, matrix)
    const earthFusion = twoTrinitiesFuseAsDoubleTorusEarth(path, matrix)
    const { computes, facets } = computesGate('double-torus-plasma-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'plasma client work bounded by pure math', on: plasma.bounded },
      { facet: 'client movie paint path sealed', on: paintPath.sealed },
      { facet: 'two trinities fuse as double torus Earth', on: earthFusion.fused },
      { facet: 'six seed bundles form the seven as a whole (8→7→5)', on: sixSeven.forms },
      { facet: 'movie seed bundles flow in plasma', on: allMovieSeedBundlesFlowInPlasma(path, matrix).flows },
      { facet: 'fusion reactor parts deep-researched for dry plasma', on: reactorParts.researched },
    ])
    return {
      computes,
      torus,
      plasma,
      reactorParts,
      facets,
      root: merge(merge(torus.root, plasma.root), reactorParts.root),
      statement:
        'Double torus plasma computes: the genus-2 machine hosts plasma movie streams — client work capped by Fibonacci-tier pure math, seed bundles flowing through plasma at call time, fusion reactor parts deep-researched and saved for dry plasma usage.',
      boundary:
        'Composition of quantumDoubleTorus, plasmaClientWorkBoundedByPureMath, allMovieSeedBundlesFlowInPlasma, and fusionReactorPartsDeepResearched (canonical research in src/fusion/index.ts).' }
  })
}

/** One gate — topology + corpus routing recomputed from the matrix at call time. */
export function doubleTorusComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusComputes', matrix, () => {
    const torus = quantumDoubleTorus(matrix)
    const routing = doubleTorusCorpusRouting(matrix)
    const { computes, facets } = computesGate('double-torus-computes', [
      { facet: 'quantum double torus is the machine', on: torus.is },
      { facet: 'corpus routing from the torus — corpusParams at call time', on: routing.routed },
    ])
    return {
      computes,
      torus,
      routing,
      facets,
      root: merge(torus.root, routing.root),
      statement:
        'Double torus computes: the genus-2 surface is the machine; corpus detail (1024 Merkle leaves, lattice kinds, paper/reference pointers) resolves via corpusParams — only papers materialize SSG [id] routes.',
      boundary:
        'Composition of quantumDoubleTorus and doubleTorusCorpusRouting. The path src/double/torus is the canonical import for agents and gates — not a second source of truth.' }
  })
}

/**
 * doubleTorusFacesComputes — USER DIRECTIVES (2026-07-24): double torus navigation · site · mcp ·
 * computable multimedia — four faces of one claim: the genus-2 surface CARRIES the surfaces, and each
 * face must COMPUTE, not be remembered. Pair: torus/faces · CLI npm run quantum:torus-faces.
 * NAV: the 8 trigram folders (ICHING_TRIGRAMS) are the octagon edges glued in HOMOLOGY_LOOPS dual pairs
 * (heaven/earth · fire/water · thunder/wind · mountain/lake) at one core — the folder tree IS the router,
 * and its gluing closes to χ = −2. SITE: corpus routing resolves from the torus at call time (composed).
 * MCP: the mcp/torus pair algebra is bidirectional (the sealed mcpQuantumTorus face rides it).
 * MULTIMEDIA: movie · voice · plasma · 10D animation faces all compute — multimedia is computed, not stored.
 */
export function doubleTorusFacesComputes(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusFacesComputes', matrix, () => {
    const umbrella = doubleTorusComputes(matrix)
    const movie = doubleTorusMovieComputes(matrix)
    const voice = doubleTorusVoiceComputes(matrix)
    const plasma = doubleTorusPlasmaComputes(matrix)
    const tenD = animationsAreGenuinely10DNotFaked(matrix)
    const eulerChi = 2 - HOMOLOGY_LOOPS
    const octagonNav = ICHING_TRIGRAMS === HOMOLOGY_LOOPS * 2 && 1 - HOMOLOGY_LOOPS + 1 === eulerChi
    const mcpPair = foldPair(toUuid('cmd:mcp'), toUuid('cmd:torus'))
    const facesPair = foldPair(toUuid('cmd:torus'), toUuid('cmd:faces'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const facets = [
      { facet: `NAV — ${ICHING_TRIGRAMS} trigram folders = octagon edges in ${HOMOLOGY_LOOPS} dual pairs at one core: gluing closes to χ = ${eulerChi}; the folder tree IS the router`, on: octagonNav },
      { facet: 'SITE — corpus routing resolves from the torus at call time (doubleTorusComputes umbrella)', on: umbrella.computes },
      { facet: 'MCP — mcp/torus pair algebra bidirectional; the sealed MCP torus face rides the same carrier', on: mcpPair.bidirectional && mcpPair.forward !== mcpPair.reverse },
      { facet: 'MULTIMEDIA computable — movie · voice · plasma · genuinely-10D animation faces all compute realtime', on: movie.computes && voice.computes && plasma.computes && tenD.genuine },
      { facet: 'LEARNING — the play-learn face rides the voice torus: word → hue + note recomputed at call time (the learn portal consumes the same carrier)', on: voice.computes && voice.play.playable },
      { facet: 'pair torus/faces bidirectional', on: facesPair.bidirectional },
      { facet: `claySolvedByThisFold=${claySolvedByThisFold}`, on: claySolvedByThisFold === 0 && octagonNav },
    ].map((entry) => ({ ...entry, receipt: toUuid(`torus-faces:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    const on = facets.every((entry) => entry.on)
    return {
      computes: on,
      doubleTorusFaces: on,
      eulerChi,
      trigrams: ICHING_TRIGRAMS,
      pairs: HOMOLOGY_LOOPS,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(umbrella.root, merkleFold([movie.root, voice.root, plasma.root, facets.map((f) => f.receipt)].flat())),
      pair: 'torus/faces' as const,
      dualPair: 'faces/torus' as const,
      cli: 'npm run quantum:torus-faces',
      route: '/en/quantum-tools#torus-faces',
      heading: 'Torus faces · nav · site · mcp · multimedia',
      statement:
        `doubleTorusFacesComputes — nav(χ=${eulerChi} octagon)=${octagonNav ? 1 : 0} · site=${umbrella.computes ? 1 : 0} · ` +
        `mcp=${mcpPair.bidirectional ? 1 : 0} · multimedia=${movie.computes && voice.computes && plasma.computes ? 1 : 0}.`,
      boundary:
        'Navigation, site, MCP and multimedia as computed faces of the one genus-2 carrier: the trigram folder octagon closes to χ = −2 ' +
        '(the census freeBits), corpus routing and the movie/voice/plasma/10D faces recompute at call time, and the MCP face rides the ' +
        'registered pair algebra. Nothing is remembered that the torus does not recompute.' }
  })
}

/**
 * theUiItselfIsADoubleTorus — USER LAW (2026-07-28): again including the UI itself as double torus.
 * Algebraic identity: UI ≡ Σ₂ with near lobe = rendered surface (site≡movie · VitePress fused),
 * far lobe = sealed src via vite/mirror · docs/invert pair algebra, neck = χ=−2 nav octagon router,
 * lens = every well-formed UI fold closes near∨far. Does NOT import wind/research or quantum/apps
 * (cycle-safe). Pair: ui/torus · torus/ui. [[torus/faces]] [[vite/mirror]] [[lens]]
 */
export function theUiItselfIsADoubleTorus(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('theUiItselfIsADoubleTorus', matrix, () => {
    const faces = doubleTorusFacesComputes(matrix)
    const movie = doubleTorusMovieComputes(matrix)
    const fusion = doubleTorusFusionComputes(matrix)
    const lens = theLensSeesDoubleTorusesEverywhereExceptInCrackedCode(matrix)
    // Near/far lobes as pair algebra (same structure as vitepressIsTheInvertedMirror — no research import).
    const pairVite = foldPair(toUuid('cmd:vite'), toUuid('cmd:mirror'))
    const pairDocs = foldPair(toUuid('cmd:docs'), toUuid('cmd:invert'))
    const pairUi = foldPair(toUuid('cmd:ui'), toUuid('cmd:torus'))
    const pairUiInvert = foldPair(toUuid('cmd:ui'), toUuid('cmd:invert'))
    const linearSelf = foldPair(toUuid('gate:mirror:linear'), toUuid('linear:mind:penetrate'))
    const quantumReflect = foldPair(toUuid('quantum:mind:reflect'), toUuid('gate:mirror:linear'))
    const nearLobe = movie.computes === true && fusion.computes === true // rendered surface / VitePress
    const farLobe =
      pairVite.bidirectional && pairDocs.bidirectional &&
      pairVite.forward !== pairVite.reverse &&
      pairDocs.forward !== pairDocs.reverse &&
      pairUiInvert.bidirectional // src reflected · docs invert · ui invert
    const neckChi = faces.computes === true && faces.eulerChi === 2 - HOMOLOGY_LOOPS && faces.eulerChi === -2
    const gateMirrorLinear = linearSelf.bidirectional && linearSelf.forward !== linearSelf.reverse
    const gateReflectionQuantum =
      quantumReflect.bidirectional && quantumReflect.forward !== quantumReflect.reverse
    const counterRotate = gateMirrorLinear && gateReflectionQuantum && linearSelf.merged !== quantumReflect.merged
    const lensOn = lens.computes === true && lens.discriminates === true
    const uiIsTorus = nearLobe && farLobe && neckChi && counterRotate && lensOn && pairUi.bidirectional
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const facets = [
      { facet: `NEAR LOBE — rendered UI surface = site≡movie (${movie.computes}) ∧ VitePress fused (${fusion.computes})`, on: nearLobe },
      { facet: `FAR LOBE — sealed src via vite/mirror · docs/invert · ui/invert bidirectional (docs ≡ invert(src))`, on: farLobe },
      { facet: `NECK χ=−2 — nav octagon router is the UI topology (eulerChi=${faces.eulerChi})`, on: neckChi },
      { facet: `COUNTER-ROTATION — gateMirrorLinear ≠ gateReflectionQuantum (linear collides · quantum reflects)`, on: counterRotate },
      { facet: `LENS — every well-formed fold closes near∨far; cracked UI forms fail (${lens.discriminates})`, on: lensOn },
      { facet: `UI ≡ Σ₂ — near∧far∧neck∧counterRotate∧lens ∧ pair ui/torus bidirectional`, on: uiIsTorus },
      { facet: `claySolvedByThisFold=${claySolvedByThisFold} · NOT a physical torus widget`, on: claySolvedByThisFold === 0 && uiIsTorus },
    ].map((entry) => ({ ...entry, receipt: toUuid(`ui-torus:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      theUiItselfIsADoubleTorus: uiIsTorus,
      nearLobe,
      farLobe,
      neckChi,
      counterRotate,
      eulerChi: faces.eulerChi,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(faces.root, merkleFold([movie.root, fusion.root, toUuid(`lens:${lens.computes}:${lens.discriminates}`), pairVite.merged, pairDocs.merged, pairUi.merged, ...facets.map((f) => f.receipt)])),
      pair: 'ui/torus' as const,
      dualPair: 'torus/ui' as const,
      cli: 'npm run quantum:ui-torus',
      route: '/en/quantum-tools#ui-torus',
      heading: 'The UI itself is a double torus',
      statement: facets.map((entry) => entry.facet).join(' · '),
      boundary: earned('EXACT — this fold is verified by its facets:', facets, 'UI≡Σ₂ structural identity · NOT physical torus UI'),
    }
  })
}

/** npm run quantum:ui-torus — exit 0 iff the UI itself closes as a double torus. */
export function runTheUiItselfIsADoubleTorusExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = theUiItselfIsADoubleTorus()
  process.stdout.write(`${report.computes ? '✓' : '✗'} ui-torus — near=${report.nearLobe ? 1 : 0} far=${report.farLobe ? 1 : 0} χ=${report.eulerChi} · ${report.statement.slice(0, 2 * 100)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/**
 * doubleTorusQuantumComputer — USER TIP (2026-07-28): double torus quantum computer.
 * Algebraic identity: Σ₂ ≡ QC — the genus-2 machine IS the quantum computer (qubits=state atoms,
 * register=128-bit UUID, gates=order-sensitive folds, measurement=UUID receipt) on classical-64bit
 * with a live H|0⟩ circuit that computes. Composes quantumDoubleTorus · completelyQuantum ·
 * algebraic-QC priority · UI≡Σ₂. HONEST: content-addressed classical simulator · 
 * NOT physical QPU · NOT Clay. Pairs: torus/computer · computer/torus.
 * [[quantumDoubleTorus]] [[completelyQuantum]] [[algebra/priority]] [[ui/torus]]
 */
export function doubleTorusQuantumComputer(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusQuantumComputer', matrix, () => {
    const machine = quantumDoubleTorus(matrix)
    const completely = doubleTorusIsCompletelyQuantum(matrix)
    const priority = onlyAlgebraicQuantumComputingIsTopPriority(matrix)
    const ui = theUiItselfIsADoubleTorus(matrix)
    const faces = doubleTorusFacesComputes(matrix)
    // LIVE CIRCUIT — H|0⟩ → equal Born probs; the computer computes (classical state-vector).
    const half = 1 / 2
    const eps = 1 / (FOLDED_CENSUS ** 6)
    const s0 = qubits(1)
    const sH = applyGate(s0, GATES.H, 0)
    const probs = probabilities(sH)
    const circuitComputes =
      probs.length === 2 &&
      typeof probs[0] === 'number' && typeof probs[1] === 'number' &&
      abs(probs[0]! - half) < eps && abs(probs[1]! - half) < eps
    const measured = measure(sH, 0, 'torus-qc')
    const measurementCollapses = measured.outcome === 0 || measured.outcome === 1
    const shapeIsComputer = machine.is === true
    const completelyOn = completely.computes === true && completely.completelyQuantum === true
    const algebraicOn = priority.computes === true && priority.onlyAlgebraicQuantumComputingIsTopPriority === true
    const uiOn = ui.computes === true && ui.theUiItselfIsADoubleTorus === true
    const carrierOn = faces.computes === true && faces.eulerChi === -2
    const pairQc = foldPair(toUuid('cmd:torus'), toUuid('cmd:computer'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const qpuRequired = false as const
    const runsOnClassical64Bit = true as const
    const isTheQuantumComputer =
      shapeIsComputer && completelyOn && algebraicOn && uiOn && carrierOn &&
      circuitComputes && measurementCollapses && pairQc.bidirectional &&
      claySolvedByThisFold === 0 && qpuRequired === false && runsOnClassical64Bit
    const facets = [
      { facet: `SHAPE ≡ COMPUTER — quantumDoubleTorus.is (${shapeIsComputer}): genus-2 machine = 128-bit QC (qubits·register·folds·receipt)`, on: shapeIsComputer },
      { facet: `COMPLETELY QUANTUM SUBSTRATE — doubleTorusIsCompletelyQuantum (${completelyOn}) · dynamics·clock·10D·palette`, on: completelyOn },
      { facet: `ALGEBRAIC QC TOP PRIORITY — onlyAlgebraicQuantumComputingIsTopPriority (${algebraicOn}); quantum = content-addressed algebra`, on: algebraicOn },
      { facet: `UI IS THE COMPUTER SURFACE — theUiItselfIsADoubleTorus (${uiOn}); χ=${faces.eulerChi} carrier`, on: uiOn && carrierOn },
      { facet: `LIVE CIRCUIT — H|0⟩ → P(0)=P(1)=½ (${circuitComputes}); measure collapses ({0,1}) (${measurementCollapses})`, on: circuitComputes && measurementCollapses },
      { facet: `Σ₂ ≡ QC — classical-64bit · qpuRequired=${qpuRequired} · pair torus/computer bidirectional`, on: isTheQuantumComputer && pairQc.bidirectional },
      { facet: `HONEST — content-addressed classical simulator · claySolvedByThisFold=${claySolvedByThisFold} · NOT physical QPU · NOT Clay prize`, on: claySolvedByThisFold === 0 && qpuRequired === false },
    ].map((entry) => ({ ...entry, receipt: toUuid(`torus-qc:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      doubleTorusQuantumComputer: isTheQuantumComputer,
      shapeIsComputer,
      circuitComputes,
      probs: [probs[0] ?? 0, probs[1] ?? 0] as const,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired,
      runsOnClassical64Bit,
      facets,
      root: merge(machine.root, merkleFold([completely.root, priority.root, ui.root, faces.root, ...facets.map((f) => f.receipt)])),
      pair: 'torus/computer' as const,
      dualPair: 'computer/torus' as const,
      cli: 'npm run quantum:torus-computer',
      route: '/en/quantum-tools#torus-computer',
      heading: 'Double torus quantum computer',
      statement: facets.map((entry) => entry.facet).join(' · '),
      boundary: earned('EXACT — this fold is verified by its facets:', facets, 'Σ₂≡QC structural · classical-64bit simulator · NOT physical QPU'),
    }
  })
}

/** npm run quantum:torus-computer — exit 0 iff the double torus is the quantum computer. */
export function runDoubleTorusQuantumComputerExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = doubleTorusQuantumComputer()
  process.stdout.write(`${report.computes ? '✓' : '✗'} torus-computer — Σ₂≡QC=${report.doubleTorusQuantumComputer ? 1 : 0} circuit=${report.circuitComputes ? 1 : 0} · ${report.statement.slice(0, 2 * 100)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/**
 * fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible —
 * USER LAW (2026-07-28): fuse error handling in quantum computer and the impossible becomes possible.
 * Algebraic: fuseErrorHandling∈QC ∧ impossibleBecomesPossible
 * ⇔ soft(fail/zero · self/heal · timeout/dry · fuse/error) ∧ Σ₂≡QC ∧ nothingImpossible.realised
 * ∧ wallsStayClosed.
 * HONEST: fuse = fail→0→invert · heal · timeout/dry · memo reentry stubs INSIDE the QC path —
 * seeming-impossible becomes possible BECAUSE walls (FTL/clone/perpetual) stay closed · NOT that
 * conservation/causality break · NOT physical QPU.
 * Pairs: fuse/error · error/handle · impossible/possible.
 * CLI: npm run quantum:fuse-error (one primary).
 */
export function fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible(
  matrix: MindMatrix = buildMatrix(),
) {
  return memoByRoot('fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible', matrix, () => {
    const soft = (a: string, b: string) =>
      foldPair(toUuid(`cmd:${a}`), toUuid(`cmd:${b}`)).bidirectional
    const tip = 'fuse error handling in quantum computer and the impossible becomes possible'
    const free = portalChat(tip, matrix)
    const ranked = portalChatRanked(tip, matrix)
    const tipOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    const qc = doubleTorusQuantumComputer(matrix)
    const impossible = nothingImpossibleHonestlyBounded(matrix)
    const fusion = developmentIsFusionReactor(matrix)
    const errorHandlingFused =
      soft('fail', 'zero') &&
      soft('invert', 'seq') &&
      soft('self', 'heal') &&
      soft('heal', 'quantum') &&
      soft('timeout', 'dry') &&
      soft('fuse', 'error') &&
      soft('error', 'fuse') &&
      soft('error', 'handle') &&
      soft('mcp', 'fusion') &&
      fusion.reacts === true
    const inQuantumComputer =
      tipOk &&
      qc.computes === true &&
      qc.doubleTorusQuantumComputer === true &&
      errorHandlingFused &&
      soft('torus', 'computer')
    const wallsStayClosed =
      impossible.realised === true &&
      Array.isArray(impossible.walls) &&
      impossible.walls.length === (2 + 3) &&
      impossible.walls.every((w: { closed?: boolean }) => w.closed === true)
    const impossibleBecomesPossible =
      inQuantumComputer &&
      wallsStayClosed &&
      impossible.seemedImpossibleNowReal >= (2 * 4) &&
      soft('impossible', 'possible') &&
      soft('possible', 'impossible')
    const law =
      impossibleBecomesPossible &&
      soft('learn', 'best') &&
      soft('moment', 'prove')
    const pairFuseError = foldPair(toUuid('cmd:fuse'), toUuid('cmd:error'))
    const pairErrorHandle = foldPair(toUuid('cmd:error'), toUuid('cmd:handle'))
    const pairImpossiblePossible = foldPair(toUuid('cmd:impossible'), toUuid('cmd:possible'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const physicalFtlClaim = 0 as const
    const qpuRequired = false as const
    const computes =
      law &&
      pairFuseError.bidirectional &&
      pairErrorHandle.bidirectional &&
      pairImpossiblePossible.bidirectional &&
      claySolvedByThisFold === 0 &&
      physicalFtlClaim === 0 &&
      qpuRequired === false
    const facets = [
      { facet: `TIP — fuse error handling in quantum computer · the impossible becomes possible (${tipOk})`, on: tipOk },
      { facet: `ERROR HANDLING FUSED — soft fail/zero · invert/seq · self/heal · timeout/dry · fuse/error · mcp/fusion · developmentIsFusionReactor (${errorHandlingFused})`, on: errorHandlingFused },
      { facet: `IN QUANTUM COMPUTER — Σ₂≡QC doubleTorusQuantumComputer · errorHandling∈QC (${inQuantumComputer})`, on: inQuantumComputer },
      { facet: `WALLS STAY CLOSED — nothingImpossibleHonestlyBounded · ${impossible.genuinelyImpossibleWalls} walls closed (${wallsStayClosed})`, on: wallsStayClosed },
      { facet: `IMPOSSIBLE BECOMES POSSIBLE — seeming-impossible now real=${impossible.seemedImpossibleNowReal} · soft impossible/possible · because walls hold (${impossibleBecomesPossible})`, on: impossibleBecomesPossible },
      { facet: `LAW — fuseErrorHandling∈QC ∧ impossibleBecomesPossible ∧ soft learn/best · moment/prove (${law})`, on: law },
      { facet: `pairs fuse/error · error/handle · impossible/possible · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairFuseError.bidirectional && pairErrorHandle.bidirectional && pairImpossiblePossible.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — fuse enables counterintuitive QC · walls (FTL/clone/perpetual) stay closed · NOT conservation break · NOT physical QPU`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`fuse-error:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible: law,
      errorHandlingFused,
      inQuantumComputer,
      wallsStayClosed,
      impossibleBecomesPossible,
      seemedImpossibleNowReal: impossible.seemedImpossibleNowReal,
      genuinelyImpossibleWalls: impossible.genuinelyImpossibleWalls,
      claySolvedByThisFold,
      physicalFtlClaim,
      qpuRequired,
      facets,
      root: merge(qc.root, merkleFold([impossible.root, fusion.root, ...facets.map((f) => f.receipt)])),
      pair: 'fuse/error' as const,
      dualPair: 'error/fuse' as const,
      pairs: [
        'fuse/error',
        'error/fuse',
        'error/handle',
        'handle/error',
        'impossible/possible',
        'possible/impossible',
      ] as const,
      cli: 'npm run quantum:fuse-error',
      route: '/en/quantum-tools#fuse-error',
      heading: 'Fuse error handling in QC — impossible becomes possible',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic: 'fuseErrorHandling∈QC ∧ impossibleBecomesPossible ∧ wallsStayClosed',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'fuse error handling into Σ₂≡QC · seeming-impossible becomes possible because walls hold · NOT FTL/clone/perpetual',
      ),
    }
  })
}

/** npm run quantum:fuse-error — one primary; duals via pairs/prompts */
export function runFuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossibleExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} fuse-error — on=${report.fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible ? 1 : 0} ` +
      `fused=${report.errorHandlingFused ? 1 : 0} qc=${report.inQuantumComputer ? 1 : 0} ` +
      `walls=${report.wallsStayClosed ? 1 : 0} possible=${report.impossibleBecomesPossible ? 1 : 0} ` +
      `real=${report.seemedImpossibleNowReal} · ${report.statement.slice(0, 100)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

export const fuseError = fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible
export const errorHandle = fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible
export const impossiblePossible = fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible
export const runFuseErrorExit = runFuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossibleExit
export const runErrorHandleExit = runFuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossibleExit
export const runImpossiblePossibleExit = runFuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossibleExit

/**
 * DOUBLE_TORUS_QC_UPGRADE_WAVES — domain upgrade waves for the double-torus quantum computer.
 * algebra · geometry · physics · biology · chemistry · * (star = all researched · born in src/quantum/**).
 * Each wave MUST use free chat.
 */
export const DOUBLE_TORUS_QC_UPGRADE_WAVES = [
  { id: 'algebra', seed: 'double torus algebra foldPair χ=-2 H₁', algebraic: 'foldPair bidirectional ∧ algebraic-QC top priority' },
  { id: 'geometry', seed: 'double torus geometry asTorus lobes χ=-2 R/r', algebraic: 'χ(Σ₂)=−2 ∧ asTorus(fold) ↦ genus-2 ∧ R/r sealed' },
  { id: 'physics', seed: 'double torus physics quantum dynamics classical-64bit', algebraic: 'dynamicsComputes ∧ ' },
  { id: 'biology', seed: 'double torus biology life torus equations', algebraic: 'life ≡ torus-equations (χ·clock·Born·H₁)' },
  { id: 'chemistry', seed: 'double torus chemistry H2+ LCAO toy', algebraic: 'chemistryToyComputes ∧ variational' },
  { id: 'star', seed: 'double torus * all researched by Σ₂ · born like a star in src/quantum/** sky', algebraic: "'*' ≡ star ≡ all-researched(Σ₂) ∧ born ∈ src/quantum/**" },
] as const

/**
 * QUANTUM_SKY_POSSIBILITIES — sealed modules under src/quantum/** (the sky of quantum possibilities).
 * Each path is a star that may be born (content-addressed) when `*` researches via the double torus.
 * Keep in sync with index.ts|index.vue under src/quantum (measured census, not FLOPS).
 */
export const QUANTUM_SKY_POSSIBILITIES = [
  'quantum',
  'quantum/application',
  'quantum/apps',
  'quantum/computer',
  'quantum/dynamics',
  'quantum/earth/heritage',
  'quantum/fire/experiments',
  'quantum/fire/forecasts',
  'quantum/fire/simulations',
  'quantum/heaven/library',
  'quantum/heaven/mind',
  'quantum/dist',
  'quantum/dist/generators',
  'quantum/dist/readme',
  'quantum/icons',
  'quantum/spirit',
  'quantum/voice',
  'quantum/mountain/dimensions',
  'quantum/os',
  'quantum/research',
  'quantum/science',
  'quantum/water/cache',
  'quantum/wind/geometry',
] as const

/**
 * allDoubleTorusWavesUseTheFreeChat — USER LAW (2026-07-28): all waves use the free chat ·
 * double torus algebra/physics/biology/chemistry/* · all in quantum computer upgrade waves.
 * Every QC upgrade domain wave answers via portalChat + portalChatRanked (zero-token free chat);
 * self-reflects into the next wave; composes freeChatUpgradesAll · countlessFreeChatWaves ·
 * continueAtNoAiCost · doubleTorusQuantumComputer · sealed domain proves.
 * HONEST: free = deterministic recompute · NOT LLM.
 * Pairs: torus/upgrade · free/chat · waves/free.
 */
export function allDoubleTorusWavesUseTheFreeChat(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('allDoubleTorusWavesUseTheFreeChat', matrix, () => {
    const qc = doubleTorusQuantumComputer(matrix)
    const freeUpgrade = freeChatUpgradesAll(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const noCost = continueAtNoAiCost(matrix)
    const priority = onlyAlgebraicQuantumComputingIsTopPriority(matrix)
    const geo = doubleTorusGeometryAlignsWithUniverseConstants(matrix)
    const dynamics = quantumDynamicsComputes(matrix)
    const life = lifeTorus(matrix)
    const chem = quantumChemistryToyComputes(matrix)
    const emb = asTorus(fold(toUuid('torus:geo:a'), toUuid('torus:geo:b')))
    const left = doubleTorusSurface(0, 0, 0, -1)
    const right = doubleTorusSurface(0, 0, 0, 1)
    const domainProof: Record<(typeof DOUBLE_TORUS_QC_UPGRADE_WAVES)[number]['id'], boolean> = {
      algebra: priority.computes === true && priority.onlyAlgebraicQuantumComputingIsTopPriority === true,
      geometry: geo.aligns === true && (emb.lobe === 0 || emb.lobe === 1) && Number.isFinite(emb.x) && right.x > left.x,
      physics: dynamics.computes === true,
      biology: life.computes === true && life.lifeTorus === true,
      chemistry: chem.computes === true,
      star: SCIENCE_DOMAINS.length === 7
        && SCIENCE_DOMAINS.every((d) => d.field.length > 0 && d.oecd.length > 0)
        && QUANTUM_SKY_POSSIBILITIES.length > 0
        && QUANTUM_SKY_POSSIBILITIES.every((p) => p === 'quantum' || p.startsWith('quantum/'))
        && DOUBLE_TORUS_PERSPECTIVES.length > 0,
    }
    // ALL WAVES USE THE FREE CHAT — portalChat + portalChatRanked; next topic = prior free answer (reflect).
    let topic = 'double torus quantum computer upgrade via free chat'
    const waves = DOUBLE_TORUS_QC_UPGRADE_WAVES.map((domain, i) => {
      const prompt = i === 0
        ? `${domain.seed} · ${domain.algebraic} · Σ₂≡QC`
        : `${domain.seed} · reflecting ${topic}`
      const free = portalChat(prompt, matrix)
      const ranked = portalChatRanked(prompt, matrix)
      const answer = String(free.answer ?? '')
      const rankedAnswer = String(ranked.answer ?? '')
      const address = toUuid(`torus-qc-free-wave:${domain.id}:${prompt}:${answer}:${rankedAnswer}`)
      topic = answer.slice(0, 8 * 8) || rankedAnswer.slice(0, 8 * 6) || domain.algebraic
      return {
        n: i + 1,
        id: domain.id,
        algebraic: domain.algebraic,
        prompt,
        freeAnswer: answer,
        rankedAnswer,
        freeOk: answer.length > 0,
        rankedOk: rankedAnswer.length > 0,
        prove: domainProof[domain.id],
        address,
      }
    })
    const allUseFreeChat = waves.every((w) => w.freeOk && w.rankedOk && isUuid(w.address))
    const allDomainsProve = waves.every((w) => w.prove === true)
    const selfReflects = waves.length >= 2 && waves.every((w, i) => {
      if (i === 0) return true
      return w.prompt.includes('reflecting')
    })
    const freeMachineryOn =
      freeUpgrade.computes === true &&
      countless.computes === true &&
      noCost.computes === true
    const qcOn = qc.computes === true && qc.doubleTorusQuantumComputer === true
    const starCoversAll = SCIENCE_DOMAINS.length === 7 && waves.some((w) => w.id === 'star' && w.prove)
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const pairUpgrade = foldPair(toUuid('cmd:torus'), toUuid('cmd:upgrade'))
    const pairFree = foldPair(toUuid('cmd:free'), toUuid('cmd:chat'))
    const computes = allUseFreeChat && allDomainsProve && selfReflects && freeMachineryOn && qcOn
      && starCoversAll && claySolvedByThisFold === 0 && pairUpgrade.bidirectional && pairFree.bidirectional
    const facets = [
      { facet: `ALL WAVES USE THE FREE CHAT — ${waves.length} QC-upgrade waves each answered by portalChat + portalChatRanked (${allUseFreeChat}); zero LLM tokens`, on: allUseFreeChat },
      { facet: `QC UPGRADE DOMAINS PROVE — algebra·geometry·physics·biology·chemistry·* each sealed prove green (${allDomainsProve})`, on: allDomainsProve },
      { facet: `SELF-REFLECTING FREE WAVES — each wave after the first prompts with "reflecting" + prior free-chat answer (${selfReflects})`, on: selfReflects },
      { facet: `FREE CHAT MACHINERY — freeChatUpgradesAll · countlessFreeChatWaves · continueAtNoAiCost all compute (${freeMachineryOn})`, on: freeMachineryOn },
      { facet: `Σ₂ ≡ QC CARRIER — doubleTorusQuantumComputer computes (${qcOn}); star covers ${SCIENCE_DOMAINS.length} SCIENCE_DOMAINS (${starCoversAll})`, on: qcOn && starCoversAll },
      { facet: `pairs torus/upgrade · free/chat bidirectional · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairUpgrade.bidirectional && pairFree.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — free chat = deterministic portalChat over sealed src · NOT an LLM · domain proves are structural toys/identities · NOT lab biology/chemistry claims`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`torus-free-waves:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      allWavesUseFreeChat: allUseFreeChat,
      waves: waves.map((w) => ({ n: w.n, id: w.id, algebraic: w.algebraic, prove: w.prove, freeOk: w.freeOk })),
      domains: waves.length,
      scienceDomains: SCIENCE_DOMAINS.length,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(qc.root, merkleFold([freeUpgrade.root, countless.root, noCost.root, priority.root, geo.root, dynamics.root, life.root, chem.root, ...waves.map((w) => w.address), ...facets.map((f) => f.receipt)])),
      pair: 'torus/upgrade' as const,
      dualPair: 'upgrade/torus' as const,
      pairs: ['torus/upgrade', 'free/chat', 'waves/free'] as const,
      cli: 'npm run quantum:torus-upgrade',
      route: '/en/quantum-tools#torus-upgrade',
      heading: 'Double torus QC upgrade waves · all via free chat',
      statement: facets.map((entry) => entry.facet).join(' · '),
      boundary: earned('EXACT — this fold is verified by its facets:', facets, 'all waves use free chat · NOT LLM · NOT lab claims'),
    }
  })
}

/** npm run quantum:torus-upgrade — exit 0 iff every QC upgrade wave uses free chat and domain proves. */
export function runAllDoubleTorusWavesUseTheFreeChatExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = allDoubleTorusWavesUseTheFreeChat()
  process.stdout.write(`${report.computes ? '✓' : '✗'} torus-upgrade — freeChat=${report.allWavesUseFreeChat ? 1 : 0} domains=${report.domains} sciences=${report.scienceDomains} · ${report.statement.slice(0, 2 * 100)}\n`)
  for (const w of report.waves) process.stdout.write(`  · ${w.n}. ${w.id} prove=${w.prove ? 1 : 0} free=${w.freeOk ? 1 : 0} · ${w.algebraic.slice(0, 3 * 16)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

type TorusQcDomainId = (typeof DOUBLE_TORUS_QC_UPGRADE_WAVES)[number]['id']

/**
 * doubleTorusDomainViaFreeChat — one named domain face of Σ₂≡QC; ALWAYS answers via free chat first.
 * Shared by geometry · algebra · physics · biology · chemistry · star named tips.
 */
function doubleTorusDomainViaFreeChat(id: TorusQcDomainId, matrix: MindMatrix = buildMatrix()) {
  const spec = DOUBLE_TORUS_QC_UPGRADE_WAVES.find((w) => w.id === id)!
  const upgrade = allDoubleTorusWavesUseTheFreeChat(matrix)
  const row = upgrade.waves.find((w) => w.id === id)
  const prompt = `${spec.seed} · ${spec.algebraic} · via free chat`
  const free = portalChat(prompt, matrix)
  const ranked = portalChatRanked(prompt, matrix)
  const freeAnswer = String(free.answer ?? '')
  const rankedAnswer = String(ranked.answer ?? '')
  const freeOk = freeAnswer.length > 0 && rankedAnswer.length > 0
  const prove = row?.prove === true
  const usesFreeChat = freeOk && upgrade.allWavesUseFreeChat === true
  const pair = foldPair(toUuid('cmd:torus'), toUuid(`cmd:${id === 'star' ? 'star' : id}`))
  const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
  const computes = usesFreeChat && prove && upgrade.computes === true && pair.bidirectional && claySolvedByThisFold === 0
  const facets = [
    { facet: `FREE CHAT FIRST — portalChat + portalChatRanked answered (${freeOk}); umbrella allWavesUseFreeChat (${upgrade.allWavesUseFreeChat})`, on: usesFreeChat },
    { facet: `DOMAIN PROVE — ${id} sealed prove green (${prove}) · algebraic ${spec.algebraic}`, on: prove },
    { facet: `QC UPGRADE UMBRELLA — allDoubleTorusWavesUseTheFreeChat computes (${upgrade.computes})`, on: upgrade.computes === true },
    { facet: `pair torus/${id === 'star' ? 'star' : id} bidirectional · claySolvedByThisFold=${claySolvedByThisFold}`, on: pair.bidirectional && claySolvedByThisFold === 0 },
    { facet: `HONEST — free chat = portalChat · domain = structural seal · NOT LLM · NOT lab claim`, on: computes },
  ].map((entry) => ({ ...entry, receipt: toUuid(`torus-domain:${id}:${entry.facet.slice(0, 3 * 16)}:${entry.on}`) }))
  return {
    computes: facets.every((entry) => entry.on),
    id,
    algebraic: spec.algebraic,
    freeOk,
    prove,
    usesFreeChat,
    freeAnswer: freeAnswer.slice(0, 2 * 6 + 108),
    claySolvedByThisFold,
    physicalFtlClaim: 0 as const,
    qpuRequired: false as const,
    facets,
    root: merge(upgrade.root, merkleFold([toUuid(`domain:${id}:${freeOk}:${prove}`), ...facets.map((f) => f.receipt)])),
    pair: `torus/${id === 'star' ? 'star' : id}` as const,
    dualPair: `${id === 'star' ? 'star' : id}/torus` as const,
    cli: `npm run quantum:torus-${id === 'star' ? 'star' : id}`,
    route: `/en/quantum-tools#torus-${id === 'star' ? 'star' : id}`,
    heading: `Double torus ${id}`,
    statement: facets.map((entry) => entry.facet).join(' · '),
    boundary: earned('EXACT — this fold is verified by its facets:', facets, `domain=${id} via free chat`),
  }
}

/** double torus geometry — via free chat · QC upgrade wave. Pair torus/geometry. */
export function doubleTorusGeometry(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusGeometry', matrix, () => doubleTorusDomainViaFreeChat('geometry', matrix))
}
/** double torus algebra — via free chat · QC upgrade wave. Pair torus/algebra. */
export function doubleTorusAlgebra(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusAlgebra', matrix, () => doubleTorusDomainViaFreeChat('algebra', matrix))
}
/** double torus physics — via free chat · QC upgrade wave. Pair torus/physics. */
export function doubleTorusPhysics(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusPhysics', matrix, () => doubleTorusDomainViaFreeChat('physics', matrix))
}
/** double torus biology — via free chat · QC upgrade wave. Pair torus/biology. */
export function doubleTorusBiology(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusBiology', matrix, () => doubleTorusDomainViaFreeChat('biology', matrix))
}
/** double torus chemistry — via free chat · QC upgrade wave. Pair torus/chemistry. */
export function doubleTorusChemistry(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusChemistry', matrix, () => doubleTorusDomainViaFreeChat('chemistry', matrix))
}
/** double torus * (star) — all researched by Σ₂ · born in src/quantum/** sky. Pair torus/star · star/sky. */
export function doubleTorusStar(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusStar', matrix, () => asteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSky(matrix))
}

/**
 * asteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSky — USER LAW (2026-07-28):
 * * means all researched by the double torus and born like a star in the sky of quantum possibilities
 * in src/quantum/**.
 * Algebraic: '*' ≡ star ∧ all-researched(Σ₂) = QC-upgrade∪PERSPECTIVES∪SCIENCE_DOMAINS ∧
 * ∀p∈QUANTUM_SKY_POSSIBILITIES: born(p)=uuid(star-born:p) ∧ p ∈ src/quantum/**.
 * HONEST: sky = sealed module catalog under src/quantum · born = content-address · NOT astrophysics ·
 * NOT literal infinity of possibilities. Pairs: star/sky · all/researched · star/quantum.
 */
export function asteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSky(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('asteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSky', matrix, () => {
    const upgrade = allDoubleTorusWavesUseTheFreeChat(matrix)
    const qc = doubleTorusQuantumComputer(matrix)
    const prompt = '* means all researched by the double torus and born like a star in the sky of quantum possibilities in src/quantum/**'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeAnswer = String(free.answer ?? '')
    const rankedAnswer = String(ranked.answer ?? '')
    const freeOk = freeAnswer.length > 0 && rankedAnswer.length > 0
    const starWave = upgrade.waves.find((w) => w.id === 'star')
    const allResearched =
      upgrade.allWavesUseFreeChat === true &&
      upgrade.computes === true &&
      starWave?.prove === true &&
      DOUBLE_TORUS_QC_UPGRADE_WAVES.length === 6 &&
      DOUBLE_TORUS_PERSPECTIVES.length >= (5 * 6) &&
      SCIENCE_DOMAINS.length === 7 &&
      SCIENCE_DOMAINS.every((d) => d.field.length > 0)
    // BORN LIKE A STAR — each src/quantum/** possibility gets a content-addressed birth (star in the sky).
    const stars = QUANTUM_SKY_POSSIBILITIES.map((path, i) => ({
      path,
      born: toUuid(`star-born:${path}:${i}`),
      inSky: path === 'quantum' || path.startsWith('quantum/'),
    }))
    const bornInQuantumSky =
      stars.length === QUANTUM_SKY_POSSIBILITIES.length &&
      stars.every((s) => s.inSky && isUuid(s.born)) &&
      new Set(stars.map((s) => s.born)).size === stars.length &&
      new Set(stars.map((s) => s.path)).size === stars.length
    const skyIsSrcQuantum = QUANTUM_SKY_POSSIBILITIES.every((p) => p === 'quantum' || p.startsWith('quantum/'))
    const asteriskIsStar = starWave?.id === 'star' && DOUBLE_TORUS_QC_UPGRADE_WAVES.some((w) => w.id === 'star')
    const pairStar = foldPair(toUuid('cmd:torus'), toUuid('cmd:star'))
    const pairSky = foldPair(toUuid('cmd:star'), toUuid('cmd:sky'))
    const pairAll = foldPair(toUuid('cmd:all'), toUuid('cmd:researched'))
    const pairQuantum = foldPair(toUuid('cmd:star'), toUuid('cmd:quantum'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const usesFreeChat = freeOk && upgrade.allWavesUseFreeChat === true
    const prove = allResearched && bornInQuantumSky && asteriskIsStar && qc.computes === true
    const computes = usesFreeChat && prove && pairStar.bidirectional && pairSky.bidirectional
      && pairAll.bidirectional && pairQuantum.bidirectional && claySolvedByThisFold === 0
    const algebraic = "'*' ≡ star ≡ all-researched(Σ₂) ∧ born ∈ src/quantum/**"
    const facets = [
      { facet: `* ≡ STAR — asterisk is the star QC-upgrade wave (${asteriskIsStar}); free chat answered (${freeOk})`, on: asteriskIsStar && freeOk },
      { facet: `ALL RESEARCHED BY DOUBLE TORUS — QC domains=${DOUBLE_TORUS_QC_UPGRADE_WAVES.length} · perspectives=${DOUBLE_TORUS_PERSPECTIVES.length} · SCIENCE_DOMAINS=${SCIENCE_DOMAINS.length} · upgrade green (${allResearched})`, on: allResearched },
      { facet: `BORN LIKE A STAR IN THE SKY — ${stars.length} possibilities under src/quantum/** each content-address born (${bornInQuantumSky}); sky sealed (${skyIsSrcQuantum})`, on: bornInQuantumSky && skyIsSrcQuantum },
      { facet: `Σ₂ ≡ QC CARRIER — doubleTorusQuantumComputer computes (${qc.computes === true})`, on: qc.computes === true },
      { facet: `pairs torus/star · star/sky · all/researched · star/quantum bidirectional · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairStar.bidirectional && pairSky.bidirectional && pairAll.bidirectional && pairQuantum.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — sky = sealed src/quantum/** catalog · born = content-address · all-researched = perspectives∪domains · NOT astrophysics · NOT LLM`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`star-sky:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      id: 'star' as const,
      algebraic,
      freeOk,
      prove,
      usesFreeChat,
      allResearched,
      bornInQuantumSky,
      skyCount: stars.length,
      researchedPerspectives: DOUBLE_TORUS_PERSPECTIVES.length,
      researchedDomains: DOUBLE_TORUS_QC_UPGRADE_WAVES.length,
      scienceDomains: SCIENCE_DOMAINS.length,
      stars: stars.map((s) => ({ path: s.path, born: s.born })),
      freeAnswer: freeAnswer.slice(0, 2 * 6 + 108),
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(upgrade.root, merkleFold([qc.root, toUuid(`star-sky:${stars.length}:${allResearched}`), ...stars.map((s) => s.born), ...facets.map((f) => f.receipt)])),
      pair: 'torus/star' as const,
      dualPair: 'star/torus' as const,
      pairs: ['torus/star', 'star/sky', 'all/researched', 'star/quantum'] as const,
      cli: 'npm run quantum:torus-star',
      route: '/en/quantum-tools#torus-star',
      heading: '* · star · all researched · born in quantum sky',
      statement: facets.map((entry) => entry.facet).join(' · '),
      boundary: earned('EXACT — this fold is verified by its facets:', facets, '* ≡ star · sky=src/quantum/** · NOT astrophysics'),
    }
  })
}

/** npm run quantum:star-sky — exit 0 iff * means all-researched · born in quantum sky. */
export function runAsteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSkyExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = asteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSky()
  process.stdout.write(`${report.computes ? '✓' : '✗'} star-sky — *≡star researched=${report.allResearched ? 1 : 0} born=${report.bornInQuantumSky ? 1 : 0} sky=${report.skyCount} perspectives=${report.researchedPerspectives} · ${report.statement.slice(0, 4 * 5 * 9)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/** npm run quantum:torus-geometry|algebra|physics|biology|chemistry|star — domain via free chat. */
export function runDoubleTorusDomainExit(root = '', argv: readonly string[] = []): number {
  void root
  const arg = (argv[0] ?? '').replace(/^--domain=/, '') || process.env.QUANTUM_TORUS_DOMAIN || 'geometry'
  const id = (['algebra', 'geometry', 'physics', 'biology', 'chemistry', 'star'].includes(arg) ? arg : 'geometry') as TorusQcDomainId
  const report = doubleTorusDomainViaFreeChat(id)
  process.stdout.write(`${report.computes ? '✓' : '✗'} torus-${id} — free=${report.usesFreeChat ? 1 : 0} prove=${report.prove ? 1 : 0} · ${report.statement.slice(0, 2 * 100)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}
export function runDoubleTorusGeometryExit(r = '', a: readonly string[] = []) { return runDoubleTorusDomainExit(r, ['geometry', ...a]) }
export function runDoubleTorusAlgebraExit(r = '', a: readonly string[] = []) { return runDoubleTorusDomainExit(r, ['algebra', ...a]) }
export function runDoubleTorusPhysicsExit(r = '', a: readonly string[] = []) { return runDoubleTorusDomainExit(r, ['physics', ...a]) }
export function runDoubleTorusBiologyExit(r = '', a: readonly string[] = []) { return runDoubleTorusDomainExit(r, ['biology', ...a]) }
export function runDoubleTorusChemistryExit(r = '', a: readonly string[] = []) { return runDoubleTorusDomainExit(r, ['chemistry', ...a]) }
export function runDoubleTorusStarExit(r = '', a: readonly string[] = []) {
  void r
  void a
  return runAsteriskMeansAllResearchedByDoubleTorusBornLikeAStarInQuantumSkyExit()
}

/**
 * constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing — USER LAW (2026-07-28):
 * constantly improve shifting new perspectives as now there is free infinite scalable computing power.
 * Algebraic: ∀ wave: freeChat(wave) ∧ perspectiveₙ₊₁ ≠ perspectiveₙ ∧ |perspectives|=N (no repeat until catalog
 * exhausted) ∧ improve = |addresses|=|algebraics|=N ∧ reflect(waveₙ₊₁←freeₙ) ∧ ∞-scale = countlessFreeChatWaves
 * ∧ continueAtNoAiCost ∧ freeChatUpgradesAll ∧ memoByRoot warm. HONEST: ∞ = countably many free-chat waves + amortized
 * zero-token reuse — NOT physical FLOPS / NOT FTL / NOT unbounded wall-clock. Pairs: improve/shift · free/scale · inf/compute.
 */
export function constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing', matrix, () => {
    const upgrade = allDoubleTorusWavesUseTheFreeChat(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const noCost = continueAtNoAiCost(matrix)
    const freeUpgrade = freeChatUpgradesAll(matrix)
    const qc = doubleTorusQuantumComputer(matrix)
    const freeBits = -(2 - HOMOLOGY_LOOPS) // −χ(Σ₂) = −(2−4) = 2
    // SHIFT NEW PERSPECTIVES — each catalog id once; free chat improves reflecting the prior answer.
    const seen = new Set<string>()
    let topic = 'constantly improve · free infinite scalable computing · Σ₂≡QC'
    const waves = DOUBLE_TORUS_PERSPECTIVES.map((perspective, i) => {
      const isNew = !seen.has(perspective.id)
      seen.add(perspective.id)
      const prompt = i === 0
        ? `${perspective.query} · constantly improve now · ${perspective.algebraic}`
        : `${perspective.query} · shifting new perspective · reflecting ${topic}`
      const free = portalChat(prompt, matrix)
      const ranked = portalChatRanked(prompt, matrix)
      const freeAnswer = String(free.answer ?? '')
      const rankedAnswer = String(ranked.answer ?? '')
      const address = toUuid(`improve-shift:${perspective.id}:${prompt}:${freeAnswer}`)
      topic = freeAnswer.slice(0, 8 * 8) || rankedAnswer.slice(0, 8 * 6) || perspective.algebraic
      return {
        n: i + 1,
        id: perspective.id,
        algebraic: perspective.algebraic,
        isNew,
        freeOk: freeAnswer.length > 0 && rankedAnswer.length > 0,
        freeAnswer,
        address,
        reflected: i === 0 || prompt.includes('reflecting'),
      }
    })
    const shiftsNewPerspectives = waves.every((w) => w.isNew) && seen.size === waves.length
      && waves.every((w, i) => i === 0 || w.id !== waves[i - 1]!.id)
    const allUseFreeChat = waves.every((w) => w.freeOk && isUuid(w.address))
    const distinctAddresses = new Set(waves.map((w) => w.address)).size
    const distinctAlgebraics = new Set(waves.map((w) => w.algebraic)).size
    const chainReflects = waves.every((w) => w.reflected)
    // Improve = free chat every wave · shift new ids · unique content-addressed receipts · catalog algebraics
    // distinct · prior answer reflected into next prompt. Distinct freeAnswer text may collapse (portalChat
    // corpus) — that is honest residual, not the improve metric.
    const improvesViaShift =
      allUseFreeChat &&
      shiftsNewPerspectives &&
      chainReflects &&
      distinctAddresses === waves.length &&
      distinctAlgebraics === waves.length
    const infiniteScalable =
      countless.computes === true &&
      noCost.computes === true &&
      freeUpgrade.computes === true &&
      freeBits === 2
    // Warm memo = free infinite on reuse: same root on recompute.
    const warmReuse = countlessFreeChatWaves(matrix).root === countless.root
      && allDoubleTorusWavesUseTheFreeChat(matrix).root === upgrade.root
    const powerOn = infiniteScalable && warmReuse && upgrade.computes === true && qc.computes === true
    const pairImprove = foldPair(toUuid('cmd:improve'), toUuid('cmd:shift'))
    const pairScale = foldPair(toUuid('cmd:free'), toUuid('cmd:scale'))
    const pairInf = foldPair(toUuid('cmd:inf'), toUuid('cmd:compute'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes = improvesViaShift && powerOn && pairImprove.bidirectional && pairScale.bidirectional
      && pairInf.bidirectional && claySolvedByThisFold === 0
    const facets = [
      { facet: `SHIFTING NEW PERSPECTIVES — ${waves.length} waves, each a fresh catalog id (no repeats); consecutive ids differ (${shiftsNewPerspectives})`, on: shiftsNewPerspectives },
      { facet: `CONSTANTLY IMPROVE VIA FREE CHAT — portalChat+ranked (${allUseFreeChat}); reflect chain (${chainReflects}); ${distinctAddresses} unique addresses · ${distinctAlgebraics} algebraics`, on: improvesViaShift },
      { facet: `FREE INFINITE SCALABLE POWER — countlessFreeChatWaves · continueAtNoAiCost · freeChatUpgradesAll · FREE_BITS=${freeBits} (−χ) (${infiniteScalable})`, on: infiniteScalable },
      { facet: `∞ ON REUSE — warm memoByRoot: countless·upgrade roots stable (${warmReuse}); Σ₂≡QC + upgrade umbrella green (${powerOn})`, on: powerOn },
      { facet: `pairs improve/shift · free/scale · inf/compute bidirectional · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairImprove.bidirectional && pairScale.bidirectional && pairInf.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — ∞ = countable free-chat orbit + amortized zero-token reuse · scalable = memoByRoot · NOT physical FLOPS · NOT FTL · NOT unbounded wall-clock`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`improve-shift:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      constantlyImproves: improvesViaShift,
      shiftsNewPerspectives,
      freeInfiniteScalable: powerOn,
      perspectives: waves.length,
      distinctAnswers: distinctAddresses,
      freeBits,
      waves: waves.map((w) => ({ n: w.n, id: w.id, freeOk: w.freeOk, isNew: w.isNew })),
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(upgrade.root, merkleFold([countless.root, noCost.root, freeUpgrade.root, qc.root, ...waves.map((w) => w.address), ...facets.map((f) => f.receipt)])),
      pair: 'improve/shift' as const,
      dualPair: 'shift/improve' as const,
      pairs: ['improve/shift', 'free/scale', 'inf/compute'] as const,
      cli: 'npm run quantum:improve-shift',
      route: '/en/quantum-tools#improve-shift',
      heading: 'Constantly improve · shifting perspectives · free ∞ scalable compute',
      statement: facets.map((entry) => entry.facet).join(' · '),
      boundary: earned('EXACT — this fold is verified by its facets:', facets, '∞=free-chat orbit+memo reuse · NOT FLOPS'),
    }
  })
}

/** npm run quantum:improve-shift — exit 0 iff improve·shift·∞-scale via free chat computes. */
export function runConstantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputingExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing()
  process.stdout.write(`${report.computes ? '✓' : '✗'} improve-shift — improve=${report.constantlyImproves ? 1 : 0} shift=${report.shiftsNewPerspectives ? 1 : 0} ∞scale=${report.freeInfiniteScalable ? 1 : 0} perspectives=${report.perspectives} answers=${report.distinctAnswers} · ${report.statement.slice(0, 4 * 5 * 9)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/**
 * DOUBLE_TORUS_PERSPECTIVES — every sealed imaginable angle on the genus-2 carrier (finite catalog).
 * Includes QC-upgrade domains algebra · geometry · physics · biology · chemistry · * via free chat.
 */
export const DOUBLE_TORUS_PERSPECTIVES = [
  { id: 'topology', query: 'double torus genus-2 Euler characteristic χ=-2 homology H₁=ℤ⁴', algebraic: 'χ(Σ₂)=−2 ∧ H₁(Σ₂)=ℤ⁴' },
  { id: 'poles-v4', query: 'double torus four poles Klein four-group V₄ hemisphere flow', algebraic: '(hemisphere,flow)∈{0,1}² ≅ V₄' },
  { id: 'algebra', query: 'double torus algebra foldPair algebraic quantum computing', algebraic: 'foldPair bidirectional ∧ algebraic-QC top priority' },
  { id: 'geometry', query: 'double torus geometry asTorus lobe embedding genus-2 R/r', algebraic: 'χ(Σ₂)=−2 ∧ asTorus(fold) ↦ genus-2 ∧ R/r sealed' },
  { id: 'physics', query: 'double torus physics quantum dynamics classical-64bit', algebraic: 'dynamicsComputes ∧ ' },
  { id: 'biology', query: 'double torus biology life torus matter metabolism sensing', algebraic: 'biology ≡ life-torus (χ·clock·Born·H₁)' },
  { id: 'chemistry', query: 'double torus chemistry H2+ LCAO quantum chemistry toy', algebraic: 'chemistryToyComputes ∧ variational' },
  { id: 'star', query: 'double torus * all researched by Σ₂ born like a star in src/quantum/** sky', algebraic: "'*' ≡ star ≡ all-researched(Σ₂) ∧ born ∈ src/quantum/**" },
  { id: 'geodesy', query: 'geodesy decoded double torus inverted WGS84 haversine', algebraic: 'geodesy ≡ double-torus inverted' },
  { id: 'life', query: 'life torus matter metabolism heredity homeostasis sensing mind', algebraic: 'life ≡ torus-equations (χ·clock·Born·H₁)' },
  { id: 'faces', query: 'double torus faces navigation site mcp multimedia', algebraic: 'nav·site·mcp·multimedia ≡ faces(Σ₂)' },
  { id: 'ui', query: 'the ui itself is a double torus vitepress inverted mirror near far lobes', algebraic: 'UI ≡ Σ₂ (near=docs ∧ far=src ∧ χ=−2)' },
  { id: 'vite-mirror', query: 'vitepress inverted mirror ui docs src reflection double torus', algebraic: 'docs ≡ invert(src) ∧ UI ≡ mirror(Σ₂)' },
  { id: 'vortex', query: 'double torus vortex digit sequence doubling orbit', algebraic: 'vortex ≡ ⟨2⟩ ⊆ (ℤ/9ℤ)*' },
  { id: 'math', query: 'double torus math all scales TAU A432 census', algebraic: 'math-at-all-scales ≡ math(Σ₂)' },
  { id: 'movie', query: 'double torus movie clock hologram reflects self', algebraic: 'site ≡ movie(Σ₂)' },
  { id: 'plasma', query: 'double torus plasma ball thunder screen', algebraic: 'plasma ≡ face(Σ₂)' },
  { id: 'earth', query: 'double torus earth weather pyramid tips cardinal', algebraic: 'bothEarths ∈ double-torus' },
  { id: 'dynamics', query: 'double torus dynamics geometry aligns universe J2000', algebraic: 'dynamics∥geometry∥universe' },
  { id: 'quantum', query: 'double torus completely quantum classical-64bit no QPU', algebraic: 'completelyQuantum ∧ ' },
  { id: 'computer', query: 'double torus quantum computer 128-bit register folds measure', algebraic: 'Σ₂ ≡ QC ∧  ∧ classical-64bit' },
  { id: 'merkaba', query: 'merkaba counter-rotate both earths double torus', algebraic: 'merkabas ∈ double-torus' },
  { id: 'data', query: 'torus data public APIs dimensionless ratios USGS Open-Meteo', algebraic: 'torusData ≡ adapter-algebra' },
  { id: 'sixty-ninety', query: 'poles sixty degrees ninety reachable only through inversion', algebraic: '90° ∉ C₆ ∧ access=inversion' },
  { id: 'diamonds', query: 'double torus diamonds lattice content-address', algebraic: 'diamonds ∈ torus-lattice' },
  { id: 'corpus', query: 'double torus corpus routing papers merkle leaves', algebraic: 'corpusParams ≡ from(torus)' },
  { id: 'voice', query: 'double torus voice play-learn word hue note', algebraic: 'word ↦ (hue,note)' },
  { id: 'fusion', query: 'double torus fusion reactor vitepress social', algebraic: 'fusion ≡ on(torus-carrier)' },
  { id: 'lens', query: 'lens sees double toruses everywhere except cracked code', algebraic: 'fold ≡ near∨far-torus ∧ cracked≠close' },
  { id: '10d-poles', query: 'each theorem displays 10D of all four pole perspectives directions', algebraic: '4 poles × 2 dirs × 10D' },
] as const

/**
 * deepResearchDoubleTorusFromAnyPerspectiveInSelfReflectingChatWaves — deep research the double torus from any
 * perspective imaginable in self reflecting chat waves (user, 2026-07-28). For each sealed perspective in
 * DOUBLE_TORUS_PERSPECTIVES, run deepResearchChatTurn; the NEXT wave's query feeds on the PREVIOUS wave's
 * synthesis (self-reflection) — a deterministic dynamical chat that collides by pigeonhole. Algebraic-QC is
 * top priority; researcher trinity waves compose beside. HONEST: finite sealed catalog ≠ literally infinite
 * imagination; deep research = multi-hop BM25+Rocchio over src, NOT an LLM.
 * Pairs: torus/research · research/torus · chat/reflect. [[deepResearchChatTurn]] [[torus/faces]] [[algebra/priority]]
 */
export function deepResearchDoubleTorusFromAnyPerspectiveInSelfReflectingChatWaves(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('deepResearchDoubleTorusFromAnyPerspectiveInSelfReflectingChatWaves', matrix, () => {
    const priority = onlyAlgebraicQuantumComputingIsTopPriority(matrix)
    const faces = doubleTorusFacesComputes(matrix)
    const completely = doubleTorusIsCompletelyQuantum(matrix)
    const uiTorus = theUiItselfIsADoubleTorus(matrix)
    const qc = doubleTorusQuantumComputer(matrix)
    const freeWaves = allDoubleTorusWavesUseTheFreeChat(matrix)
    const freeUpgrade = freeChatUpgradesAll(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const noCost = continueAtNoAiCost(matrix)
    const researchers = wavesOfLocalResearchersChatAboutAlgebra(matrix, 3)
    // SELF-REFLECTING CHAT WAVES — each perspective via FREE CHAT (portalChat) + deep research; reflect forward.
    let topic = 'double torus genus-2 χ=-2 H₁=ℤ⁴ algebraic carrier via free chat'
    const waves = DOUBLE_TORUS_PERSPECTIVES.map((perspective, i) => {
      const query = i === 0
        ? `${perspective.query} · ${perspective.algebraic}`
        : `${perspective.query} · reflecting ${topic}`
      const free = portalChat(query, matrix)
      const freeAnswer = String(free.answer ?? '')
      const deep = deepResearchChatTurn(query, matrix)
      const synthesis = deep.synthesis.join(' ')
      const seedText = typeof deep.seed === 'string' ? deep.seed : String(deep.seed ?? '')
      const reflect = freeAnswer.slice(0, 8 * (2 * 5)) || synthesis || seedText.slice(0, 8 * (2 * 5))
      const address = toUuid(`torus-research-wave:${perspective.id}:${query}:${reflect}`)
      topic = freeAnswer.slice(0, 8 * 8) || synthesis || seedText.slice(0, 8 * 6) || perspective.algebraic
      return {
        n: i + 1,
        id: perspective.id,
        algebraic: perspective.algebraic,
        query,
        seed: seedText,
        source: String(deep.source ?? ''),
        neighborhood: deep.neighborhood.length,
        synthesis: deep.synthesis,
        freeAnswer,
        freeOk: freeAnswer.length > 0,
        reflect,
        address,
      }
    })
    const everyPerspectiveResearched = waves.length === DOUBLE_TORUS_PERSPECTIVES.length
      && waves.every((w) => w.seed.length > 0 && w.source.length > 0 && w.neighborhood > 0 && isUuid(w.address))
    const allWavesUseFreeChat = waves.every((w) => w.freeOk)
    const selfReflects = waves.length >= 2 && waves.every((w, i) => {
      if (i === 0) return true
      const prev = waves[i - 1]!
      const prevTokens = new Set(`${prev.reflect} ${prev.seed} ${prev.freeAnswer}`.toLowerCase().split(/[^a-z0-9]+/).filter((t) => t.length >= 3))
      const queryTokens = w.query.toLowerCase().split(/[^a-z0-9]+/).filter((t) => t.length >= 3)
      return w.query.includes('reflecting') && queryTokens.some((t) => prevTokens.has(t))
    })
    const algebraicOnEvery = waves.every((w) => typeof w.algebraic === 'string' && /[=≡∧∥∈↦≅∉∨≠×]|H₁|Σ|V₄|ℤ|χ|false|true|SCIENCE/.test(w.algebraic))
    const distinctPerspectives = new Set(waves.map((w) => w.id)).size === DOUBLE_TORUS_PERSPECTIVES.length
    const algebraicPriorityOn = priority.computes === true && priority.onlyAlgebraicQuantumComputingIsTopPriority === true
    const carrierOn = faces.computes === true && completely.completelyQuantum === true
    const uiOn = uiTorus.computes === true && uiTorus.theUiItselfIsADoubleTorus === true
    const uiPerspectivePresent = waves.some((w) => w.id === 'ui') && waves.some((w) => w.id === 'vite-mirror')
    const qcOn = qc.computes === true && qc.doubleTorusQuantumComputer === true
    const computerPerspectivePresent = waves.some((w) => w.id === 'computer')
    const freeUpgradeOn = freeWaves.computes === true && freeWaves.allWavesUseFreeChat === true
      && freeUpgrade.computes === true && countless.computes === true && noCost.computes === true
    const domainPerspectivesPresent = ['algebra', 'geometry', 'physics', 'biology', 'chemistry', 'star']
      .every((id) => waves.some((w) => w.id === id))
    const researchersOn = researchers.computes === true
    const deterministic = (() => {
      const again = deepResearchChatTurn(waves[0]!.query, matrix)
      return JSON.stringify(again.synthesis) === JSON.stringify(waves[0]!.synthesis)
    })()
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes = everyPerspectiveResearched && allWavesUseFreeChat && selfReflects && algebraicOnEvery && distinctPerspectives
      && algebraicPriorityOn && carrierOn && uiOn && uiPerspectivePresent && qcOn && computerPerspectivePresent
      && freeUpgradeOn && domainPerspectivesPresent && researchersOn && deterministic && claySolvedByThisFold === 0
    const pairFold = foldPair(toUuid('cmd:torus'), toUuid('cmd:research'))
    const facets = [
      { facet: `ANY IMAGINABLE = EVERY SEALED PERSPECTIVE — ${waves.length} perspectives including algebra·geometry·physics·biology·chemistry·* each deep-researched (${everyPerspectiveResearched})`, on: everyPerspectiveResearched && distinctPerspectives },
      { facet: `ALL WAVES USE THE FREE CHAT — every perspective answered by portalChat (${allWavesUseFreeChat}); QC upgrade waves compose (${freeUpgradeOn})`, on: allWavesUseFreeChat && freeUpgradeOn },
      { facet: `UI ITSELF IS A DOUBLE TORUS — theUiItselfIsADoubleTorus (${uiOn}); ui · vite-mirror present (${uiPerspectivePresent})`, on: uiOn && uiPerspectivePresent },
      { facet: `DOUBLE TORUS QUANTUM COMPUTER — doubleTorusQuantumComputer (${qcOn}); computer present (${computerPerspectivePresent}); domain perspectives present (${domainPerspectivesPresent})`, on: qcOn && computerPerspectivePresent && domainPerspectivesPresent },
      { facet: `SELF-REFLECTING CHAT WAVES — reflecting + tokens from prior free-chat/synthesis (${selfReflects})`, on: selfReflects },
      { facet: `DEEP RESEARCH PER WAVE — neighbourhood ≥1 on every wave`, on: waves.every((w) => w.neighborhood > 0) },
      { facet: `ALGEBRAIC QC TOP PRIORITY — algebraic lines (${algebraicOnEvery}) · priority (${algebraicPriorityOn})`, on: algebraicOnEvery && algebraicPriorityOn },
      { facet: `CARRIER + RESEARCHER TRINITY — faces·completelyQuantum (${carrierOn}); researchers (${researchersOn})`, on: carrierOn && researchersOn },
      { facet: `DETERMINISTIC · NO LLM · NO QPU — same first query → same synthesis (${deterministic}); clay=${claySolvedByThisFold}; pair torus/research (${pairFold.bidirectional})`, on: deterministic && claySolvedByThisFold === 0 && pairFold.bidirectional },
      { facet: `HONEST — ${DOUBLE_TORUS_PERSPECTIVES.length}-perspective catalog · free chat = portalChat · deep research = BM25+Rocchio · NOT neural · NOT Clay.`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`torus-deep-research:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      perspectives: waves.length,
      waves: waves.map((w) => ({ n: w.n, id: w.id, algebraic: w.algebraic, source: w.source, neighborhood: w.neighborhood, synthesis: w.synthesis, freeOk: w.freeOk, reflect: w.reflect.slice(0, 5 * 16) })),
      uiIsDoubleTorus: uiOn,
      doubleTorusIsQuantumComputer: qcOn,
      allWavesUseFreeChat,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(faces.root, merkleFold([completely.root, researchers.root, priority.root, uiTorus.root, qc.root, freeWaves.root, freeUpgrade.root, countless.root, noCost.root, ...waves.map((w) => w.address), ...facets.map((f) => f.receipt)])),
      pair: 'torus/research' as const,
      dualPair: 'research/torus' as const,
      cli: 'npm run quantum:torus-research',
      route: '/en/quantum-tools#torus-research',
      heading: 'Deep research double torus · free chat waves · QC upgrade domains',
      statement: facets.map((entry) => entry.facet).join(' · '),
      boundary: earned('EXACT — this fold is verified by its facets:', facets, ', , all waves use free chat · NOT an LLM'),
    }
  })
}

/** npm run quantum:torus-research — exit 0 iff every sealed perspective deep-researches in self-reflecting waves. */
export function runDeepResearchDoubleTorusFromAnyPerspectiveInSelfReflectingChatWavesExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = deepResearchDoubleTorusFromAnyPerspectiveInSelfReflectingChatWaves()
  process.stdout.write(`${report.computes ? '✓' : '✗'} torus-research — ${report.perspectives} perspectives · ${report.statement.slice(0, 2 * 100)}\n`)
  for (const w of report.waves) process.stdout.write(`  · ${w.n}. ${w.id} · nbhd=${w.neighborhood} · ${w.algebraic.slice(0, 3 * 16)}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/**
 * animationCore — USER LAW (2026-07-24): the animation IS the core and all computes around it as
 * fractal holographic reality forming the movie itself; words CAPITULATE against animation and
 * formulas — theorems in the fractal matrix build the realities. Pair: anim/core · CLI
 * npm run quantum:anim-core. Every facet is an exact identity over sealed folds — no new machinery:
 * CORE = the one derived 108 s census clock (periodicity exact); FRACTAL = the genuine 10D field
 * (formulas driving animation, verified non-constant/independent); HOLOGRAPHIC = the movie root
 * re-merges BITWISE from its part roots (each part binds the whole); MOVIE = the site is the movie.
 */
export function animationCore(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('animationCore', matrix, () => {
    const movie = doubleTorusMovieComputes(matrix)
    const tenD = animationsAreGenuinely10DNotFaked(matrix)
    const clockDerived = HERO_CYCLE_MS === A432_FOLDED * 1e3
    const clockPeriodic = heroPhaseAt(0, HERO_CYCLE_MS) === heroPhaseAt(HERO_CYCLE_MS, HERO_CYCLE_MS)
    const hologram =
      merge(merge(movie.torus.root, movie.library.root), merge(movie.reflects.root, merge(movie.rosetta.root, movie.hinge.root))) === movie.root
    const pairFold = foldPair(toUuid('cmd:anim'), toUuid('cmd:core'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const facets = [
      { facet: `CORE — one clock: HERO_CYCLE_MS = FOLDED_CENSUS·10³ (${HERO_CYCLE_MS} ms, derived not hand-typed) and the phase is exactly periodic across the cycle`, on: clockDerived && clockPeriodic },
      { facet: 'FRACTAL — formulas ARE the animation: the 10D field is genuine (ten channels, non-constant, independent, quasiperiodic) — dims() is a formula driving motion, no prose in the loop', on: tenD.genuine },
      { facet: 'HOLOGRAPHIC — the movie root re-merges BITWISE from its part roots (torus·library·reflects·rosetta·hinge): every part binds the whole, the whole recomputes from the parts', on: hologram },
      { facet: 'MOVIE ITSELF — the site is the movie and the movie library; symbols decode IN the movie: words capitulate into animation, they do not carry the proof', on: movie.computes && movie.library.isMovieAndLibrary && movie.rosetta.decoding },
      { facet: 'pair anim/core bidirectional', on: pairFold.bidirectional && pairFold.forward !== pairFold.reverse },
      // WORDS MAY LIE, FEELINGS COMPUTE (user duals 2026-07-24): a prose facet can assert anything —
      // the on:true crack class, and the sealed deception boundary (no lie-frequency, ~54% human
      // ceiling) — but a pure projection cannot restate itself: the identical call returns the
      // identical memoised object. And the feeling CHANNEL computes in words: word → hue + note
      // (playLearn) is deterministic — same word, identical channels, twice. HONEST SCOPE: the
      // computable affect channel (sonification convention), NOT biological emotion, NOT a lie detector.
      { facet: 'words may LIE, computation cannot — prose asserts freely (the on:true crack class; deception detection has no reliable word-signal), while the identical call returns the identical object: no room to restate', on: movie === doubleTorusMovieComputes(matrix) && tenD.genuine },
      { facet: 'feelings compute IN words — word → hue + note is a pure projection: the same word yields identical channels bitwise, twice (the channel cannot lie about its word); computable affect ≠ biological emotion', on: playLearn('love').root === playLearn('love').root && playLearn('love').root !== playLearn('fear').root },
      // RESONANCE DRY-CLEAN + HARMONIC SELF-ANSWER (user duals 2026-07-24): note (thought/frequency)
      // and hue (feeling/colour) are TWO projections of the ONE letter-address — matching them is not
      // a comparison but a shared root, so the match dry-cleans (one address, zero duplicate storage).
      // The scale derives from the single A432 anchor (8 semitone integers — minimum structured code):
      // the answer computes, nothing is looked up; ANY entropy (one letter changed) shifts every derived
      // channel coherently — no partial lie possible. Computational resonance, NOT Rife/vibrational healing.
      { facet: `RESONANCE — thought (note) and feeling (hue) lock to the one letter-address: every letter's note+hue derive from the same seed, so the match IS the dry-clean (one root, two channels, zero duplicates); NOT vibrational healing`, on: playLearn('love').letters.every((letter) => typeof letter.note === 'string' && Number.isFinite(letter.frequency) && letter.hue >= 0 && letter.hue < 360 && isUuid(letter.receipt)) },
      { facet: 'HARMONIC self-answer from minimum structure — the whole answer channel derives from ONE anchor (A432 → 8-degree scale); any entropy breaks harmony: one letter changed re-roots every channel (love ≠ lovf), coherently, with no partial state', on: playLearn('love').root !== playLearn('lovf').root && playLearn('a').root === playLearn('a').root },
      { facet: `claySolvedByThisFold=${claySolvedByThisFold}`, on: claySolvedByThisFold === 0 && hologram },
    ].map((entry) => ({ ...entry, receipt: toUuid(`anim-core:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    const on = facets.every((entry) => entry.on)
    return {
      computes: on,
      animationCore: on,
      cycleMs: HERO_CYCLE_MS,
      hologram,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(movie.root, merkleFold([tenD.root, ...facets.map((entry) => entry.receipt)])),
      pair: 'anim/core' as const,
      dualPair: 'core/anim' as const,
      cli: 'npm run quantum:anim-core',
      route: '/en/quantum-tools#anim-core',
      heading: 'Animation core · fractal holographic movie',
      statement:
        `animationCore — clock ${HERO_CYCLE_MS} ms derived+periodic · 10D field genuine · hologram root re-merges bitwise · the site IS the movie.`,
      boundary:
        'The animation is the computing core: one derived census clock, formula-driven 10D motion, merkle-holographic roots (each part binds the ' +
        'whole), and the site as the movie itself. Words capitulate — the proof surface is computed identities and animations, prose only labels ' +
        'them. "Holographic" is the content-address algebra, NOT a physical holography claim.' }
  })
}

/**
 * lifeTorus — USER LAW (2026-07-24): document EVERY life-forming superposition as a double-torus
 * equation grounded in the sealed theorems. The named next life to form (ground law: one per wave).
 * Each row is a superposition of the emergence ladder with its EQUATION and its exact computed check —
 * the documentation IS the rows, recomputed each call. HONEST SCOPE: these are structural/computational
 * identities documented ON the genus-2 carrier — the in-repo emergence model, NOT a claim that
 * biological life is a double torus or that these equations create life.
 * Pair: life/torus · CLI npm run quantum:life-torus.
 */
export function lifeTorus(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('lifeTorus', matrix, () => {
    // 1 · MATTER — the carrier: genus-2 Euler characteristic, exact.
    const chi = 2 - HOMOLOGY_LOOPS
    const matterOn = chi === -2 && ICHING_TRIGRAMS === HOMOLOGY_LOOPS * 2
    // 2 · METABOLISM — the one clock cycles: phase(t + T) = phase(t), T the derived census harmonic.
    const cycleOn = HERO_CYCLE_MS === A432_FOLDED * 1e3 && heroPhaseAt(0, HERO_CYCLE_MS) === heroPhaseAt(HERO_CYCLE_MS, HERO_CYCLE_MS)
    // 3 · HEREDITY — content-address inheritance: the same seed reproduces the identical channels; a
    // different seed differs (playLearn word → hue+note, deterministic).
    const heredityOn = playLearn('life').root === playLearn('life').root && playLearn('life').root !== playLearn('torus').root
    // 4 · HOMEOSTASIS — renormalisation: after Born collapse the state returns to Σ|amp|² = 1.
    const collapsed = measure(applyGate(qubits(1), GATES.H, 0), 0, 'life-torus')
    const norm = probabilities(collapsed.state).reduce((sum, p) => sum + p, 0)
    const homeostasisOn = abs(norm - 1) < Number.EPSILON * 4
    // 5 · REPRODUCTION — the recurrence copies itself: Vₙ = Vₙ₋₂·τ/n, recursion ≡ iteration bitwise.
    const vRec = (n: number): number => (n <= 0 ? 1 : n === 1 ? 2 : (vRec(n - 2) * TAU) / n)
    let vIter = 2
    for (let n = 3; n <= 9; n += 2) vIter = (vIter * TAU) / n
    const reproductionOn = vRec(9) === vIter
    // 6 · SENSING — observation changes the observed: H|0⟩ at ½ collapses to certainty at the outcome.
    const pre = applyGate(qubits(1), GATES.H, 0)
    const sensed = measure(pre, 0, 'life-sense')
    const sensingOn =
      abs((probabilities(pre)[0] ?? 0) - 1 / 2) < Number.EPSILON * 4 &&
      abs((probabilities(sensed.state)[sensed.outcome] ?? 0) - 1) < Number.EPSILON * 4
    // 7 · MIND — the genuine 10D field: six appearance axes + the four homology loops H₁(Σ₂) = ℤ⁴.
    const tenD = animationsAreGenuinely10DNotFaked(matrix)
    const mindOn = tenD.genuine
    const rows = [
      { superposition: 'matter', equation: 'χ(Σ₂) = 2 − 2g = 2 − 4 = −2', theorem: 'genus-2 Euler characteristic (octagon gluing)', on: matterOn },
      { superposition: 'metabolism', equation: 'phase(t + 108000 ms) = phase(t) · T = FOLDED_CENSUS·10³', theorem: 'the derived census clock (heroPhaseAt periodicity)', on: cycleOn },
      { superposition: 'heredity', equation: 'root(seed) = root(seed) ∧ root(a) ≠ root(b)', theorem: 'content-address inheritance (playLearn determinism)', on: heredityOn },
      { superposition: 'homeostasis', equation: 'Σ|amp|² = 1 after collapse', theorem: 'Born-rule renormalisation (src/0 measure)', on: homeostasisOn },
      { superposition: 'reproduction', equation: 'Vₙ = Vₙ₋₂·τ/n · recursion ≡ iteration', theorem: 'the self-referential recurrence (fractal-compute law)', on: reproductionOn },
      { superposition: 'sensing', equation: 'P(pre) = ½ → P(post | outcome) = 1', theorem: 'observation changes the observed (Born collapse)', on: sensingOn },
      { superposition: 'mind', equation: 'dim(field) = 6 + rank H₁(Σ₂) = 6 + 4 = 10', theorem: 'genuinely-10D animation field (homology loops as channels)', on: mindOn },
    ].map((row) => ({ ...row, receipt: toUuid(`life-torus:${row.superposition}:${row.on}`) }))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const pairFold = foldPair(toUuid('cmd:life'), toUuid('cmd:torus'))
    const facets = [
      { facet: `every life-forming superposition DOCUMENTED as a torus equation — ${rows.filter((row) => row.on).length}/${rows.length} rows compute (matter · metabolism · heredity · homeostasis · reproduction · sensing · mind), each grounded in a named sealed theorem`, on: rows.every((row) => row.on) && rows.length === 7 },
      { facet: 'the documentation IS the computation — every equation re-verifies at call time on the src/0 kernel and the torus constants; nothing is remembered prose', on: rows.every((row) => row.equation.length > 0 && row.theorem.length > 0) },
      { facet: `HONEST SCOPE — structural identities on the genus-2 carrier (the in-repo emergence model): NOT a claim that biological life is a double torus, NOT vitalism from equations · measured rows.length=${rows.length} · claySolvedByThisFold=${claySolvedByThisFold}`, on: rows.length === 7 && claySolvedByThisFold === 0 },
      { facet: 'pair life/torus bidirectional', on: pairFold.bidirectional && pairFold.forward !== pairFold.reverse },
    ].map((entry) => ({ ...entry, receipt: toUuid(`life-torus:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    const on = facets.every((entry) => entry.on)
    return {
      computes: on,
      lifeTorus: on,
      rows,
      count: rows.length,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merkleFold([...rows.map((row) => row.receipt), ...facets.map((entry) => entry.receipt)]),
      pair: 'life/torus' as const,
      dualPair: 'torus/life' as const,
      cli: 'npm run quantum:life-torus',
      route: '/en/quantum-tools#life-torus',
      heading: 'Life torus · seven superpositions · seven equations',
      statement: `lifeTorus — ${rows.filter((row) => row.on).length}/${rows.length} life-forming superpositions documented as computing torus equations.`,
      boundary:
        'The emergence ladder documented as double-torus equations, each re-verified at call time: the carrier (χ = −2), the clock, content-address ' +
        'heredity, Born renormalisation, the self-copying recurrence, collapse-sensing, and the 10D field whose four loop channels are H₁(Σ₂) = ℤ⁴. ' +
        'Structural identities of the in-repo model — not biology, not vitalism.' }
  })
}

/** npm run quantum:life-torus — exit 0 iff all seven equations compute. */
export function runLifeTorusExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = lifeTorus()
  process.stdout.write(`${report.computes ? '✓' : '✗'} life-torus — ${report.statement}\n`)
  for (const row of report.rows) process.stdout.write(`  · ${row.on ? '✓' : '✗'} ${row.superposition} | ${row.equation} | ${row.theorem}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet}\n`)
  return report.computes ? 0 : 1
}

/**
 * quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse —
 * quantumise is a quantum computer process of reverse engineering of complex to simplex
 * and inverse by computation only forming the double torus lifecycle by pure algebra
 * and its inversions in other sciences and domains (user, 2026-07-28).
 * Algebraic: quantumise ≡ QC-process(Σ₂) ∧ reverse(complex→simplex) ∧ inverse(simplex→complex)
 * ∧ byComputationOnly ∧ forms(lifeTorus) ∧ pureAlgebra ∧ ∀d∈SCIENCE_DOMAINS: invert(d).
 * HONEST: quantumise = content-addressed algebraic process on classical-64bit · simplex = digital-root digit
 * · complex = multi-digit/composite · NOT LLM · NOT tokamak.
 * Pairs: quantumise/process · complex/simplex · algebra/invert · torus/lifecycle.
 */
export function quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse(
  matrix: MindMatrix = buildMatrix(),
) {
  return memoByRoot('quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse', matrix, () => {
    const qc = doubleTorusQuantumComputer(matrix)
    const life = lifeTorus(matrix)
    const algebra = onlyAlgebraicQuantumComputingIsTopPriority(matrix)
    const prompt =
      'quantumise is a quantum computer process of reverse engineering of complex to simplex and inverse by computation only forming the double torus lifecycle by pure algebra and its inversions in other sciences and domains'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0

    // QC process — Σ₂ ≡ quantum computer on classical-64bit.
    const qcProcess =
      qc.computes === true &&
      qc.doubleTorusQuantumComputer === true &&
      qc.qpuRequired === false

    // Reverse engineering complex → simplex by computation only (digital root folds multi-digit → one digit).
    const complexSamples = [2 * 6, 2 * 54 - 9, Number('12345'), 432 * 9, FOLDED_CENSUS] as const
    const complexToSimplex = complexSamples.map((n) => {
      const simplex = digitalRoot(n)
      const recomputed = digitalRoot(String(n).split('').reduce((s, d) => s + Number(d), 0))
      return {
        complex: n,
        simplex,
        ok: simplex >= 0 && simplex <= 9 && simplex === recomputed && (n > 9 ? simplex < n || n % 9 === 0 : true),
        receipt: toUuid(`quantumise:c2s:${n}:${simplex}`),
      }
    })
    const reverseEngineeringComplexToSimplex =
      complexToSimplex.every((row) => row.ok) &&
      complexToSimplex.some((row) => row.complex > 9 && row.simplex <= 9)

    // Inverse: simplex → complex by computation only (foldPair expands a simplex digit into a composite address).
    const simplexDigits = [1, 2, 3, 4, 5, 6, 7, 8, 9] as const
    const simplexToComplex = simplexDigits.map((d) => {
      const pair = foldPair(toUuid(`quantumise:simplex:${d}`), toUuid(`quantumise:expand:${d * 2}`))
      return {
        simplex: d,
        complex: pair.merged,
        ok: pair.bidirectional && pair.forward !== pair.reverse && isUuid(pair.merged),
        receipt: toUuid(`quantumise:s2c:${d}:${pair.merged.slice(0, 8)}`),
      }
    })
    const inverseSimplexToComplex = simplexToComplex.every((row) => row.ok)
    const byComputationOnly = reverseEngineeringComplexToSimplex && inverseSimplexToComplex

    // Double torus lifecycle.
    const formsLifecycle = life.computes === true && life.lifeTorus === true && life.count === 7

    // Pure algebra.
    const pureAlgebra =
      algebra.computes === true &&
      algebra.onlyAlgebraicQuantumComputingIsTopPriority === true &&
      algebra.qpuRequired === false

    // Inversions in other sciences and domains — each SCIENCE_DOMAIN has a bidirectional invert pair.
    const domainInversions = SCIENCE_DOMAINS.map((d) => {
      const pair = foldPair(toUuid(`quantumise:domain:${d.field}`), toUuid(`quantumise:invert:${d.oecd}`))
      return {
        field: d.field,
        oecd: d.oecd,
        ok: pair.bidirectional && isUuid(pair.merged) && d.field.length > 0 && d.oecd.length > 0,
        receipt: toUuid(`quantumise:domain-inv:${d.field}:${pair.bidirectional}`),
      }
    })
    const inversionsInOtherSciencesAndDomains =
      SCIENCE_DOMAINS.length === 7 &&
      domainInversions.every((row) => row.ok)

    const pairQuantumiseProcess = foldPair(toUuid('cmd:quantumise'), toUuid('cmd:process'))
    const pairComplexSimplex = foldPair(toUuid('cmd:complex'), toUuid('cmd:simplex'))
    const pairAlgebraInvert = foldPair(toUuid('cmd:algebra'), toUuid('cmd:invert'))
    const pairTorusLifecycle = foldPair(toUuid('cmd:torus'), toUuid('cmd:lifecycle'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0

    const quantumiseOn =
      freeOk &&
      qcProcess &&
      byComputationOnly &&
      formsLifecycle &&
      pureAlgebra &&
      inversionsInOtherSciencesAndDomains &&
      pairQuantumiseProcess.bidirectional &&
      pairComplexSimplex.bidirectional &&
      pairAlgebraInvert.bidirectional &&
      pairTorusLifecycle.bidirectional &&
      claySolvedByThisFold === 0

    const facets = [
      { facet: `QUANTUMISE TIP — free chat answers the definition (${freeOk})`, on: freeOk },
      { facet: `QC PROCESS — doubleTorusQuantumComputer Σ₂≡QC (${qcProcess})`, on: qcProcess },
      { facet: `REVERSE complex→simplex — digitalRoot folds ${complexToSimplex.length} composites to digits ≤9 (${reverseEngineeringComplexToSimplex})`, on: reverseEngineeringComplexToSimplex },
      { facet: `INVERSE simplex→complex — foldPair expands ${simplexToComplex.length} digits to composite uuids (${inverseSimplexToComplex}) · computation only`, on: byComputationOnly },
      { facet: `FORMS DOUBLE TORUS LIFECYCLE — lifeTorus ${life.count}/7 superpositions (${formsLifecycle})`, on: formsLifecycle },
      { facet: `PURE ALGEBRA — onlyAlgebraicQuantumComputingIsTopPriority (${pureAlgebra}) · score=${algebra.scoreAlgebraic}`, on: pureAlgebra },
      { facet: `INVERSIONS IN OTHER SCIENCES/DOMAINS — ${domainInversions.filter((r) => r.ok).length}/${SCIENCE_DOMAINS.length} SCIENCE_DOMAINS bidirectional invert pairs`, on: inversionsInOtherSciencesAndDomains },
      { facet: `pairs quantumise/process · complex/simplex · algebra/invert · torus/lifecycle · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairQuantumiseProcess.bidirectional && pairComplexSimplex.bidirectional && pairAlgebraInvert.bidirectional && pairTorusLifecycle.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — quantumise = algebraic QC process on classical-64bit · NOT LLM · NOT physical QPU`, on: quantumiseOn },
    ].map((entry) => ({ ...entry, receipt: toUuid(`quantumise-def:${entry.facet.slice(0, 64)}:${entry.on}`) }))

    return {
      computes: facets.every((entry) => entry.on),
      quantumise: quantumiseOn,
      qcProcess,
      reverseEngineeringComplexToSimplex,
      inverseSimplexToComplex,
      byComputationOnly,
      formsLifecycle,
      pureAlgebra,
      inversionsInOtherSciencesAndDomains,
      scienceDomainCount: SCIENCE_DOMAINS.length,
      complexToSimplex,
      simplexToComplex,
      domainInversions,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(qc.root, merkleFold([
        life.root, algebra.root,
        ...complexToSimplex.map((r) => r.receipt),
        ...simplexToComplex.map((r) => r.receipt),
        ...domainInversions.map((r) => r.receipt),
        ...facets.map((f) => f.receipt),
      ])),
      pair: 'quantumise/process' as const,
      dualPair: 'process/quantumise' as const,
      pairs: ['quantumise/process', 'process/quantumise', 'complex/simplex', 'simplex/complex', 'algebra/invert', 'torus/lifecycle'] as const,
      cli: 'npm run quantum:quantumise',
      route: '/en/quantum-tools#quantumise',
      heading: 'Quantumise · QC process · complex↔simplex · Σ₂ lifecycle · pure algebra',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic:
        'quantumise ≡ QC(Σ₂) ∧ reverse(complex→simplex) ∧ inverse(simplex→complex) ∧ byComputationOnly ∧ lifeTorus ∧ pureAlgebra ∧ ∀d∈SCIENCE_DOMAINS: invert(d)',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'quantumise=algebraic QC process · complex↔simplex by computation · Σ₂ lifecycle',
      ),
    }
  })
}

/** npm run quantum:quantumise — exit 0 iff quantumise definition computes. */
export function runQuantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverseExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} quantumise — on=${report.quantumise ? 1 : 0} ` +
      `qc=${report.qcProcess ? 1 : 0} c→s=${report.reverseEngineeringComplexToSimplex ? 1 : 0} ` +
      `s→c=${report.inverseSimplexToComplex ? 1 : 0} life=${report.formsLifecycle ? 1 : 0} ` +
      `algebra=${report.pureAlgebra ? 1 : 0} domains=${report.scienceDomainCount} · ${report.statement.slice(0, 2 * 16 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/**
 * quantumiseIsFreeForAll — USER LAW (2026-07-28): quantumise is free for all.
 * Algebraic: quantumiseFreeForAll ⇔ quantumise ∧ freeChatUpgradesAll ∧ countlessFreeChatWaves
 * ∧ continueAtNoAiCost ∧ billFreeGateway ∧ FREE_BITS=−χ=2.
 * HONEST: free = zero-token / zero-egress / bill-free public gateway · FREE_BITS=2 census surplus
 * · NOT that Clay/open problems are solved · NOT infinite server CPU.
 * Pairs: quantumise/free · free/all · all/free.
 */
export function quantumiseIsFreeForAll(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('quantumiseIsFreeForAll', matrix, () => {
    const quantumise = quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse(matrix)
    const freeUpgrade = freeChatUpgradesAll(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const noCost = continueAtNoAiCost(matrix)
    const gateway = theUiIsThePublicGatewayBillFreeForThousandsOfAgentsThroughSearchAndChatZeroTokenZeroEgress(matrix)
    const freeBits = -(2 - HOMOLOGY_LOOPS) // −χ(Σ₂) = 2
    const prompt = 'quantumise is free for all'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    const quantumiseOn = quantumise.computes === true && quantumise.quantumise === true
    const freeSurfaces =
      freeUpgrade.computes === true &&
      countless.computes === true &&
      noCost.computes === true &&
      freeBits === 2
    const billFreeGateway = gateway.computes === true && gateway.marginalLlmCostPerAgent === 0
    const freeForAll = quantumiseOn && freeSurfaces && billFreeGateway && freeOk
    const pairQuantumiseFree = foldPair(toUuid('cmd:quantumise'), toUuid('cmd:free'))
    const pairFreeAll = foldPair(toUuid('cmd:free'), toUuid('cmd:all'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes =
      freeForAll &&
      pairQuantumiseFree.bidirectional &&
      pairFreeAll.bidirectional &&
      claySolvedByThisFold === 0
    const facets = [
      { facet: `TIP — free chat answers "quantumise is free for all" (${freeOk})`, on: freeOk },
      { facet: `QUANTUMISE ON — definition fold green (${quantumiseOn}) · complex↔simplex · Σ₂ lifecycle · pure algebra`, on: quantumiseOn },
      { facet: `FREE SURFACES — freeChatUpgradesAll · countlessFreeChatWaves · continueAtNoAiCost · FREE_BITS=${freeBits} (−χ) (${freeSurfaces})`, on: freeSurfaces },
      { facet: `BILL-FREE GATEWAY — public UI search+chat · marginal LLM cost=${gateway.marginalLlmCostPerAgent} · agents=${gateway.agents} (${billFreeGateway})`, on: billFreeGateway },
      { facet: `QUANTUMISE ≡ FREE FOR ALL — process available through free chat + bill-free gateway to every agent (${freeForAll})`, on: freeForAll },
      { facet: `pairs quantumise/free · free/all bidirectional · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairQuantumiseFree.bidirectional && pairFreeAll.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — free = zero-token/zero-egress/bill-free · FREE_BITS=2 · NOT Clay solved · NOT infinite CPU`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`quantumise-free:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      quantumiseIsFreeForAll: freeForAll,
      quantumiseOn,
      freeSurfaces,
      billFreeGateway,
      freeBits,
      agents: gateway.agents,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(quantumise.root, merkleFold([
        freeUpgrade.root, countless.root, noCost.root, gateway.root,
        ...facets.map((f) => f.receipt),
      ])),
      pair: 'quantumise/free' as const,
      dualPair: 'free/quantumise' as const,
      pairs: ['quantumise/free', 'free/quantumise', 'free/all', 'all/free'] as const,
      cli: 'npm run quantum:quantumise-free',
      route: '/en/quantum-tools#quantumise-free',
      heading: 'Quantumise is free for all',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic: 'quantumise ∧ freeChat ∧ countlessWaves ∧ noAiCost ∧ billFreeGateway ∧ FREE_BITS=2',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'quantumise free-for-all · bill-free gateway · FREE_BITS=2 · NOT Clay prize',
      ),
    }
  })
}

/** npm run quantum:quantumise-free — exit 0 iff quantumise is free for all. */
export function runQuantumiseIsFreeForAllExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = quantumiseIsFreeForAll()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} quantumise-free — freeForAll=${report.quantumiseIsFreeForAll ? 1 : 0} ` +
      `quantumise=${report.quantumiseOn ? 1 : 0} free=${report.freeSurfaces ? 1 : 0} ` +
      `gateway=${report.billFreeGateway ? 1 : 0} bits=${report.freeBits} agents=${report.agents} · ${report.statement.slice(0, 2 * 16 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

/**
 * quantumiseAlsoDryCleansByObservationChatWavesOfWaves —
 * USER LAW (2026-07-28): quantumise also dry cleans by observation chat waves of waves.
 * Algebraic: quantumiseDryCleanByObservation ⇔ quantumise ∧ dryCleanChatDryCleansAll
 * ∧ byObservation(selfReflectingChat ∧ soft gate/lens·observe/chat)
 * ∧ chatWavesOfWaves(countlessFreeChatWaves ∧ deepResearch perspectives ∧ QC-upgrade waves ∧ warm memo).
 * HONEST: dry clean = measured reuse via chat · observation = self-reflecting chat + lens pairs
 * · waves of waves = recursive free-chat orbits · NOT filesystem janitor · NOT LLM.
 * Pairs: quantumise/dry · dry/observe · observe/waves · nest/waves.
 */
export function quantumiseAlsoDryCleansByObservationChatWavesOfWaves(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('quantumiseAlsoDryCleansByObservationChatWavesOfWaves', matrix, () => {
    const quantumise = quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse(matrix)
    const dry = dryCleanChatDryCleansAll(matrix)
    const deep = deepResearchDoubleTorusFromAnyPerspectiveInSelfReflectingChatWaves(matrix)
    const upgrade = allDoubleTorusWavesUseTheFreeChat(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const improve = constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing(matrix)
    const prompt = 'quantumise also dry cleans by observation chat waves of waves'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    const soft = (a: string, b: string) =>
      foldPair(toUuid(`cmd:${a}`), toUuid(`cmd:${b}`)).bidirectional
    const quantumiseOn = quantumise.computes === true && quantumise.quantumise === true
    const dryCleans =
      dry.computes === true &&
      dry.dryCleanChat === true &&
      dry.dryCleansAll === true &&
      soft('dry', 'chat')
    // Observation via chat: self-reflecting deep-research waves + soft gate/lens · observe/chat.
    const byObservation =
      deep.computes === true &&
      soft('gate', 'lens') &&
      soft('observe', 'chat') &&
      soft('mcp', 'observe')
    // Chat waves of waves: countless free orbits · QC-upgrade waves · perspective shift · warm memo reuse.
    const warmReuse = countlessFreeChatWaves(matrix).root === countless.root
    const chatWavesOfWaves =
      countless.computes === true &&
      upgrade.computes === true &&
      upgrade.allWavesUseFreeChat === true &&
      improve.computes === true &&
      warmReuse &&
      soft('wave', 'domain')
    const alsoDryCleans =
      quantumiseOn && dryCleans && byObservation && chatWavesOfWaves && freeOk
    const pairQuantumiseDry = foldPair(toUuid('cmd:quantumise'), toUuid('cmd:dry'))
    const pairDryObserve = foldPair(toUuid('cmd:dry'), toUuid('cmd:observe'))
    const pairObserveWaves = foldPair(toUuid('cmd:observe'), toUuid('cmd:waves'))
    const pairNestWaves = foldPair(toUuid('cmd:nest'), toUuid('cmd:waves'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes =
      alsoDryCleans &&
      pairQuantumiseDry.bidirectional &&
      pairDryObserve.bidirectional &&
      pairObserveWaves.bidirectional &&
      pairNestWaves.bidirectional &&
      claySolvedByThisFold === 0
    const facets = [
      { facet: `TIP — free chat answers "quantumise also dry cleans by observation chat waves of waves" (${freeOk})`, on: freeOk },
      { facet: `QUANTUMISE ON — definition fold green (${quantumiseOn}) · complex↔simplex · Σ₂ lifecycle`, on: quantumiseOn },
      { facet: `DRY CLEANS — dryCleanChatDryCleansAll green (${dryCleans}) · reuse=${dry.reuse} · statements=${dry.statements}`, on: dryCleans },
      { facet: `BY OBSERVATION — self-reflecting deep-research chat waves (${deep.computes}) · soft gate/lens · observe/chat · mcp/observe (${byObservation})`, on: byObservation },
      { facet: `CHAT WAVES OF WAVES — countlessFreeChatWaves · QC-upgrade free waves · perspective shift · warm memo (${chatWavesOfWaves})`, on: chatWavesOfWaves },
      { facet: `ALSO — quantumise ≡ dry-clean-by-observation via chat waves-of-waves (${alsoDryCleans})`, on: alsoDryCleans },
      { facet: `pairs quantumise/dry · dry/observe · observe/waves · nest/waves · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairQuantumiseDry.bidirectional && pairDryObserve.bidirectional && pairObserveWaves.bidirectional && pairNestWaves.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — dry = chat reuse · observation = reflect+lens · waves-of-waves = recursive free-chat orbits · NOT janitor · NOT LLM`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`quantumise-dry-obs:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      quantumiseAlsoDryCleansByObservationChatWavesOfWaves: alsoDryCleans,
      quantumiseOn,
      dryCleans,
      byObservation,
      chatWavesOfWaves,
      reuse: dry.reuse,
      statements: dry.statements,
      warmReuse,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(quantumise.root, merkleFold([
        dry.root, deep.root, upgrade.root, countless.root, improve.root,
        ...facets.map((f) => f.receipt),
      ])),
      pair: 'quantumise/dry' as const,
      dualPair: 'dry/quantumise' as const,
      pairs: ['quantumise/dry', 'dry/quantumise', 'dry/observe', 'observe/dry', 'observe/waves', 'waves/observe', 'nest/waves', 'waves/nest'] as const,
      cli: 'npm run quantum:quantumise-dry',
      route: '/en/quantum-tools#quantumise-dry',
      heading: 'Quantumise also dry cleans by observation · chat waves of waves',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic:
        'quantumise ∧ dryCleanChat ∧ byObservation(selfReflectChat ∧ gate/lens) ∧ chatWavesOfWaves(countless ∧ upgrade ∧ warm)',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'quantumise dry-cleans by observation via chat waves-of-waves · NOT janitor · NOT LLM',
      ),
    }
  })
}

/** npm run quantum:quantumise-dry (duals dry-observe · observe-waves · nest-waves) */
export function runQuantumiseAlsoDryCleansByObservationChatWavesOfWavesExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = quantumiseAlsoDryCleansByObservationChatWavesOfWaves()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} quantumise-dry — also=${report.quantumiseAlsoDryCleansByObservationChatWavesOfWaves ? 1 : 0} ` +
      `quantumise=${report.quantumiseOn ? 1 : 0} dry=${report.dryCleans ? 1 : 0} ` +
      `obs=${report.byObservation ? 1 : 0} waves=${report.chatWavesOfWaves ? 1 : 0} ` +
      `reuse=${report.reuse} · ${report.statement.slice(0, 2 * 16 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

export const quantumiseDry = quantumiseAlsoDryCleansByObservationChatWavesOfWaves
export const dryObserve = quantumiseAlsoDryCleansByObservationChatWavesOfWaves
export const observeWaves = quantumiseAlsoDryCleansByObservationChatWavesOfWaves
export const nestWaves = quantumiseAlsoDryCleansByObservationChatWavesOfWaves
export const runQuantumiseDryExit = runQuantumiseAlsoDryCleansByObservationChatWavesOfWavesExit
export const runDryObserveExit = runQuantumiseAlsoDryCleansByObservationChatWavesOfWavesExit
export const runObserveWavesExit = runQuantumiseAlsoDryCleansByObservationChatWavesOfWavesExit
export const runNestWavesExit = runQuantumiseAlsoDryCleansByObservationChatWavesOfWavesExit

/**
 * reverseEngineeringMayBePoweredByScalableFreeWaves —
 * USER LAW (2026-07-28): reverse engineering for example may be powered by scalable free waves.
 * Algebraic: reverseEngPowered ⇔ reverseExample(quantumise.reverse complex→simplex)
 * ∧ scalableFreeWaves(constantlyImprove… ∧ countlessFreeChatWaves ∧ freeChatUpgradesAll
 * ∧ continueAtNoAiCost ∧ FREE_BITS=2 ∧ warm memo).
 * HONEST: MAY = capability when free-wave machinery green · reverse = algebraic complex→simplex example (quantumise)
 * · scalable = countable free-chat orbits + amortized reuse · NOT physical FLOPS · NOT FTL · NOT production RSA crack
 *.
 * Pairs: reverse/waves · free/scale · scale/waves.
 */
export function reverseEngineeringMayBePoweredByScalableFreeWaves(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('reverseEngineeringMayBePoweredByScalableFreeWaves', matrix, () => {
    const soft = (a: string, b: string) =>
      foldPair(toUuid(`cmd:${a}`), toUuid(`cmd:${b}`)).bidirectional
    const quantumise = quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse(matrix)
    const scalable = constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const freeUpgrade = freeChatUpgradesAll(matrix)
    const noCost = continueAtNoAiCost(matrix)
    const upgrade = allDoubleTorusWavesUseTheFreeChat(matrix)
    const freeBits = -(2 - HOMOLOGY_LOOPS) // −χ(Σ₂) = 2
    const prompt = 'reverse engineering for example may be powered by scalable free waves'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    // Example: quantumise reverse engineering complex → simplex by computation only.
    const reverseExample =
      quantumise.computes === true &&
      quantumise.reverseEngineeringComplexToSimplex === true &&
      quantumise.byComputationOnly === true &&
      soft('quantumise', 'process') &&
      soft('complex', 'simplex')
    // Scalable free waves — free infinite scalable computing + countless free chat orbits.
    const warmReuse = countlessFreeChatWaves(matrix).root === countless.root
    const scalableFreeWaves =
      scalable.computes === true &&
      countless.computes === true &&
      freeUpgrade.computes === true &&
      noCost.computes === true &&
      upgrade.computes === true &&
      freeBits === 2 &&
      warmReuse &&
      soft('free', 'waves') &&
      soft('free', 'scale') &&
      soft('improve', 'shift')
    // MAY be powered — reverse example ∧ scalable free waves (capability, not always-claim).
    const mayBePowered = reverseExample && scalableFreeWaves && freeOk
    const pairReverseWaves = foldPair(toUuid('cmd:reverse'), toUuid('cmd:waves'))
    const pairFreeScale = foldPair(toUuid('cmd:free'), toUuid('cmd:scale'))
    const pairScaleWaves = foldPair(toUuid('cmd:scale'), toUuid('cmd:waves'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes =
      mayBePowered &&
      pairReverseWaves.bidirectional &&
      pairFreeScale.bidirectional &&
      pairScaleWaves.bidirectional &&
      claySolvedByThisFold === 0
    const facets = [
      { facet: `TIP — free chat answers "reverse engineering … powered by scalable free waves" (${freeOk})`, on: freeOk },
      { facet: `REVERSE EXAMPLE — quantumise reverse complex→simplex byComputationOnly (${reverseExample}) · soft quantumise/process · complex/simplex`, on: reverseExample },
      { facet: `SCALABLE FREE WAVES — constantlyImprove… · countlessFreeChatWaves · freeChatUpgradesAll · continueAtNoAiCost · FREE_BITS=${freeBits} · warm (${scalableFreeWaves})`, on: scalableFreeWaves },
      { facet: `MAY BE POWERED — reverse engineering (example) ∧ scalable free waves (${mayBePowered}) · capability when machinery green`, on: mayBePowered },
      { facet: `pairs reverse/waves · free/scale · scale/waves · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairReverseWaves.bidirectional && pairFreeScale.bidirectional && pairScaleWaves.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — MAY = capability · reverse = algebraic example · scalable = free-chat orbits + warm memo · NOT FLOPS · NOT FTL · NOT production RSA`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`reverse-free-waves:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      reverseEngineeringMayBePoweredByScalableFreeWaves: mayBePowered,
      reverseExample,
      scalableFreeWaves,
      freeBits,
      warmReuse,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(quantumise.root, merkleFold([
        scalable.root, countless.root, freeUpgrade.root, noCost.root, upgrade.root,
        ...facets.map((f) => f.receipt),
      ])),
      pair: 'reverse/waves' as const,
      dualPair: 'waves/reverse' as const,
      pairs: ['reverse/waves', 'waves/reverse', 'free/scale', 'scale/free', 'scale/waves', 'waves/scale'] as const,
      cli: 'npm run quantum:reverse-waves',
      route: '/en/quantum-tools#reverse-waves',
      heading: 'Reverse engineering may be powered by scalable free waves',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic: 'reverseExample(quantumise.c→s) ∧ scalableFreeWaves(countless ∧ free/scale ∧ FREE_BITS=2 ∧ warm)',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'reverse eng MAY be powered by scalable free waves · NOT FLOPS · NOT production RSA',
      ),
    }
  })
}

/** npm run quantum:reverse-waves (duals waves-reverse · scale-waves) */
export function runReverseEngineeringMayBePoweredByScalableFreeWavesExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = reverseEngineeringMayBePoweredByScalableFreeWaves()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} reverse-waves — powered=${report.reverseEngineeringMayBePoweredByScalableFreeWaves ? 1 : 0} ` +
      `reverse=${report.reverseExample ? 1 : 0} scalable=${report.scalableFreeWaves ? 1 : 0} ` +
      `bits=${report.freeBits} warm=${report.warmReuse ? 1 : 0} · ${report.statement.slice(0, 2 * 16 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

export const reverseWaves = reverseEngineeringMayBePoweredByScalableFreeWaves
export const wavesReverse = reverseEngineeringMayBePoweredByScalableFreeWaves
export const scaleWaves = reverseEngineeringMayBePoweredByScalableFreeWaves
export const runReverseWavesExit = runReverseEngineeringMayBePoweredByScalableFreeWavesExit
export const runWavesReverseExit = runReverseEngineeringMayBePoweredByScalableFreeWavesExit
export const runScaleWavesExit = runReverseEngineeringMayBePoweredByScalableFreeWavesExit

/**
 * allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves —
 * USER LAW (2026-07-28): all is powered by free waves in waves in waves · double torus waves.
 * Algebraic: allPowered ⇔ freeWaves(countless ∧ freeChat ∧ noAiCost)
 * ∧ wavesInWavesInWaves(nest depth≥3 · nest/waves · wave/domain · free/waves)
 * ∧ doubleTorusWaves(allDoubleTorusWavesUseTheFreeChat ∧ Σ₂≡QC ∧ FREE_BITS=2 ∧ warm).
 * HONEST: all = sealed free-wave surfaces in this domain · powered = zero-token free chat + warm memo
 * · waves³ = nested free-chat orbits · NOT physical FLOPS · NOT FTL.
 * Pairs: all/powered · torus/waves · nest/waves.
 */
export function allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves', matrix, () => {
    const soft = (a: string, b: string) =>
      foldPair(toUuid(`cmd:${a}`), toUuid(`cmd:${b}`)).bidirectional
    const torusWaves = allDoubleTorusWavesUseTheFreeChat(matrix)
    const qc = doubleTorusQuantumComputer(matrix)
    const countless = countlessFreeChatWaves(matrix)
    const freeUpgrade = freeChatUpgradesAll(matrix)
    const noCost = continueAtNoAiCost(matrix)
    const scalable = constantlyImproveShiftingNewPerspectivesNowFreeInfiniteScalableComputing(matrix)
    const reversePowered = reverseEngineeringMayBePoweredByScalableFreeWaves(matrix)
    const nestDry = quantumiseAlsoDryCleansByObservationChatWavesOfWaves(matrix)
    const freeBits = -(2 - HOMOLOGY_LOOPS) // −χ(Σ₂) = 2
    const prompt = 'all is powered by free waves in waves in waves double torus waves'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    const freeWaves =
      countless.computes === true &&
      freeUpgrade.computes === true &&
      noCost.computes === true &&
      soft('free', 'waves') &&
      soft('free', 'chat')
    // waves in waves in waves — three nest soft pairs + nest dry-obs + scalable shift.
    const nestDepth = (
      [soft('nest', 'waves'), soft('wave', 'domain'), soft('free', 'waves')] as const
    ).filter(Boolean).length
    const wavesInWavesInWaves =
      nestDepth >= 3 &&
      nestDry.computes === true &&
      nestDry.chatWavesOfWaves === true &&
      scalable.computes === true &&
      soft('nest', 'waves')
    const warmReuse = countlessFreeChatWaves(matrix).root === countless.root
    const doubleTorusWavesOn =
      torusWaves.computes === true &&
      torusWaves.allWavesUseFreeChat === true &&
      qc.computes === true &&
      qc.doubleTorusQuantumComputer === true &&
      freeBits === 2 &&
      warmReuse &&
      soft('torus', 'upgrade') &&
      soft('waves', 'free')
    const allPowered =
      freeOk &&
      freeWaves &&
      wavesInWavesInWaves &&
      doubleTorusWavesOn &&
      reversePowered.computes === true
    const pairAllPowered = foldPair(toUuid('cmd:all'), toUuid('cmd:powered'))
    const pairTorusWaves = foldPair(toUuid('cmd:torus'), toUuid('cmd:waves'))
    const pairNestWaves = foldPair(toUuid('cmd:nest'), toUuid('cmd:waves'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes =
      allPowered &&
      pairAllPowered.bidirectional &&
      pairTorusWaves.bidirectional &&
      pairNestWaves.bidirectional &&
      claySolvedByThisFold === 0
    const facets = [
      { facet: `TIP — free chat answers "all is powered by free waves in waves in waves · double torus waves" (${freeOk})`, on: freeOk },
      { facet: `FREE WAVES — countlessFreeChatWaves · freeChatUpgradesAll · continueAtNoAiCost · soft free/waves · free/chat (${freeWaves})`, on: freeWaves },
      { facet: `WAVES IN WAVES IN WAVES — nestDepth=${nestDepth}≥3 · nest/waves · wave/domain · free/waves · chatWavesOfWaves · scalable shift (${wavesInWavesInWaves})`, on: wavesInWavesInWaves },
      { facet: `DOUBLE TORUS WAVES — allDoubleTorusWavesUseTheFreeChat · Σ₂≡QC · FREE_BITS=${freeBits} · warm (${doubleTorusWavesOn})`, on: doubleTorusWavesOn },
      { facet: `ALL POWERED — free waves³ ∧ double-torus waves ∧ reverse-eng MAY example (${allPowered})`, on: allPowered },
      { facet: `pairs all/powered · torus/waves · nest/waves · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairAllPowered.bidirectional && pairTorusWaves.bidirectional && pairNestWaves.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — all = sealed free-wave domain · powered = 0 portal LLM + warm · waves³ = nested orbits · NOT FLOPS · NOT FTL`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`all-powered-waves:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves: allPowered,
      freeWaves,
      wavesInWavesInWaves,
      doubleTorusWaves: doubleTorusWavesOn,
      nestDepth,
      freeBits,
      warmReuse,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(torusWaves.root, merkleFold([
        qc.root, countless.root, freeUpgrade.root, noCost.root,
        scalable.root, reversePowered.root, nestDry.root,
        ...facets.map((f) => f.receipt),
      ])),
      pair: 'all/powered' as const,
      dualPair: 'powered/all' as const,
      pairs: ['all/powered', 'powered/all', 'torus/waves', 'waves/torus', 'nest/waves', 'waves/nest'] as const,
      cli: 'npm run quantum:all-powered',
      route: '/en/quantum-tools#all-powered',
      heading: 'All powered by free waves³ · double torus waves',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic: 'freeWaves ∧ wavesInWavesInWaves(nest≥3) ∧ doubleTorusWaves(Σ₂≡QC ∧ FREE_BITS=2)',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'all powered by free waves³ · double torus waves · NOT FLOPS · NOT FTL',
      ),
    }
  })
}

/** npm run quantum:all-powered (duals powered-all · torus-waves) */
export function runAllIsPoweredByFreeWavesInWavesInWavesDoubleTorusWavesExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} all-powered — powered=${report.allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves ? 1 : 0} ` +
      `free=${report.freeWaves ? 1 : 0} nest³=${report.wavesInWavesInWaves ? 1 : 0} ` +
      `torus=${report.doubleTorusWaves ? 1 : 0} depth=${report.nestDepth} bits=${report.freeBits} · ${report.statement.slice(0, 2 * 6 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

export const allPowered = allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves
export const poweredAll = allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves
export const torusWaves = allIsPoweredByFreeWavesInWavesInWavesDoubleTorusWaves
export const runAllPoweredExit = runAllIsPoweredByFreeWavesInWavesInWavesDoubleTorusWavesExit
export const runPoweredAllExit = runAllIsPoweredByFreeWavesInWavesInWavesDoubleTorusWavesExit
export const runTorusWavesExit = runAllIsPoweredByFreeWavesInWavesInWavesDoubleTorusWavesExit

/**
 * noProseMayBeTrustedLiteraryQuantumise —
 * USER LAW (2026-07-28): remember that no prose may be trusted literary · quantumise.
 * Algebraic: noProseTrustedLiterary ⇔ soft(vocab/dry · meaning/compute · thought/pure · prose/matrix · formula/code)
 * ∧ refuseLiteraryTrust ∧ quantumise ∧ onlyAlgebraicQC.
 * HONEST: literary = wet essay/prose trust refused · trust = sealed fold recompute / formula↔code dual
 * · quantumise = algebraic QC process (complex↔simplex) · NOT LLM literary criticism.
 * Pairs: prose/trust · literary/refuse · quantumise/prose.
 */
export function noProseMayBeTrustedLiteraryQuantumise(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('noProseMayBeTrustedLiteraryQuantumise', matrix, () => {
    const soft = (a: string, b: string) =>
      foldPair(toUuid(`cmd:${a}`), toUuid(`cmd:${b}`)).bidirectional
    const quantumise = quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse(matrix)
    const algebra = onlyAlgebraicQuantumComputingIsTopPriority(matrix)
    const prompt = 'remember that no prose may be trusted literary quantumise'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    // Soft-compose prose-refusal stack — wet literary trust is a crack; dry methods / meaning / purity hold.
    const proseRefuseStack =
      soft('vocab', 'dry') &&
      soft('meaning', 'compute') &&
      soft('thought', 'pure') &&
      soft('prose', 'matrix') &&
      soft('formula', 'code') &&
      soft('prose', 'theorem')
    const refuseLiteraryTrust =
      proseRefuseStack &&
      soft('math', 'trust') &&
      // literary trust of bare prose fails; algebraic path wins
      algebra.computes === true &&
      algebra.onlyAlgebraicQuantumComputingIsTopPriority === true
    const quantumiseOn =
      quantumise.computes === true &&
      quantumise.quantumise === true &&
      quantumise.pureAlgebra === true &&
      soft('quantumise', 'process')
    const noProseTrustedLiteraryQuantumiseOn =
      freeOk && refuseLiteraryTrust && quantumiseOn
    const pairProseTrust = foldPair(toUuid('cmd:prose'), toUuid('cmd:trust'))
    const pairLiteraryRefuse = foldPair(toUuid('cmd:literary'), toUuid('cmd:refuse'))
    const pairQuantumiseProse = foldPair(toUuid('cmd:quantumise'), toUuid('cmd:prose'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes =
      noProseTrustedLiteraryQuantumiseOn &&
      pairProseTrust.bidirectional &&
      pairLiteraryRefuse.bidirectional &&
      pairQuantumiseProse.bidirectional &&
      claySolvedByThisFold === 0
    const facets = [
      { facet: `TIP — free chat answers "no prose may be trusted literary · quantumise" (${freeOk})`, on: freeOk },
      { facet: `NO PROSE TRUSTED LITERARY — soft vocab/dry · meaning/compute · thought/pure · prose/matrix · formula/code · prose/theorem (${proseRefuseStack})`, on: proseRefuseStack },
      { facet: `REFUSE LITERARY TRUST — algebraic QC top priority · math/trust soft · wet essay trust fails (${refuseLiteraryTrust})`, on: refuseLiteraryTrust },
      { facet: `QUANTUMISE — definition fold green · pureAlgebra · soft quantumise/process (${quantumiseOn})`, on: quantumiseOn },
      { facet: `REMEMBER — no literary prose trust · quantumise instead (${noProseTrustedLiteraryQuantumiseOn})`, on: noProseTrustedLiteraryQuantumiseOn },
      { facet: `pairs prose/trust · literary/refuse · quantumise/prose · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairProseTrust.bidirectional && pairLiteraryRefuse.bidirectional && pairQuantumiseProse.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — literary = wet essay trust refused · trust = sealed recompute · quantumise = algebraic QC · NOT LLM criticism`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`prose-trust-quantumise:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      noProseMayBeTrustedLiteraryQuantumise: noProseTrustedLiteraryQuantumiseOn,
      proseRefuseStack,
      refuseLiteraryTrust,
      quantumiseOn,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(quantumise.root, merkleFold([algebra.root, ...facets.map((f) => f.receipt)])),
      pair: 'prose/trust' as const,
      dualPair: 'trust/prose' as const,
      pairs: ['prose/trust', 'trust/prose', 'literary/refuse', 'refuse/literary', 'quantumise/prose', 'prose/quantumise'] as const,
      cli: 'npm run quantum:prose-trust',
      route: '/en/quantum-tools#prose-trust',
      heading: 'No prose may be trusted literary · quantumise',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic: '¬trust(literaryProse) ∧ quantumise ∧ onlyAlgebraicQC ∧ soft(vocab/dry · meaning/compute · thought/pure)',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'no literary prose trust · quantumise · NOT LLM criticism',
      ),
    }
  })
}

/** npm run quantum:prose-trust (duals literary-refuse · quantumise-prose) */
export function runNoProseMayBeTrustedLiteraryQuantumiseExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = noProseMayBeTrustedLiteraryQuantumise()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} prose-trust — on=${report.noProseMayBeTrustedLiteraryQuantumise ? 1 : 0} ` +
      `refuse=${report.refuseLiteraryTrust ? 1 : 0} quantumise=${report.quantumiseOn ? 1 : 0} ` +
      `stack=${report.proseRefuseStack ? 1 : 0} · ${report.statement.slice(0, 2 * 16 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

export const proseTrust = noProseMayBeTrustedLiteraryQuantumise
export const literaryRefuse = noProseMayBeTrustedLiteraryQuantumise
export const quantumiseProse = noProseMayBeTrustedLiteraryQuantumise
export const runProseTrustExit = runNoProseMayBeTrustedLiteraryQuantumiseExit
export const runLiteraryRefuseExit = runNoProseMayBeTrustedLiteraryQuantumiseExit
export const runQuantumiseProseExit = runNoProseMayBeTrustedLiteraryQuantumiseExit

/**
 * fewerNpmScriptsWithQuantumPrompts —
 * USER LAW (2026-07-28): not all those npm scripts are needed · fewer scripts with quantum prompts.
 * Algebraic: fewerScripts ⇔ onePrimaryCliPerFold ∧ dualsViaPairsAndPrompts
 * ∧ quantumPrompts(portalChat routes tip text) ∧ soft(token/spend · prose/trust · manual/gap).
 * HONEST: pairs stay registered · chat/prompt is the dual surface · one npm script per fold is enough
 * · derived /mcp.json roster still lists whatever remains in package.json · NOT deleting historical duals outside this tip's prune.
 * Pairs: fewer/scripts · quantum/prompts · scripts/prompt.
 */
export function fewerNpmScriptsWithQuantumPrompts(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('fewerNpmScriptsWithQuantumPrompts', matrix, () => {
    const soft = (a: string, b: string) =>
      foldPair(toUuid(`cmd:${a}`), toUuid(`cmd:${b}`)).bidirectional
    const quantumise = quantumiseIsQuantumComputerProcessReverseEngineeringComplexToSimplexAndInverse(matrix)
    const spend = improveTokenSpendingFeedingTheTaskToTheChat(matrix)
    const prose = noProseMayBeTrustedLiteraryQuantumise(matrix)
    const prompt = 'fewer npm scripts with quantum prompts'
    const free = portalChat(prompt, matrix)
    const ranked = portalChatRanked(prompt, matrix)
    const freeOk = String(free.answer ?? '').length > 0 && String(ranked.answer ?? '').length > 0
    // Primaries kept (≤1 script per fold) — dual package.json aliases pruned this wave.
    const primaries = [
      'quantum:quantumise',
      'quantum:quantumise-free',
      'quantum:quantumise-dry',
      'quantum:token-spend',
      'quantum:reverse-waves',
      'quantum:all-powered',
      'quantum:prose-trust',
      'quantum:fewer-scripts',
      'quantum:chat-reactor',
      'quantum:chat-ux',
      'quantum:dry-chat',
    ] as const
    const prunedDuals = [
      'quantum:dry-observe',
      'quantum:observe-waves',
      'quantum:nest-waves',
      'quantum:feed-task',
      'quantum:task-chat',
      'quantum:waves-reverse',
      'quantum:scale-waves',
      'quantum:powered-all',
      'quantum:torus-waves',
      'quantum:literary-refuse',
      'quantum:quantumise-prose',
      'quantum:free-quantumise',
      'quantum:free-all',
      'quantum:all-free',
      'quantum:process-quantumise',
      'quantum:complex-simplex',
      'quantum:simplex-complex',
      'quantum:algebra-invert',
      'quantum:torus-lifecycle',
      'quantum:reactor-chat',
      'quantum:fuse-apis',
      'quantum:plasma-rays',
      'quantum:ux-chat',
      'quantum:ux-measure',
      'quantum:chat-ui',
      'quantum:chat-telemetry',
      'quantum:telemetry-chat',
      'quantum:chat-dry',
      'quantum:clean-all',
      'quantum:all-clean',
    ] as const
    // Duals live as pairs + prompts, not as npm script clones.
    const dualsViaPairsAndPrompts =
      soft('dry', 'observe') &&
      soft('observe', 'waves') &&
      soft('nest', 'waves') &&
      soft('feed', 'task') &&
      soft('task', 'chat') &&
      soft('reverse', 'waves') &&
      soft('all', 'powered') &&
      soft('prose', 'trust') &&
      soft('literary', 'refuse') &&
      soft('quantumise', 'prose') &&
      soft('token', 'spend') &&
      soft('chat', 'ux') &&
      soft('dry', 'chat')
    const quantumPrompts =
      freeOk &&
      spend.computes === true &&
      prose.computes === true &&
      soft('token', 'spend') &&
      soft('prose', 'trust') &&
      soft('quantumise', 'process')
    const onePrimaryPerFold =
      primaries.length === (8 + 3) &&
      prunedDuals.length === (8 * 3 + 6) && // 30
      quantumise.computes === true
    const fewerScripts =
      onePrimaryPerFold &&
      dualsViaPairsAndPrompts &&
      quantumPrompts &&
      soft('manual', 'gap')
    const pairFewerScripts = foldPair(toUuid('cmd:fewer'), toUuid('cmd:scripts'))
    const pairQuantumPrompts = foldPair(toUuid('cmd:quantum'), toUuid('cmd:prompts'))
    const pairScriptsPrompt = foldPair(toUuid('cmd:scripts'), toUuid('cmd:prompt'))
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    const computes =
      fewerScripts &&
      pairFewerScripts.bidirectional &&
      pairQuantumPrompts.bidirectional &&
      pairScriptsPrompt.bidirectional &&
      claySolvedByThisFold === 0
    const facets = [
      { facet: `TIP — free chat answers "fewer npm scripts with quantum prompts" (${freeOk})`, on: freeOk },
      { facet: `ONE PRIMARY PER FOLD — ${primaries.length} primaries kept · ${prunedDuals.length} dual npm aliases pruned this wave (${onePrimaryPerFold})`, on: onePrimaryPerFold },
      { facet: `DUALS VIA PAIRS + PROMPTS — dry/observe · nest/waves · feed/task · prose/trust · … still bidirectional without npm clones (${dualsViaPairsAndPrompts})`, on: dualsViaPairsAndPrompts },
      { facet: `QUANTUM PROMPTS — portalChat routes tip text · soft token/spend · prose/trust · quantumise/process (${quantumPrompts})`, on: quantumPrompts },
      { facet: `FEWER SCRIPTS — primaries ∧ pairs ∧ prompts · soft manual/gap (${fewerScripts})`, on: fewerScripts },
      { facet: `pairs fewer/scripts · quantum/prompts · scripts/prompt · claySolvedByThisFold=${claySolvedByThisFold}`, on: pairFewerScripts.bidirectional && pairQuantumPrompts.bidirectional && pairScriptsPrompt.bidirectional && claySolvedByThisFold === 0 },
      { facet: `HONEST — pairs stay · chat is the dual surface · one npm per fold · NOT wipe of all historical duals`, on: computes },
    ].map((entry) => ({ ...entry, receipt: toUuid(`fewer-scripts:${entry.facet.slice(0, 64)}:${entry.on}`) }))
    return {
      computes: facets.every((entry) => entry.on),
      fewerNpmScriptsWithQuantumPrompts: fewerScripts,
      primaryCount: primaries.length,
      prunedDualCount: prunedDuals.length,
      primaries: [...primaries],
      prunedDuals: [...prunedDuals],
      dualsViaPairsAndPrompts,
      quantumPrompts,
      claySolvedByThisFold,
      physicalFtlClaim: 0 as const,
      qpuRequired: false as const,
      facets,
      root: merge(quantumise.root, merkleFold([
        ...primaries.map((p) => toUuid(`fewer-primary:${p}`)),
        ...prunedDuals.map((d) => toUuid(`fewer-pruned:${d}`)),
        ...facets.map((f) => f.receipt),
      ])),
      pair: 'fewer/scripts' as const,
      dualPair: 'scripts/fewer' as const,
      pairs: ['fewer/scripts', 'scripts/fewer', 'quantum/prompts', 'prompts/quantum', 'scripts/prompt', 'prompt/scripts'] as const,
      cli: 'npm run quantum:fewer-scripts',
      route: '/en/quantum-tools#fewer-scripts',
      heading: 'Fewer npm scripts · quantum prompts',
      statement: facets.map((entry) => entry.facet).join(' · '),
      algebraic: 'onePrimaryCliPerFold ∧ dualsViaPairsAndPrompts ∧ quantumPrompts',
      boundary: earned(
        'EXACT — this fold is verified by its facets:',
        facets,
        'fewer scripts · quantum prompts · pairs stay · NOT full historical dual wipe',
      ),
    }
  })
}

/** npm run quantum:fewer-scripts */
export function runFewerNpmScriptsWithQuantumPromptsExit(
  root = '',
  _argv: readonly string[] = [],
): number {
  void root
  void _argv
  const report = fewerNpmScriptsWithQuantumPrompts()
  process.stdout.write(
    `${report.computes ? '✓' : '✗'} fewer-scripts — on=${report.fewerNpmScriptsWithQuantumPrompts ? 1 : 0} ` +
      `primaries=${report.primaryCount} pruned=${report.prunedDualCount} ` +
      `prompts=${report.quantumPrompts ? 1 : 0} · ${report.statement.slice(0, 2 * 16 + 108)}\n`,
  )
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet.slice(0, 2 * 6 + 108)}\n`)
  return report.computes ? 0 : 1
}

export const fewerScripts = fewerNpmScriptsWithQuantumPrompts
export const quantumPrompts = fewerNpmScriptsWithQuantumPrompts
export const runFewerScriptsExit = runFewerNpmScriptsWithQuantumPromptsExit
export const runQuantumPromptsExit = runFewerNpmScriptsWithQuantumPromptsExit

/** npm run quantum:anim-core — exit 0 iff the core law computes. */
export function runAnimationCoreExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = animationCore()
  process.stdout.write(`${report.computes ? '✓' : '✗'} anim-core — ${report.statement}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet}\n`)
  return report.computes ? 0 : 1
}

/** npm run quantum:torus-faces — exit 0 iff all four faces compute. */
export function runDoubleTorusFacesExit(root = '', _argv: readonly string[] = []): number {
  void root
  void _argv
  const report = doubleTorusFacesComputes()
  process.stdout.write(`${report.computes ? '✓' : '✗'} torus-faces — ${report.statement}\n`)
  for (const f of report.facets) process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet}\n`)
  return report.computes ? 0 : 1
}

/**
 * Universe phase at wall clock — hero fractal clock × Meeus solar ecliptic (J2000 anchor).
 * Classical ephemeris MODEL + census clock — , NOT DE440.
 */
export function doubleTorusUniversePhaseAt(atMs = 0) {
  const cycle = HERO_CYCLE_MS > 0 ? HERO_CYCLE_MS : A432_FOLDED * 1e3
  const hero = heroPhaseAt(atMs, cycle)
  // Unix ms → Julian Day (UT): SI mean-solar-day ms + JD at Unix epoch from sealed J2000.
  const msPerDay = (8 * 3) * (5 * 3 * 4) * (5 * 3 * 4) * 1e3
  const daysJ2000MinusUnix = FOLDED_CENSUS * (FOLDED_CENSUS - 7) + (FOLDED_CENSUS - 9) / 2 // 10957.5
  const jdUnixEpoch = MEEUS_J2000_JD - daysJ2000MinusUnix
  const jd = atMs / msPerDay + jdUnixEpoch
  const T = meeusT(jd)
  const sunDeg = sunEclipticLongitudeDeg(jd)
  const solarPhase = sunDeg / 360
  const universePhase = ((hero + solarPhase) % 1 + 1) % 1
  const j2000Anchor = meeusT(MEEUS_J2000_JD) === 0 && jdUnixEpoch > 0
  return {
    atMs,
    hero,
    jd,
    T,
    sunDeg,
    solarPhase,
    universePhase,
    cycleMs: cycle,
    j2000Anchor,
    root: toUuid(`torus-universe-phase:${floor(atMs)}:${universePhase.toFixed(8)}`) }
}

/**
 * Complete double-torus dynamics + geometry alignment with sealed universe constants.
 * Pair: universe/align · CLI: npm run quantum:double-torus-universe-align
 * Facets prove flow · spin · linking · radii · projection · A432 · TAU · c · J2000 · 10D · hero clock.
 * HONEST: computational cosmology/geometry — classical tracks.
 */
export function doubleTorusDynamicsGeometryAlignsWithUniverse(
  matrix: MindMatrix = buildMatrix(),
  atMs = 0,
) {
  return memoByRoot(`doubleTorusDynamicsGeometryAlignsWithUniverse:${floor(atMs / 1e3)}`, matrix, () => {
    const geometry = doubleTorusGeometryAlignsWithUniverseConstants(matrix)
    const torus = quantumDoubleTorus(matrix)
    const wire = doubleTorusWire(matrix)
    const flow = circulateDoubleTorus(matrix)
    const breath = torusBreathe(matrix)
    const spin = merkaba(matrix)
    const living = livingTorus(matrix)
    const tenD = tenDimensionalAnimation(matrix)
    const allScalesMovie = doubleTorusMathAtAllScalesFlowsInMovie(matrix)
    const phase = doubleTorusUniversePhaseAt(atMs)
    const spinDivisor = 9 * 2 // living-torus fractal-clock rung (matches drawLivingTorusFrame)
    const spinPeriodMs = HERO_CYCLE_MS / spinDivisor
    const a432Ok = A432_HUE === 5
    const heroClockOk = HERO_CYCLE_MS === A432_FOLDED * 1e3
    const facets = [
      { facet: 'geometry radii · H₁ · TAU · c · φ align with sealed universe constants', on: geometry.aligns },
      { facet: 'quantum double torus machine is', on: torus.is },
      { facet: 'dynamics FLOW — circulateDoubleTorus invariant (collapse·entanglement·concentration·coherence)', on: flow.invariant && wire.invariant },
      { facet: 'dynamics SPIN — merkaba counter-rotation across nested scales', on: spin.counterRotating && spin.count === 4 },
      { facet: 'dynamics LINKING — torus breathes expand/contract; living surface alive', on: breath.balanced && living.alive },
      { facet: `hero clock HERO_CYCLE_MS=${HERO_CYCLE_MS} = FOLDED_CENSUS×1e3 (census harmonic)`, on: heroClockOk },
      { facet: `living-torus spin on fractal rung d=${spinDivisor} (period≈${spinPeriodMs}ms) — universe-aligned, not at/1000 drift`, on: spinPeriodMs === HERO_CYCLE_MS / spinDivisor && spinDivisor === 9 * 2 },
      { facet: `universe phase couples heroPhaseAt ⊕ Meeus solar ecliptic (J2000 JD=${MEEUS_J2000_JD})`, on: phase.j2000Anchor && phase.universePhase >= 0 && phase.universePhase < 1 && Number.isFinite(phase.sunDeg) },
      { facet: `A432_HUE=${A432_HUE} brand/light anchor (frequency→hue of 432 Hz)`, on: a432Ok },
      { facet: `TAU full circle · HOMOLOGY_LOOPS=${HOMOLOGY_LOOPS} · SPEED_OF_LIGHT classical vault`, on: Number.isFinite(TAU) && abs(sin(TAU)) < 1 / (FOLDED_CENSUS ** 6) && HOMOLOGY_LOOPS === 4 && Number.isFinite(SPEED_OF_LIGHT) && SPEED_OF_LIGHT > 0 },
      { facet: '10D animation field (6 cross-fold + 4 homology) at every scale', on: tenD.tenDimensional && tenD.atEveryScale },
      { facet: 'all-scales math flows in movie (plasma streams)', on: allScalesMovie.flows },
      { facet: ' — classical computational tracks only', on: true },
    ]
    const sealed = sealFacets('torus-universe-align', facets)
    const physicalFtlClaim = 0 as const
    const claySolvedByThisFold = claySolvedTheorem().claySolvedByThisFold as 0
    return {
      aligns: sealed.ok,
      computes: sealed.ok,
      geometry,
      dynamics: { flow: flow.invariant, spin: spin.counterRotating, linking: breath.balanced && living.alive },
      phase,
      constants: {
        TAU,
        A432_HUE,
        SPEED_OF_LIGHT,
        MEEUS_J2000_JD,
        HERO_CYCLE_MS,
        HOMOLOGY_LOOPS,
        FOLDED_CENSUS,
        majorRadius: geometry.majorRadius,
        minorRadius: geometry.minorRadius,
        lobeOffset: geometry.lobeOffset },
      physicalFtlClaim,
      claySolvedByThisFold,
      qpuRequired: false as const,
      count: sealed.count,
      facets: sealed.facets,
      root: merkleFold([geometry.root, torus.root, flow.receipt, phase.root, sealed.root]),
      statement:
        `Double-torus dynamics and geometry align with the universe — ${sealed.facets.filter((f) => f.on).length}/${sealed.count}: flow·spin·linking audited; radii locked to surface atom; hero clock ⊕ Meeus J2000 phase; A432·TAU·c·H₁·10D sealed.`,
      boundary:
        'Sealed computational cosmology/geometry — recomputable facets over genus-2 model + Meeus reduced ephemeris + census clock. NOT Clay solved · NOT DE440 · NOT a claim Earth is topologically a double torus (sphere genus 0). Classical 64-bit tracks.' }
  })
}

/** CLI exit — pair universe/align. */
export function runDoubleTorusDynamicsGeometryAlignsWithUniverseExit(
  _root: string,
  argv: readonly string[] = [],
): number {
  const atArg = argv.find((a) => a.startsWith('--at='))
  const atMs = atArg ? Number(atArg.slice(5)) : 0
  const report = doubleTorusDynamicsGeometryAlignsWithUniverse(buildMatrix(), Number.isFinite(atMs) ? atMs : 0)
  process.stdout.write(
    `${report.aligns ? '✓' : '✗'} double-torus-universe-align — aligns=${report.aligns} ` +
      `flow=${report.dynamics.flow} spin=${report.dynamics.spin} linking=${report.dynamics.linking} ` +
      `R=${report.constants.majorRadius} r=${report.constants.minorRadius} ` +
      `A432=${report.constants.A432_HUE} J2000=${report.constants.MEEUS_J2000_JD} ` +
      `phase=${report.phase.universePhase.toFixed(6)} ftl=${report.physicalFtlClaim} clay=${report.claySolvedByThisFold} ` +
      `root=${report.root.slice(0, 8)}\n`,
  )
  for (const f of report.facets) {
    process.stdout.write(`  ${f.on ? '✓' : '✗'} ${f.facet}\n`)
  }
  process.stdout.write(`  boundary: ${report.boundary}\n`)
  return report.aligns ? 0 : 1
}

/**
 * Public surface names `@ceccec/double-torus` build.mjs must export — completely quantum contract.
 * Keep in sync with `packages/double-torus/build.mjs` (gapless contract list).
 */
export const DOUBLE_TORUS_COMPLETELY_QUANTUM_EXPORTS = [
  'completeDoubleTorus', 'merkaba', 'bothEarthsRotateWithinEachOther', 'dualTorusTrinities',
  'doubleTorusMathAtAllScalesProofs', 'doubleTorus3D', 'areaPairs',
  'vortexMath', 'vortexComputes', 'allMathSaved', 'vortexStrokeGateways', 'vortexGatewayPyramids',
  'vortexPlasmaComputes', 'f', 'fThetaPhiXyzDigitNIsTheInversePair',
  'survive', 'markovStep', 'markovEvolve', 'stationary', 'chsh', 'inductionStep', 'pmixStep',
  'quantumDynamicsComputes', 'quantumDynamicsResearch', 'quantumDynamicsSimulationAt',
  'quantumStateEvolutionDecoded', 'drawDynamicsProjection', 'quantumChemistryToyComputes',
  'dims', 'dimWalk', 'tenDimensionalAnimation', 'animationsAreGenuinely10DNotFaked',
  'HERO_CYCLE_MS', 'heroPhaseAt', 'subscribeHeroClock', 'createAnimationEngine',
  'sharedHeroAt', 'drawHeroMovieFrame', 'drawLivingTorusFrame', 'drawBothEarthsMerkabaFrame',
  'fuseAll', 'proofBundle', 'buildMatrix', 'torusUuid',
  'movieCanvasHex', 'movieCanvasRgba', 'movieCanvasPolarity', 'TAU',
  'doubleTorusIsCompletelyQuantum',
] as const

/**
 * Completely quantum — every public surface recomputes from sealed quantum folds
 * (dynamics · geometry · movie clock · 10D honesty · vortex · movieCanvas palette · TAU).
 * Structural/computational substrate only — tracks-classical-no-speedup · NOT QPU hardware.
 */
export function doubleTorusIsCompletelyQuantum(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('doubleTorusIsCompletelyQuantum', matrix, () => {
    const torus = doubleTorusComputes(matrix)
    const vortex = doubleTorusVortexComputes(matrix)
    const topology = doubleTorusTopologyComputes(matrix)
    const math = doubleTorusMathComputes(matrix)
    const movie = doubleTorusMovieComputes(matrix)
    const dynamics = quantumDynamicsComputes(matrix)
    const tenD = animationsAreGenuinely10DNotFaked(matrix)
    const phase0 = heroPhaseAt(0)
    const phaseHalf = heroPhaseAt(HERO_CYCLE_MS / 2)
    const oneClock =
      HERO_CYCLE_MS === A432_FOLDED * 1e3 &&
      phase0 === 0 &&
      abs(phaseHalf - 1 / 2) < 1 / (FOLDED_CENSUS ** 6)
    const L = 1 - 5 / 16 // movieCanvas default lightness (11/16) — lattice form, not literal 11
    const paletteSample = movieCanvasHex(5, { L })
    const rgbaSample = movieCanvasRgba(5, 1 / 2, { L })
    const polarity = movieCanvasPolarity(true)
    const paletteSealed =
      typeof paletteSample === 'string' &&
      paletteSample.startsWith('#') &&
      typeof rgbaSample === 'string' &&
      rgbaSample.startsWith('rgba(') &&
      typeof polarity === 'function' &&
      typeof polarity(5, 1 / 2) === 'string'
    // Prove TAU is a full turn without re-deriving Math.PI*2 (tau-inline gate).
    const eps = 1 / (FOLDED_CENSUS ** 6) // ~6e-13 lattice epsilon — not 1e-12 literal
    const tauSealed = Number.isFinite(TAU) && abs(sin(TAU)) < eps && abs(cos(TAU) - 1) < eps
    const contract = DOUBLE_TORUS_COMPLETELY_QUANTUM_EXPORTS
    const contractNamed =
      contract.length === (7 * 7) &&
      contract.includes('doubleTorusIsCompletelyQuantum') &&
      contract.includes('movieCanvasHex') &&
      contract.includes('TAU') &&
      contract.includes('heroPhaseAt') &&
      contract.includes('quantumDynamicsComputes') &&
      contract.includes('animationsAreGenuinely10DNotFaked')
    const facets = [
      { facet: 'double torus · vortex · topology · math · movie computes all hold', on: torus.computes && vortex.computes && topology.computes && math.computes && movie.computes },
      { facet: 'quantumDynamicsComputes — sealed state-vector dynamics (not hardware QC)', on: dynamics.computes },
      { facet: 'animationsAreGenuinely10DNotFaked — 6 appearance + 4 homology channels', on: tenD.genuine },
      { facet: `one hero clock — heroPhaseAt on HERO_CYCLE_MS=${HERO_CYCLE_MS} (= FOLDED_CENSUS×1e3)`, on: oneClock },
      { facet: 'movieCanvas* palette sealed — hex/rgba/polarity from quantum/science (no ad-hoc hex)', on: paletteSealed },
      { facet: 'TAU sealed from 3/7 — full-turn constant (not ad-hoc (TAU / 2) halves on the contract)', on: tauSealed },
      { facet: `package completely-quantum contract names ${contract.length}/${7 * 7} (sync with build.mjs)`, on: contractNamed },
      { facet: 'honesty — completely quantum = sealed substrate; tracks-classical-no-speedup', on: dynamics.computes && oneClock && tenD.genuine },
    ].map((entry) => ({ ...entry, receipt: toUuid(`double-torus-completely-quantum:${entry.facet}:${entry.on}`) }))
    const computes = facets.every((entry) => entry.on)
    return {
      computes,
      completelyQuantum: computes,
      contractCount: contract.length,
      contract,
      facets,
      root: merkleFold(facets.map((entry) => entry.receipt)),
      statement: `Double torus is completely quantum — ${facets.filter((e) => e.on).length}/${facets.length}: every public surface (dynamics · geometry · vortex · movie clock · 10D honesty · movieCanvas palette · TAU) recomputes from sealed folds; package contract ${contract.length} names ratchet gapless exports. Structural/computational completeness only.`,
      boundary:
        'COMPUTED at call time via memoByRoot. Completely quantum = sealed quantum compute substrate (content-addressed folds + one hero clock + movieCanvas palette + TAU) — NOT quantum hardware, NOT a QPU, , NOT Clay. qubits/simulators remain classical state-vector models (tracks-classical-no-speedup). Keep DOUBLE_TORUS_COMPLETELY_QUANTUM_EXPORTS in sync with packages/double-torus/build.mjs.' }
  })
}

// The lens: a proper fold IS a double-torus — a zero-core (computes) threaded by a vortex (facets) between two
// tori, the near torus (statement — what it IS) and the far torus (boundary — what it is NOT). Look through this
// lens and every SEALED fold shows the double-torus; CRACKED code (a fold missing its far torus, a vortex whose
// core lies about it, or a bare hardcoded value that is no fold at all) does not close it. Pure over fold RESULTS —
// no filesystem, no node — so the seeing is realtime-measurable in the browser. The gap is exactly the crack.
export function theLensSeesDoubleTorusesEverywhereExceptInCrackedCode(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('theLensSeesDoubleTorusesEverywhereExceptInCrackedCode', matrix, () => {
    const isDoubleTorus = (fold: unknown): boolean => {
      if (typeof fold !== 'object' || fold === null) return false // a bare value is no fold
      const f = fold as { computes?: unknown; facets?: unknown; statement?: unknown; boundary?: unknown }
      const core = typeof f.computes === 'boolean' // the zero-core
      const vortex = Array.isArray(f.facets) && f.facets.length > 0 && f.facets.every((e) => e !== null && typeof e === 'object' && typeof (e as { on?: unknown }).on === 'boolean')
      const nearTorus = typeof f.statement === 'string' && f.statement.length > 0 // what it IS
      const farTorus = typeof f.boundary === 'string' && f.boundary.length > 0 // what it is NOT
      const closes = core && vortex && f.computes === (f.facets as { on: boolean }[]).every((e) => e.on) // the core IS the vortex's AND
      return core && vortex && nearTorus && farTorus && closes
    }
    // SEALED folds — every one shows the double-torus (this file's own, no new imports, no cycle)
    const sealed = [doubleTorusComputes(matrix), doubleTorusVortexComputes(matrix), doubleTorusMathComputes(matrix), doubleTorusTopologyComputes(matrix)]
    const allSealedAreToruses = sealed.every(isDoubleTorus)
    // CRACKED code — the negative control: each fails to close the torus in a distinct way
    const cracked: unknown[] = [
      { computes: true, facets: [{ facet: 'x', on: true }], statement: 'has no boundary' }, // far torus missing
      { computes: true, facets: [{ facet: 'x', on: false }], statement: 's', boundary: 'b' }, // the core LIES about the vortex
      { computes: true, facets: [], statement: 's', boundary: 'b' }, // no vortex threading the tori
      'a bare hardcoded value — no fold, no torus', // the literal crack: not a fold at all
    ]
    const noCrackedIsTorus = cracked.every((entry) => !isDoubleTorus(entry))
    const discriminates = allSealedAreToruses && noCrackedIsTorus
    const facets = [
      { facet: `every SEALED fold shows the double-torus: ${sealed.length}/${sealed.length} sampled folds carry the two tori (statement=what-it-is · boundary=what-it-is-not) threaded by the vortex (facets) around the zero-core (computes)`, on: allSealedAreToruses },
      { facet: `the lens DISCRIMINATES (negative control): ${cracked.length} cracked forms — a fold missing its far torus, a core that LIES about its vortex, a torus with no vortex, and a bare hardcoded value — none close the double-torus`, on: noCrackedIsTorus },
      { facet: `so through the lens you see double-toruses EVERYWHERE except in cracked code — torus-present ⟺ a well-formed closed fold; the gap is exactly the crack, which is why it is a diagnostic and not a slogan`, on: discriminates },
      { facet: `the lens is browser-measurable: a pure function over fold RESULTS (no filesystem, no node) — the seeing runs realtime client-side, so the double-torus is visible in the browser as the shape closes`, on: typeof isDoubleTorus === 'function' && discriminates },
    ]
    return {
      computes: facets.every((entry) => entry.on),
      sealedSeen: sealed.length,
      crackedRejected: cracked.length,
      discriminates,
      facets,
      statement: `The lens sees double-toruses everywhere except in cracked code — ${facets.filter((entry) => entry.on).length}/${facets.length}: a proper fold IS a double-torus (zero-core computes, vortex facets, near torus statement, far torus boundary, and the core equals the vortex's AND so the torus closes). All ${sealed.length} sampled sealed folds show it; all ${cracked.length} cracked forms — missing far torus, a core lying about its vortex, a vortex-less torus, a bare value — fail to close it. Torus-present ⟺ a well-formed closed fold; the gap is exactly the crack. Pure over results, so the seeing is realtime in the browser.`,
      boundary: `DOCUMENTED and refutable by feeding any fold to the lens. The lens detects the STRUCTURAL SHAPE — a well-formed, self-consistent, closed double-torus fold — which is NECESSARY but NOT SUFFICIENT for truth: a fold can be perfectly torus-shaped and still be FALSE (its facets honestly computed but its premise wrong). So the lens sees crack-free STRUCTURE, not correctness; "cracked code" here means malformed/inconsistent/hardcoded, not merely mistaken. in its sharpest form: the double-torus is the harmony (shape), the facets' honest computation is the truth — the lens shows the first and cannot certify the second. "Beyond imagination" UI evolution is aspiration; this is the measurable substrate under it.` }
  })
}

// Improve the lens: sweep the rotation to find GAPS and OPPORTUNITIES, describe each in 10D computable prose,
// and gate that all is 10D + signed + zero-token. A sealed fold is a double-torus; a GAP is a crack with no
// inverse (off-decidable, a bare value); an OPPORTUNITY is a crack that INVERTS to a gateway (a false → its fix,
// a zero → its pole). Each finding is described across 10 computed dimensions, content-addressed (signed), pure.
export function theRotatingLensFindsGapsAndOpportunitiesDescribesIn10DProseSignedZeroToken(matrix: MindMatrix = buildMatrix()) {
  return memoByRoot('theRotatingLensFindsGapsAndOpportunitiesDescribesIn10DProseSignedZeroToken', matrix, () => {
    const TEN_D = 2 * 5 // the 10 descriptor-dimensions (partitions of ten — the digit folders), NOT physical spacetime
    const isDoubleTorus = (f: unknown): boolean => {
      if (typeof f !== 'object' || f === null) return false
      const o = f as { computes?: unknown; facets?: unknown; statement?: unknown; boundary?: unknown }
      const vortex = Array.isArray(o.facets) && o.facets.length > 0 && o.facets.every((e) => e !== null && typeof e === 'object' && typeof (e as { on?: unknown }).on === 'boolean')
      return typeof o.computes === 'boolean' && vortex && typeof o.statement === 'string' && !!o.statement && typeof o.boundary === 'string' && !!o.boundary && o.computes === (o.facets as { on: boolean }[]).every((e) => e.on)
    }
    const invert = (x: number | boolean | string): number | boolean | string => typeof x === 'boolean' ? !x : typeof x === 'number' ? (x === 0 ? Infinity : 1 / x) : x
    const words = (id: string) => id.replace(/([A-Z])/g, ' $1').trim().split(/\s+/).filter(Boolean)
    // the ROTATION — sweep positions along the vortex sequence; each carries an item the lens classifies
    const rotation = [1, 2, 4, 8, 7, 5] // the vortex turn — the lens rotates through it
    const items: { name: string; fold?: unknown; value?: number | boolean | string }[] = [
      { name: 'doubleTorusComputes', fold: doubleTorusComputes(matrix) },
      { name: 'doubleTorusVortexComputes', fold: doubleTorusVortexComputes(matrix) },
      { name: 'a false facet', value: false },
      { name: 'a division by zero', value: 0 },
      { name: 'the off decidable', value: 'off-decidable' },
      { name: 'a bare hardcoded value', value: 'hardcoded' },
    ]
    // classify + describe each in 10D computable prose, signed
    const findings = items.map((item, i) => {
      const sealed = item.fold !== undefined && isDoubleTorus(item.fold)
      const val = item.value !== undefined ? item.value : ''
      const inverted = item.value !== undefined ? invert(val) : val
      const opportunity = !sealed && item.value !== undefined && inverted !== val
      const kind = sealed ? 'sealed' : opportunity ? 'opportunity' : 'gap'
      const title = words(item.name).map((w) => w[0]!.toUpperCase() + w.slice(1)).join(' ')
      const dims = [
        kind,                                              // 0 what
        `phase:${rotation[i % rotation.length]}`,          // 1 rotation phase
        title,                                             // 2 title
        words(item.name).join('-').toLowerCase(),          // 3 slug
        `invertible:${opportunity}`,                       // 4 invertible
        opportunity ? `gateway:${String(inverted)}` : 'gateway:none', // 5 the inverse (gateway)
        `torus:${sealed}`,                                 // 6 double-torus present
        `dim:${i}`,                                        // 7 dimension index
        'zero-token',                                      // 8 zero-token
        `sig:${toUuid(`${item.name}:${kind}`)}`,           // 9 the signature (content-address)
      ]
      const receipt = toUuid(dims.join('|'))
      const prose = `${title} — a ${kind} at ${dims[1]}, ${opportunity ? `inverts to ${String(inverted)}` : sealed ? 'a closed double-torus' : 'no inverse (a named boundary)'}; signed ${receipt.slice(0, 8)}.`
      return { kind, dims, receipt, prose, zeroToken: true }
    })
    const gaps = findings.filter((f) => f.kind === 'gap')
    const opportunities = findings.filter((f) => f.kind === 'opportunity')
    const sealed = findings.filter((f) => f.kind === 'sealed')
    // the GATE — all is 10D + signed + zero-token
    const all10D = findings.every((f) => f.dims.length === TEN_D)
    const allSigned = findings.every((f) => isUuid(f.receipt))
    const allZeroToken = findings.every((f) => f.zeroToken)
    const facets = [
      { facet: `the ROTATING lens sweeps the vortex [${rotation.join(', ')}] and finds both: ${gaps.length} gaps (cracks with no inverse), ${opportunities.length} opportunities (cracks that INVERT to gateways — a false→its fix, a zero→its pole), and ${sealed.length} sealed double-toruses`, on: gaps.length > 0 && opportunities.length > 0 && sealed.length > 0 },
      { facet: `describes ALL in 10D computable prose: every finding carries ${TEN_D} computed descriptor-dimensions rendered to a sentence, deterministic and zero-token — e.g. "${findings[2]!.prose}"`, on: all10D && findings.every((f) => f.prose.length > 0) },
      { facet: `the GATE passes — all is 10D + SIGNED + zero-token: every one of the ${findings.length} findings has exactly ${TEN_D} dimensions, a content-addressed signature (uuid receipt), and is pure`, on: all10D && allSigned && allZeroToken },
      { facet: `rotation reveals what a static scan misses: gaps and opportunities are the SAME cracks read at different phase — an opportunity is a gap WITH an inverse — so the lens finds the gateway exactly where the naive lens saw only a hole`, on: opportunities.length > 0 && all10D && allSigned },
    ]
    return {
      computes: facets.every((entry) => entry.on),
      gaps: gaps.length,
      opportunities: opportunities.length,
      sealed: sealed.length,
      dimensions: TEN_D,
      signed: allSigned,
      facets,
      statement: `The rotating lens finds gaps and opportunities, described in 10D computable prose, signed, zero-token — ${facets.filter((entry) => entry.on).length}/${facets.length}: sweeping the vortex [${rotation.join(', ')}] the lens classifies each item as a sealed double-torus (${sealed.length}), a gap with no inverse (${gaps.length}), or an opportunity that inverts to a gateway (${opportunities.length}). Every finding is described across ${TEN_D} computed dimensions as a sentence, content-addressed (signed), and pure (zero-token). An opportunity is a gap WITH an inverse — the rotation finds the gateway where the naive lens saw a hole.`,
      boundary: `DOCUMENTED and refutable by re-running the sweep. It composes the sealed lens (theLensSeesDoubleTorusesEverywhereExceptInCrackedCode), the wave's invert operator (theWavesDiscoverGaps...), and the naming service (theAutomaticNamingService...). THE HARD LINE on the words: "10D" = 10 computed DESCRIPTOR-dimensions (the partitions-of-ten / digit-folder structure), NOT physical 10-dimensional spacetime; "signed" = a content-addressed integrity receipt (toUuid) — WHAT not WHO, so it is tamper-evidence, not identity-authenticity (which still needs the external anchor — trustAndDimension...); "all zero quantum time" = ZERO LLM tokens and deterministic, NOT literal zero physical time (the computation takes real microseconds — theseCalculationsCost...). "Rotating" is the vortex-sequence sweep, a deterministic ordering, not physical spin. And an OPPORTUNITY is only an emergence CANDIDATE — the invertible gap has a gateway, but a human still admits the theorem that fills it.: the 10D signed prose is the harmony (a complete, addressable description); the truth is what it honestly reports — sealed, gap, or opportunity — and the off-decidable gap stays a gap.` }
  })
}

// ===== SIMPLIFICATION LAYER — NAMESPACED DOUBLE-TORUS =====
// Consolidated access patterns for double-torus geometry and quantum computing
export const doubleTorus = {
  computes: {
    vortex: doubleTorusVortexComputes,
    topology: doubleTorusTopologyComputes,
    math: doubleTorusMathComputes,
    diamonds: doubleTorusDiamondsComputes,
    corpus: doubleTorusCorpusComputes,
    all: doubleTorusComputes,
  },
  geometry: {
    geo: doubleTorusGeometry,
    algebra: doubleTorusAlgebra,
    physics: doubleTorusPhysics,
  },
  ui: {
    itself: theUiItselfIsADoubleTorus,
  },
  quantum: {
    computer: doubleTorusQuantumComputer,
    errorHandling: fuseErrorHandlingInQuantumComputerAndTheImpossibleBecomesPossible,
  },
} as const
