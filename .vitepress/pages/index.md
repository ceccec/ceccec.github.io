---
# COMPUTED PAGE — do not author here.
# The home body is homeMarkdown() (src/quantum/lake/dist/readme) — the ONE theorem generator shared
# with README.md; .vitepress/computed-pages.mts loads it at build/dev time (this stub exists so
# VitePress discovers the / route), the bg/gla homes transform the same output, and the hero stays
# computed via homeHero() in transformPageData.
---
