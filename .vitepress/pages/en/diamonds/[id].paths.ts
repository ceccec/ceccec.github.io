// English diamonds catch-all — sample ids only; corpusSliceFromRoute + parseHarmonicRequest at runtime.
import { corpusCatchAllPaths } from '../../../../src/routes/corpus'

export default { paths: () => corpusCatchAllPaths('diamonds') }
