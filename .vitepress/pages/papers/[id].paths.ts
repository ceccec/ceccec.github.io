// Papers catch-all — sample ids only; corpusSliceFromRoute + parseHarmonicRequest at runtime.
import { corpusCatchAllPaths } from '../../../src/wind/routes/corpus'

export default { paths: () => corpusCatchAllPaths('papers') }
