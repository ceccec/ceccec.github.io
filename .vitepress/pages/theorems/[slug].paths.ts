// Theorem catch-all — one dedicated page per proven atom; slugs computed from the sealed registry at build.
import { theoremPagePaths } from '../../../src/wind/routes/corpus'

export default { paths: () => theoremPagePaths() }
